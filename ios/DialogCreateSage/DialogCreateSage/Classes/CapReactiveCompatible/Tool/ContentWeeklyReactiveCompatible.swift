
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let const_discountMsg:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let dataHowName:String = "/listassistant on"

/*: "mf/moment/moments" :*/
fileprivate let appTillName:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let kTapContent:String = "course"
fileprivate let k_textureTheFormat:String = "/momentsbecome blanket"

/*: "mf/moment/like" :*/
fileprivate let userCanvasTitle:[Character] = ["m","f","/","m","o"]
fileprivate let const_governExtentFormat:String = "ment/likeopening possibility until green"

/*: "mf/crush/send" :*/
fileprivate let const_libraryFormat:[Character] = ["m","f","/","c","r","u","s","h","/","s","e","n","d"]

/*: "mf/moment/view" :*/
fileprivate let k_wholeAuthorGiantStr:[Character] = ["m"]
fileprivate let mainDelayValue:[Character] = ["f","/","m","o","m","e","n","t","/","v","i","e","w"]

/*: "mf/momentReply/list" :*/
fileprivate let noti_extendInsideFormat:String = "status levelmf/mom"
fileprivate let constSessionFormat:String = "project why turnply/list"

/*: "mf/momentReply/save" :*/
fileprivate let user_issueTooMeetingTitle:[Character] = ["m","f","/","m"]
fileprivate let user_verticalData:[Character] = ["o","m","e","n","t","R","e","p","l","y","/","s","a","v","e"]

/*: "mf/moment/videoSign" :*/
fileprivate let app_frankMsg:String = "anyone anglemf/moment"
fileprivate let constVoiceName:String = "condition house tone send/vide"

/*: "dynamicPic.zip" :*/
fileprivate let userDueUrl:String = "turkey drivedynamicP"
fileprivate let k_springKey:[Character] = ["i","c",".","z","i","p"]

/*: "pic" :*/
fileprivate let showMenuFormat:[UInt8] = [0xfd,0xe4,0xee]

private func roundingLook(running num: UInt8) -> UInt8 {
    return num ^ 141
}

/*: "content" :*/
fileprivate let kSenseText:[Character] = ["c","o","n","t","e","n","t"]

/*: "mf/moment/save" :*/
fileprivate let mainExistingTitle:String = "include immediate missing dominantmf/mom"
fileprivate let main_directWhatKey:[Character] = ["a","v","e"]

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let data_exampleValue:[UInt8] = [0x77,0x65,0x69,0x76,0x20,0x6f,0x74,0x20,0x73,0x63,0x69,0x6d,0x61,0x6e,0x79,0x44,0x20,0x79,0x4d,0x20,0x6f,0x74,0x20,0x6f,0x47,0x20,0x2c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x53,0x20,0x68,0x73,0x69,0x6c,0x62,0x75,0x50]

/*: "mf/moment/saveVideo" :*/
fileprivate let constTransitionThemData:String = "topic publicity colorful ping definemf/mo"
fileprivate let notiApplicationValue:String = "separate care fundaveV"
fileprivate let data_successfulPath:String = "idedifference"

/*: .png" :*/
fileprivate let notiLittleStr:String = "relation flexible isolate remove.png"

/*: "dynamicPic" :*/
fileprivate let k_stillWaitMessage:[Character] = ["d","y","n","a","m","i","c","P","i","c"]

/*: "freeImage" :*/
fileprivate let notiCreateMsg:String = "opportunityr"
fileprivate let user_everyName:String = "disable announcement fronteeImage"

/*: "mf/momentReply/replylist" :*/
fileprivate let show_retText:String = "m private color nobody effectivemf/m"
fileprivate let showThresholdName:String = "play play say nor joineply/"
fileprivate let mainAcquisitionName:String = "resucceed"
fileprivate let dataSelectMsg:[Character] = ["y","l","i","s","t"]

/*: "mf/moment/delete" :*/
fileprivate let app_biteKey:String = "mf/momhost invite"
fileprivate let dataLittleHourPath:String = "enearbyete"

/*: "mf/momentReply/delete" :*/
fileprivate let noti_glassName:String = "yet housemf/mo"
fileprivate let showExpressionPureOfferFormat:String = "along beyond neighbor time selfReply/"
fileprivate let noti_robotEmptyData:[Character] = ["d"]
fileprivate let userApplyKey:String = "elegesturee"

/*: "mf/moment/pin" :*/
fileprivate let userSaltUrl:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let kDynamicsUrl:String = "t/pinminimize tot err"

/*: "mf/moment/remindNum" :*/
fileprivate let notiReminderReserveStr:String = "enter bench span disabled simultaneouslymf/mo"
fileprivate let data_flagTitle:String = "ment/except clear need hung key"
fileprivate let constMergePath:[Character] = ["n","d","N","u","m"]

/*: "mf/moment/remindList" :*/
fileprivate let userOrganicStateStr:String = "vehicle level contextmf/mom"
fileprivate let constVisualSpaceTruthMsg:[Character] = ["i","n","d","L","i","s","t"]

/*: "page" :*/
fileprivate let kAcquireValue:[UInt8] = [0x8,0x19,0x1f,0x1d]

private func doingOrganic(visitor num: UInt8) -> UInt8 {
    return num ^ 120
}

/*: "cos/getCredential" :*/
fileprivate let user_okContent:[Character] = ["c","o","s","/","g","e","t","C","r"]
fileprivate let main_announcementKey:String = "dialog"
fileprivate let app_futureKey:String = "denheadial"

/*: "type" :*/
fileprivate let showSpendPairId:[Character] = ["t","y","p","e"]

/*: "user/reportUserInfo" :*/
fileprivate let k_blackId:String = "user/rping exposure"
fileprivate let const_futureStr:[Character] = ["e","p","o","r","t","U","s","e"]
fileprivate let noti_agoMsg:[Character] = ["r","I","n","f","o"]

/*: "uid" :*/
fileprivate let show_enableStr:String = "umenud"

/*: "deviceId" :*/
fileprivate let app_eventHandleId:[Character] = ["d","e","v","i","c","e"]
fileprivate let main_backgroundUrl:String = "build program decisionId"

/*: "deviceName" :*/
fileprivate let kMedicationValue:[Character] = ["d","e","v","i","c","e","N","a","m","e"]

/*: "iphone" :*/
fileprivate let kHaveValue:[Character] = ["i","p","h","o","n","e"]

/*: "fileUrl" :*/
fileprivate let userAcrossContent:[Character] = ["f","i","l","e","U","r"]
fileprivate let kRefNumberryId:[Character] = ["l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ContentWeeklyReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class ContentWeeklyReactiveCompatible: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func sparkRecord(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(const_discountMsg) + String(dataHowName.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func confirmAnimal(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(appTillName) + kTapContent.replacingOccurrences(of: "course", with: "t") + String(k_textureTheFormat.prefix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func bearEnable(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(userCanvasTitle) + String(const_governExtentFormat.prefix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func crushWithSpark(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(const_libraryFormat))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func pass(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(k_wholeAuthorGiantStr) + String(mainDelayValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func direct(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(noti_extendInsideFormat.suffix(6)) + "entRe" + String(constSessionFormat.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func draw(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(user_issueTooMeetingTitle) + String(user_verticalData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func general(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(app_frankMsg.suffix(9)) + String(constVoiceName.suffix(5)) + "oSign")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func turnName(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        dismiss()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            broadcast(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((String(userDueUrl.suffix(8)) + String(k_springKey)))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: showMenuFormat.map{roundingLook(running: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(String(kSenseText))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = RoundReactiveCompatible()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(mainExistingTitle.suffix(6)) + "ent/s" + String(main_directWhatKey))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.through(showMsg: String(bytes: data_exampleValue.reversed(), encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.dismiss()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func earlyDecision(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(constTransitionThemData.suffix(5)) + "ment/s" + String(notiApplicationValue.suffix(4)) + data_successfulPath.replacingOccurrences(of: "difference", with: "o"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func broadcast(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.availableData()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(notiLittleStr.suffix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((String(k_stillWaitMessage)))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func dismiss() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((notiCreateMsg.replacingOccurrences(of: "opportunity", with: "f") + String(user_everyName.suffix(7))))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func kibbitz(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(show_retText.suffix(4)) + "omentR" + String(showThresholdName.suffix(5)) + mainAcquisitionName.replacingOccurrences(of: "succeed", with: "pl") + String(dataSelectMsg))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func currencyMp(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(app_biteKey.prefix(6)) + "ent/d" + dataLittleHourPath.replacingOccurrences(of: "nearby", with: "l"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func us(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(noti_glassName.suffix(5)) + "ment" + String(showExpressionPureOfferFormat.suffix(6)) + String(noti_robotEmptyData) + userApplyKey.replacingOccurrences(of: "gesture", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func cagey(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(userSaltUrl) + String(kDynamicsUrl.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func delayAcrossCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(notiReminderReserveStr.suffix(5)) + String(data_flagTitle.prefix(5)) + "remi" + String(constMergePath))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func getCompletion(page: Int, completion: @escaping (_ data: [GenModelType]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(userOrganicStateStr.suffix(6)) + "ent/rem" + String(constVisualSpaceTruthMsg))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: kAcquireValue.map{doingOrganic(visitor: $0)}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [GenModelType] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<GenModelType>.deserialize(from: arr as? Array) as? [GenModelType] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func population(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(user_okContent) + main_announcementKey.replacingOccurrences(of: "dialog", with: "e") + app_futureKey.replacingOccurrences(of: "head", with: "t"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(String(showSpendPairId)): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func removeDown(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestServer = CompositionReactiveCompatible.share.appConfigMode.reportDomain
        reqModel.requestServer = CompositionReactiveCompatible.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(k_blackId.prefix(6)) + String(const_futureStr) + String(noti_agoMsg))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = CompositionReactiveCompatible.share.loginUserMode.userID
        params[(show_enableStr.replacingOccurrences(of: "menu", with: "i"))] = CompositionReactiveCompatible.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(app_eventHandleId) + String(main_backgroundUrl.suffix(2)))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(kMedicationValue))] = (String(kHaveValue))
        //: params["type"] = 1
        params[(String(showSpendPairId))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(userAcrossContent) + String(kRefNumberryId))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
