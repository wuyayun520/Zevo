
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_laborValue:[UInt8] = [0x9d,0x9a,0x9d,0x80,0xdc,0x97,0x9b,0x90,0x91,0x86,0xce,0xdd,0xd4,0x9c,0x95,0x87,0xd4,0x9a,0x9b,0x80,0xd4,0x96,0x91,0x91,0x9a,0xd4,0x9d,0x99,0x84,0x98,0x91,0x99,0x91,0x9a,0x80,0x91,0x90]

private func phaseOfTheMoon(bright num: UInt8) -> UInt8 {
    return num ^ 244
}

/*: "uid" :*/
fileprivate let userAccessName:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let showTransferStr:String = "whether lovely ref beneath statusheadPic"

/*: "nickname" :*/
fileprivate let user_bridgeData:String = "NI"
fileprivate let data_givenValue:String = "ckphoneme"

/*: "birthday" :*/
fileprivate let const_filterId:String = "bcapturerth"
fileprivate let constRobotContent:String = "mpy"

/*: "videoUrl" :*/
fileprivate let show_readingData:String = "videoUralive tick"
fileprivate let k_targetStr:[Character] = ["l"]

/*: "location" :*/
fileprivate let const_mirrorStr:String = "loground"
fileprivate let userProductStr:[Character] = ["a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let app_swimFindPath:[UInt8] = [0xab,0xa5,0xac]

/*: "refuseCall" :*/
fileprivate let show_richPath:String = "REFUS"
fileprivate let k_pathStr:String = "lease need play alleged cognitiveeCall"

/*: "data" :*/
fileprivate let noti_freshStr:[UInt8] = [0x36,0x33,0x26,0x33]

private func reExplore(carry num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "Call interruption" :*/
fileprivate let k_minimizeMsg:String = "angle currently processor then replyCall"
fileprivate let const_homeValue:String = "RUPT"
fileprivate let userStopValue:String = "businessn"

/*: "stopCall" :*/
fileprivate let appTitlePath:String = "pair option king packagestopCall"

/*: "logId" :*/
fileprivate let k_genreKey:[UInt8] = [0x80,0x83,0x7b,0x5d,0x78]

fileprivate func editObserve(own num: UInt8) -> UInt8 {
    let value = Int(num) - 20
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let k_lengthKey:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let dataProductRequireAppMessage:String = "No aearn camera invitation structure given"

/*: "duration" :*/
fileprivate let k_behindTitle:String = "anotheruratio"
fileprivate let appSunClubValue:[Character] = ["n"]

/*: "She has hung up" :*/
fileprivate let const_trustUrl:[Character] = ["S","h","e"," ","h","a","s"," ","h","u"]
fileprivate let appInnerPath:String = "ng upbasic thanks pot max"

/*: "cancelSex" :*/
fileprivate let noti_yesName:[Character] = ["c","a","n","c","e","l","S"]
fileprivate let kTruthKey:[Character] = ["e","x"]

/*: "He has hung up" :*/
fileprivate let const_readerUrl:String = "He hprofile branch and"
fileprivate let userNobodyData:String = "comment tool during sparkng up"

/*: "endType" :*/
fileprivate let data_collectOkUrl:[Character] = ["e","n","d","T","y"]
fileprivate let dataSlowSinkUrl:String = "pad"

/*: "stopSex" :*/
fileprivate let data_markCustomName:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let main_whyKey:[UInt8] = [0x64,0x65,0x64,0x6e,0x65,0x20,0x73,0x61,0x68,0x20,0x6c,0x6c,0x61,0x63,0x20,0x2c,0x73,0x6e,0x69,0x6f,0x63,0x20,0x64,0x6c,0x6f,0x67,0x20,0x74,0x6e,0x65,0x69,0x63,0x69,0x66,0x66,0x75,0x73,0x6e,0x49]

/*: "The other hung up the phone" :*/
fileprivate let constYesKey:String = "point column bright instance weightThe ot"
fileprivate let appSideDailyMsg:String = "ng up transfer middle receive base"
fileprivate let const_nobodySnapTakePath:[Character] = ["e"]

/*: "type" :*/
fileprivate let user_linkDetailData:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let notiExerciseName:[Character] = ["j","u","m","p","U"]
fileprivate let k_parentSampleIdentificationFormat:String = "privacyl"

/*: "Cancel" :*/
fileprivate let appLoseSurgeryName:[Character] = ["C","a","n","c","e"]
fileprivate let main_easternName:String = "given"

/*: "Get gold coins" :*/
fileprivate let notiCharacteristicStr:String = "gold change signature programGet go"

/*: "addBean" :*/
fileprivate let data_meetUrl:String = "addBebeat compare exercise i"
fileprivate let k_trackTitle:String = "thinn"

/*: "refuseSex" :*/
fileprivate let userPublishOffFormat:[Character] = ["r","e","f","u","s","e","S"]
fileprivate let k_demonstrateVoiceFormat:[Character] = ["e","x"]

/*: "onAcceptCall" :*/
fileprivate let const_starTitle:String = "onAccless nonetheless personal medication"
fileprivate let notiSquareData:String = "eptCallage able what"

/*: "acceptCall" :*/
fileprivate let showWorkerAFormat:[Character] = ["a","c","c","e"]
fileprivate let constExceptText:[Character] = ["p","t","C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let app_neverValue:String = "oral attentionCall"
fileprivate let showSourceValue:String = "bound"
fileprivate let showWantUrl:String = "ELLED"

/*: "The call has ended" :*/
fileprivate let noti_unableUrl:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," ","e","n"]
fileprivate let kBullFormat:String = "dgenerated"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TastyPartyDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let dataFileStr = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class TastyPartyDelegate: MergeContactViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: CompositionRoundChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ExecCosPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: WeeklyMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        FileSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        FileSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        FileSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_laborValue.map{phaseOfTheMoon(bright: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! SeriesHasDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.cover()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            favoriteGreet()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currStrategyVC = nil
            FileSocketManager.shared.currStrategyVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.capacityData()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        center()
        //: setupSubViewsConstraint()
        whenConstraint()
        //: initNotificationObserver()
        guide()
        //: req_callGetUserInfo()
        supersede()
        //: timeoutRules()
        searchStatement()
        //: resumeTalk()
        spendTalk()
        //: setUpBGMPlayer()
        airRating()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if CompositionReactiveCompatible.share.appConfigMode.disableShootScreen {
            if CompositionReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = MpCarrierThen(frame: CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isStrategy = false
        FileSocketManager.shared.isStrategy = false
        //: TalkingSocketManager.shared.isCalling = false
        FileSocketManager.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.reticulumActivties()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: MeetView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = MeetView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: FrankReactiveCompatible = {
        //: let m = TalkingVoiceChatManager()
        let m = FrankReactiveCompatible()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension TastyPartyDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func supersede() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        RaceThen.endBy(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: userAccessName.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(showTransferStr.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(user_bridgeData.lowercased() + data_givenValue.replacingOccurrences(of: "phone", with: "na"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(const_filterId.replacingOccurrences(of: "capture", with: "i") + constRobotContent.replacingOccurrences(of: "mp", with: "da"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(show_readingData.prefix(7)) + String(k_targetStr))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(const_mirrorStr.replacingOccurrences(of: "ground", with: "c") + String(userProductStr))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.formPress()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension TastyPartyDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func favoriteGreet() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        MeetRouteThen.panelSpring(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.foolishness(showMsg: user_sceneKey)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                FileSocketManager.shared.bagOriginal(info: [String(bytes: app_swimFindPath.map{$0^200}, encoding: .utf8)!: (show_richPath.lowercased() + String(k_pathStr.suffix(5))), String(bytes: noti_freshStr.map{reExplore(carry: $0)}, encoding: .utf8)!: [String(bytes: userAccessName.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.animated()
                //: return
                return
            }

            //: self.stopBGM()
            self.attender()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ExecCosPlayer.layerType(type: .Response)
            //: self.startBGM()
            self.cover()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func reticulumActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.attender()
        //: TalkingSocketManager.shared.callerDelegate = nil
        FileSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        FileSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        FileSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func animated(animated: Bool = true) {
        //: self.stopBGM()
        self.attender()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func communication() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.listener(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func spendTalk() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            communication()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension TastyPartyDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func guide() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(pairOf(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(banReason(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: const_beginStr,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        dataFileStr.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func pairOf(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            animated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func banReason(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard FileSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        FileSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        FileSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        FileSocketManager.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.component(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension TastyPartyDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.foolishness(showMsg: (String(k_minimizeMsg.suffix(4)) + " inter" + const_homeValue.lowercased() + userStopValue.replacingOccurrences(of: "business", with: "io")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                FileSocketManager.shared.bagOriginal(info: [String(bytes: app_swimFindPath.map{$0^200}, encoding: .utf8)!: (String(appTitlePath.suffix(8))), String(bytes: noti_freshStr.map{reExplore(carry: $0)}, encoding: .utf8)!: [String(bytes: k_genreKey.map{editObserve(own: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                FileSocketManager.shared.bagOriginal(info: [String(bytes: app_swimFindPath.map{$0^200}, encoding: .utf8)!: (String(k_lengthKey)), String(bytes: noti_freshStr.map{reExplore(carry: $0)}, encoding: .utf8)!: [String(bytes: k_genreKey.map{editObserve(own: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                FileSocketManager.shared.bagOriginal(info: [String(bytes: app_swimFindPath.map{$0^200}, encoding: .utf8)!: (show_richPath.lowercased() + String(k_pathStr.suffix(5))), String(bytes: noti_freshStr.map{reExplore(carry: $0)}, encoding: .utf8)!: [String(bytes: userAccessName.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.saucer()
                //: TalkingSocketManager.shared.currStrategyVC = nil
                FileSocketManager.shared.currStrategyVC = nil
            }
            //: popCurrentViewController()
            animated()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.act(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension TastyPartyDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func airRating() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? SightDefaultsSerializable.Call : SightDefaultsSerializable.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ExecCosPlayer.layerType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func cover() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.negociateLoad()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func attender() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.allowRidTwo()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension TastyPartyDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func searchStatement() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? show_lastValue : self.chatModel?.drumstickUnitedlyNorSnip()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            betweenNorth()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.betweenNorth()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func betweenNorth() {
        //: guard !TalkingSocketManager.shared.isStrategy else { return }
        guard !FileSocketManager.shared.isStrategy else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.foolishness(showMsg: (String(dataProductRequireAppMessage.prefix(4)) + "nswer").localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: app_swimFindPath.map{$0^200}, encoding: .utf8)!: (String(k_lengthKey)), String(bytes: noti_freshStr.map{reExplore(carry: $0)}, encoding: .utf8)!: [String(bytes: k_genreKey.map{editObserve(own: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: app_swimFindPath.map{$0^200}, encoding: .utf8)!: (show_richPath.lowercased() + String(k_pathStr.suffix(5))), String(bytes: noti_freshStr.map{reExplore(carry: $0)}, encoding: .utf8)!: [String(bytes: userAccessName.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        FileSocketManager.shared.bagOriginal(info: socketDict)
        //: popCurrentViewController()
        animated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension TastyPartyDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func wrapAcross(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: k_genreKey.map{editObserve(own: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(k_behindTitle.replacingOccurrences(of: "another", with: "d") + String(appSunClubValue))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.partyTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.borderStage()
            //: createTalkRoom()
            communication()
            //: self.stopBGM()
            self.attender()
        }
    }
}

// MARK: - FormatSceneObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension TastyPartyDelegate: FormatSceneObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func zygotene(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(const_trustUrl) + String(appInnerPath.prefix(5))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(noti_yesName) + String(kTruthKey))].stringValue == ArrowGender.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(const_readerUrl.prefix(4)) + "as hu" + String(userNobodyData.suffix(5))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.foolishness(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.saucer()
            //: TalkingSocketManager.shared.currStrategyVC = nil
            FileSocketManager.shared.currStrategyVC = nil
        }

        //: popCurrentViewController()
        animated()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func down(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: k_genreKey.map{editObserve(own: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.attender()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.borderStage()
        //: self.createTalkRoom()
        self.communication()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func submitPerform(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(data_collectOkUrl) + dataSlowSinkUrl.replacingOccurrences(of: "pad", with: "pe"))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(const_trustUrl) + String(appInnerPath.prefix(5))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(data_markCustomName))].stringValue == ArrowGender.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(const_readerUrl.prefix(4)) + "as hu" + String(userNobodyData.suffix(5))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: main_whyKey.reversed(), encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(constYesKey.suffix(6)) + "her hu" + String(appSideDailyMsg.prefix(6)) + "the phon" + String(const_nobodySnapTakePath)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.foolishness(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.saucer()
            //: TalkingSocketManager.shared.currStrategyVC = nil
            FileSocketManager.shared.currStrategyVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.primaryDestination()
        //: self.popCurrentViewController()
        self.animated()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kReasonDataName, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func rewardOverlook(msg: String, data: [String: Any]) {
        //: guard CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(user_linkDetailData))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(notiExerciseName) + k_parentSampleIdentificationFormat.replacingOccurrences(of: "privacy", with: "r"))] as? String else { return }
            //: let httpsUrl = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = RoundConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: SightReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            SightReactiveCompatible.share.getConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            YetAlertShow.trackSpace(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(appLoseSurgeryName) + main_easternName.replacingOccurrences(of: "given", with: "l")).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(notiCharacteristicStr.suffix(6)) + "ld coins").localized)
            {
                //: TalkingAlertShow.hideAlert()
                YetAlertShow.usance()
                //: } rightBlock: {
            } rightBlock: {
                //: SightReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                SightReactiveCompatible.share.secondBusy(clickEvent: constInfoName, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func chatRaw(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(data_meetUrl.prefix(5)) + k_trackTitle.replacingOccurrences(of: "thin", with: "a"))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.includeDoing(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.quantityersectionPoint(point: addBean)
        }
    }
}

// MARK: - FactorObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension TastyPartyDelegate: FactorObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func entire(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(const_trustUrl) + String(appInnerPath.prefix(5))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(userPublishOffFormat) + String(k_demonstrateVoiceFormat))].stringValue == ArrowGender.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(const_readerUrl.prefix(4)) + "as hu" + String(userNobodyData.suffix(5))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.foolishness(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.saucer()
            //: TalkingSocketManager.shared.currStrategyVC = nil
            FileSocketManager.shared.currStrategyVC = nil
        }
        //: popCurrentViewController()
        animated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func acceptShouldTag(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: k_genreKey.map{editObserve(own: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.attender()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.borderStage()
        //: self.createTalkRoom()
        self.communication()
    }
}

// MARK: - ToolMeetErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension TastyPartyDelegate: ToolMeetErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func lock(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (show_richPath.lowercased() + String(k_pathStr.suffix(5))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.foolishness(showMsg: (String(dataProductRequireAppMessage.prefix(4)) + "nswer").localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.saucer()
                //: TalkingSocketManager.shared.currStrategyVC = nil
                FileSocketManager.shared.currStrategyVC = nil
            }
            //: self.popCurrentViewController()
            self.animated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(const_starTitle.prefix(5)) + String(notiSquareData.prefix(7))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue else { return }
            //: SightReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            SightReactiveCompatible.share.secondBusy(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(const_starTitle.prefix(5)) + String(notiSquareData.prefix(7))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.foolishness(showMsg: msg)
            //: self.popCurrentViewController()
            self.animated()
        }
    }
}

// MARK: - CarrierObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension TastyPartyDelegate: CarrierObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func fruitBar() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        FileSocketManager.shared.bagOriginal(info: [String(bytes: app_swimFindPath.map{$0^200}, encoding: .utf8)!: (String(showWorkerAFormat) + String(constExceptText)), String(bytes: noti_freshStr.map{reExplore(carry: $0)}, encoding: .utf8)!: [String(bytes: userAccessName.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func fromGoAway() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.foolishness(showMsg: (String(app_neverValue.suffix(4)) + "s are can" + showSourceValue.replacingOccurrences(of: "bound", with: "c") + showWantUrl.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            FileSocketManager.shared.bagOriginal(info: [String(bytes: app_swimFindPath.map{$0^200}, encoding: .utf8)!: (String(k_lengthKey)), String(bytes: noti_freshStr.map{reExplore(carry: $0)}, encoding: .utf8)!: [String(bytes: k_genreKey.map{editObserve(own: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.foolishness(showMsg: (String(app_neverValue.suffix(4)) + "s are can" + showSourceValue.replacingOccurrences(of: "bound", with: "c") + showWantUrl.lowercased()).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            FileSocketManager.shared.bagOriginal(info: [String(bytes: app_swimFindPath.map{$0^200}, encoding: .utf8)!: (show_richPath.lowercased() + String(k_pathStr.suffix(5))), String(bytes: noti_freshStr.map{reExplore(carry: $0)}, encoding: .utf8)!: [String(bytes: userAccessName.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.foolishness(showMsg: (String(noti_unableUrl) + kBullFormat.replacingOccurrences(of: "generate", with: "e")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            FileSocketManager.shared.bagOriginal(info: [String(bytes: app_swimFindPath.map{$0^200}, encoding: .utf8)!: (String(appTitlePath.suffix(8))), String(bytes: noti_freshStr.map{reExplore(carry: $0)}, encoding: .utf8)!: [String(bytes: k_genreKey.map{editObserve(own: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.primaryDestination()
        //: self.popCurrentViewController()
        self.animated()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func straddle(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.shrinkBy(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func threePlace(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.handsfree(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func pointWindow() {
        //: TalkingSocketManager.shared.currStrategyVC = self
        FileSocketManager.shared.currStrategyVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = WeeklyMiniView.shadow()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.mediumSave()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.nowadays()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.arrowPress(talkingTime)
        //: popCurrentViewController()
        self.animated()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func unnamed(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.arrowPress(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func giftAccountSuite(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.worker(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension TastyPartyDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func center() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func whenConstraint() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
