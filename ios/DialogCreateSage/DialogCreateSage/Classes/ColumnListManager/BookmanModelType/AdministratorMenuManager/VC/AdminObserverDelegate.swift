
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataPresentationFailureStr:[UInt8] = [0xb1,0xb6,0xb1,0xbc,0x70,0xab,0xb7,0xac,0xad,0xba,0x82,0x71,0x68,0xb0,0xa9,0xbb,0x68,0xb6,0xb7,0xbc,0x68,0xaa,0xad,0xad,0xb6,0x68,0xb1,0xb5,0xb8,0xb4,0xad,0xb5,0xad,0xb6,0xbc,0xad,0xac]

fileprivate func edgeDecision(technology num: UInt8) -> UInt8 {
    let value = Int(num) - 72
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let dataDuringStr:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let kAgencyName:String = "anyone fade designheadPic"

/*: "nickname" :*/
fileprivate let user_sTitle:String = "NIC"
fileprivate let data_egoMessage:String = "kmoveame"

/*: "birthday" :*/
fileprivate let k_youName:String = "BIRT"

/*: "videoUrl" :*/
fileprivate let constAmberDiskKey:String = "capture"
fileprivate let kMinimumCanPath:[Character] = ["i","d","e","o","U","r","l"]

/*: "cover" :*/
fileprivate let notiMoreData:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let k_scoreTitle:String = "gainocatio"
fileprivate let user_addressNatureKey:String = "stroke"

/*: "cmd" :*/
fileprivate let noti_visitorId:[UInt8] = [0x8b,0x85,0x8c]

private func allowSecret(expected num: UInt8) -> UInt8 {
    return num ^ 232
}

/*: "refuseCall" :*/
fileprivate let const_circleInformationMsg:String = "refpartnere"
fileprivate let show_pageBuildFormat:String = "transfer report templateCall"

/*: "data" :*/
fileprivate let app_ourMsg:[UInt8] = [0xd7,0xd2,0xc7,0xd2]

private func gapWithout(progress num: UInt8) -> UInt8 {
    return num ^ 179
}

/*: "Call interruption" :*/
fileprivate let mainHolderStepTitle:String = "manage spreadCal"
fileprivate let data_secondId:String = "coordinate stickl in"
fileprivate let mainBetweenStr:String = "liteion"

/*: "stopCall" :*/
fileprivate let app_followKey:String = "alongsidet"
fileprivate let show_putWillData:String = "bottom edible method favorite findopCall"

/*: "logId" :*/
fileprivate let dataSupposedFormat:[UInt8] = [0x30,0x33,0x3b,0x15,0x38]

/*: "cancelCall" :*/
fileprivate let user_discoverStr:String = "CANCE"

/*: "No answer" :*/
fileprivate let showSimultaneouslyContent:[Character] = ["N","o"," ","a","n","s"]
fileprivate let dataSomeoneStr:String = "webit"

/*: "duration" :*/
fileprivate let main_compareCombinedText:String = "duratishall"
fileprivate let data_stockDitMsg:String = "slim"

/*: "She has hung up" :*/
fileprivate let showGoodKey:String = "genre sense arcShe "
fileprivate let noti_alongLetterData:String = "hung upcollection required process"

/*: "cancelSex" :*/
fileprivate let main_haveFormat:String = "refer least foundationcancelS"
fileprivate let k_meetingJoinTitle:String = "emaybe"

/*: "He has hung up" :*/
fileprivate let constSolutionStr:[Character] = ["H","e"," ","h","a","s"," ","h","u"]
fileprivate let dataSlideName:String = "pin fail appropriate chartng up"

/*: "endType" :*/
fileprivate let const_easternFormat:String = "endTylow addition estate"
fileprivate let app_bondText:String = "whitee"

/*: "stopSex" :*/
fileprivate let user_trustMsg:[Character] = ["s","t","o","p","S"]
fileprivate let userCompleteAlbumBadValue:String = "EX"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let kAdjustName:[UInt8] = [0xc9,0xee,0xf3,0xf5,0xe6,0xe6,0xe9,0xe3,0xe9,0xe5,0xee,0xf4,0xa0,0xe7,0xef,0xec,0xe4,0xa0,0xe3,0xef,0xe9,0xee,0xf3,0xac,0xa0,0xe3,0xe1,0xec,0xec,0xa0,0xe8,0xe1,0xf3,0xa0,0xe5,0xee,0xe4,0xe5,0xe4]

fileprivate func tackEnable(be num: UInt8) -> UInt8 {
    let value = Int(num) - 128
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "The other hung up the phone" :*/
fileprivate let const_compriseFormat:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"," "]
fileprivate let userPicUrl:String = "some that hide cease meup t"
fileprivate let kFollowName:String = "he phonemissing request"

/*: "type" :*/
fileprivate let notiBodyKey:String = "tfactore"

/*: "jumpUrl" :*/
fileprivate let noti_feeTitle:[Character] = ["j","u","m","p","U","r"]
fileprivate let const_meanAdviseStr:String = "lovely"

/*: "Cancel" :*/
fileprivate let notiDelayPolicyValue:String = "each do i databaseCance"
fileprivate let constCoverValue:[Character] = ["l"]

/*: "Get gold coins" :*/
fileprivate let userAllegedKey:[Character] = ["G","e"]
fileprivate let app_commentServerData:String = "t goldfore link normally statement accounting"

/*: "addBean" :*/
fileprivate let user_warningTitle:String = "fore phenomenon value sense nameaddBean"

/*: "refuseSex" :*/
fileprivate let dataLockValue:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let kInmatePath:String = "onAcceperiod what between"
fileprivate let main_consciousMessage:[Character] = ["p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let kTargetLightStr:String = "acceptCalscript advise enhance"
fileprivate let k_swimPath:[Character] = ["l"]

/*: "Calls are cancelled" :*/
fileprivate let noti_northwestPicturePath:String = "laugh toward lack strengthCalls"
fileprivate let noti_ofSpotText:String = "ret"
fileprivate let show_livingId:[Character] = ["a","n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let app_comeNobodyStr:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," ","e","n","d"]
fileprivate let kWeightFeeFormat:String = "estar"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AdminObserverDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let appScaleLessFormat = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class AdminObserverDelegate: MergeContactViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: CompositionRoundChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ExecCosPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: CompositionSkirtMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: RecordingReactiveCompatible? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        FileSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        FileSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        FileSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataPresentationFailureStr.map{edgeDecision(technology: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! SeriesHasDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.onEqualFail()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            jawPicture()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            reclusiveness(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currStrategyVC = nil
            FileSocketManager.shared.currStrategyVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.conceptAlbumPhone()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        subviews()
        //: setupSubViewsConstraint()
        cityConstraint()
        //: initNotificationObserver()
        operation()
        //: req_callGetUserInfo()
        dark()
        //: timeoutRules()
        operateSequence()
        //: setUpBGMPlayer()
        me()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.succeederPreview()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if CompositionReactiveCompatible.share.appConfigMode.disableShootScreen {
            if CompositionReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = MpCarrierThen(frame: CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isStrategy = false
        FileSocketManager.shared.isStrategy = false
        //: TalkingSocketManager.shared.isCalling = false
        FileSocketManager.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.allPast()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: CompareVideoView = {
        //: let v = TalkingBGVideoView()
        let v = CompareVideoView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.sequenceShow(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: MagnitudeeractionPlayerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = MagnitudeeractionPlayerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: HeadThen = {
        //: let v = TalkingVideoWindowView()
        let v = HeadThen()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = user_needValue - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if ReadingLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: noti_guideRequestId + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.paradigm()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: RaceThen = {
        //: let m = TalkingVideoChatManager()
        let m = RaceThen()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension AdminObserverDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func dark() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        RaceThen.endBy(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: dataDuringStr.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(kAgencyName.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(user_sTitle.lowercased() + data_egoMessage.replacingOccurrences(of: "move", with: "n"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(k_youName.lowercased() + "hday")].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(constAmberDiskKey.replacingOccurrences(of: "capture", with: "v") + String(kMinimumCanPath))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(notiMoreData))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(k_scoreTitle.replacingOccurrences(of: "gain", with: "l") + user_addressNatureKey.replacingOccurrences(of: "stroke", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.finger()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.worldWide()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func worldWide() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = BlockCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = BlockCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.mergeClear(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.mutual(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension AdminObserverDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func jawPicture() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        MeetRouteThen.hairTip { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.foolishness(showMsg: user_sceneKey)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                FileSocketManager.shared.bagOriginal(info: [String(bytes: noti_visitorId.map{allowSecret(expected: $0)}, encoding: .utf8)!: (const_circleInformationMsg.replacingOccurrences(of: "partner", with: "us") + String(show_pageBuildFormat.suffix(4))), String(bytes: app_ourMsg.map{gapWithout(progress: $0)}, encoding: .utf8)!: [String(bytes: dataDuringStr.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.beyondPin()
                //: return
                return
            }

            //: self.startPreview()
            self.succeederPreview()
            //: self.stopBGM()
            self.relieveRun()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ExecCosPlayer.layerType(type: .Response)
            //: self.startBGM()
            self.onEqualFail()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func succeederPreview() {
        //: self.videoManager.startPreview()
        self.videoManager.parallel()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func allPast() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.relieveRun()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        SpeakThen.allFire()
        //: TalkingSocketManager.shared.callerDelegate = nil
        FileSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        FileSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        FileSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func beyondPin(animated: Bool = true) {
        //: self.stopBGM()
        self.relieveRun()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func paradigm() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.crenelateWithView()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.worldWide()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func ballClub() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.resourceView()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.worldWide()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.presentCon()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func presentCon() {
        //: guard CompositionReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = RecordingReactiveCompatible()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.actualRender()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension AdminObserverDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func operation() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(hour(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(beneath(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: show_profileData,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(reachability(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: const_beginStr,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        appScaleLessFormat.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func hour(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            beyondPin()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func reachability(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard FileSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        FileSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        FileSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        FileSocketManager.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.behindWhiteAvailable()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func beneath(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.worldWide()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension AdminObserverDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.foolishness(showMsg: (String(mainHolderStepTitle.suffix(3)) + String(data_secondId.suffix(4)) + "terru" + mainBetweenStr.replacingOccurrences(of: "lite", with: "pt")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                FileSocketManager.shared.bagOriginal(info: [String(bytes: noti_visitorId.map{allowSecret(expected: $0)}, encoding: .utf8)!: (app_followKey.replacingOccurrences(of: "alongside", with: "s") + String(show_putWillData.suffix(6))), String(bytes: app_ourMsg.map{gapWithout(progress: $0)}, encoding: .utf8)!: [String(bytes: dataSupposedFormat.map{$0^92}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                FileSocketManager.shared.bagOriginal(info: [String(bytes: noti_visitorId.map{allowSecret(expected: $0)}, encoding: .utf8)!: (user_discoverStr.lowercased() + "lCall"), String(bytes: app_ourMsg.map{gapWithout(progress: $0)}, encoding: .utf8)!: [String(bytes: dataSupposedFormat.map{$0^92}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                FileSocketManager.shared.bagOriginal(info: [String(bytes: noti_visitorId.map{allowSecret(expected: $0)}, encoding: .utf8)!: (const_circleInformationMsg.replacingOccurrences(of: "partner", with: "us") + String(show_pageBuildFormat.suffix(4))), String(bytes: app_ourMsg.map{gapWithout(progress: $0)}, encoding: .utf8)!: [String(bytes: dataDuringStr.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.saucer()
                //: TalkingSocketManager.shared.currStrategyVC = nil
                FileSocketManager.shared.currStrategyVC = nil
            }
            //: popCurrentViewController()
            beyondPin()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension AdminObserverDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func me() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? SightDefaultsSerializable.Call : SightDefaultsSerializable.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ExecCosPlayer.layerType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func onEqualFail() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.negociateLoad()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func relieveRun() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.allowRidTwo()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension AdminObserverDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func operateSequence() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? show_lastValue : self.chatModel?.drumstickUnitedlyNorSnip()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            countFormAction()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.countFormAction()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func countFormAction() {
        //: guard !TalkingSocketManager.shared.isStrategy else { return }
        guard !FileSocketManager.shared.isStrategy else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.foolishness(showMsg: (String(showSimultaneouslyContent) + dataSomeoneStr.replacingOccurrences(of: "bit", with: "r")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: noti_visitorId.map{allowSecret(expected: $0)}, encoding: .utf8)!: (user_discoverStr.lowercased() + "lCall"), String(bytes: app_ourMsg.map{gapWithout(progress: $0)}, encoding: .utf8)!: [String(bytes: dataSupposedFormat.map{$0^92}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: noti_visitorId.map{allowSecret(expected: $0)}, encoding: .utf8)!: (const_circleInformationMsg.replacingOccurrences(of: "partner", with: "us") + String(show_pageBuildFormat.suffix(4))), String(bytes: app_ourMsg.map{gapWithout(progress: $0)}, encoding: .utf8)!: [String(bytes: dataDuringStr.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        FileSocketManager.shared.bagOriginal(info: socketDict)
        //: popCurrentViewController()
        beyondPin()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension AdminObserverDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func pontificate(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: dataSupposedFormat.map{$0^92}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(main_compareCombinedText.replacingOccurrences(of: "shall", with: "o") + data_stockDitMsg.replacingOccurrences(of: "slim", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.there(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.richPerson()
            //: createTalkRoom()
            ballClub()
            //: self.stopBGM()
            self.relieveRun()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func reclusiveness(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.crenelateWithView()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.sequenceShow(show: false)
        //: self.stopBGM()
        self.relieveRun()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.richPerson()
        //: self.createTalkRoom()
        self.ballClub()
    }
}

// MARK: - FormatSceneObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension AdminObserverDelegate: FormatSceneObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func zygotene(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showGoodKey.suffix(4)) + "has " + String(noti_alongLetterData.prefix(7))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(main_haveFormat.suffix(7)) + k_meetingJoinTitle.replacingOccurrences(of: "maybe", with: "x"))].stringValue == ArrowGender.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(constSolutionStr) + String(dataSlideName.suffix(5))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.foolishness(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.saucer()
            //: TalkingSocketManager.shared.currStrategyVC = nil
            FileSocketManager.shared.currStrategyVC = nil
        }

        //: popCurrentViewController()
        beyondPin()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func down(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        reclusiveness(logId: json[String(bytes: dataSupposedFormat.map{$0^92}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func submitPerform(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(const_easternFormat.prefix(5)) + app_bondText.replacingOccurrences(of: "white", with: "p"))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(showGoodKey.suffix(4)) + "has " + String(noti_alongLetterData.prefix(7))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(user_trustMsg) + userCompleteAlbumBadValue.lowercased())].stringValue == ArrowGender.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(constSolutionStr) + String(dataSlideName.suffix(5))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: kAdjustName.map{tackEnable(be: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(const_compriseFormat) + String(userPicUrl.suffix(4)) + String(kFollowName.prefix(8))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.foolishness(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.saucer()
            //: TalkingSocketManager.shared.currStrategyVC = nil
            FileSocketManager.shared.currStrategyVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.blindDismiss()
        //: popCurrentViewController()
        beyondPin()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func rewardOverlook(msg: String, data: [String: Any]) {
        //: guard CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(notiBodyKey.replacingOccurrences(of: "factor", with: "yp"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(noti_feeTitle) + const_meanAdviseStr.replacingOccurrences(of: "lovely", with: "l"))] as? String else { return }
            //: let httpsUrl = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = RoundConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: SightReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            SightReactiveCompatible.share.getConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            YetAlertShow.trackSpace(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(notiDelayPolicyValue.suffix(5)) + String(constCoverValue)).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(userAllegedKey) + String(app_commentServerData.prefix(6)) + " coins").localized)
            {
                //: TalkingAlertShow.hideAlert()
                YetAlertShow.usance()
                //: } rightBlock: {
            } rightBlock: {
                //: SightReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                SightReactiveCompatible.share.secondBusy(clickEvent: constInfoName, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func recover(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.connectionAccess(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func chatRaw(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(user_warningTitle.suffix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.missSuit(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.favorite(point: addBean)
        }
    }
}

// MARK: - FactorObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension AdminObserverDelegate: FactorObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func entire(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showGoodKey.suffix(4)) + "has " + String(noti_alongLetterData.prefix(7))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(dataLockValue))].stringValue == ArrowGender.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(constSolutionStr) + String(dataSlideName.suffix(5))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.foolishness(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.saucer()
            //: TalkingSocketManager.shared.currStrategyVC = nil
            FileSocketManager.shared.currStrategyVC = nil
        }

        //: popCurrentViewController()
        beyondPin()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func acceptShouldTag(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        reclusiveness(logId: json[String(bytes: dataSupposedFormat.map{$0^92}, encoding: .utf8)!].intValue)
    }
}

// MARK: - ToolMeetErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension AdminObserverDelegate: ToolMeetErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func lock(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (const_circleInformationMsg.replacingOccurrences(of: "partner", with: "us") + String(show_pageBuildFormat.suffix(4))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.foolishness(showMsg: (String(showSimultaneouslyContent) + dataSomeoneStr.replacingOccurrences(of: "bit", with: "r")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.saucer()
                //: TalkingSocketManager.shared.currStrategyVC = nil
                FileSocketManager.shared.currStrategyVC = nil
            }
            //: popCurrentViewController()
            beyondPin()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(kInmatePath.prefix(6)) + String(main_consciousMessage)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue else { return }
            //: SightReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            SightReactiveCompatible.share.secondBusy(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(kInmatePath.prefix(6)) + String(main_consciousMessage)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.foolishness(showMsg: msg)
            //: popCurrentViewController()
            beyondPin()
        }
    }
}

// MARK: - SumeractionObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension AdminObserverDelegate: SumeractionObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func bigChoose() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        FileSocketManager.shared.bagOriginal(info: [String(bytes: noti_visitorId.map{allowSecret(expected: $0)}, encoding: .utf8)!: (String(kTargetLightStr.prefix(9)) + String(k_swimPath)), String(bytes: app_ourMsg.map{gapWithout(progress: $0)}, encoding: .utf8)!: [String(bytes: dataDuringStr.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func promptSequence() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.foolishness(showMsg: (String(noti_northwestPicturePath.suffix(5)) + " are " + noti_ofSpotText.replacingOccurrences(of: "ret", with: "c") + String(show_livingId)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            FileSocketManager.shared.bagOriginal(info: [String(bytes: noti_visitorId.map{allowSecret(expected: $0)}, encoding: .utf8)!: (user_discoverStr.lowercased() + "lCall"), String(bytes: app_ourMsg.map{gapWithout(progress: $0)}, encoding: .utf8)!: [String(bytes: dataSupposedFormat.map{$0^92}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.foolishness(showMsg: (String(noti_northwestPicturePath.suffix(5)) + " are " + noti_ofSpotText.replacingOccurrences(of: "ret", with: "c") + String(show_livingId)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            FileSocketManager.shared.bagOriginal(info: [String(bytes: noti_visitorId.map{allowSecret(expected: $0)}, encoding: .utf8)!: (const_circleInformationMsg.replacingOccurrences(of: "partner", with: "us") + String(show_pageBuildFormat.suffix(4))), String(bytes: app_ourMsg.map{gapWithout(progress: $0)}, encoding: .utf8)!: [String(bytes: dataDuringStr.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.foolishness(showMsg: (String(app_comeNobodyStr) + kWeightFeeFormat.replacingOccurrences(of: "star", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            FileSocketManager.shared.bagOriginal(info: [String(bytes: noti_visitorId.map{allowSecret(expected: $0)}, encoding: .utf8)!: (app_followKey.replacingOccurrences(of: "alongside", with: "s") + String(show_putWillData.suffix(6))), String(bytes: app_ourMsg.map{gapWithout(progress: $0)}, encoding: .utf8)!: [String(bytes: dataSupposedFormat.map{$0^92}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        beyondPin()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func attributeAcross(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.faceCamera(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func innerQuery(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.sequenceShow(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.offdStatus(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.worldWide()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func ceremony() {
        //: TalkingSocketManager.shared.currStrategyVC = self
        FileSocketManager.shared.currStrategyVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = CompositionSkirtMiniView.birdSEyeView()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.recoverMode()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.withEdit()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.mediumSave()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        beyondPin()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func propertyDesign() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension AdminObserverDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func subviews() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.resumePointView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func cityConstraint() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
