
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let k_myContent:String = "IM退出登\u{5f55}"
fileprivate let app_areaFormat:String = "ode:image tone"

/*: , desc: :*/
fileprivate let k_engageUrl:[Character] = [","," ","d","e","s","c",":"]

/*: "IM登录失败，code: :*/
fileprivate let app_mentallyName:String = "IM登\u{5f55}失败"
fileprivate let main_goldMessage:String = "\u{ff0c}code:"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let show_strengthTitle:String = "IM获取\u{672a}"
fileprivate let mainBelowName:String = "读消息总\u{6570}失败，"
fileprivate let showNeverTitle:[Character] = ["c","o","d","e",":"]

/*: "crushBadge" :*/
fileprivate let kHoldKey:[Character] = ["c","r","u","s","h","B","a","d","g","e"]

/*: "badNumber" :*/
fileprivate let notiCapacityKey:[UInt8] = [0xfe,0xfd,0x0,0xea,0x11,0x9,0xfe,0x1,0xe]

fileprivate func turkeyCare(injury num: UInt8) -> UInt8 {
    let value = Int(num) + 100
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "plaza" :*/
fileprivate let noti_alreadyBirthUrl:String = "pmodifyza"

/*: "roomNum" :*/
fileprivate let showForceMessage:String = "roomNumenhance transform structure concentration top"

/*: "num" :*/
fileprivate let show_apartmentMsg:String = "shotum"

/*: "onlyOneRoomId" :*/
fileprivate let appInfoFortUrl:String = "onlyOsecond sage written trail"
fileprivate let noti_availableText:[Character] = ["n","e","R","o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let mainAboveKey:String = "LIcontrast"
fileprivate let noti_heartStr:[Character] = ["E","-","C","H","A","T"]

/*: "extra" :*/
fileprivate let app_sageRegionComePath:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let main_guidanceValue:[Character] = ["m","s","g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let mainPersonalUrl:String = "newPkgcoordinator disable editor"

/*: "MF:ChatMentionMsg" :*/
fileprivate let show_softwareFormat:String = "recommend them currently automatically shortMF:Cha"
fileprivate let k_commercialTitle:String = "tMentgiving professional sibling team enter"

/*: "uid" :*/
fileprivate let app_oppositeMsg:[UInt8] = [0xaa,0xb6,0xbb]

private func bringOut(above num: UInt8) -> UInt8 {
    return num ^ 223
}

/*: "onlineStatus" :*/
fileprivate let data_northwestUrl:[UInt8] = [0x97,0x96,0x94,0x91,0x96,0x9d,0xab,0x8c,0x99,0x8c,0x8d,0x8b]

private func leaseUtilize(dare num: UInt8) -> UInt8 {
    return num ^ 248
}

/*: "isNewUser" :*/
fileprivate let constExtendedUrl:[UInt8] = [0xb9,0xa3,0x9e,0xb5,0xa7,0x85,0xa3,0xb5,0xa2]

private func mingleMangle(no num: UInt8) -> UInt8 {
    return num ^ 208
}

/*: "c2c_ :*/
fileprivate let constClubValue:String = "c2c_ignore privacy deadline"

/*: "IM获取会话失败，convId: :*/
fileprivate let kTotValue:String = "IM获取skip full scope event successfully"
fileprivate let show_bucketBounceTitle:String = "convId:laboratory too track"

/*: , code: :*/
fileprivate let const_fastChoiceMessage:String = "hypothesis stereo, code"
fileprivate let noti_voiceTitle:[Character] = [":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let k_chemicalMessage:String = "IM\u{63d2}入\u{6d88}息"
fileprivate let kFitPath:[Character] = ["\u{5931}","败","，","t","a","r","g","e","t","I","d",":"]

/*: "changeDirection" :*/
fileprivate let const_pairValue:String = "changeDhowever king frame white attractive"
fileprivate let showContentText:[Character] = ["i","r","e","c","t","i","o","n"]

/*: "rYMsgType" :*/
fileprivate let data_weekPath:String = "type phase item intervalrYMsgT"
fileprivate let data_allowSideTitle:String = "yvery"

/*: "GJ:CallCustom" :*/
fileprivate let k_proximatePath:String = "GJ:Cpunish rocket translation between to"
fileprivate let appStepOuterValue:String = "ttransport"

/*: "IM获取会话列表失败，code: :*/
fileprivate let constWithId:String = "Iblank"
fileprivate let noti_wellFormat:[Character] = ["获","取","会","话","列","表","\u{5931}","\u{8d25}","\u{ff0c}","c","o","d","e",":"]

/*: "msgInfo" :*/
fileprivate let main_tapPath:[Character] = ["m","s","g","I","n"]
fileprivate let mainPairPath:[Character] = ["f","o"]

/*: "needFold" :*/
fileprivate let dataMarginStr:String = "needFdecrease inform pic destination"
fileprivate let showScoreEgoWouldFormat:[Character] = ["o","l","d"]

/*: "group_ :*/
fileprivate let mainRocketMsg:[Character] = ["g","r","o","u","p","_"]

/*: "IM删除会话失败，convID: :*/
fileprivate let show_starSomeonePath:String = "IM删除会话失败oral able blank living"
fileprivate let userUtilityName:[Character] = ["，","c","o","n","v","I","D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let const_ceremonyPath:String = "IM\u{7f6e}顶会话失败，"
fileprivate let noti_incidentGoodComplaintData:String = "convId:build decrease recommendation"

/*: "SDK 正在连接到服务器" :*/
fileprivate let userTrailUrl:String = "giving supportSDK 正"
fileprivate let dataPrimaryCeaseStr:[Character] = ["服","\u{52a1}","器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let constEveryoneContent:[Character] = ["S","D","K"," ","已","经","成","功","连","接"]
fileprivate let appWriteUrl:String = "到服pair器"

/*: "IM连接服务器失败，code: :*/
fileprivate let app_boneMsg:String = "IM连\u{63a5}服"
fileprivate let k_pointTitle:[Character] = ["务","器","失","败","，","c","o","d","e",":"]

/*: , error: :*/
fileprivate let mainVerticalFormat:[Character] = [","]
fileprivate let main_shadeMsg:String = "broad cloud good translation error:"

/*: "IM账号被挤下线。" :*/
fileprivate let user_rangeMessage:String = "IM账号first once since"
fileprivate let data_springId:String = "。"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let main_earName:[UInt8] = []

/*: "Offline Notification" :*/
fileprivate let app_shOutsideName:String = "Offlincarry take"
fileprivate let notiObserverCombineClearlyName:String = "boldfic"
fileprivate let app_oldExPath:[Character] = ["n"]

/*: "Cancel" :*/
fileprivate let mainVarietyFruitMessage:String = "Cancelstyle by"

/*: "Re Login" :*/
fileprivate let app_drawingStr:[Character] = ["R","e"," ","L","o"]
fileprivate let const_handleValue:String = "giball"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LightConversationListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol MisestimationManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func opposite(data: [ModelReactiveCompatible])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func across(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func back(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum OriginalDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class LightConversationListener: NSObject {
@objcMembers public class LightConversationListener: NSObject {
    //: public static let shared = LightConversationListener()
    public static let shared = LightConversationListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [ModelReactiveCompatible] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [ModelReactiveCompatible] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [ModelReactiveCompatible] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [ModelReactiveCompatible] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [ModelReactiveCompatible] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: ModelReactiveCompatible]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.confirm()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func inmate() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(main_packageRobotValue, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func confirm() {
        //: func__removeAllDelegate()
        passByDelegate()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.step()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func step() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = ModelReactiveCompatible.squareCap(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = ModelReactiveCompatible.squareCap(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func observerOnicial() -> [ModelReactiveCompatible] {
        // 审核模式，在消息列表移除官方客服
        //: if CompositionReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue {
        if CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == DirtyMacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == DirtyMacroDefine.resGain() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension LightConversationListener {
extension LightConversationListener {
    //: func func__removeAllDelegate() {
    func passByDelegate() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func frontBurnerDelegate(_ delegate: MisestimationManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func am(_ delegate: MisestimationManagerDelegate) {
        //: self.func__invokeDelegate {
        self.turnDelegate {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func turnDelegate(_ invocation: (MisestimationManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! MisestimationManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension LightConversationListener {
extension LightConversationListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func stopMin() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            FinishThen.onicial(msg: (k_myContent + "\u{5931}\u{8d25}，c" + String(app_areaFormat.prefix(4))) + "\(code)" + (String(k_engageUrl)) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.confirm()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func carryDoing(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = CompositionReactiveCompatible.share.appUserConfigMode.uSig
        let uSigStr = CompositionReactiveCompatible.share.appUserConfigMode.uSig
        //: let uidStr = CompositionReactiveCompatible.share.loginUserMode.userID
        let uidStr = CompositionReactiveCompatible.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.comprise()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            FinishThen.onicial(msg: (app_mentallyName + main_goldMessage) + "\(code)" + (String(k_engageUrl)) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func notPriority() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || CompositionReactiveCompatible.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || CompositionReactiveCompatible.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.foolishness(showMsg: showTimeId)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension LightConversationListener {
extension LightConversationListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func comprise() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            FinishThen.onicial(msg: (show_strengthTitle + mainBelowName + String(showNeverTitle)) + "\(code)" + (String(k_engageUrl)) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func preserveMsg() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        ReminderRequestTool.spend { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(kHoldKey))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: main_beautyMessage, object: self, userInfo: [String(bytes: notiCapacityKey.map{turkeyCare(injury: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func hang(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        ReminderRequestTool.asAttribute { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(noti_alreadyBirthUrl.replacingOccurrences(of: "modify", with: "la"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(showForceMessage.prefix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = ModelReactiveCompatible.squareCap(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(show_apartmentMsg.replacingOccurrences(of: "shot", with: "n"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(appInfoFortUrl.prefix(5)) + String(noti_availableText))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension LightConversationListener: V2TIMAdvancedMsgListener {
extension LightConversationListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((mainAboveKey.replacingOccurrences(of: "contrast", with: "V") + String(noti_heartStr)))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            EnableceAgainReactiveCompatible.runningPrivacy().doingTask(msg: msg)
            //: return
            return
        }
        //: if msg.sender == CompositionReactiveCompatible.share.loginUserMode.userID { return }
        if msg.sender == CompositionReactiveCompatible.share.loginUserMode.userID { return }

        //: let dic = AdoptiveParentThen.parseTXMessageData(data: msg.customElem.data)
        let dic = AdoptiveParentThen.adjust(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(String(app_sageRegionComePath))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? SpotModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: k_needData, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(main_guidanceValue))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(mainPersonalUrl.prefix(6)) + "Gift") { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: noti_buildId, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if AdoptiveParentThen.checkVoiceMessage(message: msg) {
            if AdoptiveParentThen.packet(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = ManagerThen.totalSince(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                HelpReaderReactiveCompatible.burn(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.laden(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            FactorAnimatTool.shared.nearNight(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            FactorAnimatTool.shared.chatEnable(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(show_softwareFormat.suffix(6)) + String(k_commercialTitle.prefix(5)) + "ionMsg") { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            ReaderMedalThen.share.compare(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: ChockThen.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: ChockThen.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != DirtyMacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != DirtyMacroDefine.markMedalReplace() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.turnDelegate { $0.back?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            GenReactiveCompatible.shared.resource(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: app_oppositeMsg.map{bringOut(above: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: data_northwestUrl.map{leaseUtilize(dare: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: constExtendedUrl.map{mingleMangle(no: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: appCapturePath,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            OverAgainInfoManager.readRender(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            ChockThen.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: constWithoutStr, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: TwoCellData, isPrivete: Bool) {
    @objc public func purePrivete(targetId: String, msgCellData: TwoCellData, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.magnitudeo(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(constClubValue.prefix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.today(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.jump(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func magnitudeo(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = CompositionReactiveCompatible.share.loginUserMode.userID
        let senderStr = CompositionReactiveCompatible.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                FinishThen.onicial(msg: (k_chemicalMessage + String(kFitPath)) + "\(targetId)" + (String(const_fastChoiceMessage.suffix(6)) + String(noti_voiceTitle)) + "\(code)" + (String(k_engageUrl)) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func laden(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(const_pairValue.prefix(7)) + String(showContentText))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && SightReactiveCompatible.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(data_weekPath.suffix(6)) + data_allowSideTitle.replacingOccurrences(of: "very", with: "pe"))].stringValue == (String(k_proximatePath.prefix(4)) + "allCus" + appStepOuterValue.replacingOccurrences(of: "transport", with: "om")) && isSelf && SightReactiveCompatible.share.reserveUp()!.isKind(of: ParentControllerDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: constStreamName) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: constStreamName)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension LightConversationListener: V2TIMConversationListener {
extension LightConversationListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.today(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.jump(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.today(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.jump(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: CompositionReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        CompositionReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.turnDelegate {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.across?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func jump(conVersationList: [ModelReactiveCompatible]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if OverAgainInfoManager.primaryShared().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                OverAgainInfoManager.primaryShared().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.turnDelegate {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.opposite?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func map(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [ModelReactiveCompatible] = []

        // V2TIMConversation->ModelReactiveCompatible
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = ModelReactiveCompatible(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.executeWithinConvSave(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [ModelReactiveCompatible] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = PercentageInfoTable.user(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.quickBirth(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension LightConversationListener {
extension LightConversationListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func delay(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.map(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.jump(conVersationList: arr as? [ModelReactiveCompatible] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            FinishThen.onicial(msg: (constWithId.replacingOccurrences(of: "blank", with: "M") + String(noti_wellFormat)) + "\(code)" + (String(k_engageUrl)) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func hockeyPlayer(targetId: String) -> ModelReactiveCompatible? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func today(convList: [V2TIMConversation]) -> [ModelReactiveCompatible] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [ModelReactiveCompatible] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = ModelReactiveCompatible(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.executeWithinConvSave(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.quickBirth(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func quickBirth(convModel: ModelReactiveCompatible, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.step()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: OriginalDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - CompositionReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - CompositionReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = OverAgainInfoManager.chart(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = AdoptiveParentThen.parseTXMessageData(data: elemData)
            let dict = AdoptiveParentThen.adjust(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(String(app_sageRegionComePath))][(String(main_tapPath) + String(mainPairPath))][(String(dataMarginStr.prefix(5)) + String(showScoreEgoWouldFormat))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.ping(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.ping(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func ping(convList: [ModelReactiveCompatible], dataType: OriginalDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func advise(targetId: String, dataType: OriginalDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(constClubValue.prefix(4))) + "\(targetId)" : (String(mainRocketMsg)) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            OverAgainInfoManager.popEnable(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(CompositionReactiveCompatible.share.loginUserMode.userID, toUid: targetId)
            HelpReaderReactiveCompatible.voiceDrown(CompositionReactiveCompatible.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.comprise()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            FinishThen.onicial(msg: (String(show_starSomeonePath.prefix(8)) + String(userUtilityName)) + "\(convID)" + (String(const_fastChoiceMessage.suffix(6)) + String(noti_voiceTitle)) + "\(code)" + (String(k_engageUrl)) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func sommelier(convModel: ModelReactiveCompatible, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            FinishThen.onicial(msg: (const_ceremonyPath + String(noti_incidentGoodComplaintData.prefix(7))) + "\(convModel.conversationID)" + (String(const_fastChoiceMessage.suffix(6)) + String(noti_voiceTitle)) + "\(code)" + (String(k_engageUrl)) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.ping(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.ping(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension LightConversationListener: V2TIMSDKListener {
extension LightConversationListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(userTrailUrl.suffix(5)) + "\u{5728}连接到" + String(dataPrimaryCeaseStr)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(constEveryoneContent) + appWriteUrl.replacingOccurrences(of: "pair", with: "务")))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        FinishThen.onicial(msg: (app_boneMsg + String(k_pointTitle)) + "\(code)" + (String(mainVerticalFormat) + String(main_shadeMsg.suffix(7))) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        FinishThen.onicial(msg: (String(user_rangeMessage.prefix(4)) + "\u{88ab}挤下线" + data_springId.capitalized))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        FileSocketManager.shared.programBy(userLoginOut: true)
        //: LightConversationListener.shared.isConnection = false
        LightConversationListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (SceneKingfisherCompatible.shared as! SceneKingfisherCompatible).searched()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: main_earName.reversed(), encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = ReminderAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if CosSweetReactiveCompatible.share.interfaceLang == ShadesOfTransformable.ar.rawValue || CosSweetReactiveCompatible.share.interfaceLang == ShadesOfTransformable.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            CosSweetReactiveCompatible.share.interfaceLang == ShadesOfTransformable.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.counteraction(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        YetAlertShow.trackSpace()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        YetAlertShow.trackSpace(title: (String(app_shOutsideName.prefix(6)) + "e No" + notiObserverCombineClearlyName.replacingOccurrences(of: "bold", with: "ti") + "atio" + String(app_oldExPath)).localized, message: msg, leftBtnTitle: (String(mainVarietyFruitMessage.prefix(6))).localized, rightBtnTitle: (String(app_drawingStr) + const_handleValue.replacingOccurrences(of: "ball", with: "n")).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            SelectedThen.panCaptureShared().miss()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            MidRequestTool.disappear { t in
                //: ProgressHUD.dismiss()
                PointerReactiveCompatible.motivationCharmDismiss()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: constMinNameTitle, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            MidRequestTool.audienceMiss { t in
                //: if t {
                if t {
                    //: LightConversationListener.shared.func__LogingIn { _ in
                    LightConversationListener.shared.carryDoing { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if SelectedThen.panCaptureShared().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(SelectedThen.panCaptureShared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    FileSocketManager.shared.modifyBy()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
