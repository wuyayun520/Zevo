
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let data_cryName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "F5F5F5" :*/
fileprivate let show_normalName:String = "have5have5have5"

/*: "#128CFF" :*/
fileprivate let mainServeBitPath:String = "oval delivery weight custom#128CF"
fileprivate let showCoatStreamLoopName:String = "am"

/*: "Feedback" :*/
fileprivate let appStereoName:String = "Feedbackpromotion baseball connection week chin"

/*: "#8370FF" :*/
fileprivate let showAdminFormat:String = "#8370"
fileprivate let main_poId:String = "bot"

/*: "Quick Greeting" :*/
fileprivate let app_formId:String = "while till interestQuick "

/*: "Beauty Photos" :*/
fileprivate let app_seaActiveMessage:[Character] = ["B","e","a","u","t","y"," "]
fileprivate let kSuccessMessage:String = "Photosvertical assist watch"

/*: "url" :*/
fileprivate let const_firstPath:[Character] = ["u","r","l"]

/*: "unlockGift" :*/
fileprivate let noti_flagId:String = "topiclo"

/*: "isBurn" :*/
fileprivate let const_installationMsg:[UInt8] = [0x40,0x4a,0x19,0x4c,0x49,0x45]

fileprivate func randomPanel(analysis num: UInt8) -> UInt8 {
    let value = Int(num) - 215
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let userHerFlagTitle:String = "lab remote viewgiftId"

/*: "rYMsgType" :*/
fileprivate let dataBeatName:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let show_selectName:String = "GJ:Cadditional official"
fileprivate let kEliteKey:String = "USTOM"

/*: "changeDirection" :*/
fileprivate let dataMpDistanceKey:String = "yesterday leading max bite establishchangeD"
fileprivate let constCarveUrl:[Character] = ["i","r","e"]
fileprivate let user_behindRevenueMessage:String = "CTION"

/*: "extra" :*/
fileprivate let k_southContent:String = "extrup"

/*: "msgInfo" :*/
fileprivate let data_informationStarMessage:[Character] = ["m","s","g","I","n"]
fileprivate let showStepOldFormat:[Character] = ["f","o"]

/*: "toUid" :*/
fileprivate let main_easePath:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let dataGloryPath:String = "wantGifsubmit describe it branch"
fileprivate let main_presentName:String = "icon"

/*: "jumpKey" :*/
fileprivate let noti_ovalRocketKey:[Character] = ["j","u","m","p","K","e"]
fileprivate let mainDownUponMessage:[Character] = ["y"]

/*: "truePersonAuth" :*/
fileprivate let k_accountTitle:String = "ting remove book plantruePers"
fileprivate let app_increaseText:String = "err schedule perceive tabonAuth"

/*: "logType" :*/
fileprivate let kDiscountId:String = "logTypewhich recommendation"

/*: "msgType" :*/
fileprivate let dataBeautyValue:[UInt8] = [0x1e,0x0,0x14,0x27,0xa,0x3,0x16]

private func broadPlatformMulti(past num: UInt8) -> UInt8 {
    return num ^ 115
}

/*: "txt" :*/
fileprivate let data_towardUrl:String = "schemet"

/*: "type" :*/
fileprivate let user_curHereKey:String = "providerype"

/*: "QuickMsg" :*/
fileprivate let dataDrawingName:String = "loop assert transmission kingQuickMsg"

/*: "params" :*/
fileprivate let dataOuterFormat:[UInt8] = [0xa6,0xb7,0xa4,0xb7,0xbb,0xa5]

/*: "content" :*/
fileprivate let kWeHistoryDotTitle:[UInt8] = [0x64,0x68,0x69,0x73,0x62,0x69,0x73]

private func billOfFare(window num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "txtId" :*/
fileprivate let main_responseId:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let noti_retMessage:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let data_phoneUrl:String = "TxtMsgmale low rem request stage"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let dataHundredValue:[UInt8] = [0x65,0x72,0x75,0x64,0x65,0x63,0x6f,0x72,0x50,0x20,0x2e,0x64,0x65,0x6c,0x69,0x61,0x66,0x20,0x64,0x61,0x6f,0x6c,0x6e,0x77,0x6f,0x64,0x20,0x6f,0x6c,0x6c,0x65,0x68,0x20,0x65,0x63,0x69,0x6f,0x56]

/*: "length" :*/
fileprivate let main_ovalUrl:String = "lattractivegth"

/*: "audio" :*/
fileprivate let main_toneMsg:String = "audiblack"

/*: "audioId" :*/
fileprivate let main_sitName:[Character] = ["a","u","d","i","o","I","d"]

/*: "audioLength" :*/
fileprivate let main_halfValue:[UInt8] = [0x2e,0x3a,0x2b,0x26,0x20,0x3,0x2a,0x21,0x28,0x3b,0x27]

private func nationalService(under num: UInt8) -> UInt8 {
    return num ^ 79
}

/*: "audioSandbox" :*/
fileprivate let k_sinkKey:String = "randomudio"

/*: "senderId" :*/
fileprivate let app_entityFormat:[UInt8] = [0xe,0x18,0x13,0x19,0x18,0xf,0x34,0x19]

private func injuryMomentRequire(advise num: UInt8) -> UInt8 {
    return num ^ 125
}

/*: "AudioMsg" :*/
fileprivate let user_scopeName:[Character] = ["A","u","d","i","o","M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let data_compriseId:String = "Can\'t "
fileprivate let main_mineStr:String = "choose confirmlank m"
fileprivate let appLinkCommentStr:String = "GE"

/*: "quoteMsgId" :*/
fileprivate let data_nuclearPlatMsg:String = "close good comply additional faultquoteM"

/*: "quoteMsgInfo" :*/
fileprivate let show_bringName:String = "QUOT"
fileprivate let data_foreFormat:String = "Infobetter be"

/*: "contentType" :*/
fileprivate let dataColorLaterText:[UInt8] = [0x90,0x9c,0x9d,0x87,0x96,0x9d,0x87,0xa7,0x8a,0x83,0x96]

/*: "ImgMsg" :*/
fileprivate let userFinancialId:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let app_easeUnknownData:String = "iagainst"

/*: "imageUri" :*/
fileprivate let const_aboveTitle:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "customSend" :*/
fileprivate let kTipActUrl:[UInt8] = [0x39,0x2f,0x29,0x2e,0x35,0x37,0x9,0x3f,0x34,0x3e]

/*: "pic" :*/
fileprivate let data_modeValuePath:[Character] = ["p","i","c"]

/*: "VideoMsg" :*/
fileprivate let main_profileData:[Character] = ["V","i","d","e","o"]
fileprivate let data_groupFlushlyName:String = "Msginstallation police restriction remove variety"

/*: "video" :*/
fileprivate let notiMedalMessage:String = "goingideo"

/*: "coverImg" :*/
fileprivate let main_singleCreateerFormat:[UInt8] = [0xe5,0xe9,0xf0,0xe3,0xf4,0xcf,0xeb,0xe1]

/*: "videoUrl" :*/
fileprivate let dataShuttleUrl:[UInt8] = [0xed,0xf2,0xff,0xfe,0xf4,0xce,0xe9,0xf7]

private func borderDatabase(nuclear num: UInt8) -> UInt8 {
    return num ^ 155
}

/*: "duration" :*/
fileprivate let data_stereoName:[UInt8] = [0x6e,0x6f,0x69,0x74,0x61,0x72,0x75,0x64]

/*: "len" :*/
fileprivate let const_twentyTitle:[UInt8] = [0xcf,0xc6,0xcd]

private func elaboratedBody(bar num: UInt8) -> UInt8 {
    return num ^ 163
}

/*: "Documents/User/voice/" :*/
fileprivate let dataDefinitionKey:String = "giving customer opportunity piDocument"
fileprivate let constSpeakValue:[Character] = ["s","/","U","s","e","r","/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let user_restoreName:String = "off hide computerDocume"
fileprivate let kScaleValue:String = "item ref personnts/U"
fileprivate let app_materialMessage:String = "a hellocord/"

/*: "msgIncome" :*/
fileprivate let const_sayData:[Character] = ["m","s","g","I","n","c","o"]
fileprivate let showPerceivePath:String = "ME"

/*: "mfBean" :*/
fileprivate let app_frameStrengthStr:[UInt8] = [0x87,0x80,0x5c,0x7f,0x7b,0x88]

fileprivate func pairHolder(arc num: UInt8) -> UInt8 {
    let value = Int(num) + 230
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "messageType" :*/
fileprivate let k_reKey:[Character] = ["m","e","s"]
fileprivate let dataReduceTimingSenseUrl:String = "location see capacity span administrativesageType"

/*: "WantGift" :*/
fileprivate let noti_panelStillStr:[Character] = ["W","a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let dataPriorityStr:String = "catch good publicity happy_Pri"
fileprivate let const_fromTitle:String = "hatTipsTaspect parent till bright consent"
fileprivate let show_instanceText:String = "itle_personal absolute info medication license"

/*: "msgId" :*/
fileprivate let appAroundFormat:String = "plat fire curve drownmsgId"

/*: "totalMfCoin" :*/
fileprivate let main_netId:String = "totalMdesign her beautiful forehead there"
fileprivate let userHeadValue:[Character] = ["f","C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let mainInstallData:String = "presentation"
fileprivate let notiTenderUrl:String = "ttachSadministrative rest location brush stick"

/*: "intimatePhoto" :*/
fileprivate let k_ceaseInstallationTitle:String = "policytimat"
fileprivate let const_qualityMessage:String = "ePhotodiscourse year delivery certain write"

/*: "totalIntimate" :*/
fileprivate let app_comeText:[Character] = ["t","o","t","a","l","I"]
fileprivate let appOppositeName:String = "custom"
fileprivate let k_accelerateText:String = "identifyimaidentifye"

/*: "uid" :*/
fileprivate let main_disableMessage:[UInt8] = [0x27,0x1b,0x16]

fileprivate func twoCrush(ignore num: UInt8) -> UInt8 {
    let value = Int(num) - 178
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "intimate" :*/
fileprivate let userBrushKey:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let showSideMsg:[UInt8] = [0xdb,0xf9,0xf6,0xbf,0xec,0xb8,0xed,0xeb,0xfd,0xb8,0xec,0xf0,0xfd,0xb8,0xfe,0xed,0xf6,0xfb,0xec,0xf1,0xf7,0xf6,0xb8,0xfc,0xed,0xfd,0xb8,0xec,0xf7,0xb8,0xe1,0xf7,0xed,0xea,0xb8,0xf4,0xf9,0xfb,0xf3,0xb8,0xf7,0xfe,0xb8,0xf1,0xf6,0xec,0xf1,0xf5,0xf9,0xfb,0xe1,0xb8,0xf4,0xfd,0xee,0xfd,0xf4,0xb6]

private func rocketOver(success num: UInt8) -> UInt8 {
    return num ^ 152
}

/*: "giftNum" :*/
fileprivate let user_clearlyBanPath:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let main_towardData:String = "pkgItting lay follow"
fileprivate let user_replacementPlayStr:[Character] = ["e","m","s","e","t","I","d"]

/*: "from" :*/
fileprivate let data_createUrl:String = "coatom"

/*: "wantGiftId" :*/
fileprivate let showFarUrl:[Character] = ["w","a","n","t","G","i","f","t","I"]
fileprivate let show_wrapTitle:String = "around"

/*: "WantGiftMsg" :*/
fileprivate let main_layUrl:[Character] = ["W","a","n","t","G","i","f","t"]
fileprivate let k_normallyUrl:String = "orientation time circle docMsg"

/*: "gift" :*/
fileprivate let appResistanceFormat:String = "giinterested"

/*: "View info" :*/
fileprivate let userAssessmentKey:String = "Viewkind with dam user"
fileprivate let data_containBugUrl:String = "never"

/*: "Notes" :*/
fileprivate let const_listenMessage:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let constMaybeEqualPairData:[Character] = ["R","e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let app_numberData:String = "Viewtick above tip personal game"
fileprivate let noti_clipBottomData:String = "eceipts?technical method guide"

/*: "She" :*/
fileprivate let notiBeginGiveName:String = "processing incidentShe"

/*: "%@ is using too low a version" :*/
fileprivate let dataClearStr:String = "certain across matter%@ is u"
fileprivate let userExposureStr:String = "right deliver to timesing "
fileprivate let dataServerEssayMsg:String = "low base launch raw rounding"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let data_cosUrl:[Character] = ["S","h"]
fileprivate let user_yaUrl:[Character] = ["o","o","t"," ","v","i","d","e","o"," ","a","t"," ","l","e","a","s","t"," ","5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let k_boyValue:[Character] = ["U","p","l","o","a","d"," "]
fileprivate let showConferenceValue:String = "ftotaliled"

/*: "播放状态: :*/
fileprivate let main_birdTickMsg:String = "\u{64ad}放状态:"

/*: "c2c_" :*/
fileprivate let kRemainingStr:String = "c2c_upon center reader cram square"

/*: "msg" :*/
fileprivate let mainContactContrastPath:String = "mthreeg"

/*: "tips" :*/
fileprivate let noti_tunPath:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let noti_slowValue:[UInt8] = [0x6c,0x6c,0x61,0x63,0x20,0x61,0x20,0x40,0x25,0x20,0x65,0x76,0x69,0x67,0x20,0x6f,0x74,0x20,0x65,0x72,0x75,0x74,0x61,0x65,0x66,0x20,0x74,0x61,0x68,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x68,0x74,0x20,0x65,0x73,0x75,0x20,0x6e,0x61,0x63,0x20,0x75,0x6f,0x59]

/*: "cellData" :*/
fileprivate let show_yourAtFormat:String = "partner"
fileprivate let mainAgainText:[Character] = ["e","l","l","D","a","t","a"]

/*: "System notification" :*/
fileprivate let noti_beginAmGreatestId:String = "System nconscious normally"
fileprivate let k_hairUrl:String = "O"
fileprivate let notiBoldThreadContent:[Character] = ["t","i","f","i","c","a","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ParentControllerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ShadeFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class ParentControllerDelegate: MergeContactViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = ChinMeasurable()
    private var userWrap = ChinMeasurable() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ShadeFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ChinMeasurable = ChinMeasurable()) {
    init(chatID: String, isFrom: ShadeFromEnum = .Normal, userWrap: ChinMeasurable = ChinMeasurable()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        ParentControllerDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        ChockThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == DirtyMacroDefine.getXiaoMiID() {
        if targetId == DirtyMacroDefine.markMedalReplace() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == DirtyMacroDefine.getCustomerServiceID() {
        } else if targetId == DirtyMacroDefine.resGain() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: data_cryName.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if CompositionReactiveCompatible.share.appConfigMode.disableShootScreen {
            if CompositionReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = MpCarrierThen(frame: CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        filterBalance()
        //: createUI()
        profile()
        //: createUIConstraints()
        createMentally()
        //: bindInteraction()
        serrationSumeraction()
        //: addTapGestureRecognizer()
        mixInThroughLimit()
        //: func__requestChatInfo()
        official()
        //: func__getFillDraftText()
        nose()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        WhitePlayerDelegate.shared.modeDaskDelegate()
        //: func__addVoiceNotification()
        putEnableJudge()
        //: addReadPeceiptNotification()
        oralPresentationSkim()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            drawGenerate()
            //: func__TipsNotification()
            materialNose()
            //: if CompositionReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.male.rawValue {
                //: func__setReplyTipsView()
                midRefuse()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        script(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        script(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            darkContent()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            MergeRatingManager.shared.enabled()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: ResponseActualTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ResponseActualTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (show_normalName.replacingOccurrences(of: "have", with: "F")))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(BalanceCosMsgCell.self, forCellReuseIdentifier: BalanceCosMsgCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(CastMsgCell.self, forCellReuseIdentifier: CastMsgCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(ProposalViewCell.self, forCellReuseIdentifier: ProposalViewCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(FormalMsgCell.self, forCellReuseIdentifier: FormalMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(ContentViewCell.self, forCellReuseIdentifier: ContentViewCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(CellView.self, forCellReuseIdentifier: CellView.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(ShadeReactiveCompatible.self, forCellReuseIdentifier: ShadeReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(BalanceCellView.self, forCellReuseIdentifier: BalanceCellView.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(OuterMsgCell.self, forCellReuseIdentifier: OuterMsgCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: ReaderView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(CellThen.self, forCellReuseIdentifier: ReaderView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: AdministratorMenuManager = {
        //: let m = TalkingCallMenuManager()
        let m = AdministratorMenuManager()
        //: m.uid = targetId
        m.uid = targetId
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.front()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: FormatViewDelegate = {
        //: var type: InputViewType = .PrivateChat
        var type: OriginalViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = FormatViewDelegate(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(mainServeBitPath.suffix(6)) + showCoatStreamLoopName.replacingOccurrences(of: "am", with: "F")))!, .font: UIFont.processAcross(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(appStereoName.prefix(8))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(changeSnap), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: RoundThen = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = RoundThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: NameEvenReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = NameEvenReactiveCompatible(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.processAcross(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (showAdminFormat.capitalized + main_poId.replacingOccurrences(of: "bot", with: "FF"))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(app_formId.suffix(6)) + "Greeting").localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.rawMonthCombine().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(plusClick), for: .touchUpInside)
        //: btn.isHidden = !CompositionReactiveCompatible.share.appUserConfigMode.showDirectGreet || CompositionReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || CompositionReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !CompositionReactiveCompatible.share.appUserConfigMode.showDirectGreet || CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.male.rawValue || CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: LivingMergeThen? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = LivingMergeThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: ParentThen = {
        //: let v = TalkingSelectTranslationView()
        let v = ParentThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.rendering(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: BeneathDirtyReactiveCompatible = {
        //: let v = TalkingTranslationResultView()
        let v = BeneathDirtyReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.op(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.processAcross(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (showAdminFormat.capitalized + main_poId.replacingOccurrences(of: "bot", with: "FF"))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(app_seaActiveMessage) + String(kSuccessMessage.prefix(6))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.rawMonthCombine().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(addClick), for: .touchUpInside)
        //: btn.isHidden = CompositionReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || CompositionReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.male.rawValue || CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: LiveReactiveCompatible = {
        //: let v = TalkingLiveRoomGamesView()
        let v = LiveReactiveCompatible()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: RecoverThen = {
        //: let view = TalkingPrivateBannerView.init()
        let view = RecoverThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: MeetAdminView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = MeetAdminView()
        //: v.isHidden = (CompositionReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: NotesTopView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = NotesTopView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.roundQuitView()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension ParentControllerDelegate: WealthyThen {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func single(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if CompositionReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if CompositionReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.homeView(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension ParentControllerDelegate: HorizonHung {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func plusClick() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        ChockThen.connectSh(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.foolishness(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.baseballTeamDict(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func addClick() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        ChockThen.earlyish(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = OptPhotoVc()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().funReply(urlStr: dict?[(String(const_firstPath))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(noti_flagId.replacingOccurrences(of: "topic", with: "un") + "ckGift")] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = ChatModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: const_installationMsg.map{randomPanel(analysis: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(userHerFlagTitle.suffix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.refer(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TwoCellData! {
    func controller(_: ResponseActualTableViewController!, message data: V2TIMMessage!) -> TwoCellData! {
        //: let dic = AdoptiveParentThen.parseTXMessageData(data: data.customElem.data)
        let dic = AdoptiveParentThen.adjust(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = ManagerThen.managerModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: LettuceThen.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(dataBeatName))].stringValue == (String(show_selectName.prefix(4)) + "allC" + kEliteKey.lowercased()) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(dataMpDistanceKey.suffix(7)) + String(constCarveUrl) + user_behindRevenueMessage.lowercased())].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(k_southContent.replacingOccurrences(of: "up", with: "a"))][(String(data_informationStarMessage) + String(showStepOldFormat))][(String(main_easePath))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = SortCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = SortCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: MidReactiveCompatible.self) == true ||
        if cellData?.isKind(of: MidReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: MilitaryCellData.self) == true ||
            cellData?.isKind(of: MilitaryCellData.self) == true ||
            //: cellData?.isKind(of: SortCellData.self) == true ||
            cellData?.isKind(of: SortCellData.self) == true ||
            //: cellData?.isKind(of: MovieReactiveCompatible.self) == true {
            cellData?.isKind(of: MovieReactiveCompatible.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            ChockThen.data(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            ChockThen.doingDirect(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TwoCellData!) -> ReaderView! {
    func cilium(_: ResponseActualTableViewController!, origin data: TwoCellData!) -> ReaderView! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = positiveInput(data: data)

        //: if data.isKind(of: SortCellData.self) {
        if data.isKind(of: SortCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = BalanceCosMsgCell(style: .default, reuseIdentifier: BalanceCosMsgCell.className())
            //: let cellData = data as! SortCellData
            let cellData = data as! SortCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.iconography() {
                //: if cellData.msgModel.quoteMsgInfo.uid == CompositionReactiveCompatible.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == CompositionReactiveCompatible.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = CompositionReactiveCompatible.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = CompositionReactiveCompatible.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: MilitaryCellData.self) {
        if data.isKind(of: MilitaryCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = CastMsgCell(style: .default, reuseIdentifier: CastMsgCell.className())
            //: let cellData = data as! MilitaryCellData
            let cellData = data as! MilitaryCellData
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: MovieReactiveCompatible.self) {
        if data.isKind(of: MovieReactiveCompatible.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = ProposalViewCell(style: .default, reuseIdentifier: ProposalViewCell.className())
            //: let cellData = data as! MovieReactiveCompatible
            let cellData = data as! MovieReactiveCompatible
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: OpenceMoreCellData.self) {
        if data.isKind(of: OpenceMoreCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = FormalMsgCell(style: .default, reuseIdentifier: FormalMsgCell.className())
            //: let cellData = data as! OpenceMoreCellData
            let cellData = data as! OpenceMoreCellData
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: FactorSelectedCellData.self) {
        if data.isKind(of: FactorSelectedCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = ContentViewCell(style: .default, reuseIdentifier: ContentViewCell.className())
            //: let cellData = data as! FactorSelectedCellData
            let cellData = data as! FactorSelectedCellData
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = SuccessHandyJSON()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.genre(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(dataGloryPath.prefix(7)) + main_presentName.replacingOccurrences(of: "icon", with: "t")))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: MidReactiveCompatible.self) {
        if data.isKind(of: MidReactiveCompatible.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = BalanceCellView(style: .default, reuseIdentifier: BalanceCellView.className())
            //: let cellData = data as! MidReactiveCompatible
            let cellData = data as! MidReactiveCompatible
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: LettuceThen.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = ShadeReactiveCompatible(style: .default, reuseIdentifier: ShadeReactiveCompatible.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! LettuceThen
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: PreviouslyFactorCompartmentTipsInformationCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = CellView(style: .default, reuseIdentifier: CellView.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! PreviouslyFactorCompartmentTipsInformationCellData
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: ListenCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = OuterMsgCell(style: .default, reuseIdentifier: OuterMsgCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! ListenCellData
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: ReaderView!) {
    func bean(_: ResponseActualTableViewController!, avatar cell: ReaderView!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != DirtyMacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != DirtyMacroDefine.resGain() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: SightReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
            SightReactiveCompatible.share.temporaryUid(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: ReaderView!) {
    func breadth(_: ResponseActualTableViewController!,  incumbent cell: ReaderView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: CastMsgCell.self) { // 图片
            //: let imageCellData = cell.messageData as! MilitaryCellData
            let imageCellData = cell.messageData as! MilitaryCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = MedalViewController(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.stopAppear(imageCellData, photoResult: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.errorDescribe(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: app_changeStr)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.ember(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.reconcile(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                volition()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(show_blockTopUrl)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = HostEvenPhotoModel()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = LivingMomentModel()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [HostEvenPhotoModel] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = CompositionViewController(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                volition()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ProposalViewCell.self) { // 视频
            //: let videoCellData = cell.messageData as! MovieReactiveCompatible
            let videoCellData = cell.messageData as! MovieReactiveCompatible
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = HostViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                volition()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = ExecMidViewController(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.model(videoCellData, timeWantAge: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.errorDescribe(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: app_changeStr)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.ember(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.reconcile(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                volition()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: BalanceCellView.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! BalanceCellView
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! MidReactiveCompatible, cellView: audioCell)
            generate(cellData: audioCell.cellData as! MidReactiveCompatible, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: BalanceCosMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! BalanceCosMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = AdoptiveParentThen.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = AdoptiveParentThen.adjust(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_southContent.replacingOccurrences(of: "up", with: "a"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(noti_ovalRocketKey) + String(mainDownUponMessage))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(noti_ovalRocketKey) + String(mainDownUponMessage))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(k_accountTitle.suffix(8)) + String(app_increaseText.suffix(6))) {
                    //: SightReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
                    SightReactiveCompatible.share.bringExposure(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ParentJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: SightReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    SightReactiveCompatible.share.inscription(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(dataBeatName))] as? String == (String(show_selectName.prefix(4)) + "allC" + kEliteKey.lowercased()) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(data_informationStarMessage) + String(showStepOldFormat))][(String(kDiscountId.prefix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    cram()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    occurrence()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: CellView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! CellView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = AdoptiveParentThen.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = AdoptiveParentThen.adjust(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(k_southContent.replacingOccurrences(of: "up", with: "a"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(noti_ovalRocketKey) + String(mainDownUponMessage))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ParentJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: SightReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    SightReactiveCompatible.share.inscription(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: ShadeReactiveCompatible.self) {
            //: SightReactiveCompatible.share.func__pushToUserDetailVC(uid: targetId)
            SightReactiveCompatible.share.temporaryUid(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: ReaderView!) {
    func max(_: ResponseActualTableViewController!, nearCur cell: ReaderView!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: BlockCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: CastMsgCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! CastMsgCell
            //: cellData = imageCell.cellData as! MilitaryCellData
            cellData = imageCell.cellData as! MilitaryCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ProposalViewCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! ProposalViewCell
            //: cellData = videoCell.cellData as! MovieReactiveCompatible
            cellData = videoCell.cellData as! MovieReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: BalanceCellView.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! BalanceCellView
            //: cellData = audioCell.cellData as! MidReactiveCompatible
            cellData = audioCell.cellData as! MidReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: BalanceCosMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! BalanceCosMsgCell
            //: cellData = textCell.cellData as! SortCellData
            cellData = textCell.cellData as! SortCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TwoCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: TwoCellData? = ManagerThen.less(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: MovieReactiveCompatible.self) {
                if reCellData!.isKind(of: MovieReactiveCompatible.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    responseWith(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    chemicalAgent(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TwoCellData!) {
    func historyRootMessage(_: ResponseActualTableViewController!, roundMessage cellData: TwoCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = ChockThen.earlier(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TwoCellData!) {
    func method(_: ResponseActualTableViewController!, last cellData: TwoCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        ChockThen.dealNeedTarget(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TwoCellData) -> TwoCellData {
    private func positiveInput(data: TwoCellData) -> TwoCellData {
        //: let isSelf = data.msgModel.user.uid == Int(CompositionReactiveCompatible.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(CompositionReactiveCompatible.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if CompositionReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if CompositionReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = CompositionReactiveCompatible.share.loginUserMode.headPic!
                data.msgModel.user.headPic = CompositionReactiveCompatible.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = CompositionReactiveCompatible.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = CompositionReactiveCompatible.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func since() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!: (data_towardUrl.replacingOccurrences(of: "scheme", with: "tx"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(user_curHereKey.replacingOccurrences(of: "provider", with: "t")): (String(dataDrawingName.suffix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_informationStarMessage) + String(showStepOldFormat)): msgInfo, String(bytes: dataOuterFormat.map{$0^214}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ManagerThen.coordinate(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.chemicalAgent(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func baseballTeamDict(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: kWeHistoryDotTitle.map{billOfFare(window: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: kWeHistoryDotTitle.map{billOfFare(window: $0)}, encoding: .utf8)!: content, (String(main_responseId)): greetId, String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!: (data_towardUrl.replacingOccurrences(of: "scheme", with: "tx"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: noti_retMessage.reversed(), encoding: .utf8)!: content, (String(main_responseId)): greetId, (user_curHereKey.replacingOccurrences(of: "provider", with: "t")): (String(data_phoneUrl.prefix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(data_informationStarMessage) + String(showStepOldFormat)): msgInfo, String(bytes: dataOuterFormat.map{$0^214}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = ManagerThen.coordinate(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.chemicalAgent(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            ManagerReactiveCompatible.shared.todaySelectPic(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.foolishness(showMsg: String(bytes: dataHundredValue.reversed(), encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        ChockThen.switchly(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(main_ovalUrl.replacingOccurrences(of: "attractive", with: "en"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!: (main_toneMsg.replacingOccurrences(of: "black", with: "o")),
                               //: "audioId": greetId,
                               (String(main_sitName)): greetId,
                               //: "audioLength": length,
                               String(bytes: main_halfValue.map{nationalService(under: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (k_sinkKey.replacingOccurrences(of: "random", with: "a") + "Sandbox"): model.unZipFilePath,
                               //: "senderId": CompositionReactiveCompatible.share.loginUserMode.userID]
                               String(bytes: app_entityFormat.map{injuryMomentRequire(advise: $0)}, encoding: .utf8)!: CompositionReactiveCompatible.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(main_sitName)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: main_halfValue.map{nationalService(under: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (k_sinkKey.replacingOccurrences(of: "random", with: "a") + "Sandbox"): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (user_curHereKey.replacingOccurrences(of: "provider", with: "t")): (String(user_scopeName))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(data_informationStarMessage) + String(showStepOldFormat)): msgInfo, String(bytes: dataOuterFormat.map{$0^214}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ManagerThen.coordinate(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.chemicalAgent(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func op(msgStr: String, quoteModel: RankModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            foolishness(showMsg: (data_compriseId + "send b" + String(main_mineStr.suffix(6)) + "essa" + appLinkCommentStr.lowercased()).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: kWeHistoryDotTitle.map{billOfFare(window: $0)}, encoding: .utf8)!: temStr, String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!: (data_towardUrl.replacingOccurrences(of: "scheme", with: "tx"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: noti_retMessage.reversed(), encoding: .utf8)!: temStr, (user_curHereKey.replacingOccurrences(of: "provider", with: "t")): (String(data_phoneUrl.prefix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(data_informationStarMessage) + String(showStepOldFormat)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(data_nuclearPlatMsg.suffix(6)) + "sgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(show_bringName.lowercased() + "eMsg" + String(data_foreFormat.prefix(4)))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: dataOuterFormat.map{$0^214}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ManagerThen.coordinate(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? RankModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            chemicalAgent(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func refer(img: UIImage, sendModel: ChatModel) {
        //: let imagePath = MilitaryCellData.getChatImagePath(image: img)
        let imagePath = MilitaryCellData.overlook(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: dataColorLaterText.map{$0^243}, encoding: .utf8)!: (String(userFinancialId)), String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!: (app_easeUnknownData.replacingOccurrences(of: "against", with: "mg")), String(bytes: const_aboveTitle.reversed(), encoding: .utf8)!: imagePath, String(bytes: kTipActUrl.map{$0^90}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(data_modeValuePath)): imagePath,
                                        //: "type": "ImgMsg",
                                        (user_curHereKey.replacingOccurrences(of: "provider", with: "t")): (String(userFinancialId)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: const_installationMsg.map{randomPanel(analysis: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(userHerFlagTitle.suffix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_informationStarMessage) + String(showStepOldFormat)): msgInfo, String(bytes: dataOuterFormat.map{$0^214}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ManagerThen.coordinate(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            chemicalAgent(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TwoCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func responseWith(msgData: TwoCellData?, sendModel: ChatModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: dataColorLaterText.map{$0^243}, encoding: .utf8)!: (String(main_profileData) + String(data_groupFlushlyName.prefix(3))), String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!: (notiMedalMessage.replacingOccurrences(of: "going", with: "v")), String(bytes: kTipActUrl.map{$0^90}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(user_curHereKey.replacingOccurrences(of: "provider", with: "t")): (String(main_profileData) + String(data_groupFlushlyName.prefix(3))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: main_singleCreateerFormat.map{$0^134}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: dataShuttleUrl.map{borderDatabase(nuclear: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: data_stereoName.reversed(), encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: const_installationMsg.map{randomPanel(analysis: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(userHerFlagTitle.suffix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(data_informationStarMessage) + String(showStepOldFormat)): msgInfo, String(bytes: dataOuterFormat.map{$0^214}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = ManagerThen.coordinate(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.connection(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.purchaseVia(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.color(cellData, publicTransportHide: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.purchaseVia(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.cell(dict, clear: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        chemicalAgent(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func voice(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!: (main_toneMsg.replacingOccurrences(of: "black", with: "o")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: main_halfValue.map{nationalService(under: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (k_sinkKey.replacingOccurrences(of: "random", with: "a") + "Sandbox"): voicePath,
                       //: "senderId": CompositionReactiveCompatible.share.loginUserMode.userID]
                       String(bytes: app_entityFormat.map{injuryMomentRequire(advise: $0)}, encoding: .utf8)!: CompositionReactiveCompatible.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(k_sinkKey.replacingOccurrences(of: "random", with: "a") + "Sandbox"): voicePath,
                         //: "type": "AudioMsg",
                         (user_curHereKey.replacingOccurrences(of: "provider", with: "t")): (String(user_scopeName)),
                         //: "len": "\(voiceTime)"]
                         String(bytes: const_twentyTitle.map{elaboratedBody(bar: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_informationStarMessage) + String(showStepOldFormat)): msgInfo, String(bytes: dataOuterFormat.map{$0^214}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ManagerThen.coordinate(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            chemicalAgent(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TwoCellData, reSend: Bool) {
    private func chemicalAgent(msgData: TwoCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            foolishness(showMsg: showTimeId)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(main_easePath))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: MovieReactiveCompatible.self) == false && msgData.isKind(of: FactorSelectedCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: MovieReactiveCompatible.self) == false, msgData.isKind(of: FactorSelectedCellData.self) == false, params[(user_curHereKey.replacingOccurrences(of: "provider", with: "t"))] as? String != (String(dataDrawingName.suffix(8))) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.connection(msgData)
        }
        //: let recordTime = AdoptiveParentThen.getMessageInsertTime()
        let recordTime = AdoptiveParentThen.todayTo()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((String(data_modeValuePath))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(data_modeValuePath))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(data_modeValuePath))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(user_curHereKey.replacingOccurrences(of: "provider", with: "t"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(user_scopeName)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((k_sinkKey.replacingOccurrences(of: "random", with: "a") + "Sandbox")) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(k_sinkKey.replacingOccurrences(of: "random", with: "a") + "Sandbox")] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (k_sinkKey.replacingOccurrences(of: "random", with: "a") + "Sandbox"))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(dataDefinitionKey.suffix(8)) + String(constSpeakValue))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = user_explainPath + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(user_restoreName.suffix(6)) + String(kScaleValue.suffix(5)) + "ser/re" + String(app_materialMessage.suffix(5)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kCleanMsg + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(main_sitName))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(main_toneMsg.replacingOccurrences(of: "black", with: "o"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        ReminderRequestTool.followingActive(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(dataDrawingName.suffix(8))) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.distance(msgData, nameure: nil, behindValue: NSNumber(value: 0), push: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.errorDescribe(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: data_explainName)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(main_responseId))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    ChockThen.switchly(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.texture(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(data_informationStarMessage) + String(showStepOldFormat))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(const_sayData) + showPerceivePath.lowercased())) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(const_sayData) + showPerceivePath.lowercased())] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if SelectedThen.panCaptureShared().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: mainMomentKey, object: nil, userInfo: [String(bytes: app_frameStrengthStr.map{pairHolder(arc: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.pin(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(k_reKey) + String(dataReduceTimingSenseUrl.suffix(8)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(dataDrawingName.suffix(8))) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!] = (String(noti_panelStillStr))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    noti_liveLevelUrl.recordGender(eventID: app_closedMessage)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(dataDrawingName.suffix(8))) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!] = (data_towardUrl.replacingOccurrences(of: "scheme", with: "tx"))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(data_informationStarMessage) + String(showStepOldFormat)): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ManagerThen.coordinate(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.connection(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(CompositionReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(CompositionReactiveCompatible.share.loginUserMode.userID)" + (String(dataPriorityStr.suffix(4)) + "vateC" + String(const_fromTitle.prefix(8)) + String(show_instanceText.prefix(5))) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if CompositionReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.male.rawValue, type != (String(dataDrawingName.suffix(8))) {
                //: self.removeReplyTipsView()
                self.towardMouth()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.distance(updateMsgData, nameure: msgInfo[(String(appAroundFormat.suffix(5)))] as? String, behindValue: msgIncome, push: .Msg_Status_Succ)
            
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            ChockThen.doingDirect(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(main_netId.prefix(6)) + String(userHeadValue))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(main_netId.prefix(6)) + String(userHeadValue))] as! NSNumber
                //: CompositionReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                CompositionReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((mainInstallData.replacingOccurrences(of: "presentation", with: "a") + String(notiTenderUrl.prefix(6)) + "ysMsg")) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(mainInstallData.replacingOccurrences(of: "presentation", with: "a") + String(notiTenderUrl.prefix(6)) + "ysMsg")] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.essay(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((k_ceaseInstallationTitle.replacingOccurrences(of: "policy", with: "in") + String(const_qualityMessage.prefix(6)))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(k_ceaseInstallationTitle.replacingOccurrences(of: "policy", with: "in") + String(const_qualityMessage.prefix(6)))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? OnerModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.stopAppear(updateMsgData, photoResult: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((notiMedalMessage.replacingOccurrences(of: "going", with: "v"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(notiMedalMessage.replacingOccurrences(of: "going", with: "v"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? SubmitModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.model(updateMsgData, timeWantAge: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        limitMsg()

        //: updateSession()
        cutSession()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func cutSession() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        OverAgainInfoManager.conference(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func errorDescribe(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard CompositionReactiveCompatible.share.loginUserMode.status != 1 else {
            guard CompositionReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    foolishness(showMsg: errorStr)
                }
                //: return
                return
            }

            //: SightReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            SightReactiveCompatible.share.secondBusy(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.dismissSmart()
            //: CompositionReactiveCompatible.share.loginUserMode.freeMsgTimes = ""
            CompositionReactiveCompatible.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.front()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = SiblingViewThen(frame: CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle))
            //: view.show()
            view.writtenToInvitation()
            //: } else if errorCode == 60003, !CompositionReactiveCompatible.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !CompositionReactiveCompatible.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            GenerateingReactiveCompatible.shared.nuclearFamilyConversion()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                foolishness(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func reconcile(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(data_informationStarMessage) + String(showStepOldFormat))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.pin(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func written() {
        //: if self.userWrap.intimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.stackTotalimate(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(CompositionReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(CompositionReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (user_liveMsg.array(forKey: appLineFormat)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.arc()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                user_liveMsg.set(intimateCacheData, forKey: appLineFormat)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func eigenvalueOfASquareMatrix(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = AdoptiveParentThen.parseTXMessageData(data: data.customElem.data)
        let dic = AdoptiveParentThen.adjust(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(k_southContent.replacingOccurrences(of: "up", with: "a"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(data_informationStarMessage) + String(showStepOldFormat))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(app_comeText) + appOppositeName.replacingOccurrences(of: "custom", with: "n") + k_accelerateText.replacingOccurrences(of: "identify", with: "t"))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.begin(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func pin(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(app_comeText) + appOppositeName.replacingOccurrences(of: "custom", with: "n") + k_accelerateText.replacingOccurrences(of: "identify", with: "t"))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(app_comeText) + appOppositeName.replacingOccurrences(of: "custom", with: "n") + k_accelerateText.replacingOccurrences(of: "identify", with: "t"))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.begin(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func begin(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = OverAgainInfoManager.chart(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.stackTotalimate(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.magnitudeimate(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.formAcquire(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.formAcquire(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        OverAgainInfoManager.appearance(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: main_disableMessage.map{twoCrush(ignore: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: userBrushKey.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: notiRequestFrameText,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension ParentControllerDelegate: ReadingWhiteReactiveCompatible {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func slowPopulation() {
        //: guard CompositionReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.distanceDown(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func announcement(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func fore(actionType: SeeSendable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    activityMark()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    statementBegin()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                activityMark()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if CompositionReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if CompositionReactiveCompatible.share.loginUserMode.loungePlus {
                if CompositionReactiveCompatible.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.alongCurrency(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                statementBegin()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    fleeting()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    statementBegin()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                fleeting()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.rid(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func statementBegin() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.foolishness(showMsg: String(bytes: showSideMsg.map{rocketOver(success: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func cry(giftModel _: SuccessHandyJSON, giftNum _: String, model _: WealthyMemberModel) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func malePage(giftModel: SuccessHandyJSON, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        genre(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: FactorSelectedCellData?, fromStr: String?) {
    func genre(giftModel: SuccessHandyJSON, giftNum: String, cellData: FactorSelectedCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            foolishness(showMsg: showTimeId)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(main_easePath))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(userHerFlagTitle.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(user_clearlyBanPath))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(main_towardData.prefix(5)) + String(user_replacementPlayStr))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(data_createUrl.replacingOccurrences(of: "coat", with: "fr"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        ReminderRequestTool.sheerCircle(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.errorDescribe(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: mainNextData)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.historyPopular(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.ember(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.cutSession()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(data_informationStarMessage) + String(showStepOldFormat))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(app_comeText) + appOppositeName.replacingOccurrences(of: "custom", with: "n") + k_accelerateText.replacingOccurrences(of: "identify", with: "t"))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(app_comeText) + appOppositeName.replacingOccurrences(of: "custom", with: "n") + k_accelerateText.replacingOccurrences(of: "identify", with: "t"))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.begin(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.burn(toRound: cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                noti_liveLevelUrl.recordGender(eventID: user_faceUrl)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func exposure(giftModel: SuccessHandyJSON, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        noti_liveLevelUrl.recordGender(eventID: dataMaleName)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            foolishness(showMsg: showTimeId)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= CompositionReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            statementBegin()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(showFarUrl) + show_wrapTitle.replacingOccurrences(of: "around", with: "d"))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(user_curHereKey.replacingOccurrences(of: "provider", with: "t"))] = (String(main_layUrl) + String(k_normallyUrl.suffix(3)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!: (String(noti_panelStillStr))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(data_informationStarMessage) + String(showStepOldFormat)): msgInfo, String(bytes: dataOuterFormat.map{$0^214}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ManagerThen.coordinate(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            chemicalAgent(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func renew(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(main_tableHugeKey - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.burnPlus(true)
            self.msgView.burnPlus(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func medalHeight(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.burnPlus(true)
            self.msgView.burnPlus(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func listen(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.listen(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                positiveAwake(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func thumb() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if WhitePlayerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            WhitePlayerDelegate.shared.commitPending()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func ember(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(data_informationStarMessage) + String(showStepOldFormat))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!] = (appResistanceFormat.replacingOccurrences(of: "interested", with: "ft"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(data_informationStarMessage) + String(showStepOldFormat))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ManagerThen.coordinate(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.connection(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(const_sayData) + showPerceivePath.lowercased())) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(const_sayData) + showPerceivePath.lowercased())] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.distance(cellData, nameure: msgInfoDic[(String(appAroundFormat.suffix(5)))] as? String, behindValue: msgIncome, push: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        alleged(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(main_netId.prefix(6)) + String(userHeadValue))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(main_netId.prefix(6)) + String(userHeadValue))] as! NSNumber
            //: CompositionReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            CompositionReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: CompositionReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.rowMember(needReload: false, mf_coin: CompositionReactiveCompatible.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func drawGenerate() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.behindCos(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func alleged(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.purblind(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func assemblage(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.running(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func targetContact() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func cram() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.computeComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.cutSession()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func occurrence() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.context { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.cutSession()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func changeSnap() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = AgainViewController()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        volition()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func onerClick() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = AddressPopView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.mapFile(cellTitleList: [(String(userAssessmentKey.prefix(4)) + " inf" + data_containBugUrl.replacingOccurrences(of: "never", with: "o")).localized, (String(const_listenMessage)).localized, (String(constMaybeEqualPairData)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .antiquityColor()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: SightReactiveCompatible.share.func__pushToUserDetailVC(uid: self.targetId)
                SightReactiveCompatible.share.temporaryUid(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                roundQuitView()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = ArrowCapAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.whenReport(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func roundQuitView() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = BlockWarningReactiveCompatible()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.northPlay(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.belowInRestore(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func sinceResolution() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            positiveAwake(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            volition()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func occurResult(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = CastMeasurable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.betwixt(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func oralPresentationSkim() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(picNotification(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: constWithoutStr,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(takeBack),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: appPriceMsg,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func picNotification(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard CompositionReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        guard CompositionReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func takeBack() {
        //: CompositionReactiveCompatible.share.loginUserMode.loungePlus = true
        CompositionReactiveCompatible.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func plantWeightMsg() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TwoCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TwoCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if ChockThen.motivation(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.cannulizeSignatureLocal(tipStr: (String(app_numberData.prefix(4)) + " read r" + String(noti_clipBottomData.prefix(8))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ParentControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func activityMark() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        MeetRouteThen.funcFromNog(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = StickPickTool.beyondVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.delouse(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func momentPhotos() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        MeetRouteThen.funcPersonDomino(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.delouse(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func delouse(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
            CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ChatModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.refer(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = CompositionViewController(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ChatModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.refer(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        volition()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func fleeting() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard ChockThen.talkAbout(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == ArrowGender.male.rawValue ? "He".localized : (String(notiBeginGiveName.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.foolishness(showMsg: (String(dataClearStr.suffix(7)) + String(userExposureStr.suffix(5)) + "too " + String(dataServerEssayMsg.prefix(4)) + "a version").temporaryWorker(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.beforeDragPicker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func beforeDragPicker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = StickPickTool.beyondVideo(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            StickPickTool.displayEnable(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.acceptBy(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.foolishness(showMsg: (String(data_cosUrl) + String(user_yaUrl)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func acceptBy(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: ShadeEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = PreviouslyCastControllerRecognizerDelegate(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ChatModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.responseWith(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        PointerReactiveCompatible.motivationCharmDismiss()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        volition()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TwoCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func purchaseVia(msgData: TwoCellData, sendModel: ChatModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        FormatCreateListener.shared.licenseAfterCompletion(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.foolishness(showMsg: (String(k_boyValue) + showConferenceValue.replacingOccurrences(of: "total", with: "a")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.distance(msgData, nameure: nil, behindValue: NSNumber(value: 0), push: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.responseWith(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    //: func func__addVoiceNotification() {
    func putEnableJudge() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(aboutWhenBig),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(pickEnter),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(sight(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(tempEquity(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: constOutputName,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(eigenvalueOfASquareMatrix(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: k_needData,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func script(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            WhitePlayerDelegate.shared.modeDaskDelegate()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            WhitePlayerDelegate.shared.product()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func aboutWhenBig() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        script(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func pickEnter() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        script(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func sight(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            script(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            script(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: MidReactiveCompatible, cellView: TalkingChatAudioMsgCell) {
    func generate(cellData: MidReactiveCompatible, cellView: BalanceCellView) {
        //: guard TalkingSocketManager.shared.isStrategy == false else {
        guard FileSocketManager.shared.isStrategy == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.foolishness(showMsg: constEnterMessage)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TwoCellData)!.isKind(of: MidReactiveCompatible.self) == false {
            if (indexModel as? TwoCellData)!.isKind(of: MidReactiveCompatible.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! MidReactiveCompatible
            let audioModel = indexModel as! MidReactiveCompatible
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = OverAgainInfoManager.clubMessage(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        WhitePlayerDelegate.shared.commitPending()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if WhitePlayerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (main_birdTickMsg) + "\(WhitePlayerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if WhitePlayerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                WhitePlayerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        WhitePlayerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        WhitePlayerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TwoCellData = self.msgView.uiMsgs.object(at: index) as! TwoCellData
                let subModel: TwoCellData = self.msgView.uiMsgs.object(at: index) as! TwoCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: MidReactiveCompatible.self) {
                if subModel.isKind(of: MidReactiveCompatible.self) {
                    //: let audioModel = subModel as! MidReactiveCompatible
                    let audioModel = subModel as! MidReactiveCompatible
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = OverAgainInfoManager.clubMessage(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    HelpReaderReactiveCompatible.atALowerPlace(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        WhitePlayerDelegate.shared.terminal(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func official() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.giftWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.baseballTeamDict(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TwoCellData
                let indexModel = model as? TwoCellData
                //: if indexModel?.isKind(of: MidReactiveCompatible.self) == true || indexModel?.isKind(of: MilitaryCellData.self) == true || indexModel?.isKind(of: SortCellData.self) == true || indexModel?.isKind(of: MovieReactiveCompatible.self) == true {
                if indexModel?.isKind(of: MidReactiveCompatible.self) == true || indexModel?.isKind(of: MilitaryCellData.self) == true || indexModel?.isKind(of: SortCellData.self) == true || indexModel?.isKind(of: MovieReactiveCompatible.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.limitMsg()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.tip()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.plantWeightMsg()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func giftWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.agreeFinish(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.off(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.written()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == CompositionReactiveCompatible.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == CompositionReactiveCompatible.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == CompositionReactiveCompatible.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == CompositionReactiveCompatible.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.belowInRestore(userWrap)
    }

    //: func func__getFillDraftText() {
    func nose() {
        //: let userID = "c2c_"+targetId
        let userID = (String(kRemainingStr.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.slowPopulation()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func darkContent() {
        //: let userID = "c2c_"+targetId
        let userID = (String(kRemainingStr.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    //: func func__TipsNotification() {
    func materialNose() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(storage(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: notiSessionStr,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func storage(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: PreviouslyFactorCompartmentTipsInformationCellData = userinfo[(mainContactContrastPath.replacingOccurrences(of: "three", with: "s"))] as! PreviouslyFactorCompartmentTipsInformationCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.burnPlus(true)
        self.msgView.burnPlus(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func cannulizeSignatureLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: kWeHistoryDotTitle.map{billOfFare(window: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: dataBeautyValue.map{broadPlatformMulti(past: $0)}, encoding: .utf8)!: (String(noti_tunPath))]
        //: var message: TwoCellData?
        var message: TwoCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = ManagerThen.beforeTingTip(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ManagerThen.coordinate(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.burnPlus(true)
        self.msgView.burnPlus(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func tip() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        ChockThen.red { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.cannulizeSignatureLocal(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func essay(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.cannulizeSignatureLocal(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func limitMsg() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        ChockThen.destroyHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.cannulizeSignatureLocal(tipStr: String(bytes: noti_slowValue.reversed(), encoding: .utf8)!.temporaryWorker(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func tempEquity(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TwoCellData = dict?["cellData"] as! TwoCellData
        let cellData: TwoCellData = dict?[(show_yourAtFormat.replacingOccurrences(of: "partner", with: "c") + String(mainAgainText))] as! TwoCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.burnPlus(true)
        self.msgView.burnPlus(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension ParentControllerDelegate: RelevantTipsProtocol {
    //: func func__setReplyTipsView() {
    func midRefuse() {
        //: let key =  "\(CompositionReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(CompositionReactiveCompatible.share.loginUserMode.userID)" + (String(dataPriorityStr.suffix(4)) + "vateC" + String(const_fromTitle.prefix(8)) + String(show_instanceText.prefix(5))) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.juxtapose()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            single(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            towardMouth()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func shrink(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        op(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        towardMouth()
    }

    //: func removeReplyTipsView() {
    func towardMouth() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            single(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func filterBalance() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: userNearbyControlMessage, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kStreamTitle - main_tableHugeKey, width: user_needValue, height: kStreamTitle)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: user_needValue,
                                     //: height: ScreenHeight)
                                     height: kStreamTitle)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func positiveAwake(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.mediumSave() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: GiantViewController.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kStreamTitle - main_tableHugeKey,
                                     //: width: ScreenWidth,
                                     width: user_needValue,
                                     //: height: ScreenHeight)
                                     height: kStreamTitle)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension ParentControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func profile() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(noti_beginAmGreatestId.prefix(8)) + k_hairUrl.lowercased() + String(notiBoldThreadContent)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func createMentally() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? main_tableHugeKey : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.produceRadii(width: contentView.width,
                                     //: height: contentView.height,
                                     height: contentView.height,
                                     //: corners: [.topLeft, .topRight],
                                     corners: [.topLeft, .topRight],
                                     //: cornerRadii: .init(width: 8, height: 8))
                                     cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? main_buildValue : userResolutionUrl)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + userAcceptFormat)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? main_buildValue : userResolutionUrl)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + userAcceptFormat)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + userAcceptFormat)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func serrationSumeraction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.sinceResolution()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.onerClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.fore(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                noti_liveLevelUrl.recordGender(eventID: constCleanData, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
