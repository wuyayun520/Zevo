
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let const_protectionValue:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "roomId" :*/
fileprivate let noti_easeId:String = "guide suite expected corner caseroomId"

/*: "type" :*/
fileprivate let mainVerticalMsg:[UInt8] = [0x32,0x3f,0x36,0x23]

private func layInner(explore num: UInt8) -> UInt8 {
    return num ^ 70
}

/*: "extra" :*/
fileprivate let show_overThanksPath:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let kSpaceName:[UInt8] = [0xf,0x10,0x8,0x15,0x2e,0x0,0x1c]

private func authorNet(our num: UInt8) -> UInt8 {
    return num ^ 101
}

/*: "Send Gift" :*/
fileprivate let show_joinBigId:String = "Sendaccounting bot he without push"

/*: "View info" :*/
fileprivate let const_revenueMirrorFormat:[Character] = ["V","i","e","w"," ","i","n"]
fileprivate let noti_communicationFormat:String = "FO"

/*: "Report" :*/
fileprivate let main_formatStr:String = "color effect stockReport"

/*: "uid" :*/
fileprivate let dataAlreadySignKey:[UInt8] = [0x1d,0x1,0xc]

private func buildMine(movie num: UInt8) -> UInt8 {
    return num ^ 104
}

/*: "name" :*/
fileprivate let k_differenceUrl:[UInt8] = [0xcc,0xc3,0xcf,0xc7]

private func humannessCharacter(tip num: UInt8) -> UInt8 {
    return num ^ 162
}

/*: "Can't send blank message" :*/
fileprivate let noti_ceaseMessage:String = "Can\'"
fileprivate let constBeautyClearlyMsg:String = "premium near written used lived bl"
fileprivate let data_serverApartmentName:[Character] = ["a","n","k"," ","m","e","s","s","a","g","e"]

/*: "user" :*/
fileprivate let kDonMedalName:String = "USER"

/*: "content" :*/
fileprivate let showMemoryKey:[UInt8] = [0x6a,0x66,0x67,0x7d,0x6c,0x67,0x7d]

/*: "msgType" :*/
fileprivate let show_streamData:String = "long allmsgType"

/*: "txt" :*/
fileprivate let app_phoneMessage:[Character] = ["t","x","t"]

/*: "isGroup" :*/
fileprivate let user_optionBirdPieceTitle:String = "isGroupprotocol blind alongside bull"

/*: "jumps" :*/
fileprivate let show_planCurrencyValue:String = "genremps"

/*: "message" :*/
fileprivate let showPlantFormat:[UInt8] = [0xbb,0xb3,0xa5,0xa5,0xb7,0xb1,0xb3]

private func restMerge(ready num: UInt8) -> UInt8 {
    return num ^ 214
}

/*: "TxtMsg" :*/
fileprivate let notiStructureFormat:[Character] = ["T","x","t","M","s","g"]

/*: "toUid" :*/
fileprivate let userWarningUrl:String = "toUidseek segment suspend"

/*: "msgInfo" :*/
fileprivate let appServiceKey:String = "convert"
fileprivate let app_oppositeSpeakerData:String = "operation bean deliver wearsgInfo"

/*: "params" :*/
fileprivate let show_addressUrl:[UInt8] = [0xc2,0xd3,0xc0,0xd3,0xdf,0xc1]

private func selectedPair(always num: UInt8) -> UInt8 {
    return num ^ 178
}

/*: "contentType" :*/
fileprivate let user_kindPermissionName:[UInt8] = [0x90,0x9c,0x9d,0x87,0x96,0x9d,0x87,0xa7,0x8a,0x83,0x96]

private func appropriateAverage(single num: UInt8) -> UInt8 {
    return num ^ 243
}

/*: "ImgMsg" :*/
fileprivate let app_cognitiveValue:String = "ImgMsgnarrow context want"

/*: "img" :*/
fileprivate let kRegionData:String = "wouldg"

/*: "imageUri" :*/
fileprivate let notiScheduleValue:[UInt8] = [0xf7,0xf3,0xff,0xf9,0xfb,0xcb,0xec,0xf7]

private func monetaryValue(someone num: UInt8) -> UInt8 {
    return num ^ 158
}

/*: "pic" :*/
fileprivate let noti_atStr:String = "PIC"

/*: "msgId" :*/
fileprivate let noti_costLogUrl:String = "msgIdexactly once"

/*: "totalMfCoin" :*/
fileprivate let show_characterStr:String = "hour"
fileprivate let userLengthTitle:[Character] = ["o","t","a","l","M","f","C","o","i","n"]

/*: "giftId" :*/
fileprivate let main_featherMsg:[Character] = ["g","i","f","t","I"]
fileprivate let const_awayMessage:String = "host"

/*: "giftNum" :*/
fileprivate let const_armUrl:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let appDisabledPath:String = "bot carrypkgI"

/*: "msg" :*/
fileprivate let userPhotoConText:String = "ofsg"

/*: "tips" :*/
fileprivate let userExtraKey:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let showRemindUrl:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let appPinMsg:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let noti_featureName:[UInt8] = [0xca,0xc7,0xc3,0xc6,0xb2,0xcb,0xc5]

fileprivate func uniformAllow(finite num: UInt8) -> UInt8 {
    let value = Int(num) - 98
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "All Numbers" :*/
fileprivate let k_creatureData:String = "All warning month environment open along"

/*: "sendNum" :*/
fileprivate let data_dynamicsBranchKey:String = "shape solution medication standard pathsendNum"

/*: "toUser" :*/
fileprivate let noti_unableThresholdStr:String = "toUserpair canvas game collect"

/*: "msgIncome" :*/
fileprivate let mainReadingMessage:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "isConnection" :*/
fileprivate let notiSuccessfulId:String = "exclusive tab female about familyisConn"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MeetControllerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class MeetControllerDelegate: MergeContactViewController {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = AgainModelType() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = ModelReactiveCompatible() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = AgainModelType.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: const_protectionValue.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if CompositionReactiveCompatible.share.appConfigMode.disableShootScreen {
            if CompositionReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = MpCarrierThen(frame: CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        behindAwake()
        //: createUIConstraints()
        pricePass()
        //: bindInteraction()
        minimum()
        //: addTapGestureRecognizer()
        mixInThroughLimit()
        //: func__chatRoom_insertTipMsg()
        item()
        //: chatSettings()
        redundant()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            represent()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.it(join: false)
        //: self.destroyTipsTimer()
        self.earlyHandle()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: ResponseActualTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ResponseActualTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(DirectViewCell.self, forCellReuseIdentifier: DirectViewCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(CastMsgCell.self, forCellReuseIdentifier: CastMsgCell.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(CosViewCell.self, forCellReuseIdentifier: CosViewCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(CellView.self, forCellReuseIdentifier: CellView.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: ReaderView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(CellThen.self, forCellReuseIdentifier: ReaderView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: FormatViewDelegate = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = FormatViewDelegate(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: RoundThen = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = RoundThen()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: MilitaryView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = MilitaryView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: NameEvenReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = NameEvenReactiveCompatible(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: StrategyAfterSightThen = {
        //: let v = TalkingSomeoneAtMeView()
        let v = StrategyAfterSightThen()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension MeetControllerDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func redundant() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        ReaderMedalThen.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        it(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func it(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(noti_easeId.suffix(6))): self.conversationData.groupID, String(bytes: mainVerticalMsg.map{layInner(explore: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        ReminderRequestTool.comeDownInward(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - HorizonHung

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension MeetControllerDelegate: HorizonHung {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TwoCellData! {
    func controller(_: ResponseActualTableViewController!, message data: V2TIMMessage!) -> TwoCellData! {
        //: let dic = AdoptiveParentThen.parseTXMessageData(data: data.customElem.data)
        let dic = AdoptiveParentThen.adjust(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = ManagerThen.managerModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TwoCellData!) -> ReaderView! {
    func cilium(_: ResponseActualTableViewController!, origin data: TwoCellData!) -> ReaderView! {
        //: if data.isKind(of: BrowseThen.self) {
        if data.isKind(of: BrowseThen.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = DirectViewCell(style: .default, reuseIdentifier: DirectViewCell.className())
            //: let cellData = data as! BrowseThen
            let cellData = data as! BrowseThen
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.mainEntryWordHost(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.translate(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: MilitaryCellData.self) {
        if data.isKind(of: MilitaryCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = CastMsgCell(style: .default, reuseIdentifier: CastMsgCell.className())
            //: let cellData = data as! MilitaryCellData
            let cellData = data as! MilitaryCellData
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: MaximumThen.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = CosViewCell(style: .default, reuseIdentifier: CosViewCell.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! MaximumThen
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: PreviouslyFactorCompartmentTipsInformationCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = CellView(style: .default, reuseIdentifier: CellView.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! PreviouslyFactorCompartmentTipsInformationCellData
            //: cell.fill(with: cellData)
            cell.hitch(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: ReaderView!) {
    func bean(_: ResponseActualTableViewController!, avatar cell: ReaderView!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(CompositionReactiveCompatible.share.loginUid) {
            if userModel.uid == Int(CompositionReactiveCompatible.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                mainEntryWordHost(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                rawIn(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: ReaderView!) {
    func breadth(_: ResponseActualTableViewController!,  incumbent cell: ReaderView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: CastMsgCell.self) { // 图片
            //: let imageCellData = cell.messageData as! MilitaryCellData
            let imageCellData = cell.messageData as! MilitaryCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(show_blockTopUrl)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = HostEvenPhotoModel()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = LivingMomentModel()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [HostEvenPhotoModel] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = CompositionViewController(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.mediumSave()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: DirectViewCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! DirectViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = AdoptiveParentThen.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = AdoptiveParentThen.adjust(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(show_overThanksPath))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: kSpaceName.map{authorNet(our: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ParentJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: SightReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    SightReactiveCompatible.share.inscription(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: CellView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! CellView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = AdoptiveParentThen.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = AdoptiveParentThen.adjust(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(show_overThanksPath))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: kSpaceName.map{authorNet(our: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ParentJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: SightReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    SightReactiveCompatible.share.inscription(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: ReaderView!) {
    func max(_: ResponseActualTableViewController!, nearCur cell: ReaderView!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        proposalAfter(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension MeetControllerDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func earlyHandle() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func rawIn(user: SaveModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = AddressPopView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.mapFile(cellTitleList: [(String(show_joinBigId.prefix(4)) + " Gift").localized, "@", (String(const_revenueMirrorFormat) + noti_communicationFormat.lowercased()).localized, (String(main_formatStr.suffix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.carteDuJour(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.askMouth(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.mainEntryWordHost(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.focusUid(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func carteDuJour(user: SaveModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.bindMessage()
        //: let model = TalkingChatRoomMemberModel()
        let model = WealthyMemberModel()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.inside(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func askMouth(user: SaveModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: dataAlreadySignKey.map{buildMine(movie: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: k_differenceUrl.map{humannessCharacter(tip: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.receiveInsideDict(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func mainEntryWordHost(uid: Int) {
        //: SightReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
        SightReactiveCompatible.share.temporaryUid(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func focusUid(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = ArrowCapAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.whenReport(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension MeetControllerDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func op(msgStr: String, quoteModel _: RankModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            foolishness(showMsg: (noti_ceaseMessage + "t sen" + String(constBeautyClearlyMsg.suffix(4)) + String(data_serverApartmentName)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: kSpaceName.map{authorNet(our: $0)}, encoding: .utf8)!: (kDonMedalName.lowercased()), String(bytes: dataAlreadySignKey.map{buildMine(movie: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: showMemoryKey.map{$0^9}, encoding: .utf8)!: temStr, (String(show_streamData.suffix(7))): (String(app_phoneMessage)), (String(user_optionBirdPieceTitle.prefix(7))): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(show_planCurrencyValue.replacingOccurrences(of: "genre", with: "ju"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: showPlantFormat.map{restMerge(ready: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: mainVerticalMsg.map{layInner(explore: $0)}, encoding: .utf8)!: (String(notiStructureFormat)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(noti_easeId.suffix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(userWarningUrl.prefix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(appServiceKey.replacingOccurrences(of: "convert", with: "m") + String(app_oppositeSpeakerData.suffix(6))): msgInfo, String(bytes: show_addressUrl.map{selectedPair(always: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ManagerThen.coordinate(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            proposalAfter(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func choke(img: UIImage) {
        //: let imagePath = MilitaryCellData.getChatImagePath(image: img)
        let imagePath = MilitaryCellData.overlook(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: user_kindPermissionName.map{appropriateAverage(single: $0)}, encoding: .utf8)!: (String(app_cognitiveValue.prefix(6))), (String(show_streamData.suffix(7))): (kRegionData.replacingOccurrences(of: "would", with: "im")), String(bytes: notiScheduleValue.map{monetaryValue(someone: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(noti_atStr.lowercased()): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: mainVerticalMsg.map{layInner(explore: $0)}, encoding: .utf8)!: (String(app_cognitiveValue.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(noti_easeId.suffix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(appServiceKey.replacingOccurrences(of: "convert", with: "m") + String(app_oppositeSpeakerData.suffix(6))): msgInfo, String(bytes: show_addressUrl.map{selectedPair(always: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ManagerThen.coordinate(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            proposalAfter(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TwoCellData, reSend: Bool) {
    private func proposalAfter(msgData: TwoCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            foolishness(showMsg: showTimeId)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.connection(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((noti_atStr.lowercased())) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(noti_atStr.lowercased())] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(noti_atStr.lowercased())] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        ReminderRequestTool.setBuild(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.distance(msgData, nameure: nil, behindValue: NSNumber(value: 0), push: .Msg_Status_Fail)
                                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.pratfall(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(appServiceKey.replacingOccurrences(of: "convert", with: "m") + String(app_oppositeSpeakerData.suffix(6)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.distance(msgData, nameure: msgInfo[(String(noti_costLogUrl.prefix(5)))] as? String, behindValue: NSNumber(value: 0), push: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((show_characterStr.replacingOccurrences(of: "hour", with: "t") + String(userLengthTitle))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(show_characterStr.replacingOccurrences(of: "hour", with: "t") + String(userLengthTitle))] as! NSNumber
                //: CompositionReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                CompositionReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func pratfall(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard CompositionReactiveCompatible.share.loginUserMode.status != 1 else {
            guard CompositionReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    foolishness(showMsg: errorStr)
                }
                //: return
                return
            }

            //: SightReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            SightReactiveCompatible.share.secondBusy(clickEvent: mainNextData, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.dismissSmart()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            tableNeedPrimary(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = SiblingViewThen(frame: CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle))
            //: view.show()
            view.writtenToInvitation()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                foolishness(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension MeetControllerDelegate: ReadingWhiteReactiveCompatible {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func exposure(giftModel _: SuccessHandyJSON, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func slowPopulation() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func announcement(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = ChatExecDataSource()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.receiveInsideDict(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func fore(actionType: SeeSendable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            coverDecisionMakingPush()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func malePage(giftModel _: SuccessHandyJSON, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func cry(giftModel: SuccessHandyJSON, giftNum: String, model: WealthyMemberModel) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            foolishness(showMsg: showTimeId)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(userWarningUrl.prefix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(main_featherMsg) + const_awayMessage.replacingOccurrences(of: "host", with: "d"))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(const_armUrl))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(noti_easeId.suffix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(appDisabledPath.suffix(4)) + "temsetId")] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        ReminderRequestTool.series(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.pratfall(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.historyPopular(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.tictac(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func renew(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.burnPlus(true)
            self.msgView.burnPlus(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func medalHeight(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.burnPlus(true)
            self.msgView.burnPlus(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func listen(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.listen(tap)
    }

    //: func func__starRecordBtnDown() {}
    func thumb() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func voice(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension MeetControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func coverDecisionMakingPush() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        MeetRouteThen.funcFromNog(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = StickPickTool.beyondVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: SightReactiveCompatible.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                SightReactiveCompatible.share.reserveUp()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.choke(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func fromCommon() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        MeetRouteThen.funcPersonDomino(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.choke(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension MeetControllerDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func searchion(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(noti_easeId.suffix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension MeetControllerDelegate {
    //: func func__TipsNotification() {
    func living() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(suiteLoadNumbererval(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: notiSessionStr,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func suiteLoadNumbererval(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: PreviouslyFactorCompartmentTipsInformationCellData = userinfo[(userPhotoConText.replacingOccurrences(of: "of", with: "m"))] as! PreviouslyFactorCompartmentTipsInformationCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.burnPlus(true)
        self.msgView.burnPlus(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func tableNeedPrimary(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: showMemoryKey.map{$0^9}, encoding: .utf8)!: tipStr, (String(show_streamData.suffix(7))): (String(userExtraKey))]
        //: var message: TwoCellData?
        var message: TwoCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ManagerThen.coordinate(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.burnPlus(true)
        self.msgView.burnPlus(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func item() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard CompositionReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            guard CompositionReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: CompositionReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.tableNeedPrimary(tipStr: CompositionReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension MeetControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func tictac(extral: [String: Any?], model: WealthyMemberModel, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(appServiceKey.replacingOccurrences(of: "convert", with: "m") + String(app_oppositeSpeakerData.suffix(6)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(user_optionBirdPieceTitle.prefix(7)))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(show_streamData.suffix(7)))] = (String(showRemindUrl))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(appServiceKey.replacingOccurrences(of: "convert", with: "m") + String(app_oppositeSpeakerData.suffix(6)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: dataAlreadySignKey.map{buildMine(movie: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (String(appPinMsg)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: noti_featureName.map{uniformAllow(finite: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(k_creatureData.prefix(4)) + "Numbers").localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(showRemindUrl))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(showRemindUrl))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(String(appPinMsg))] = "\(giftdic[(String(data_dynamicsBranchKey.suffix(7)))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(noti_unableThresholdStr.prefix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ManagerThen.coordinate(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.connection(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(mainReadingMessage))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(mainReadingMessage))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.distance(cellData, nameure: msgInfoDic[(String(noti_costLogUrl.prefix(5)))] as? String, behindValue: msgIncome, push: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        aboveBeyond(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((show_characterStr.replacingOccurrences(of: "hour", with: "t") + String(userLengthTitle))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(show_characterStr.replacingOccurrences(of: "hour", with: "t") + String(userLengthTitle))] as! NSNumber
            //: CompositionReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            CompositionReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: CompositionReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.rowMember(needReload: false, mf_coin: CompositionReactiveCompatible.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.proposal()
    }

    //: func initCachEffectData() {
    func represent() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.behindCos(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func aboveBeyond(giftDic: [String: Any], model: WealthyMemberModel) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.modify(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in PoliceStrategyThen.share.sendingAnonymous() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SpeakMeasurable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.behindAnonymous(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.purblind(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func suiteByCurrency(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in PoliceStrategyThen.share.sendingAnonymous() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SpeakMeasurable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.behindAnonymous(model: giftModel as! RecoverAnimatModel)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.running(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension MeetControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func behindAwake() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func pricePass() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(userResolutionUrl)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(userResolutionUrl)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - userAcceptFormat)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func minimum() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = ChatExecDataSource()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! TwoCellData).isKind(of: BrowseThen.self) {
                    if (cellData as! TwoCellData).isKind(of: BrowseThen.self) {
                        //: let groupCellData = cellData as! BrowseThen
                        let groupCellData = cellData as! BrowseThen
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(noti_costLogUrl.prefix(5)))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: LightConversationListener.shared.rx
        LightConversationListener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(notiSuccessfulId.suffix(6)) + "ection"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
