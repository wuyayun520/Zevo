
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let mainParentSelectedPath:[Character] = ["S","O","C","K","E","T","_","A","D","D","R","_","K"]
fileprivate let userOperateSiblingId:String = "speakY"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let const_markData:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4c,0x4c,0x41,0x43,0x54,0x53,0x45,0x55,0x51,0x45,0x52,0x5f,0x54,0x41,0x48,0x43,0x4f,0x45,0x44,0x49,0x56,0x5f,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let app_shMsg:[UInt8] = [0x1,0x16,0x10,0x1a,0x5,0x16,0xc,0x15,0x1f,0x1c,0x12,0x7,0x0,0x10,0x1,0x16,0x16,0x1d,0xc,0x1d,0x1c,0x7,0x1a,0x15,0x1a,0x10,0x12,0x7,0x1a,0x1c,0x1d]

private func viaBind(full num: UInt8) -> UInt8 {
    return num ^ 83
}

/*: "isCalling" :*/
fileprivate let constSuccessfulStr:String = "isCallininstall outside"
fileprivate let kDemonstrateId:String = "language"
 
/*: "SOCKET 连接成功." :*/
fileprivate let user_standardTitle:[Character] = ["S"]
fileprivate let dataExpressionAmPath:String = "clickKE"

/*: "SOCKET 连接失败, error: :*/
fileprivate let const_sideMsg:String = "SOmodelK"
fileprivate let mainCityDeleteTitle:String = "接失\u{8d25}, "
fileprivate let mainBullContent:String = "angle remindererror:"

/*: "SOCKET 失败 :*/
fileprivate let dataAppearanceId:[Character] = ["S","O","C","K","E","T"," ","失","败"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let appInformMsg:String = "次, 切换过"
fileprivate let showBusinessName:String = "t域名重新尝score hide opposition colorful bear"
fileprivate let userMirrorValue:String = "试，\u{5730}址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let showProvinceKey:String = "SOCKET 已control index pick"
fileprivate let user_channelKey:[Character] = ["\u{5173}","闭",","," "]
fileprivate let k_expressionPath:[Character] = ["c","o","d","e",":"]

/*: , reason: :*/
fileprivate let showCharmName:String = "mention today, re"

/*: /?sid= :*/
fileprivate let showPictureText:String = "/?sid=price stretch length"

/*: &uid= :*/
fileprivate let notiSoonValue:String = "small retirement presentation&uid="

/*: &platform=iphone&version= :*/
fileprivate let user_connectionMsg:String = "&plafuture aspect ignore happy under"
fileprivate let kFadeStr:String = "reminderorm"
fileprivate let show_dropStr:String = "protocol exercise matter technicalne&ver"

/*: &packageId= :*/
fileprivate let noti_capacityKey:[Character] = ["&","p","a","c","k","a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let dataTrainFlowText:[Character] = ["1","v","1","s","e","r","v","e","r","."]

/*: .com:7766" :*/
fileprivate let data_bigSaltUrl:String = ".com:7766trust under life priority appearance"

/*: "43.129.185.194:7767" :*/
fileprivate let const_detailedValue:String = "43.1"
fileprivate let kAccelerateData:[Character] = [".","1","9","4",":","7","7","6","7"]

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let data_dropFormat:[UInt8] = [0x44,0x58,0x54,0x5c,0x52,0x43,0x37,0xf0,0xaa,0x86,0xf0,0xac,0x8b,0x65,0x72,0x76,0x74,0x7f,0x76,0x75,0x7e,0x7b,0x7e,0x63,0x6e,0x54,0x7f,0x76,0x79,0x70,0x72,0x73,0xf0,0x8c,0x86,0xf2,0x87,0xbb,0xf8,0xab,0x9b,0xf1,0x81,0xba,0xf0,0xaa,0x86,0xf2,0x92,0xa4,0xfe,0x80,0xba,0x39]

/*: "cmd" :*/
fileprivate let main_beforeFormat:String = "cidentity"

/*: "ping" :*/
fileprivate let showFlowText:String = "pichoiceg"

/*: "callHeartbeat" :*/
fileprivate let app_thanFormat:[Character] = ["c","a","l","l","H","e","a"]
fileprivate let showSiblingPath:String = "radeat"

/*: "recievedDate" :*/
fileprivate let showOperationName:String = "recieresent"

/*: "liveHeartBeat" :*/
fileprivate let data_searchKey:String = "administrative center profit nextliveH"
fileprivate let mainPickFormat:[Character] = ["e"]
fileprivate let main_springMsg:[Character] = ["a","r","t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let data_libraryName:[Character] = ["🔗"," ","关","闭"," ","s"]
fileprivate let mainAircraftStr:String = "OCKET"

/*: "The user logs out" :*/
fileprivate let mainNoticeMessage:String = "The usdiscourse and"
fileprivate let main_hostContrastName:[Character] = ["o"]
fileprivate let appDocTitle:[Character] = ["g","s"," ","o","u","t"]

/*: "SOCKET  :*/
fileprivate let kGainText:[Character] = ["S","O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let appSkinText:String = "重连. \u{91cd}试次数"
fileprivate let k_organicComputeMessage:String = "\u{ff1a}"

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let kMaterialStr:[Character] = ["S","O","C","K","E","T"," ","\u{8fde}","\u{63a5}","\u{6b63}","常"," ","不","需","要","\u{91cd}","新","连","\u{63a5}"]
fileprivate let noti_yearId:String = "."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let show_birthdayMessage:String = "benchCK"
fileprivate let main_thinDiskStr:String = "\u{5728}连接 "
fileprivate let user_allowText:String = "concept."

/*: "&resume=1" :*/
fileprivate let k_popularStr:[Character] = ["&","r","e","s","u"]
fileprivate let constHouseName:[Character] = ["m","e","=","1"]

/*: "&lang= :*/
fileprivate let notiAcquisitionMsg:String = "&lang=installation consumer"

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let app_yesPlayName:String = "SOCKET \u{5f00}始"
fileprivate let noti_betterData:String = "连接, 地址："

/*: "onNetDisabled" :*/
fileprivate let mainProcessValue:String = "onNetping least"

/*: "errno" :*/
fileprivate let dataBearUrl:String = "errattention"

/*: "data" :*/
fileprivate let kSplitBranchGuideKey:String = "ddetecta"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let const_tillPath:String = "workplace status following shadowSOCKET "
fileprivate let appBoxStr:String = "连接保活cwork abs"
fileprivate let dataStickName:String = "ping.some owner reason tone template"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let mainWatchName:[UInt8] = [0x59,0x45,0x49,0x41,0x4f,0x5e,0x2a,0x7e,0x78,0x7e,0x69,0xee,0xb5,0x97,0xec,0xbe,0xb1,0x69,0x67,0x6e,0x30,0x2a,0x69,0x6b,0x66,0x66,0x42,0x6f,0x6b,0x78,0x7e,0x68,0x6f,0x6b,0x7e,0x24]

private func analyzeDecision(res num: UInt8) -> UInt8 {
    return num ^ 10
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let noti_waitDistributionMessage:[UInt8] = [0x2e,0x74,0x61,0x65,0x42,0x74,0x72,0x61,0x65,0x48,0x65,0x76,0x69,0x6c,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x65,0x76,0x69,0x6c,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "onP" :*/
fileprivate let notiSureData:String = "human nativeonP"

/*: "msg" :*/
fileprivate let constRateId:String = "MSG"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let showStrategyDoinglyTitle:String = "🔗 \u{5904}理消"
fileprivate let constBackgroundId:String = "dName:gray mine thread"

/*: , errNo: :*/
fileprivate let main_checkValue:String = "lay broadcast eh snap author, err"
fileprivate let main_transportationFormat:String = "No:drown access transmission"

/*: , msg: :*/
fileprivate let data_layPath:String = ", msg:clothes retain rank section come"

/*: , data: :*/
fileprivate let userOfferName:String = "given per brow discourse, data:"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let noti_agreeFormat:String = "layerCKE"
fileprivate let show_targetMessage:String = "异常消息"
fileprivate let app_miniUrl:String = "arm wave creature base sinkame:"

/*: "onReceivedGift" :*/
fileprivate let kBiteValue:[Character] = ["o","n","R","e","c","e","i","v","e","d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let const_liftWhyMsg:String = "onVidecomplaint inside author thread suite"
fileprivate let noti_actualFormat:String = "oMatchselection opposition mark"

/*: "onRequestCall" :*/
fileprivate let appUserPath:String = "point draft disable present pathonReq"

/*: "onCancelCall" :*/
fileprivate let user_acceptKey:String = "admin process fill randomonCanc"

/*: "onRefuseCall" :*/
fileprivate let notiBusinessMessage:[Character] = ["o","n","R","e","f","u","s","e","C"]
fileprivate let user_servePath:String = "atransform"

/*: "onAcceptCall" :*/
fileprivate let data_buildStr:[Character] = ["o","n","A","c","c","e","p","t"]
fileprivate let const_expectedPath:String = "Callbefore merge pick black finite"

/*: "onStopCall" :*/
fileprivate let data_environmentContent:[Character] = ["o","n","S","t","o","p"]
fileprivate let noti_secretStr:String = "Calltype read search"

/*: "freeCallTimes" :*/
fileprivate let app_searchData:[Character] = ["f","r","e","e","C","a","l","l","T","i"]
fileprivate let dataFastMsg:[Character] = ["m","e","s"]

/*: "mfCoin" :*/
fileprivate let mainGapText:String = "mfCoimention gallery"
fileprivate let main_easternObserveAssetUrl:String = "closed"

/*: "onMissCoin" :*/
fileprivate let mainCheckedContactToData:[Character] = ["o","n","M","i","s","s","C","o","i"]
fileprivate let user_inputTruthCurKey:[Character] = ["n"]

/*: "acceptCall" :*/
fileprivate let app_airText:[Character] = ["a","c","c","e","p"]
fileprivate let userSwitcherText:[Character] = ["t","C","a","l","l"]

/*: "stopCall" :*/
fileprivate let appDonKey:String = "stopCallbread provider tolerance daily"

/*: "requestCall" :*/
fileprivate let userLoseValue:String = "relatues"
fileprivate let notiEasternUrl:[Character] = ["t","C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let show_appealValue:[UInt8] = [0x26,0x34,0x13,0xb,0x14,0x9,0x18,0x5d,0x4,0x12,0x8,0x5d,0x9,0x12,0x5d,0x1c,0x5d,0xb,0x14,0x19,0x18,0x12,0x5d,0x1e,0x1c,0x11,0x11,0x53,0x53,0x53,0x20]

private func popularDestroy(skirt num: UInt8) -> UInt8 {
    return num ^ 125
}

/*: "type" :*/
fileprivate let app_technologyFormat:String = "tremovee"

/*: "[Invite you to a voice call...]" :*/
fileprivate let kDriveSpherePath:[UInt8] = [0x34,0x26,0x1,0x19,0x6,0x1b,0xa,0x4f,0x16,0x0,0x1a,0x4f,0x1b,0x0,0x4f,0xe,0x4f,0x19,0x0,0x6,0xc,0xa,0x4f,0xc,0xe,0x3,0x3,0x41,0x41,0x41,0x32]

/*: "uid" :*/
fileprivate let k_nearbyStr:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let show_tingUrl:String = "moveickmovea"
fileprivate let user_sourceValue:String = "discounte"

/*: "headPic" :*/
fileprivate let show_publishId:String = "bot exhibit careheadPic"

/*: "onCallResume" :*/
fileprivate let userDirectionFormat:[Character] = ["o","n","C","a","l"]
fileprivate let data_startValue:String = "lResumechallenge lite advise content"

/*: "cancelCall" :*/
fileprivate let showExerciseComePath:String = "cancecamera"
fileprivate let noti_ablePath:String = "Callarrow commercial any valid"

/*: "refuseCall" :*/
fileprivate let userPlayMessage:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let userGoKey:String = "good sight must near saveonCall"
fileprivate let constIdentificationTitle:[Character] = ["v","a","t","i","o","n"]

/*: "onLiveWarning" :*/
fileprivate let user_movieFunFormat:String = "onLipair choose stem send"
fileprivate let show_commitUrl:String = "rntickg"

/*: "reason" :*/
fileprivate let data_relieveValue:String = "reinformationon"

/*: "time" :*/
fileprivate let notiNeighborData:String = "tianalyze"

/*: "onStopLive" :*/
fileprivate let userSplitId:[Character] = ["o","n","S","t","o"]
fileprivate let data_depthUrl:String = "pLiveconversation define"

/*: "onSendProgramGift" :*/
fileprivate let userMiniMessage:String = "onSencombine member"
fileprivate let kTechnologyKey:String = "grsupport"

/*: "onGameFs" :*/
fileprivate let data_agencyName:String = "onGacomponent off"

/*: "broadStatus" :*/
fileprivate let app_showOldTitle:[Character] = ["b","r","o","a","d","S","t","a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let kMaybeStr:[Character] = ["o","n","C","a","l","l"]
fileprivate let data_clingUrl:String = "ChatMsgcurrently original us reply terms"

/*: "onNotificationPopupWin" :*/
fileprivate let main_featherStr:[Character] = ["o","n","N","o","t"]
fileprivate let showUnlessValue:String = "IFIC"
fileprivate let mainDailyManFrameFormat:String = "acuro"

/*: "onUserBlock" :*/
fileprivate let userReLocalFunctionStr:[Character] = ["o","n","U","s","e","r","B","l","o","c","k"]

/*: "isRecharge" :*/
fileprivate let const_familyCropMessage:[Character] = ["i","s","R","e","c","h","a","r","g"]
fileprivate let mainUntilRestSystemContent:[Character] = ["e"]

/*: "source" :*/
fileprivate let appSituationValue:String = "sourcpossibility"

/*: "invitePop" :*/
fileprivate let notiSizeFormat:String = "invireading"
fileprivate let dataChooseFormat:String = "Popmystery called chart"

/*: "videoPop" :*/
fileprivate let noti_girlWordUrl:String = "VIDE"

/*: "coin" :*/
fileprivate let appFactorStr:String = "cfragmentin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FileSocketManager.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let appSaltConnectKey = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let data_pointMessage = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let notiInformationMessage = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let show_priceMessage = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let noti_gamePath = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let kJumpData = (String(mainParentSelectedPath) + userOperateSiblingId.replacingOccurrences(of: "speak", with: "E"))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let app_buildValue = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let kTurnUrl = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let mainKeyUrl = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let appPlainName = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let mainGuideSelectKey = NSNotification.Name(rawValue: String(bytes: const_markData.reversed(), encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let noti_restoreValue = NSNotification.Name(rawValue: String(bytes: app_shMsg.map{viaBind(full: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol ToolMeetErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func lock(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol BenchAddressObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func delayGenderTrack(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func uniformListen(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol FormatSceneObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func down(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func submitPerform(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func zygotene(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func rewardOverlook(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol FactorObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func entire(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func acceptShouldTag(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func chatRaw(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class FileSocketManager: NSObject {
    //: enum SocketRetryType: String {
    enum AppearanceType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currStrategyVC: AnyObject?
    var currStrategyVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ReadingLanguageManager.shared.direction == .rightToLeft ? 0 : user_needValue,
        //: y: StatusBarHeight+190
        y: noti_guideRequestId + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ReadingLanguageManager.shared.direction == .rightToLeft ? 0 : user_needValue,
        //: y: StatusBarHeight+190
        y: noti_guideRequestId + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: ToolMeetErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: BenchAddressObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: FormatSceneObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: FactorObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isStrategy = false
    @objc dynamic var isStrategy = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = FileSocketManager()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(commonNote(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(one),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(constSuccessfulStr.prefix(8)) + kDemonstrateId.replacingOccurrences(of: "language", with: "g")))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (SceneKingfisherCompatible.shared as! SceneKingfisherCompatible).shapeInfo()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isStrategy")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (SceneKingfisherCompatible.shared as! SceneKingfisherCompatible).shapeInfo()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension FileSocketManager: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        FinishThen.onicial(msg: (String(user_standardTitle) + dataExpressionAmPath.replacingOccurrences(of: "click", with: "OC") + "T 连接成功."))
        //: sendPing()
        secureVid()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: const_beginStr,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: kJumpData)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        exaggerate()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        FinishThen.onicial(msg: (const_sideMsg.replacingOccurrences(of: "model", with: "C") + "ET 连" + mainCityDeleteTitle + String(mainBullContent.suffix(6))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = CompositionReactiveCompatible.share.reachability?.connection, status != .unavailable else {
        guard let status = CompositionReactiveCompatible.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == show_priceMessage {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = reRetry(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            FinishThen.onicial(msg: (String(dataAppearanceId)) + "\(show_priceMessage)" + (appInformMsg.capitalized + "Socke" + String(showBusinessName.prefix(6)) + userMirrorValue) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.quantityervalStream(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.activeSum(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        FinishThen.onicial(msg: (String(showProvinceKey.prefix(8)) + String(user_channelKey) + String(k_expressionPath)) + "\(code)" + (String(showCharmName.suffix(4)) + "ason:") + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.programBy(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == appPlainName {
            //: return
            return
        }
        //: if let status = CompositionReactiveCompatible.share.reachability?.connection, status != .unavailable {
        if let status = CompositionReactiveCompatible.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.quantityervalStream(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func reRetry(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = CompositionReactiveCompatible.share.appConfigMode.ws
        self.linkAddrArr = CompositionReactiveCompatible.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = beforeDoingce()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: kJumpData)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = CompositionReactiveCompatible.share.loginUid
        let uid = CompositionReactiveCompatible.share.loginUid
        //: let sid = CompositionReactiveCompatible.share.loginSessionId
        let sid = CompositionReactiveCompatible.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(showPictureText.prefix(6))) + "\(sid)" + (String(notiSoonValue.suffix(5))) + "\(uid)" + (String(user_connectionMsg.prefix(4)) + kFadeStr.replacingOccurrences(of: "reminder", with: "tf") + "=ipho" + String(show_dropStr.suffix(6)) + "sion=") + "\(mainControlUrl)" + (String(noti_capacityKey)) + "\(kLogData)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func beforeDoingce() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return dataPostValue ? [(String(dataTrainFlowText)) + "\(userDeviceIndexStr)" + (String(data_bigSaltUrl.prefix(9)))] : [(const_detailedValue.capitalized + "29.185" + String(kAccelerateData))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension FileSocketManager {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func commonNote(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            FinishThen.onicial(msg: String(bytes: data_dropFormat.map{$0^23}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.programBy(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.quantityervalStream(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func one() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.quantityervalStream(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.checkComplete()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension FileSocketManager {
    /// socket保活心跳包
    //: private func sendPing() {
    private func secureVid() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: appSaltConnectKey, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.bagOriginal(info: [(main_beforeFormat.replacingOccurrences(of: "identity", with: "md")): (showFlowText.replacingOccurrences(of: "choice", with: "n"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func ordinalPing() {
        //: endTalkingPing()
        tutorial()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: data_pointMessage, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isStrategy || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currStrategyVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currStrategyVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isStrategy || (self.mediumSave()?.isKind(of: AdminObserverDelegate.self) == false && self.mediumSave()?.isKind(of: TastyPartyDelegate.self) == false) && (self.currStrategyVC?.isKind(of: AdminObserverDelegate.self) == false && self.currStrategyVC?.isKind(of: TastyPartyDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.bagOriginal(info: [(main_beforeFormat.replacingOccurrences(of: "identity", with: "md")): (String(app_thanFormat) + showSiblingPath.replacingOccurrences(of: "ad", with: "tb"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func tutorial() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func checkComplete() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(showOperationName.replacingOccurrences(of: "resent", with: "v") + "edDate")] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < app_enterFormat {
                //: CompositionReactiveCompatible.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                CompositionReactiveCompatible.share.exitNorth(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension FileSocketManager {
    /// 开启直播心跳包
    //: func startLivePing() {
    func fastWrap() {
        //: endLivePing()
        centerEnd()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: notiInformationMessage, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.bagOriginal(info: [(main_beforeFormat.replacingOccurrences(of: "identity", with: "md")): (String(data_searchKey.suffix(5)) + String(mainPickFormat) + String(main_springMsg))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func centerEnd() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension FileSocketManager {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func exaggerate() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = PermissionVanguardView()
        //: let keyWindow = DirtyMacroDefine.getWindow()
        let keyWindow = DirtyMacroDefine.effectRequire()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isStrategy
        self.containerView?.isHidden = !self.isStrategy
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func mediumRoot() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard SelectedThen.panCaptureShared().isLive == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension FileSocketManager {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func programBy(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(data_libraryName) + mainAircraftStr.lowercased()))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: appPlainName, reason: (String(mainNoticeMessage.prefix(6)) + "er l" + String(main_hostContrastName) + String(appDocTitle)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        tutorial()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            mediumRoot()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func modifyBy() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = FileSocketManager.shared.reRetry(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.quantityervalStream(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func quantityervalStream(timeInterval: TimeInterval, type: AppearanceType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            FinishThen.onicial(msg: (String(kGainText)) + "\(type.rawValue)" + (appSkinText + k_organicComputeMessage) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.banRefuseFile(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func banRefuseFile(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                FinishThen.onicial(msg: (String(kMaterialStr) + noti_yearId.capitalized))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                FinishThen.onicial(msg: (show_birthdayMessage.replacingOccurrences(of: "bench", with: "SO") + "ET 正" + main_thinDiskStr + "不需要\u{91cd}新\u{8fde}" + user_allowText.replacingOccurrences(of: "concept", with: "接")))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isStrategy {
        if self.isStrategy {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(k_popularStr) + String(constHouseName)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(notiAcquisitionMsg.prefix(6))) + "\(CosSweetReactiveCompatible.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        programBy(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = noti_gamePath
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        FinishThen.onicial(msg: (app_yesPlayName + noti_betterData.capitalized) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func bagOriginal(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if CompositionReactiveCompatible.share.reachability?.connection == nil || CompositionReactiveCompatible.share.reachability?.connection == .unavailable {
        if CompositionReactiveCompatible.share.reachability?.connection == nil || CompositionReactiveCompatible.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(main_beforeFormat.replacingOccurrences(of: "identity", with: "md")): (String(mainProcessValue.prefix(5)) + "Disabled"),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (dataBearUrl.replacingOccurrences(of: "attention", with: "no")): "\(mainKeyUrl)",
                                       //: "data": [:]]
                                       (kSplitBranchGuideKey.replacingOccurrences(of: "detect", with: "at")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            activeSum(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(main_beforeFormat.replacingOccurrences(of: "identity", with: "md"))] as? String == (showFlowText.replacingOccurrences(of: "choice", with: "n")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                FinishThen.onicial(msg: (String(const_tillPath.suffix(7)) + String(appBoxStr.prefix(5)) + "md: " + String(dataStickName.prefix(5))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(main_beforeFormat.replacingOccurrences(of: "identity", with: "md"))] as? String == (String(app_thanFormat) + showSiblingPath.replacingOccurrences(of: "ad", with: "tb")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                FinishThen.onicial(msg: String(bytes: mainWatchName.map{analyzeDecision(res: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(main_beforeFormat.replacingOccurrences(of: "identity", with: "md"))] as? String == (String(data_searchKey.suffix(5)) + String(mainPickFormat) + String(main_springMsg)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                FinishThen.onicial(msg: String(bytes: noti_waitDistributionMessage.reversed(), encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(main_beforeFormat.replacingOccurrences(of: "identity", with: "md"))] as? String == (showFlowText.replacingOccurrences(of: "choice", with: "n")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(main_beforeFormat.replacingOccurrences(of: "identity", with: "md")): (String(notiSureData.suffix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (dataBearUrl.replacingOccurrences(of: "attention", with: "no")): "\(app_buildValue)",
                                           //: "data": [:]]
                                           (kSplitBranchGuideKey.replacingOccurrences(of: "detect", with: "at")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                activeSum(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.quantityervalStream(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func activeSum(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(main_beforeFormat.replacingOccurrences(of: "identity", with: "md"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(dataBearUrl.replacingOccurrences(of: "attention", with: "no"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(constRateId.lowercased())].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(kSplitBranchGuideKey.replacingOccurrences(of: "detect", with: "at"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (showStrategyDoinglyTitle + "\u{606f}：cm" + String(constBackgroundId.prefix(6))) + "\(cmdName)" + (String(main_checkValue.suffix(5)) + String(main_transportationFormat.prefix(3))) + "\(errorNo)" + (String(data_layPath.prefix(6))) + "\(msg)" + (String(userOfferName.suffix(7))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        salem(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            FinishThen.onicial(msg: (noti_agreeFormat.replacingOccurrences(of: "layer", with: "SO") + "T 接收" + show_targetMessage.capitalized + "\u{ff1a}cmdN" + String(app_miniUrl.suffix(4))) + "\(cmdName)" + (String(main_checkValue.suffix(5)) + String(main_transportationFormat.prefix(3))) + "\(errorNo)" + (String(data_layPath.prefix(6))) + "\(msg)" + (String(userOfferName.suffix(7))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: CompositionReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                CompositionReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !CompositionReactiveCompatible.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !CompositionReactiveCompatible.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                GenerateingReactiveCompatible.shared.nuclearFamilyConversion()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.lock(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(kBiteValue)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: user_failFormat, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(const_liftWhyMsg.prefix(6)) + String(noti_actualFormat.prefix(6))) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.delayGenderTrack(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(appUserPath.suffix(5)) + "uestCall") { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.uniformListen(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(user_acceptKey.suffix(6)) + "elCall") { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(notiBusinessMessage) + user_servePath.replacingOccurrences(of: "transform", with: "ll")) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(data_buildStr) + String(const_expectedPath.prefix(4))) { // 接收方发送acceptCall回执
            //: self.isStrategy = true
            self.isStrategy = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            ordinalPing()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.acceptShouldTag(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(data_environmentContent) + String(noti_secretStr.prefix(4))) { // 接收方发送stopCall回执
            //: self.isStrategy = false
            self.isStrategy = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            tutorial()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(app_searchData) + String(dataFastMsg))] as? Int {
                //: CompositionReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                CompositionReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: CompositionReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            CompositionReactiveCompatible.share.loginUserMode.mf_coin = json[(kSplitBranchGuideKey.replacingOccurrences(of: "detect", with: "at"))][(String(mainGapText.prefix(5)) + main_easternObserveAssetUrl.replacingOccurrences(of: "closed", with: "n"))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            extend(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kReasonDataName, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (SceneKingfisherCompatible.shared as! SceneKingfisherCompatible).toilingStatusDamDescribe()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(mainCheckedContactToData) + String(user_inputTruthCurKey)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.rewardOverlook(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(app_airText) + String(userSwitcherText)) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isStrategy = true
            self.isStrategy = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            ordinalPing()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.down(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(appDonKey.prefix(8))) { // 接收方/拨打方 挂断了通话
            //: self.isStrategy = false
            self.isStrategy = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            tutorial()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.submitPerform(data: data)
            // 更新余额
            //: CompositionReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            CompositionReactiveCompatible.share.loginUserMode.mf_coin = json[(kSplitBranchGuideKey.replacingOccurrences(of: "detect", with: "at"))][(String(mainGapText.prefix(5)) + main_easternObserveAssetUrl.replacingOccurrences(of: "closed", with: "n"))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(app_searchData) + String(dataFastMsg))] as? Int {
                //: CompositionReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                CompositionReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            extend(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kReasonDataName, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (SceneKingfisherCompatible.shared as! SceneKingfisherCompatible).toilingStatusDamDescribe()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (userLoseValue.replacingOccurrences(of: "lat", with: "q") + String(notiEasternUrl)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: mainGuideSelectKey, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: CompositionReactiveCompatible.share.recieve1v1TalkRequest(info: data)
                CompositionReactiveCompatible.share.exitNorth(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(showOperationName.replacingOccurrences(of: "resent", with: "v") + "edDate")] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: show_appealValue.map{popularDestroy(skirt: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(kSplitBranchGuideKey.replacingOccurrences(of: "detect", with: "at"))][(app_technologyFormat.replacingOccurrences(of: "remove", with: "yp"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: kDriveSpherePath.map{$0^111}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                AppBaseDelegate.smallFeature(uid: String(json[(kSplitBranchGuideKey.replacingOccurrences(of: "detect", with: "at"))][(String(k_nearbyStr))].intValue),
                                               //: title: data["nickname"] as? String,
                                               title: data[(show_tingUrl.replacingOccurrences(of: "move", with: "n") + user_sourceValue.replacingOccurrences(of: "discount", with: "m"))] as? String,
                                               //: body: body,
                                               body: body,
                                               //: imageUrl: data["headPic"] as? String)
                                               imageUrl: data[(String(show_publishId.suffix(7)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                AppBaseDelegate.shared.look()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(userDirectionFormat) + String(data_startValue.prefix(7))) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isStrategy = true
            self.isStrategy = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            ordinalPing()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(userDirectionFormat) + String(data_startValue.prefix(7)))] = "1"
            //: CompositionReactiveCompatible.share.recieve1v1TalkResumeRequest(info: resumeData)
            CompositionReactiveCompatible.share.postulation(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (SceneKingfisherCompatible.shared as! SceneKingfisherCompatible).shapeInfo()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (showExerciseComePath.replacingOccurrences(of: "camera", with: "l") + String(noti_ablePath.prefix(4))) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isStrategy = false
            self.isStrategy = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            tutorial()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.zygotene(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                AppBaseDelegate.sustenance(identifier: String(json[(kSplitBranchGuideKey.replacingOccurrences(of: "detect", with: "at"))][(String(k_nearbyStr))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                AppBaseDelegate.shared.blackSize()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(userPlayMessage)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isStrategy = false
            self.isStrategy = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            tutorial()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.entire(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(userGoKey.suffix(6)) + "Moti" + String(constIdentificationTitle)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if CompositionReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.chatRaw?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(user_movieFunFormat.prefix(4)) + "veWa" + show_commitUrl.replacingOccurrences(of: "tick", with: "in")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            SelectedThen.awake(content: msg, reason: json[(kSplitBranchGuideKey.replacingOccurrences(of: "detect", with: "at"))][(data_relieveValue.replacingOccurrences(of: "information", with: "as"))].stringValue, time: json[(kSplitBranchGuideKey.replacingOccurrences(of: "detect", with: "at"))][(notiNeighborData.replacingOccurrences(of: "analyze", with: "me"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(userSplitId) + String(data_depthUrl.prefix(5))) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            SelectedThen.panCaptureShared().miss()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(userMiniMessage.prefix(5)) + "dPro" + kTechnologyKey.replacingOccurrences(of: "support", with: "am") + "Gift") { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.through(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(data_agencyName.prefix(4)) + "meFs") { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(app_showOldTitle))] as? Int ?? -1
            //: if CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: noti_restoreValue, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: user_failFormat, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: noti_restoreValue, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(kMaybeStr) + String(data_clingUrl.prefix(7))) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            SpeakThen.quickSuiteStill().subscribeThat(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(main_featherStr) + showUnlessValue.lowercased() + mainDailyManFrameFormat.replacingOccurrences(of: "cur", with: "ti") + "nPopupWin") { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            GenerateingReactiveCompatible.shared.anPageDic(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(userReLocalFunctionStr)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            SelectedThen.panCaptureShared().miss()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: constMinNameTitle, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension FileSocketManager {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func salem(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (userLoseValue.replacingOccurrences(of: "lat", with: "q") + String(notiEasternUrl)) || cmdName == (String(appUserPath.suffix(5)) + "uestCall") {
            //: if TalkingLiveManager.shared().isLive == true {
            if SelectedThen.panCaptureShared().isLive == true {
                //: NEED_RESTORE_LIVE = true
                constToTitle = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                SelectedThen.who(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                SelectedThen.panCaptureShared().miss()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard constToTitle == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (showExerciseComePath.replacingOccurrences(of: "camera", with: "l") + String(noti_ablePath.prefix(4))) || cmdName == (String(user_acceptKey.suffix(6)) + "elCall") ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(userPlayMessage)) || cmdName == (String(notiBusinessMessage) + user_servePath.replacingOccurrences(of: "transform", with: "ll")) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(appDonKey.prefix(8))) || cmdName == (String(data_environmentContent) + String(noti_secretStr.prefix(4)))
        {
            //: NEED_RESTORE_LIVE = false
            constToTitle = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = ChinPromiseViewReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                SelectedThen.panCaptureShared().translation()
            }
            //: alert.show()
            alert.announcementOf()
        }
    }

    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func extend(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        MergeRatingManager.shared.enabled()
        //: guard CompositionReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else {
        guard CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(const_familyCropMessage) + String(mainUntilRestSystemContent))] as? Bool == false {
            //: SightReactiveCompatible.share.func_pushToDiscountRechargeView()
            SightReactiveCompatible.share.personage()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(appSituationValue.replacingOccurrences(of: "possibility", with: "e"))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (notiSizeFormat.replacingOccurrences(of: "reading", with: "te") + String(dataChooseFormat.prefix(3))) || source == (noti_girlWordUrl.lowercased() + "oPop") else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(appFactorStr.replacingOccurrences(of: "fragment", with: "o"))] as? Int) ?? 0 < 60 {
            //: SightReactiveCompatible.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            SightReactiveCompatible.share.patched(webViewType: .RechargeHalfPage)
        }
    }
}
