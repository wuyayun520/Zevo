
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let showPastUrl:String = "limit chemical existmf/b"
fileprivate let mainFeedMessage:String = "ess/gimedia resource plant labor harassment"
fileprivate let noti_publicityTitle:[Character] = ["f"]
fileprivate let data_skilledFormat:[Character] = ["t","L","i","s","t"]

/*: "type" :*/
fileprivate let noti_normalPath:[UInt8] = [0x4f,0x42,0x4b,0x5e]

private func aroundStrikeTarget(look num: UInt8) -> UInt8 {
    return num ^ 59
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let k_cleanBorderFormat:String = "address lift privacy cookingmf/cha"
fileprivate let user_bringMustUrl:String = "tRoodown coordinator equity"
fileprivate let mainClipId:[Character] = ["t","L","i","s","t"]

/*: "live/giftList" :*/
fileprivate let constCycleData:[Character] = ["l","i","v","e","/","g"]
fileprivate let constOuterQuickMsg:String = "side sea finger currently boneiftList"

/*: "IM/getMessageList" :*/
fileprivate let data_fitPath:String = "giving performIM/get"
fileprivate let app_lovelyWindowTitle:[Character] = ["L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let constHiddenStr:[Character] = ["m","f","/","b","u","s","i","n","e","s"]
fileprivate let dataEveryAttentionFormat:String = "finite approval clouds/s"
fileprivate let userWorkerName:String = "endMsgboth truth feedback smooth"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let dataAnimaStr:String = "mouth notice orbmf/ch"
fileprivate let appReflectionMessage:String = "detail analysis identitym/sen"

/*: "mf/business/sendGift" :*/
fileprivate let k_collectionStr:String = "mini automatic argument shirtmf/bu"
fileprivate let appBranchData:[Character] = ["s","i","n","e","s"]
fileprivate let appAgainstSeparateTitle:String = "lose yesterday main contents/sen"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let k_throughId:[Character] = ["m","f","/","c","h","a","t","R","o","o","m"]
fileprivate let mainBindUrl:[Character] = ["/","s","e","n","d","G","i","f","t"]

/*: "live/sendGift" :*/
fileprivate let const_minName:String = "event still category lightlive/s"

/*: "mf/user/getChatInfo" :*/
fileprivate let data_contrastMessage:String = "separate session overlookmf/use"
fileprivate let main_anonymousId:String = "user portraitr/get"
fileprivate let app_quoteUrl:String = "center category trace publicityInfo"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let constDialogStr:String = "off period closedIM/g"
fileprivate let notiCarryTitle:String = "houseag"
fileprivate let k_entireUrl:String = "InfoLisprint middle"
fileprivate let showEngagePath:[Character] = ["t"]

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let mainTopName:[Character] = ["m","f","/","c","r","u","s","h","/","w","h","o","L","i"]
fileprivate let k_worldwideName:[Character] = ["k","e","M","e","L","i","s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let mainLandBlueMessage:[Character] = ["m","f","/","c","r","u","s","h","/","i","g","n","o"]
fileprivate let user_foreStr:[Character] = ["r","e"]

/*: "mf/crush/like" :*/
fileprivate let app_toId:String = "mf/crvalue talk middle"
fileprivate let kDealValue:String = "lmultie"

/*: "mf/user/getRelation" :*/
fileprivate let main_workplaceLabelYellowMessage:String = "mf/usrefuse canvas fade finish"
fileprivate let kMarkValue:String = "tRelthrow cell progress"
fileprivate let appWarnUrl:[Character] = ["a","t","i","o","n"]

/*: "user/attention" :*/
fileprivate let app_blackWeightMsg:[UInt8] = [0x6e,0x6f,0x69,0x74,0x6e,0x65,0x74,0x74,0x61,0x2f,0x72,0x65,0x73,0x75]

/*: "user/removeAttention" :*/
fileprivate let show_identifyId:String = "user/refer entire contrast human"
fileprivate let showChallengeTitle:String = "rmixo"
fileprivate let show_readingFormat:String = "veAttemid wide distance"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let constGalleryStr:[UInt8] = [0x3c,0x1e,0x11,0x58,0xb,0x5f,0x1a,0x1e,0xd,0x11,0x5f,0xf,0x10,0x16,0x11,0xb,0xc,0x5f,0x16,0x19,0x5f,0x6,0x10,0xa,0x5f,0x19,0x10,0x13,0x13,0x10,0x8,0x5f,0x1a,0x1e,0x1c,0x17,0x5f,0x10,0xb,0x17,0x1a,0xd,0x1]

private func militaryAcquisition(interaction num: UInt8) -> UInt8 {
    return num ^ 127
}

/*: "follow Success" :*/
fileprivate let appEditPriceText:[Character] = ["f","o","l","l","o","w"," ","S","u","c"]
fileprivate let notiViolationMessage:String = "csinces"

/*: "unfollow Success" :*/
fileprivate let userCurrencyId:[Character] = ["u","n","f","o","l","l","o","w"," ","S","u","c","c","e","s","s"]

/*: "translate/msg" :*/
fileprivate let user_endUrl:String = "TRAN"
fileprivate let const_minuteShotText:[Character] = ["e","/","m","s","g"]

/*: "msgId" :*/
fileprivate let userFeatherStr:[UInt8] = [0xa8,0xb6,0xa2,0x8c,0xa1]

/*: "targetLng" :*/
fileprivate let kAdvertisingStr:[UInt8] = [0xfe,0xeb,0xfc,0xf1,0xef,0xfe,0xd6,0xf8,0xf1]

fileprivate func proposeServe(index num: UInt8) -> UInt8 {
    let value = Int(num) - 138
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let showDoingingSphereKey:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","A","u","t","o","G","r","e","e","t","C","o","n","t"]
fileprivate let const_mentionTitle:String = "enpositive"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let showPanelHimKey:String = "mf/woman partner disabled stat"
fileprivate let userWhetherDayId:[Character] = ["g","r","e","e","t","/","g","e","t","I","n","t","i","m","a","t","e","P","h","o","t","o"]

/*: "toUid" :*/
fileprivate let mainYaContent:[UInt8] = [0x47,0x5c,0x66,0x5a,0x57]

/*: "mf/chatRoom/info" :*/
fileprivate let dataTransformMsg:[Character] = ["m","f","/","c","h","a"]
fileprivate let notiEmberReachNameMessage:String = "slide what discourse functiontRoom/"

/*: "mf/chatRoom/list" :*/
fileprivate let constTechnicalKey:[Character] = ["m","f","/","c","h","a","t"]
fileprivate let app_careerRenderPlantUrl:String = "Room/page challenge following"

/*: "roomId" :*/
fileprivate let show_everyoneKey:[UInt8] = [0x64,0x49,0x6d,0x6f,0x6f,0x72]

/*: "needExtraInfo" :*/
fileprivate let dataCoursePath:[UInt8] = [0xd8,0xd3,0xd3,0xd2,0xf3,0xce,0xc2,0xc4,0xd7,0xff,0xd8,0xd0,0xd9]

/*: "mf/chatRoom/check" :*/
fileprivate let showMaxData:String = "method overlookmf/cha"
fileprivate let show_wrapValue:String = "m/checkre pop connection heavy"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let showExtendFormat:String = "mf/ctransition short sacrifice ping access"
fileprivate let main_pendingMsg:String = "om/joisomeone lease principal robot"

/*: "mf/chatRoom/members" :*/
fileprivate let noti_timeFormat:[Character] = ["m","f","/","c","h"]
fileprivate let main_distributionKey:String = "skip cell featureatRo"
fileprivate let main_termsStr:String = "bolders"

/*: "translate/text" :*/
fileprivate let show_particleMsg:[Character] = ["t","r","a","n","s","l","a","t","e","/","t","e"]
fileprivate let k_beginFormat:String = "announcementt"

/*: "text" :*/
fileprivate let noti_powCarveData:[UInt8] = [0x8f,0x9e,0x83,0x8f]

private func fireDam(scene num: UInt8) -> UInt8 {
    return num ^ 251
}

/*: "mf/user/queryStatus" :*/
fileprivate let notiRecommendPath:[Character] = ["m","f","/","u","s","e"]
fileprivate let kPowPath:String = "r/quevar resistance"

/*: "toUids" :*/
fileprivate let appOrganicPreservePath:[UInt8] = [0x55,0x4e,0x74,0x48,0x45,0x52]

private func colorPin(correct num: UInt8) -> UInt8 {
    return num ^ 33
}

/*: "call/getCallTab" :*/
fileprivate let main_historyMsg:[Character] = ["c","a","l","l","/","g","e"]
fileprivate let data_problemFormat:String = "tCallTabconsent zz thumb"

/*: "mf/user/remark" :*/
fileprivate let appExpressionName:[Character] = ["m","f","/","u","s","e","r","/","r","e","m"]
fileprivate let show_statementKey:[Character] = ["a","r","k"]

/*: "content" :*/
fileprivate let kResponseData:[UInt8] = [0xba,0xc6,0xc5,0xcb,0xbc,0xc5,0xcb]

fileprivate func weddingCeremony(fort num: UInt8) -> UInt8 {
    let value = Int(num) - 87
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "top" :*/
fileprivate let kIsolateMsg:[UInt8] = [0x5b,0x40,0x5f]

private func proTem(manager num: UInt8) -> UInt8 {
    return num ^ 47
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ReminderRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class TalkingChatRequestTool: NSObject {
public class ReminderRequestTool: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [ChinMeasurable]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [ChinMeasurable]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func capacityWho(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(showPastUrl.suffix(4)) + "usin" + String(mainFeedMessage.prefix(6)) + String(noti_publicityTitle) + String(data_skilledFormat))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: noti_normalPath.map{aroundStrikeTarget(look: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func too(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(k_cleanBorderFormat.suffix(6)) + String(user_bringMustUrl.prefix(4)) + "m/gif" + String(mainClipId))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func minimumAgain(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(constCycleData) + String(constOuterQuickMsg.suffix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func spend(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(data_fitPath.suffix(6)) + "Message" + String(app_lovelyWindowTitle))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func followingActive(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(constHiddenStr) + String(dataEveryAttentionFormat.suffix(3)) + String(userWorkerName.prefix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func setBuild(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(dataAnimaStr.suffix(5)) + "atRoo" + String(appReflectionMessage.suffix(5)) + "dMsg")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sheerCircle(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(k_collectionStr.suffix(5)) + String(appBranchData) + String(appAgainstSeparateTitle.suffix(5)) + "dGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func series(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(k_throughId) + String(mainBindUrl))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func whiteCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(const_minName.suffix(6)) + "endGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sectionRefresh(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(data_contrastMessage.suffix(6)) + String(main_anonymousId.suffix(5)) + "Chat" + String(app_quoteUrl.suffix(4)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func straightAndNarrow(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(constDialogStr.suffix(4)) + "etMe" + notiCarryTitle.replacingOccurrences(of: "house", with: "ss") + "eUser" + String(k_entireUrl.prefix(7)) + String(showEngagePath))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<ChinMeasurable>.deserialize(from: result as? Array) {
            if let datas = Array<ChinMeasurable>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [ChinMeasurable])
                completion(succeed, datas as! [ChinMeasurable])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func formatCompletion(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        constNextKey.everyScreen(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func system(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(mainTopName) + String(k_worldwideName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func exampleComment(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(mainLandBlueMessage) + String(user_foreStr))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func tillWho(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(app_toId.prefix(5)) + "ush/" + kDealValue.replacingOccurrences(of: "multi", with: "ik"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func inmateHair(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(main_workplaceLabelYellowMessage.prefix(5)) + "er/ge" + String(kMarkValue.prefix(4)) + String(appWarnUrl))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func saveCompletion(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: app_blackWeightMsg.reversed(), encoding: .utf8)! : (String(show_identifyId.prefix(5)) + showChallengeTitle.replacingOccurrences(of: "mix", with: "em") + String(show_readingFormat.prefix(6)) + "ntion")
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if CompositionReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
                    if CompositionReactiveCompatible.share.loginUserMode.sex == ArrowGender.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        appRecordValue.through(showMsg: String(bytes: constGalleryStr.map{militaryAcquisition(interaction: $0)}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        appRecordValue.through(showMsg: (String(appEditPriceText) + notiViolationMessage.replacingOccurrences(of: "since", with: "es")).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    appRecordValue.through(showMsg: (String(userCurrencyId)).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                appRecordValue.foolishness(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func listReading(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (user_endUrl.lowercased() + "slat" + String(const_minuteShotText))
        //: reqModel.params = ["msgId": msgId, "targetLng": DirtyMacroDefine.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: userFeatherStr.map{$0^197}, encoding: .utf8)!: msgId, String(bytes: kAdvertisingStr.map{proposeServe(index: $0)}, encoding: .utf8)!: DirtyMacroDefine.instrumentalityClick(), String(bytes: noti_normalPath.map{aroundStrikeTarget(look: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func get(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(showDoingingSphereKey) + const_mentionTitle.replacingOccurrences(of: "positive", with: "t"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func directCompletion(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(showPanelHimKey.prefix(3)) + String(userWhetherDayId))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: mainYaContent.map{$0^51}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func asAttribute(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(dataTransformMsg) + String(notiEmberReachNameMessage.suffix(6)) + "info")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func layerM(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(constTechnicalKey) + String(app_careerRenderPlantUrl.prefix(5)) + "list")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func betweenBoxOrigin(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: show_everyoneKey.reversed(), encoding: .utf8)!: roomId, String(bytes: dataCoursePath.map{$0^182}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(showMaxData.suffix(6)) + "tRoo" + String(show_wrapValue.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func comeDownInward(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(showExtendFormat.prefix(4)) + "hatRo" + String(main_pendingMsg.prefix(6)) + "nOrQuit")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func belowMemory(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(noti_timeFormat) + String(main_distributionKey.suffix(4)) + "om/me" + main_termsStr.replacingOccurrences(of: "bold", with: "mb"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func subjectMatterCompletion(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (String(show_particleMsg) + k_beginFormat.replacingOccurrences(of: "announcement", with: "x"))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: noti_powCarveData.map{fireDam(scene: $0)}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func analogDigitalConverterDrownCompletion(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(notiRecommendPath) + String(kPowPath.prefix(5)) + "ryStatus")
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: appOrganicPreservePath.map{colorPin(correct: $0)}, encoding: .utf8)!: toUids, String(bytes: noti_normalPath.map{aroundStrikeTarget(look: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func spread(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(main_historyMsg) + String(data_problemFormat.prefix(8)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func ballpark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(appExpressionName) + String(show_statementKey))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: kResponseData.map{weddingCeremony(fort: $0)}, encoding: .utf8)!: content, String(bytes: kIsolateMsg.map{proTem(manager: $0)}, encoding: .utf8)!: isTop, String(bytes: mainYaContent.map{$0^51}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
