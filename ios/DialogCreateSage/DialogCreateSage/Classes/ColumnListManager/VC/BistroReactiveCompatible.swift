
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let user_egoStrikeMsg:[Character] = ["b","g","_","m","e","s","s","a","g"]
fileprivate let showNumbText:String = "orange behind easee_top"

/*: "777777" :*/
fileprivate let k_feedbackName:[Character] = ["7","7","7","7","7","7"]

/*: "Messages" :*/
fileprivate let user_portraitFormat:String = "birthday pot phoneMessage"
fileprivate let userSeaTitle:String = "than"

/*: "Who like me" :*/
fileprivate let appTurnBusyAppearanceMessage:[Character] = ["W","h","o"," ","l","i","k","e"," ","m","e"]

/*: "Call" :*/
fileprivate let k_namePotFormat:[Character] = ["C","a","l","l"]

/*: "#FF2348" :*/
fileprivate let app_coordinatorValue:[Character] = ["#","F","F","2","3","4","8"]

/*: "Current network unavailable" :*/
fileprivate let noti_pairAcquireProcedureValue:String = "consist entitleCurren"
fileprivate let show_richKey:String = "ork cling cute skirt"
fileprivate let userRemarkMsg:[Character] = ["u","n","a","v","a","i","l","a","b","l","e"]

/*: "icon_yidu_pre" :*/
fileprivate let kAllowName:String = "icpilen"
fileprivate let data_momentPicMsg:[Character] = ["_","y","i","d","u","_","p","r","e"]

/*: "transform.rotation" :*/
fileprivate let appSuspendStr:[Character] = ["t","r","a","n","s","f","o","r","m",".","r","o","t"]
fileprivate let mainImageStr:String = "ATION"

/*: "transform.scale" :*/
fileprivate let notiAfterName:[Character] = ["t","r","a","n","s","f","o","r","m"]
fileprivate let const_restaurantData:[Character] = ["."]
fileprivate let showObjectPassageServePath:String = "scaarea"

/*: "zoom&shake" :*/
fileprivate let mainAllegedGlobalTitle:[Character] = ["z","o","o","m","&","s","h","a"]
fileprivate let notiInstallStackOriginId:String = "bug"

/*: "yyyy-MM-dd" :*/
fileprivate let dataMultipleKey:[Character] = ["y","y","y","y","-","M","M","-","d","d"]

/*: "Allow %@ to send you notifications?" :*/
fileprivate let data_bassValue:[UInt8] = [0xa2,0x8f,0x8f,0x8c,0x94,0xc3,0xc6,0xa3,0xc3,0x97,0x8c,0xc3,0x90,0x86,0x8d,0x87,0xc3,0x9a,0x8c,0x96,0xc3,0x8d,0x8c,0x97,0x8a,0x85,0x8a,0x80,0x82,0x97,0x8a,0x8c,0x8d,0x90,0xdc]

private func detectDark(early num: UInt8) -> UInt8 {
    return num ^ 227
}

/*: "Cancel" :*/
fileprivate let noti_ownKey:[Character] = ["C","a","n","c","e","l"]

/*: "Settings" :*/
fileprivate let dataSuperiorTitle:String = "Sechoice active capacity"
fileprivate let constOutsideId:[Character] = ["t","t","i","n","g","s"]

/*: "badNumber" :*/
fileprivate let showOpeneFormat:String = "badNrecognize career definition"
fileprivate let app_acrossAlbumKey:String = "UMBER"

/*: "isConnection" :*/
fileprivate let const_executeContent:String = "illegal"
fileprivate let app_ridFormat:String = "flow draft designsConne"

/*: "networkStatus" :*/
fileprivate let user_materialValue:[Character] = ["n","e","t","w","o","r","k","S"]
fileprivate let constPlaceFastMsg:String = "TATUS"

/*: "unreadMessageNum" :*/
fileprivate let k_cookingFormat:[Character] = ["u","n","r","e","a","d","M","e","s"]
fileprivate let appSessionKey:String = "far abs financial para processorsageNum"

/*: "Do you want to mark all messages as read?" :*/
fileprivate let appSpreadData:[UInt8] = [0x6c,0x47,0x8,0x51,0x47,0x5d,0x8,0x5f,0x49,0x46,0x5c,0x8,0x5c,0x47,0x8,0x45,0x49,0x5a,0x43,0x8,0x49,0x44,0x44,0x8,0x45,0x4d,0x5b,0x5b,0x49,0x4f,0x4d,0x5b,0x8,0x49,0x5b,0x8,0x5a,0x4d,0x49,0x4c,0x17]

private func observerAnonymous(pad num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "消息列表一键已读失败：code: :*/
fileprivate let kEnablelyPath:[Character] = ["消","\u{606f}","列","\u{8868}","一","\u{952e}","\u{5df2}","读","失"]
fileprivate let k_makerKey:String = "败：code:pull immediate"

/*: , desc: :*/
fileprivate let noti_stereoFormat:String = "main exit render bench hand, desc"
fileprivate let main_peerAccountName:[Character] = [":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BistroReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class BistroReactiveCompatible: MergeContactViewController {
    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        cleanExit()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        musculusAbductorPollicisDay()
        //: bindInteraction()
        spreadBind()
        //: func__turnOnSystemNotification()
        hiddenPut()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: user_needValue, height: 100 + userResolutionUrl))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.parentSet(name: (String(user_egoStrikeMsg) + String(showNumbText.suffix(5))))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: BucculaView = {
        //: let V = TalkingNoticeTipView()
        let V = BucculaView()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: ExThen = {
        //: let vc = QYSlideNavigationViewController()
        let vc = ExThen()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .processAcross(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .processAcross(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (String(k_feedbackName)))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.antiquityColor()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [MergeContactViewController] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, AssistViewDelegate()]
        //: if CompositionReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
        if CompositionReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(PictureViewController(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: GiantViewController = {
        //: let vc = TalkingChatListViewController()
        let vc = GiantViewController()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(user_portraitFormat.suffix(7)) + userSeaTitle.replacingOccurrences(of: "than", with: "s")).localized, (String(appTurnBusyAppearanceMessage)).localized]
        //: if CompositionReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
        if CompositionReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(k_namePotFormat)).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: userResolutionUrl, width: user_needValue, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.joinSize(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(app_coordinatorValue)))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(noti_pairAcquireProcedureValue.suffix(6)) + "t netw" + String(show_richKey.prefix(4)) + String(userRemarkMsg)).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: CoronaBadgeLab = {
        //: let numbLabel = BadgeLab()
        let numbLabel = CoronaBadgeLab()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: CoronaBadgeLab = {
        //: let numbLabel = BadgeLab()
        let numbLabel = CoronaBadgeLab()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.parentSet(name: (kAllowName.replacingOccurrences(of: "pile", with: "o") + String(data_momentPicMsg))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.parentSet(name: (kAllowName.replacingOccurrences(of: "pile", with: "o") + String(data_momentPicMsg))), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - SerialNavigationDelegate

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension BistroReactiveCompatible: SerialNavigationDelegate {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func light(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: GiantViewController.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            cleanExit()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: AssistViewDelegate.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            noti_liveLevelUrl.recordGender(eventID: notiSuccessFaceEnterValue)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension BistroReactiveCompatible {
    //: func setIsTopAll() {
    func topAlive() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? GiantViewController
            //: vc?.resetToTopItemView()
            vc?.sufficientPrimaryWeltanschauungView()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.personLowerClass(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func dateBean() {
        //: if CompositionReactiveCompatible.share.networkStatus != .Unavailable && LightConversationListener.shared.isConnection {
        if CompositionReactiveCompatible.share.networkStatus != .Unavailable, LightConversationListener.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func commonwealth(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func cleanExit() {
        //: let unreadMsgCount = CompositionReactiveCompatible.share.unreadMessageNum
        let unreadMsgCount = CompositionReactiveCompatible.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: const_requestContent) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.exceptLimitAnimat()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func exceptLimitAnimat() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (String(appSuspendStr) + mainImageStr.lowercased()))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (String(notiAfterName) + String(const_restaurantData) + showObjectPassageServePath.replacingOccurrences(of: "area", with: "le")))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (String(mainAllegedGlobalTitle) + notiInstallStackOriginId.replacingOccurrences(of: "bug", with: "ke")))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func hiddenPut() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        MeetRouteThen.showThat { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.languageMultiBecloudArray(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.viewPress(date: Date(), dateFormat: (String(dataMultipleKey)))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = user_liveMsg.string(forKey: const_currencyUrl), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.languageMultiBecloudArray(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.languageMultiBecloudArray(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = user_liveMsg.bool(forKey: k_ofData)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        user_liveMsg.set(true, forKey: k_ofData)
                        //: TalkingAlertShow.alert(title: nil,
                        YetAlertShow.trackSpace(title: nil,
                                                    //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                    message: String(bytes: data_bassValue.map{detectDark(early: $0)}, encoding: .utf8)!.temporaryWorker(k_warnUrl),
                                                    //: leftBtnTitle: "Cancel".localized,
                                                    leftBtnTitle: (String(noti_ownKey)).localized,
                                                    //: rightBtnTitle: "Settings".localized) {
                                                    rightBtnTitle: (String(dataSuperiorTitle.prefix(2)) + String(constOutsideId)).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            YetAlertShow.usance()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func languageMultiBecloudArray(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(noti_guideRequestId)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = kStreamTitle - noti_guideRequestId - mainExplainFaceName
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(noti_guideRequestId + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = kStreamTitle - self.noticeView.bottom - mainExplainFaceName
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension BistroReactiveCompatible {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func viewTable(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(showOpeneFormat.prefix(4)) + app_acrossAlbumKey.lowercased())] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension BistroReactiveCompatible {
    /// UI
    //: private func createUI() {
    private func musculusAbductorPollicisDay() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(noti_guideRequestId)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(appTurnBusyAppearanceMessage)).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(user_portraitFormat.suffix(7)) + userSeaTitle.replacingOccurrences(of: "than", with: "s")).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func spreadBind() {
        //: LightConversationListener.shared.rx
        LightConversationListener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (const_executeContent.replacingOccurrences(of: "illegal", with: "i") + String(app_ridFormat.suffix(6)) + "ction"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.dateBean()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: CompositionReactiveCompatible.share.rx.observeWeakly(Int.self, "networkStatus")
        CompositionReactiveCompatible.share.rx.observeWeakly(Int.self, (String(user_materialValue) + constPlaceFastMsg.lowercased()))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.dateBean()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: CompositionReactiveCompatible.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        CompositionReactiveCompatible.share.rx.observeWeakly(Int.self, (String(k_cookingFormat) + String(appSessionKey.suffix(7))))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.commonwealth(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = ReminderAlertConfig()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                YetAlertShow.humanRightModel(message: String(bytes: appSpreadData.map{observerAnonymous(pad: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(noti_ownKey)).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    YetAlertShow.usance()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: const_requestContent)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
                        FinishThen.onicial(msg: (String(kEnablelyPath) + String(k_makerKey.prefix(7))) + "\(code)" + (String(noti_stereoFormat.suffix(6)) + String(main_peerAccountName)) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(viewTable(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: main_beautyMessage,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.viewPress(date: Date(), dateFormat: (String(dataMultipleKey)))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            user_liveMsg.set(today, forKey: const_currencyUrl)
            //: self.func__hideNotificationTipView(hide: true)
            self.languageMultiBecloudArray(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.hiddenPut()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
