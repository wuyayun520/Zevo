
//: Declare String Begin

/*: "setPassword" :*/
fileprivate let constButtonName:String = "trust editor tail consumer towardsetPassw"
fileprivate let data_burnName:[Character] = ["o","r","d"]

/*: "user/appleLogin" :*/
fileprivate let app_laborPath:String = "ufocuser"
fileprivate let show_projectValue:String = "native replacement catch medium estateeLogin"

/*: "user/googleLogin" :*/
fileprivate let appBigFormat:[Character] = ["u","s","e","r","/","g","o","o","g","l","e","L","o","g"]
fileprivate let app_actionUrl:String = "resentn"

/*: "user/smsLogin" :*/
fileprivate let k_withinCharmId:[Character] = ["u","s","e","r","/","s","m","s","L","o"]
fileprivate let show_cropStr:String = "gplatformn"

/*: "user/emailVcodeLogin" :*/
fileprivate let app_allBothVarietyValue:String = "user/eground hand appropriate"
fileprivate let app_rankMagnitudeMsg:String = "odeLogviolation appropriate giant"
fileprivate let show_complyMsg:String = "totn"

/*: "user/pwdLogin" :*/
fileprivate let mainSpecUsMsg:String = "bind exactly yet singleuser/"
fileprivate let data_serveKey:String = "ogsink"

/*: "user/emailPwdLogin" :*/
fileprivate let appReUrl:String = "user/ewhat compete toward"
fileprivate let mainPersonalityData:String = "factor"
fileprivate let app_mirrorText:[Character] = ["i","l","P","w","d","L","o","g","i","n"]

/*: "mobile" :*/
fileprivate let app_pendingHiddenBehindMsg:[UInt8] = [0xf5,0xf7,0xfa,0xf1,0xf4,0xfd]

private func getMagnitudeerval(then num: UInt8) -> UInt8 {
    return num ^ 152
}

/*: "areaCode" :*/
fileprivate let k_connectionSphereExclusiveStr:[UInt8] = [0x92,0xa3,0x96,0x92,0x74,0xa0,0x95,0x96]

fileprivate func individualHome(lick num: UInt8) -> UInt8 {
    let value = Int(num) + 207
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/sendLoginSms" :*/
fileprivate let constFailureHypothesisMsg:String = "user/statement eff weight calculate when"
fileprivate let dataCommitValue:[Character] = ["s","e","n","d"]
fileprivate let const_phenomenonPinStr:String = "LoginSmshoney administrative don"

/*: "RSA加密失败" :*/
fileprivate let appPreviousText:[Character] = ["R","S","A","加","密","失"]
fileprivate let noti_consentPath:String = "surgery"

/*: "password" :*/
fileprivate let show_aircraftData:[UInt8] = [0x64,0x72,0x6f,0x77,0x73,0x73,0x61,0x70]

/*: "vcode" :*/
fileprivate let k_portHimFormat:[UInt8] = [0x17,0x2,0xe,0x5,0x4]

private func recognizeWhy(police num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "user/modifyPassword" :*/
fileprivate let noti_whichData:[Character] = ["u","s","e","r","/","m","o","d","i","f","y","P","a","s","s","w"]
fileprivate let dataDetectUrl:String = "ordisappear"

/*: "user/sendModifyPwdSms" :*/
fileprivate let mainManStr:[Character] = ["u","s","e","r","/","s","e","n","d","M","o","d","i","f"]
fileprivate let app_skirtId:[Character] = ["y","P"]
fileprivate let data_postShakeUrl:String = "way database would tellwdSms"

/*: "user/sendModifyPwdEmail" :*/
fileprivate let mainSmoothPath:String = "USER"
fileprivate let notiAdvertisingData:String = "discoverif"
fileprivate let app_keepTagText:[Character] = ["m","a","i","l"]

/*: "mf/user/getMyInfo" :*/
fileprivate let kDefineId:String = "mf/usename trust listener word own"
fileprivate let showRejectName:String = "Infobase guard you oval"

/*: "invite/bindInviter" :*/
fileprivate let userPaperFormat:String = "inchallenget"
fileprivate let app_thanAspectKey:String = "ndInvicompare alongside wed mention editor"
fileprivate let show_signName:[Character] = ["t","e","r"]

/*: "invite/isBind" :*/
fileprivate let k_beautyId:String = "iexistingvite"

/*: "mf/user/CheckInviteCode" :*/
fileprivate let notiOutsideSpendName:[Character] = ["m","f","/","u","s","e","r","/","C","h","e","c","k","I","n","v"]
fileprivate let dataFloorAlwaysTitleFormat:String = "iteCodebehind that magnitude visual"

/*: "mf/user/updateInfo" :*/
fileprivate let noti_fadeId:String = "mobile bitemf/u"
fileprivate let const_instructionTitle:[Character] = ["p","d","a","t","e","I","n","f","o"]

/*: "email" :*/
fileprivate let k_needName:[UInt8] = [0xe2,0xea,0xe6,0xee,0xeb]

private func effExist(just num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "user/getEmailBindVCode" :*/
fileprivate let show_programFormat:String = "user/gesea condition"
fileprivate let user_natureSlideTitle:[Character] = ["t","E","m","a","i","l","B","i","n","d","V","C","o","d","e"]

/*: "user/sendLoginEmail" :*/
fileprivate let show_bondStr:[Character] = ["u","s","e","r","/","s","e","n","d","L","o","g","i","n","E","m","a","i","l"]

/*: "user/emailBind" :*/
fileprivate let show_cramDriveMessage:[Character] = ["u","s","e","r","/"]
fileprivate let appEarlyName:[Character] = ["e","m","a","i","l","B","i","n","d"]

/*: "user/logout" :*/
fileprivate let mainDoingePath:String = "user/yellow coordinator"
fileprivate let app_becomeStr:String = "communicate"

/*: "app/countryList" :*/
fileprivate let constFindData:[Character] = ["a","p","p","/","c","o","u","n","t","r","y"]
fileprivate let dataDelayFormat:String = "Listhi error manage"

/*: "user/getMobileBindVCode" :*/
fileprivate let data_workName:[Character] = ["u","s","e","r","/","g"]
fileprivate let showAlreadyData:String = "pot lovely conversation provider anyetMobil"
fileprivate let data_meetingMsg:[Character] = ["e","B","i","n","d","V","C","o","d","e"]

/*: "user/mobileBind" :*/
fileprivate let notiReaderErrorName:String = "user/mchange separate blind decide lay"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MidRequestTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/29.
//

//: import UIKit
import UIKit

//: class TalkingLoginRequestTool: NSObject {
class MidRequestTool: NSObject {
    /// 第三方登录请求
    /// - Parameters:
    ///   - type: 登录类型
    ///   - content: 未加密手机号/邮箱
    ///   - params: 参数
    ///   - completion: 回调

    //: class func req_thirdLogin(type: LoginType, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
    class func request(type: AddressContiguousBytes, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = self.req_thirdLoginModel(type: type, params: params)
        let reqModel = self.describe(type: type, params: params)
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: return
                return
            }
            // 跳转设置密码页
            //: let json = JSON(result ?? [:])
            let json = JSON(result ?? [:])
            //: if json["setPassword"].boolValue == true {
            if json[(String(constButtonName.suffix(8)) + String(data_burnName))].boolValue == true {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: SightReactiveCompatible.share.func__pushToSetNewPasswordVC(type: type, content: content, isNewPwd: true)
                SightReactiveCompatible.share.bear(type: type, content: content, isNewPwd: true)
                //: return
                return
            }

            // 获取用户信息
            //: TalkingLoginRequestTool.req_LoginUserInfoData(type, completion: completion)
            MidRequestTool.menu(type, completion: completion)
        }
    }

    /// 第三方登录请求模型
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    /// - Returns: 请求模型
    //: private class func req_thirdLoginModel(type: LoginType, params: [String: String]) -> TalkingRequestModel {
    private class func describe(type: AddressContiguousBytes, params: [String: String]) -> RoundReactiveCompatible {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: switch type {
        switch type {
        //: case .AppleLogin:
        case .AppleLogin:
            //: reqModel.requestPath = "user/appleLogin"
            reqModel.requestPath = (app_laborPath.replacingOccurrences(of: "focus", with: "s") + "/appl" + String(show_projectValue.suffix(6)))
        //: case .GoogleLogin:
        case .GoogleLogin:
            //: reqModel.requestPath = "user/googleLogin"
            reqModel.requestPath = (String(appBigFormat) + app_actionUrl.replacingOccurrences(of: "resent", with: "i"))
        //: case .PhoneLogin:
        case .PhoneLogin:
            //: reqModel.requestPath = "user/smsLogin"
            reqModel.requestPath = (String(k_withinCharmId) + show_cropStr.replacingOccurrences(of: "platform", with: "i"))
        //: case .EmailLogin:
        case .EmailLogin:
            //: reqModel.requestPath = "user/emailVcodeLogin"
            reqModel.requestPath = (String(app_allBothVarietyValue.prefix(6)) + "mailVc" + String(app_rankMagnitudeMsg.prefix(6)) + show_complyMsg.replacingOccurrences(of: "tot", with: "i"))
        //: case .PwdPhoneLogin:
        case .PwdPhoneLogin:
            //: reqModel.requestPath = "user/pwdLogin"
            reqModel.requestPath = (String(mainSpecUsMsg.suffix(5)) + "pwdL" + data_serveKey.replacingOccurrences(of: "sink", with: "in"))
        //: case .PwdEmailLogin:
        case .PwdEmailLogin:
            //: reqModel.requestPath = "user/emailPwdLogin"
            reqModel.requestPath = (String(appReUrl.prefix(6)) + mainPersonalityData.replacingOccurrences(of: "factor", with: "ma") + String(app_mirrorText))
        }
        //: reqModel.params = params
        reqModel.params = params

        //: return reqModel
        return reqModel
    }

    /// 获取验证码
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_getPhoneCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func regularPolygon(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: notiPurchaseProfileCaptureValue)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: app_pendingHiddenBehindMsg.map{getMagnitudeerval(then: $0)}, encoding: .utf8)!: reslutStr, String(bytes: k_connectionSphereExclusiveStr.map{individualHome(lick: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = RoundReactiveCompatible()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/sendLoginSms"
            reqModel.requestPath = (String(constFailureHypothesisMsg.prefix(5)) + String(dataCommitValue) + String(const_phenomenonPinStr.prefix(8)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appPreviousText) + noti_consentPath.replacingOccurrences(of: "surgery", with: "败")))
        }
    }

    /// 密码登录
    /// - Parameters:
    ///   - phoneStr: 手机号登录
    ///   - areaStr: 区号
    ///   - pwd: 密码
    ///   - completion: 回调
    //: class func req_passwordLogin(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func pushVertical(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultPhoneStr = ""
        var resultPhoneStr = ""
        //: var resultCodeStr = ""
        var resultCodeStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: notiPurchaseProfileCaptureValue)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": resultPhoneStr, "areaCode": resultCodeStr, "password": resultPwdStr]
            let params = [String(bytes: app_pendingHiddenBehindMsg.map{getMagnitudeerval(then: $0)}, encoding: .utf8)!: resultPhoneStr, String(bytes: k_connectionSphereExclusiveStr.map{individualHome(lick: $0)}, encoding: .utf8)!: resultCodeStr, String(bytes: show_aircraftData.reversed(), encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdPhoneLogin, params: params, completion: completion)
            MidRequestTool.request(type: .PwdPhoneLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appPreviousText) + noti_consentPath.replacingOccurrences(of: "surgery", with: "败")))
        }
    }

    /// 修改密码
    /// - Parameters:
    ///   - pwd: 密码
    ///   - vcode: 验证码
    ///   - completion: 回调
    //: class func req_modifyPassword(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
    class func completionAllow(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: notiPurchaseProfileCaptureValue)
            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: var params = ["password": resultPwdStr]
            var params = [String(bytes: show_aircraftData.reversed(), encoding: .utf8)!: resultPwdStr]

            //: if vcode?.count ?? 0 > 0 {
            if vcode?.count ?? 0 > 0 {
                //: params["vcode"] = vcode
                params[String(bytes: k_portHimFormat.map{recognizeWhy(police: $0)}, encoding: .utf8)!] = vcode
            }

            //: let reqModel = TalkingRequestModel()
            let reqModel = RoundReactiveCompatible()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/modifyPassword"
            reqModel.requestPath = (String(noti_whichData) + dataDetectUrl.replacingOccurrences(of: "disappear", with: "d"))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appPreviousText) + noti_consentPath.replacingOccurrences(of: "surgery", with: "败")))
        }
    }

    /// 获取修改【手机号】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdSms(completion: @escaping FinishBlock) {
    class func day(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdSms"
        reqModel.requestPath = (String(mainManStr) + String(app_skirtId) + String(data_postShakeUrl.suffix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取修改【邮箱】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdEmail(completion: @escaping FinishBlock) {
    class func tagDown(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdEmail"
        reqModel.requestPath = (mainSmoothPath.lowercased() + "/sendM" + notiAdvertisingData.replacingOccurrences(of: "discover", with: "od") + "yPwdE" + String(app_keepTagText))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户信息接口
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_LoginUserInfoData(_ type: LoginType, completion: FinishBlock? = nil) {
    class func menu(_ type: AddressContiguousBytes, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(kDefineId.prefix(6)) + "r/getMy" + String(showRejectName.prefix(4)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: return
                return
            }
            //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
            user_liveMsg.set(result, forKey: noti_lineImageBlockValue)
            //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<OptReactiveCompatible>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: CompositionReactiveCompatible.share.loginUserMode = userModel
                CompositionReactiveCompatible.share.loginUserMode = userModel

                // 记录上次登录类型
                //: Defaults.set(type.rawValue, forKey: TalkingLastLoginTypeCacheKey)
                user_liveMsg.set(type.rawValue, forKey: dataCancelName)
                //: if CompositionReactiveCompatible.share.loginUserMode.updateInfo == false {
                if CompositionReactiveCompatible.share.loginUserMode.updateInfo == false { // 需要完善资料
                    //: completion?(false, nil, errorModel)
                    completion?(false, nil, errorModel)
                    //: SightReactiveCompatible.share.func__pushToFillUserInfoVC()
                    SightReactiveCompatible.share.pluralForm()
                    //: return
                    return
                }

                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: userRecordMsg, object: nil, userInfo: result as! [String: Any])

                //: if CompositionReactiveCompatible.share.loginUserMode.remindBindEmail == true {
                if CompositionReactiveCompatible.share.loginUserMode.remindBindEmail == true { // 需要绑定邮箱
                    //: DispatchQueue.main.asyncAfter(deadline: .now()+0.5) {
                    DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                        //: SightReactiveCompatible.share.func__pushToLockUserEmailVC(isShowBack: false)
                        SightReactiveCompatible.share.showPer(isShowBack: false)
                    }
                }
            }
        }
    }

    /// 绑定邀请邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_inviteBindInviter(params: [String: String], completion: @escaping FinishBlock) {
    class func analogDigitalConverter(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "invite/bindInviter"
        reqModel.requestPath = (userPaperFormat.replacingOccurrences(of: "challenge", with: "vi") + "e/bi" + String(app_thanAspectKey.prefix(6)) + String(show_signName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 是否已经绑定过邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_isBindInviter(completion: @escaping FinishBlock) {
    class func formCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "invite/isBind"
        reqModel.requestPath = (k_beautyId.replacingOccurrences(of: "existing", with: "n") + "/isBind")

        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测邀请码
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_CheckCodeData(params: [String: String], completion: @escaping FinishBlock) {
    class func device(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/CheckInviteCode"
        reqModel.requestPath = (String(notiOutsideSpendName) + String(dataFloorAlwaysTitleFormat.prefix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新用户信息
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_updateUserInfo(params: [String: Any], completion: @escaping FinishBlock) {
    class func failSecret(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/updateInfo"
        reqModel.requestPath = (String(noti_fadeId.suffix(4)) + "ser/u" + String(const_instructionTitle))
        //: var temparams = params
        var temparams = params
        //: temparams += UIDevice.deviceInfoDic
        temparams += UIDevice.deviceInfoDic
        //: if let poStr = temparams["p0"] {
        if let poStr = temparams["p0"] {
            //: let token = NSDate.getCurrentTimeStamp()
            let token = NSDate.duringDirection()
            //: reqModel.addHeaderToken = token
            reqModel.addHeaderToken = token
            //: let key = token.appending(TokenSaltStr)
            let key = token.appending(dataHomeMsg)
            //: temparams["p0"] = (poStr as? String)?.encrypt(withKey: key)
            temparams["p0"] = (poStr as? String)?.bottom(key)
        }
        //: reqModel.params = temparams
        reqModel.params = temparams
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// Email Code
    /// - Parameters:
    ///   - emailStr: Email
    ///   - emailType: Login or Bind
    //: class func req_getBindEmailCode( emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
    class func note(emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: notiPurchaseProfileCaptureValue)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: let params = ["email": reslutStr]
            let params = [String(bytes: k_needName.map{effExist(just: $0)}, encoding: .utf8)!: reslutStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = RoundReactiveCompatible()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: if isBind {
            if isBind {
                //: reqModel.requestPath = "user/getEmailBindVCode"
                reqModel.requestPath = (String(show_programFormat.prefix(7)) + String(user_natureSlideTitle))
                //: } else {
            } else {
                //: reqModel.requestPath = "user/sendLoginEmail"
                reqModel.requestPath = (String(show_bondStr))
            }
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appPreviousText) + noti_consentPath.replacingOccurrences(of: "surgery", with: "败")))
        }
    }

    /// 绑定邮箱验证码
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_bindEmailCode( emailCode: String, completion: @escaping FinishBlock) {
    class func transportation(emailCode: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "user/emailBind"
        reqModel.requestPath = (String(show_cramDriveMessage) + String(appEarlyName))
        //: reqModel.params = ["vcode": emailCode]
        reqModel.params = [String(bytes: k_portHimFormat.map{recognizeWhy(police: $0)}, encoding: .utf8)!: emailCode]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: class func req_passwordEmailLogin(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func duringSection(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultEmailStr = ""
        var resultEmailStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: notiPurchaseProfileCaptureValue)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["email": resultEmailStr, "password": resultPwdStr]
            let params = [String(bytes: k_needName.map{effExist(just: $0)}, encoding: .utf8)!: resultEmailStr, String(bytes: show_aircraftData.reversed(), encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdEmailLogin, params: params, completion: completion)
            MidRequestTool.request(type: .PwdEmailLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appPreviousText) + noti_consentPath.replacingOccurrences(of: "surgery", with: "败")))
        }
    }

    //: typealias RequestLoginOutBlock = (_ t: Bool) -> Void
    typealias RequestLoginOutBlock = (_ t: Bool) -> Void

    //: @objc class func req_loginOut(completion: @escaping RequestLoginOutBlock) {
    @objc class func disappear(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "user/logout"
        reqModel.requestPath = (String(mainDoingePath.prefix(5)) + "logou" + app_becomeStr.replacingOccurrences(of: "communicate", with: "t"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    //: @objc class func func__requestReloginUserInfo(completion: @escaping RequestLoginOutBlock) {
    @objc class func audienceMiss(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(kDefineId.prefix(6)) + "r/getMy" + String(showRejectName.prefix(4)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
                user_liveMsg.set(result, forKey: noti_lineImageBlockValue)
                //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
                if let userModel = JSONDeserializer<OptReactiveCompatible>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                    //: CompositionReactiveCompatible.share.loginUserMode = userModel
                    CompositionReactiveCompatible.share.loginUserMode = userModel
                }
            }
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 获取允许国家列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_countryList( completion: @escaping FinishBlock) {
    class func arrayLock(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "app/countryList"
        reqModel.requestPath = (String(constFindData) + String(dataDelayFormat.prefix(4)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 绑定手机号获取验证码
    //: class func req_bindPhoneGetCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func shapeRemain(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: notiPurchaseProfileCaptureValue)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: app_pendingHiddenBehindMsg.map{getMagnitudeerval(then: $0)}, encoding: .utf8)!: reslutStr, String(bytes: k_connectionSphereExclusiveStr.map{individualHome(lick: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = RoundReactiveCompatible()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/getMobileBindVCode"
            reqModel.requestPath = (String(data_workName) + String(showAlreadyData.suffix(7)) + String(data_meetingMsg))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appPreviousText) + noti_consentPath.replacingOccurrences(of: "surgery", with: "败")))
        }
    }

    /// 绑定手机号
    //: class func req_MobileBind(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
    class func bind(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: notiPurchaseProfileCaptureValue)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr, "vcode": vcode]
            let params = [String(bytes: app_pendingHiddenBehindMsg.map{getMagnitudeerval(then: $0)}, encoding: .utf8)!: reslutStr, String(bytes: k_connectionSphereExclusiveStr.map{individualHome(lick: $0)}, encoding: .utf8)!: reslutCodeStr, String(bytes: k_portHimFormat.map{recognizeWhy(police: $0)}, encoding: .utf8)!: vcode]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = RoundReactiveCompatible()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/mobileBind"
            reqModel.requestPath = (String(notiReaderErrorName.prefix(6)) + "obileBind")
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appPreviousText) + noti_consentPath.replacingOccurrences(of: "surgery", with: "败")))
        }
    }
}
