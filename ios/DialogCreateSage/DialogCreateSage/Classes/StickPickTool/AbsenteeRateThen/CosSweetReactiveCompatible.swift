
//: Declare String Begin

/*: "http://" :*/
fileprivate let userReflectionMsg:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let const_materialMsg:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let appRepresentationName:String = "http://represent week land err project"

/*: "serverSelctIndex =  :*/
fileprivate let noti_herFormat:[Character] = ["s","e","r","v","e","r","S","e","l","c","t","I","n"]
fileprivate let main_engageFormat:String = "dex = translation laboratory its"

/*: "serverUrlStr =  :*/
fileprivate let app_subName:[Character] = ["s","e","r","v","e","r","U","r"]
fileprivate let show_serviceKey:String = "jump phone sagelStr = "

/*: "app. :*/
fileprivate let constPowDetailData:String = "football can foundation additionalapp."

/*: .com" :*/
fileprivate let notiRunningText:String = "problem business possible.com"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let main_shareMsg:[Character] = ["m","f","/","c","h","a","t","R","o","o"]
fileprivate let noti_saltLiftMessage:String = "icon meetm/sen"
fileprivate let k_phenomenonFlagBetterKey:[Character] = ["d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let dataModifyName:[Character] = ["r","e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y","N","o","t","i","f","y"]

/*: "user/appleLogin" :*/
fileprivate let showInsideUrl:String = "user/smooth deadline"
fileprivate let mainStreamName:String = "APP"
fileprivate let kWindowUrl:[Character] = ["l","e","L","o","g","i","n"]

/*: "IM/getMyBlackList" :*/
fileprivate let notiPlatformText:String = "IM/getMunder connect everyone"
fileprivate let const_insertData:String = "yBladeliver distant"

/*: "mf/business/sendMsg" :*/
fileprivate let dataVoiceUrl:[Character] = ["m","f","/","b","u","s","i"]
fileprivate let const_humanPath:[Character] = ["n","e","s","s","/","s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let userWhyMsg:[Character] = ["m","f","/","b","u"]
fileprivate let k_gameWritingValue:[Character] = ["s","i","n","e","s","s","/","s","e","n","d","G","i","f","t"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let appBirthTitle:String = "until pack medalmf/cha"
fileprivate let userLinkMessage:String = "m/serandom stock background king moment"
fileprivate let noti_retainConvertMsg:[Character] = ["g"]

/*: "GJ21FB" :*/
fileprivate let notiProcedureContent:String = "scene"
fileprivate let notiRecommendPath:String = "j21fb"

/*: "完成了检测,请求状态正常" :*/
fileprivate let dataMakeMsg:String = "完\u{6210}了\u{68c0}测"
fileprivate let notiSpecStr:[Character] = ["\u{5e38}"]

/*: "接口请求失败" :*/
fileprivate let mainNoteContent:[Character] = ["\u{63a5}","口","请","求","失","败"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let mainDonTitle:String = "\u{5b8c}成了检"
fileprivate let notiMidPath:String = "clip observer upper local orange换serve"

/*: "HTTPProxy" :*/
fileprivate let kCaptureId:String = "lose transmission worldwideHTTPProxy"

/*: "__SCOPED__" :*/
fileprivate let userFieldKey:[Character] = ["_","_","S","C","O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let k_indicatorTitle:String = "neverp"

/*: "tun" :*/
fileprivate let noti_aId:String = "bubbleun"

/*: "ipsec" :*/
fileprivate let data_startMessage:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let app_careerStr:String = "naturalp"

/*: "func__updateTalkingCookies =  :*/
fileprivate let userBindMessage:String = "func_am last star better leading"
fileprivate let const_mentionMsg:String = "report she physicalateT"
fileprivate let kHomePath:String = "fit occupation huge hod bitngCo"
fileprivate let const_opValue:String = "okies = lease football all cram resign"

/*: "https:// :*/
fileprivate let appPremiumData:[Character] = ["h","t","t"]
fileprivate let mainPoseValue:String = "ps://pick should only forehead"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let main_glassTitle:[UInt8] = [0x8a,0x99,0x92,0x87,0x83,0x83,0x88,0x89,0x90,0x89,0x98,0x89,0x77,0x85,0x91,0x89,0x67,0x93,0x93,0x8f,0x8d,0x89,0x97,0x7b,0x8d,0x98,0x8c,0x79,0x96,0x90,0x44,0x61,0x44]

fileprivate func capturePlanBullet(post num: UInt8) -> UInt8 {
    let value = Int(num) + 220
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "UID" :*/
fileprivate let k_highlightText:String = "landID"

/*: "PHPSESSID" :*/
fileprivate let data_limitUrl:String = "PeditPS"

/*: "func__saveAllCookies =  :*/
fileprivate let notiLevelStr:String = "anyoneunc"
fileprivate let show_absKey:String = "__saagainst find"
fileprivate let dataMaxUrl:String = "technology reserveCook"

/*: "func__saveAllCookies  error" :*/
fileprivate let main_makeBlindKey:String = "fupiece"
fileprivate let constQuantityervalMessage:String = "bar conscious black turkeyAllCo"
fileprivate let const_narrowUrl:String = "okies gap deny popular proximate mark"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CosSweetReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum ShadesOfTransformable: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class CosSweetReactiveCompatible: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = CosSweetReactiveCompatible()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        completeDescription()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(userReflectionMsg))) || newValue.hasPrefix((String(const_materialMsg))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if dataPostValue && !constAgentTitle {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(appRepresentationName.prefix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(appRepresentationName.prefix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    italianRegion(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func completeDescription() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = user_liveMsg.integer(forKey: appCleanName)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(noti_herFormat) + String(main_engageFormat.prefix(6))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = spendComponent()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(userReflectionMsg))
        //: if Environment_Formal && !Encryption_Request {
        if dataPostValue && !constAgentTitle {
            //: serverPrefix = "http://"
            serverPrefix = (String(userReflectionMsg))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(app_subName) + String(show_serviceKey.suffix(7))) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func spendComponent() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func smallPosition(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(constPowDetailData.suffix(4))) + "\(userDeviceIndexStr)" + (String(notiRunningText.suffix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(main_shareMsg) + String(noti_saltLiftMessage.suffix(5)) + String(k_phenomenonFlagBetterKey)),
         //: "recharge/applePayNotify",
         (String(dataModifyName)),
         //: "user/appleLogin"] + slowPathArr1
         (String(showInsideUrl.prefix(5)) + mainStreamName.lowercased() + String(kWindowUrl))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(notiPlatformText.prefix(7)) + String(const_insertData.prefix(4)) + "ckList"),
         //: "mf/business/sendMsg",
         (String(dataVoiceUrl) + String(const_humanPath)),
         //: "mf/business/sendGift",
         (String(userWhyMsg) + String(k_gameWritingValue)),
         //: "mf/chatRoom/sendMsg"]
         (String(appBirthTitle.suffix(6)) + "tRoo" + String(userLinkMessage.prefix(4)) + "ndMs" + String(noti_retainConvertMsg))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = DirtyMacroDefine.getSystemLangCode()
        let lang = DirtyMacroDefine.instrumentalityClick()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .followingDialog(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (notiProcedureContent.replacingOccurrences(of: "scene", with: "G") + notiRecommendPath.uppercased()) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension CosSweetReactiveCompatible {
    //: func checkServerUrl() {
    func maturityDate() {
        //: if CompositionReactiveCompatible.share.networkStatus != .Unavailable {
        if CompositionReactiveCompatible.share.networkStatus != .Unavailable {
            //: func_requestHost()
            brushDischarge()
            //: } else {
        } else {
            //: func_starCodeTime()
            timeConstant()
        }
    }

    //: func func_starCodeTime() {
    func timeConstant() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.brushDischarge()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func atSaveTime() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func brushDischarge() {
        //: if CompositionReactiveCompatible.share.networkStatus == .Unavailable {
        if CompositionReactiveCompatible.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        atSaveTime()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        PlightThen.reevaluation { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (dataMakeMsg + ",请求状态正" + String(notiSpecStr)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (String(mainNoteContent)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.timeConstant()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (mainDonTitle + "测,需要更" + String(notiMidPath.suffix(6)) + "r url"))
                    //: self.func__changeHostUrl()
                    self.grownup()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func grownup() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        atSaveTime()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        user_liveMsg.set(serverSelctIndex, forKey: appCleanName)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(userReflectionMsg))
        //: if Environment_Formal && !Encryption_Request {
        if dataPostValue, !constAgentTitle {
            //: serverPrefix = "http://"
            serverPrefix = (String(userReflectionMsg))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension CosSweetReactiveCompatible {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func proxy() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(kCaptureId.suffix(9)))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func fund() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(userFieldKey))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((k_indicatorTitle.replacingOccurrences(of: "never", with: "ta"))) || keyStr.contains((noti_aId.replacingOccurrences(of: "bubble", with: "t"))) || keyStr.contains((String(data_startMessage))) || keyStr.contains((app_careerStr.replacingOccurrences(of: "natural", with: "pp"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension CosSweetReactiveCompatible {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func italianRegion(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(userBindMessage.prefix(5)) + "_upd" + String(const_mentionMsg.suffix(4)) + "alki" + String(kHomePath.suffix(4)) + String(const_opValue.prefix(8))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = oldFactor()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain
        var urlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(userReflectionMsg)), with: (String(const_materialMsg)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(const_materialMsg))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(appPremiumData) + String(mainPoseValue.prefix(5))) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            theListen(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        dealingExtra()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func theListen(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: main_glassTitle.map{capturePlanBullet(post: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func oldFactor() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(userReflectionMsg))
        //: if Environment_Formal && !Encryption_Request {
        if dataPostValue, !constAgentTitle {
            //: serverPrefix = "https://"
            serverPrefix = (String(const_materialMsg))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(constPowDetailData.suffix(4))) + "\(userDeviceIndexStr)" + (String(notiRunningText.suffix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func nearby(note _: Notification) {
        //: if CompositionReactiveCompatible.share.loginUid.isEmptyString {
        if CompositionReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            eventSequence()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            ifExample()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func eventSequence() {
        //: var htmlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (k_highlightText.replacingOccurrences(of: "land", with: "U")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(k_highlightText.replacingOccurrences(of: "land", with: "U"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID")] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (k_highlightText.replacingOccurrences(of: "land", with: "U")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(k_highlightText.replacingOccurrences(of: "land", with: "U"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID") {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID")] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((k_highlightText.replacingOccurrences(of: "land", with: "U"))), h5Cookie.keys.contains((k_highlightText.replacingOccurrences(of: "land", with: "U"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(k_highlightText.replacingOccurrences(of: "land", with: "U"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(k_highlightText.replacingOccurrences(of: "land", with: "U"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID")), h5Cookie.keys.contains((data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID")) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID")]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID")]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func ifExample() {
        //: let htmlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain
        let htmlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == CompositionReactiveCompatible.share.loginUserMode.userID {
            if cookie.name == (k_highlightText.replacingOccurrences(of: "land", with: "U")), cookie.value == CompositionReactiveCompatible.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID") {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (k_highlightText.replacingOccurrences(of: "land", with: "U")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(k_highlightText.replacingOccurrences(of: "land", with: "U"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID")] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((k_highlightText.replacingOccurrences(of: "land", with: "U"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(k_highlightText.replacingOccurrences(of: "land", with: "U"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID")) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(data_limitUrl.replacingOccurrences(of: "edit", with: "H") + "ESSID")] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func dealingExtra() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (notiLevelStr.replacingOccurrences(of: "anyone", with: "f") + String(show_absKey.prefix(4)) + "veAll" + String(dataMaxUrl.suffix(4)) + "ies = ") + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            user_liveMsg.set(cookieData, forKey: mainDisplayPurchaseTitle)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (main_makeBlindKey.replacingOccurrences(of: "piece", with: "nc") + "__save" + String(constQuantityervalMessage.suffix(5)) + String(const_narrowUrl.prefix(6)) + " error"))
        }
    }

    //: func func__setAppCookies() {
    func behindLike() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = user_liveMsg.object(forKey: mainDisplayPurchaseTitle)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func beforeSpark() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        user_liveMsg.removeObject(forKey: mainDisplayPurchaseTitle)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
