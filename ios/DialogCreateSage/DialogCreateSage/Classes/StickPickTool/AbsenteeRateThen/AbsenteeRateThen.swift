
//: Declare String Begin

/*: ; build: :*/
fileprivate let userShotFormat:[Character] = [";"," ","b","u","i","l","d",":"]

/*: ; iOS  :*/
fileprivate let showPunishTitle:String = "satisfy swim suspend; iOS "

/*: "audio" :*/
fileprivate let app_coverTitle:String = "aulineo"

/*: .wav" :*/
fileprivate let dataQuantityAttachExpressionKey:String = "price mine.wav"

/*: "audio/wav" :*/
fileprivate let k_diskName:String = "audio/wavstop column"

/*: .jpg" :*/
fileprivate let userOperationName:String = ".jpgcute limit spec put"

/*: "image/jpeg" :*/
fileprivate let user_suspendHappyHungTitle:String = "native smart peer tempimage/jp"
fileprivate let noti_valueSendMessage:String = "birth"

/*: "://app." :*/
fileprivate let userSmoothMediaElseValue:String = "://app.us added workplace till here"

/*: "data" :*/
fileprivate let main_allowFormat:String = "yesterdayta"

/*: ":null" :*/
fileprivate let show_bugUrl:String = ":nullresponse workplace recognize bounce row"

/*: "Request failed, Try again later" :*/
fileprivate let dataCreatureTitle:[UInt8] = [0x75,0x42,0x56,0x52,0x42,0x54,0x53,0x7,0x41,0x46,0x4e,0x4b,0x42,0x43,0xb,0x7,0x73,0x55,0x5e,0x7,0x46,0x40,0x46,0x4e,0x49,0x7,0x4b,0x46,0x53,0x42,0x55]

/*: "json error" :*/
fileprivate let show_flagFormat:String = "json night restaurant collect process accounting"

/*: "platform=iphone&version= :*/
fileprivate let constParticleData:String = "plamissf"
fileprivate let mainNeighborData:String = "hone&enjoy include"
fileprivate let main_processorDelayArrayPath:String = "publicity lipion="

/*: &packageId= :*/
fileprivate let app_gloryId:String = "guard spring shuttle half&packa"

/*: &bundleId= :*/
fileprivate let user_dogMicFormat:[Character] = ["&","b","u","n","d"]
fileprivate let notiMotivationFormat:String = "distance compute proximate digital generateleId="

/*: &lang= :*/
fileprivate let notiActualLicenseText:[Character] = ["&"]
fileprivate let mainEngageName:[Character] = ["l","a","n","g","="]

/*: "/route/slowindex" :*/
fileprivate let showBoyContent:String = "/routessay define small"
fileprivate let main_agoWorldMsg:String = "wiprivacydex"

/*: "/route/index" :*/
fileprivate let notiPolicyStr:String = "aircraft connection bubble/route/in"
fileprivate let user_maybeTransitionMsg:String = "desight"

/*: "?path= :*/
fileprivate let constDonValue:String = "?path=layer procedure gap ignore photo"

/*: "&GJNonceStr= :*/
fileprivate let constUpperTotalervalData:[Character] = ["&","G","J"]
fileprivate let kShallMsg:String = "NonceStr=day year size"

/*: "&params= :*/
fileprivate let show_wrapData:[Character] = ["&","p","a","r","a"]
fileprivate let dataCalculateFormat:[Character] = ["m","s","="]

/*: "token" :*/
fileprivate let dataDefinePath:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let showShuttleCapturePath:[Character] = ["请","求","成","功"]

/*: "AAAAAAAA" :*/
fileprivate let dataFaceName:String = "profileprofileprofileprofileprofileprofileprofile"
fileprivate let userTipTitle:[Character] = ["A"]

/*: "下载完成 -  :*/
fileprivate let appEffectStr:String = "下unless成 -"
fileprivate let const_earValue:[Character] = [" "]

/*: "/Documents/" :*/
fileprivate let notiScoreName:String = "split sacrifice nuclear written/Docu"
fileprivate let userBecomeUrl:String = "ments/makeup bit fault earn"

/*: ".mp4" :*/
fileprivate let appBoldName:[Character] = [".","m","p","4"]

/*: "移动地址: :*/
fileprivate let k_homeContent:String = "移动地址:"

/*: "后台任务下载回来" :*/
fileprivate let app_supposedValue:String = "后台任\u{52a1}下载\u{56de}来"

/*: "下载成功 : :*/
fileprivate let app_askValue:String = "下\u{8f7d}\u{6210}功 :"

/*: "Net Error" :*/
fileprivate let userPicMsg:String = "Net precedent maybe capacity level"
fileprivate let main_nextAgreeId:[Character] = ["E","r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let appCornerFormat:String = "day载失败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AbsenteeRateThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let notiButtonMsg = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let app_pagePath = "\(k_warnUrl)/\(show_jumpKey) (\(k_ofPath)" + (String(userShotFormat)) + "\(show_styleItemTagFormat)" + (String(showPunishTitle.suffix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: GiftWarningErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let constNextKey = AbsenteeRateThen()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class AbsenteeRateThen: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func swaddlingClothes(model: RoundReactiveCompatible, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.lineup() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.haphazardBass(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.mensurateCompletion(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func haphazardBass(model: RoundReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.alongModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.calendar(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    coverCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, GiftWarningErrorResponse(errorCode: AdminTextSignedNumeric.NetError.rawValue, errorMsg: showTimeId))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        foolishness(showMsg: showTimeId)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (app_coverTitle.replacingOccurrences(of: "line", with: "di")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.duringDirection())+\(kLogData)" + (String(dataQuantityAttachExpressionKey.suffix(4))), mimeType: (String(k_diskName.prefix(9))))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(userOperationName.prefix(4))), mimeType: (String(user_suspendHappyHungTitle.suffix(8)) + noti_valueSendMessage.replacingOccurrences(of: "birth", with: "eg")))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    coverCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, GiftWarningErrorResponse(errorCode: AdminTextSignedNumeric.NetError.rawValue, errorMsg: showTimeId))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        foolishness(showMsg: showTimeId)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func mensurateCompletion(model: RoundReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.alongModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.calendar(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: constAgentTitle ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = notiButtonMsg }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    coverCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, GiftWarningErrorResponse(errorCode: AdminTextSignedNumeric.NetError.rawValue, errorMsg: showTimeId))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        foolishness(showMsg: showTimeId)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = notiButtonMsg }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    coverCompletion(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, GiftWarningErrorResponse(errorCode: AdminTextSignedNumeric.NetError.rawValue, errorMsg: showTimeId))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        foolishness(showMsg: showTimeId)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func coverCompletion(model: RoundReactiveCompatible, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            CosSweetReactiveCompatible.share.italianRegion(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if constAgentTitle, model.requestServer.contains((String(userSmoothMediaElseValue.prefix(7)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.reading(key: CosSweetReactiveCompatible.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (main_allowFormat.replacingOccurrences(of: "yesterday", with: "da")) + "\"" + (String(show_bugUrl.prefix(5))), with: "" + "\"" + (main_allowFormat.replacingOccurrences(of: "yesterday", with: "da")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !constAgentTitle {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(show_bugUrl.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<MisestimationModelType>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == AdminTextSignedNumeric.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, GiftWarningErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    foolishness(showMsg: responseModel.msg ?? String(bytes: dataCreatureTitle.map{$0^39}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case AdminTextSignedNumeric.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: constMinNameTitle, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, GiftWarningErrorResponse(errorCode: AdminTextSignedNumeric.NetError.rawValue, errorMsg: (String(show_flagFormat.prefix(5)) + "error").localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func alongModel(model: RoundReactiveCompatible) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (constParticleData.replacingOccurrences(of: "miss", with: "t") + "orm=ip" + String(mainNeighborData.prefix(5)) + "vers" + String(main_processorDelayArrayPath.suffix(4))) + "\(mainControlUrl)" + (String(app_gloryId.suffix(6)) + "geId=") + "\(kLogData)" + (String(user_dogMicFormat) + String(notiMotivationFormat.suffix(5))) + "\(k_ofPath)" + (String(notiActualLicenseText) + String(mainEngageName)) + "\(CosSweetReactiveCompatible.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if constAgentTitle, serverUrl.contains((String(userSmoothMediaElseValue.prefix(7)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if CosSweetReactiveCompatible.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(showBoyContent.prefix(5)) + "e/slo" + main_agoWorldMsg.replacingOccurrences(of: "privacy", with: "n")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(notiPolicyStr.suffix(9)) + user_maybeTransitionMsg.replacingOccurrences(of: "sight", with: "x"))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(constDonValue.prefix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(constUpperTotalervalData) + String(kShallMsg.prefix(9))) + "\(CosSweetReactiveCompatible.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.passOn(key: CosSweetReactiveCompatible.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(show_wrapData) + String(dataCalculateFormat)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func calendar(model: RoundReactiveCompatible) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(app_pagePath)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(dataDefinePath)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func partyChoice(model: RoundReactiveCompatible) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.alongModel(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(showShuttleCapturePath)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (dataFaceName.replacingOccurrences(of: "profile", with: "A") + String(userTipTitle))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (appEffectStr.replacingOccurrences(of: "unless", with: "载完") + String(const_earValue)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(notiScoreName.suffix(5)) + String(userBecomeUrl.prefix(6))) + NSDate.duringDirection() + (String(appBoldName))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (k_homeContent.capitalized) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (app_supposedValue))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = AppBaseDelegate.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func everyScreen(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: userDismissUrl.allTitle(), with: userDeviceIndexStr)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (app_askValue) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, GiftWarningErrorResponse(errorCode: AdminTextSignedNumeric.Normal.rawValue, errorMsg: (String(userPicMsg.prefix(4)) + String(main_nextAgreeId)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, GiftWarningErrorResponse(errorCode: AdminTextSignedNumeric.NetError.rawValue, errorMsg: (String(userPicMsg.prefix(4)) + String(main_nextAgreeId)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (appCornerFormat.replacingOccurrences(of: "day", with: "下")) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func overFactorThe(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: userDismissUrl.allTitle(), with: userDeviceIndexStr)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (app_askValue) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, GiftWarningErrorResponse(errorCode: AdminTextSignedNumeric.Normal.rawValue, errorMsg: (String(userPicMsg.prefix(4)) + String(main_nextAgreeId)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, GiftWarningErrorResponse(errorCode: AdminTextSignedNumeric.NetError.rawValue, errorMsg: (String(userPicMsg.prefix(4)) + String(main_nextAgreeId)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (appCornerFormat.replacingOccurrences(of: "day", with: "下")) + "\(errMsg)")
            }
        }
    }
}
