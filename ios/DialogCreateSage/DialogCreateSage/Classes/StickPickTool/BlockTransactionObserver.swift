
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let const_approvalMessage:[UInt8] = [0x64,0x75,0x75,0x69,0x60,0x7a,0x6c,0x64,0x75,0x7a,0x75,0x64,0x7c,0x7a,0x76,0x70,0x66,0x66,0x60,0x60,0x61,0x7a,0x6b,0x6a,0x71,0x6c,0x63,0x6c,0x66,0x64,0x71,0x6c,0x6a,0x6b]

private func springParty(ago num: UInt8) -> UInt8 {
    return num ^ 37
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let kAgeData:[UInt8] = [0xd4,0xc5,0xc5,0xd9,0xd0,0xca,0xdc,0xd4,0xc5,0xca,0xc6,0xc0,0xd7,0xc6,0xd6,0xc7,0xdc,0xd7,0xd0,0xca,0xc6,0xc0,0xd6,0xd6,0xd0,0xd0,0xd1,0xca,0xdb,0xda,0xc1,0xdc,0xd3,0xdc,0xd6,0xd4,0xc1,0xdc,0xda,0xdb]

private func strategyPhase(nuclear num: UInt8) -> UInt8 {
    return num ^ 149
}

/*: "mf/recharge/createApplePay" :*/
fileprivate let showMyStr:String = "mf/redge volume edible"
fileprivate let showCapSourceCareName:String = "REATE"

/*: "productId" :*/
fileprivate let showPromptTitle:[Character] = ["p","r","o","d","u","c","t","I","d"]

/*: "source" :*/
fileprivate let kChildValue:[Character] = ["s","o","u","r","c"]
fileprivate let appRoundingValue:String = "fill"

/*: "IAP充值创建订单失败: :*/
fileprivate let userTheName:String = "IAP充值\u{521b}"

/*: "orderNum" :*/
fileprivate let notiPriseData:String = "professional progressorderNum"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let appPreserveName:String = "random focus powmf/re"
fileprivate let data_mediumHiddenUrl:String = "timing interval database outside schedulee/app"
fileprivate let showRawBeneathScopeContent:String = "tolerance constraint lipNotif"
fileprivate let showFingerKey:[Character] = ["y"]

/*: "IAP充值校验失败: :*/
fileprivate let userTogetherMessage:String = "publish"
fileprivate let kNearId:[Character] = ["A","P","充","\u{503c}","校","验","失","败",":"]

/*: "transactionId" :*/
fileprivate let showWorldwideFirmValue:String = "hangansa"
fileprivate let showToleranceProjectStr:String = "greatest elite still targetctionId"

/*: "reportMoney" :*/
fileprivate let user_clearUrl:[Character] = ["r","e","p","o","r"]
fileprivate let user_relieveName:String = "tMoneyunless discount equally moment consumer"

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let notiBetweenBecomeManagerName:String = "mf/Autwell floor"
fileprivate let showSeekId:String = "through remote visual turkeyAppleC"
fileprivate let show_emberStr:String = "refuse estimated superior message thinOrder"

/*: "IAP订阅创建订单失败: :*/
fileprivate let show_enterprisePath:String = "IAP订阅创建theory filter own"
fileprivate let showPoTitle:[Character] = ["订","\u{5355}","\u{5931}","\u{8d25}",":"]

/*: "orderId" :*/
fileprivate let const_phenomenonKey:[UInt8] = [0x64,0x49,0x72,0x65,0x64,0x72,0x6f]

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let app_memoryUrl:String = "mconcept"
fileprivate let app_wedStr:String = "/Autapartment keep simultaneously"
fileprivate let userStartFormat:String = "pplePthread restaurant factor giant estimated"
fileprivate let show_latOutputPath:String = "cceconstraintconstraint"

/*: "IAP订阅校验失败: :*/
fileprivate let main_disabledTitle:String = "IAP\u{8ba2}阅"
fileprivate let userDotName:String = "beautiful"

/*: "App" :*/
fileprivate let data_hundredMsg:String = "Appdiscourse appear"

/*: "OrderTransactionInfo_ :*/
fileprivate let appSceneBroadcastPath:String = "recommendation edible required title continueOrderT"
fileprivate let kOverTitle:String = "resentcti"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let dataInjuryWhiteMouthId:[UInt8] = [0x5f,0x65,0x62,0x69,0x72,0x63,0x73,0x62,0x75,0x53,0x5f,0x6f,0x66,0x6e,0x49,0x6e,0x6f,0x69,0x74,0x63,0x61,0x73,0x6e,0x61,0x72,0x54,0x72,0x65,0x64,0x72,0x4f]

/*: "verifyData" :*/
fileprivate let data_followMessage:[UInt8] = [0xff,0xec,0xfb,0xe0,0xef,0xf0,0xcd,0xe8,0xfd,0xe8]

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let showPassageStr:String = "say dismiss outcomeIAP查"
fileprivate let mainPictureId:String = "\u{8d25}, p"
fileprivate let const_shallTitle:String = ":"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let main_remindStr:String = "IAP查produce opposition security"
fileprivate let userEntityText:String = "失败, pbullet green point one"
fileprivate let const_deliveryValue:String = "s.coustereo distant"

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let constBehindStr:String = "IAP查询内bean report receive"
fileprivate let data_lowName:String = "\u{8d25}, E"

/*: "IAP-purchased: :*/
fileprivate let k_nightData:[Character] = ["I","A","P","-","p","u","r","c"]
fileprivate let show_organicFormat:[Character] = ["h","a","s","e","d",":"]

/*: , 订单号: :*/
fileprivate let app_logId:String = ", 订单accounting:"

/*: , 商品Id: :*/
fileprivate let const_possibleText:String = ", 商\u{54c1}Id:"

/*: , 苹果transId: :*/
fileprivate let mainWhyPath:String = ", cell"
fileprivate let kMapFormat:String = "sId:a represent somebody property"

/*: "IAP-failed: :*/
fileprivate let showParaContent:String = "tender differenceIAP-fa"

/*: "IAP-deferred: :*/
fileprivate let mainTenderName:[Character] = ["I","A"]
fileprivate let kFortTunKey:String = "express span resistance existingP-defe"

/*: "IAP未知交易类型: :*/
fileprivate let constConnectEveryoneData:[Character] = ["I","A","P","未","\u{77e5}","\u{4ea4}","易"]
fileprivate let notiColumnKey:String = "paper型:"

/*: " 未知的交易类型" :*/
fileprivate let k_humanUrl:String = " 未知them"
fileprivate let main_policeId:String = "交易television型"

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let const_propertyName:[Character] = ["I","A","P","获","取","\u{672c}","地","收","据","数","\u{636e}","失","败",","," ","t","r","a","n","s","a","c","t","i","o","n","I"]
fileprivate let mainNarrowFormat:String = "d:plan around"

/*: , orderId: :*/
fileprivate let appThanksName:String = "there resign border string, ord"

/*: , payType: :*/
fileprivate let notiUpperRegularPortMessage:[Character] = [","," ","p","a","y","T","y","p"]
fileprivate let mainRemUrl:[Character] = ["e",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BlockTransactionObserver.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let appGreetCoverPath = NSNotification.Name(rawValue: String(bytes: const_approvalMessage.map{springParty(ago: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let kSkipName = NSNotification.Name(rawValue: String(bytes: kAgeData.map{strategyPhase(nuclear: $0)}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let kRobotMessage = 9

/// 支付类型
//: enum ApplePayType {
enum RecoverType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum SceneValueConvertible: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (SceneValueConvertible, Double, RecoverType) -> Void

//: class AppleIAPManager: NSObject {
class BlockTransactionObserver: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: RecoverType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = BlockTransactionObserver()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(hairNote),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func hairNote() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension BlockTransactionObserver {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func atMain(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(showMyStr.prefix(4)) + "echarge/c" + showCapSourceCareName.lowercased() + "ApplePay")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(showPromptTitle))] = productId
        //: dict["source"] = source
        dict[(String(kChildValue) + appRoundingValue.replacingOccurrences(of: "fill", with: "e"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                FinishThen.onicial(msg: (userTheName + "\u{5efa}订单失败:") + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(notiPriseData.suffix(8)))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func publishFore(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(appPreserveName.suffix(5)) + "charg" + String(data_mediumHiddenUrl.suffix(5)) + "lePay" + String(showRawBeneathScopeContent.suffix(5)) + String(showFingerKey))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    FinishThen.onicial(msg: (userTogetherMessage.replacingOccurrences(of: "publish", with: "I") + String(kNearId)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.signalLayObserveComplete(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7)))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.mentionPath()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: appGreetCoverPath, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(String(user_clearUrl) + String(user_relieveName.prefix(6)))].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension BlockTransactionObserver {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func subscribeCurrentProcessHandle(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(notiBetweenBecomeManagerName.prefix(6)) + "oSub/" + String(showSeekId.suffix(6)) + "reate" + String(show_emberStr.suffix(5)))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(showPromptTitle))] = productId
        //: dict["source"] = source
        dict[(String(kChildValue) + appRoundingValue.replacingOccurrences(of: "fill", with: "e"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                FinishThen.onicial(msg: (String(show_enterprisePath.prefix(7)) + String(showPoTitle)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: const_phenomenonKey.reversed(), encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func makeFor(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (app_memoryUrl.replacingOccurrences(of: "concept", with: "f") + String(app_wedStr.prefix(4)) + "oSub/A" + String(userStartFormat.prefix(5)) + "aySu" + show_latOutputPath.replacingOccurrences(of: "constraint", with: "s"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    FinishThen.onicial(msg: (main_disabledTitle + "校\u{9a8c}失\u{8d25}" + userDotName.replacingOccurrences(of: "beautiful", with: ":")) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.signalLayObserveComplete(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7)))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.primrosePathFront()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kSkipName, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(String(user_clearUrl) + String(user_relieveName.prefix(6)))].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension BlockTransactionObserver {
    /// 初始化数据
    //: private func iap_initData() {
    private func oldness() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = document(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = document(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func document(payType: RecoverType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = mentionPath()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = primrosePathFront()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func mentionPath() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(data_hundredMsg.prefix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = user_liveMsg.string(forKey: app_packageTitle) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(appSceneBroadcastPath.suffix(6)) + "rans" + kOverTitle.replacingOccurrences(of: "resent", with: "a") + "onInfo_") + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func primrosePathFront() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(data_hundredMsg.prefix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = user_liveMsg.string(forKey: app_packageTitle) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: dataInjuryWhiteMouthId.reversed(), encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func sampleAppear(_ transactionId: String, _ payType: RecoverType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7)))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7)))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: data_followMessage.map{$0^137}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: data_followMessage.map{$0^137}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension BlockTransactionObserver {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func unfinished() {
        //: iap_initData()
        oldness()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            shivah(dict[(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7)))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            shivah(dict[(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7)))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func shivah(_ transactionId: String?, _ payType: RecoverType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        signalLayObserveComplete(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension BlockTransactionObserver {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func blackHandle(productId: String, payType: RecoverType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        oldness()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            atMain(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.softwareProduct(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            subscribeCurrentProcessHandle(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.softwareProduct(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func softwareProduct(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            FinishThen.onicial(msg: (String(showPassageStr.suffix(4)) + "询内购商\u{54c1}失" + mainPictureId + "roductId" + const_shallTitle.capitalized) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.formatBot()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func formatBot() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension BlockTransactionObserver: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            FinishThen.onicial(msg: (String(main_remindStr.prefix(4)) + "\u{8be2}内购商品" + String(userEntityText.prefix(5)) + "roduct" + String(const_deliveryValue.prefix(5)) + "nt为0."))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        FinishThen.onicial(msg: (String(constBehindStr.prefix(6)) + "购\u{5546}品失" + data_lowName + "rror:") + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension BlockTransactionObserver: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    FinishThen.onicial(msg: (String(k_nightData) + String(show_organicFormat)) + "\(currentPayType)" + (app_logId.replacingOccurrences(of: "accounting", with: "号")) + "\(String(describing: createOrderId))" + (const_possibleText) + "\(transaction.payment.productIdentifier)" + (mainWhyPath.replacingOccurrences(of: "cell", with: "苹果") + "tran" + String(kMapFormat.prefix(4))) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    signalLayObserveComplete(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                FinishThen.onicial(msg: (String(showParaContent.suffix(6)) + "iled:") + "\(currentPayType)" + (app_logId.replacingOccurrences(of: "accounting", with: "号")) + "\(String(describing: createOrderId))" + (const_possibleText) + "\(transaction.payment.productIdentifier)" + (mainWhyPath.replacingOccurrences(of: "cell", with: "苹果") + "tran" + String(kMapFormat.prefix(4))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: let vc = TalkingContactServiceView.init()
                let vc = CarrierView()
                //: vc.show()
                vc.pairEnd()
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                FinishThen.onicial(msg: (String(mainTenderName) + String(kFortTunKey.suffix(6)) + "rred:") + "\(currentPayType)" + (app_logId.replacingOccurrences(of: "accounting", with: "号")) + "\(String(describing: createOrderId))" + (const_possibleText) + "\(transaction.payment.productIdentifier)" + (mainWhyPath.replacingOccurrences(of: "cell", with: "苹果") + "tran" + String(kMapFormat.prefix(4))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                FinishThen.onicial(msg: (String(constConnectEveryoneData) + notiColumnKey.replacingOccurrences(of: "paper", with: "类")) + "\(currentPayType)" + (app_logId.replacingOccurrences(of: "accounting", with: "号")) + "\(String(describing: createOrderId))" + (const_possibleText) + "\(transaction.payment.productIdentifier)" + (mainWhyPath.replacingOccurrences(of: "cell", with: "苹果") + "tran" + String(kMapFormat.prefix(4))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((k_humanUrl.replacingOccurrences(of: "them", with: "的") + main_policeId.replacingOccurrences(of: "television", with: "类")))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func signalLayObserveComplete(_ transactionId: String, _ payType: RecoverType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = sampleAppear(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            FinishThen.onicial(msg: (String(const_propertyName) + String(mainNarrowFormat.prefix(2))) + "\(transactionId)" + (String(appThanksName.suffix(5)) + "erId:") + "\(String(describing: createOrderId))" + (String(notiUpperRegularPortMessage) + String(mainRemUrl)) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7)))] == transactionId || $0[String(bytes: const_phenomenonKey.reversed(), encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: const_phenomenonKey.reversed(), encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: data_followMessage.map{$0^137}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.mentionPath()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7)))] == transactionId || $0[String(bytes: const_phenomenonKey.reversed(), encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: const_phenomenonKey.reversed(), encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: data_followMessage.map{$0^137}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.primrosePathFront()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > kRobotMessage {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            publishFore(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(showWorldwideFirmValue.replacingOccurrences(of: "hang", with: "tr") + String(showToleranceProjectStr.suffix(7)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            makeFor(transactionId, params: paramsArr.first!)
        }
    }
}
