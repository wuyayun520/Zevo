
//: Declare String Begin

/*: "fee" :*/
fileprivate let main_managerFormat:String = "transforme"

/*: "VIPFee" :*/
fileprivate let k_stereoWindowKey:[Character] = ["V","I","P","F","e","e"]

/*: "chattingNum" :*/
fileprivate let mainMediaValue:[Character] = ["c","h","a","t","t","i","n","g"]
fileprivate let noti_findSharedRepresentId:[Character] = ["N","u","m"]

/*: "You're already in her live room" :*/
fileprivate let dataForeFormat:[UInt8] = [0x6d,0x6f,0x6f,0x72,0x20,0x65,0x76,0x69,0x6c,0x20,0x72,0x65,0x68,0x20,0x6e,0x69,0x20,0x79,0x64,0x61,0x65,0x72,0x6c,0x61,0x20,0x65,0x72,0x27,0x75,0x6f,0x59]

/*: "toUid" :*/
fileprivate let data_hockeyText:[UInt8] = [0x78,0x63,0x59,0x65,0x68]

private func southMissing(consist num: UInt8) -> UInt8 {
    return num ^ 12
}

/*: "momentId" :*/
fileprivate let constBalanceTitle:[UInt8] = [0x30,0x32,0x30,0x38,0x33,0x29,0x14,0x39]

private func theAbsurd(mention num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "extra" :*/
fileprivate let appSpotSizeValue:[UInt8] = [0x9f,0x82,0x8e,0x88,0x9b]

private func levelHead(warn num: UInt8) -> UInt8 {
    return num ^ 250
}

/*: "uid" :*/
fileprivate let kChangeNativeStr:[UInt8] = [0x55,0x49,0x44]

fileprivate func commercialEnterprise(dare num: UInt8) -> UInt8 {
    let value = Int(num) - 224
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let userTransferBearKey:[UInt8] = [0x4d,0x4c,0x4e,0x4b,0x4c,0x47,0x71,0x56,0x43,0x56,0x57,0x51]

/*: "isNewUser" :*/
fileprivate let appPerceiveId:[UInt8] = [0xd8,0xc2,0xff,0xd4,0xc6,0xe4,0xc2,0xd4,0xc3]

/*: "userStatus" :*/
fileprivate let appLayerVersionId:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x72,0x65,0x73,0x75]

/*: "Account is restricted！" :*/
fileprivate let userBankCookingName:String = "max game failAcco"
fileprivate let user_anonymousFormat:String = "s resaudience equity worker analyze"
fileprivate let notiCoverName:String = "fit"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let mainAirMessage:[UInt8] = [0xc4,0xe3,0xeb,0xee,0xe7,0xe6,0xa2,0xf6,0xed,0xa2,0xe7,0xec,0xf6,0xe7,0xf0,0xa2,0xf6,0xea,0xe7,0xa2,0xe1,0xea,0xe3,0xf6,0xa2,0xf0,0xed,0xed,0xef,0xac,0xa2,0xd2,0xee,0xe7,0xe3,0xf1,0xe7,0xa2,0xf6,0xf0,0xfb,0xa2,0xe3,0xe5,0xe3,0xeb,0xec,0xa2,0xee,0xe3,0xf6,0xe7,0xf0]

private func linkUnknown(pending num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "url" :*/
fileprivate let kPeerId:String = "URL"

/*: "userDetail" :*/
fileprivate let show_birdConversationId:[Character] = ["u","s"]
fileprivate let appAppearMsg:[Character] = ["e","r","D","e","t","a","i","l"]

/*: "&type=6" :*/
fileprivate let userBiteStr:String = "&type=6since inform begin drop op"

/*: "&type=7" :*/
fileprivate let const_greenFairData:String = "how wave&type=7"

/*: "amount" :*/
fileprivate let show_oralStr:[UInt8] = [0x10,0x1c,0x1e,0x4,0x1f,0x5]

private func financialStall(side num: UInt8) -> UInt8 {
    return num ^ 113
}

/*: "https://" :*/
fileprivate let main_landId:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http://" :*/
fileprivate let kRowText:String = "ember conversationhttp:"
fileprivate let user_disappearMsg:[Character] = ["/","/"]

/*: "transparency" :*/
fileprivate let dataProblemLickText:String = "tcurrentlyns"

/*: "version= :*/
fileprivate let show_againSufficientText:String = "version=after mirror represent extra overlook"

/*: &packageId= :*/
fileprivate let userRunningId:String = "&packareduce help lovely incident computer"

/*: &bundleId= :*/
fileprivate let showRootKey:String = "up entitle problem&bund"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SightReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class SightReactiveCompatible: NSObject {
public class SightReactiveCompatible: NSObject {
    //: @objc static public let share = SightReactiveCompatible()
    @objc public static let share = SightReactiveCompatible()
    //: private override init() {}
    override private init() {}
}

//: extension SightReactiveCompatible {
extension SightReactiveCompatible {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func rewardRemove(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        noti_liveLevelUrl.recordGender(eventID: k_streamMsg)
        //: guard TalkingSocketManager.shared.isStrategy == false else {
        guard FileSocketManager.shared.isStrategy == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.foolishness(showMsg: constEnterMessage)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        MeetRouteThen.hairTip { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            PreviouslyVideoManager.punish { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = HaphazardMatchDelegate()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(main_managerFormat.replacingOccurrences(of: "transform", with: "fe"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(k_stereoWindowKey))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(String(mainMediaValue) + String(noti_findSharedRepresentId))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.mediumSave()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func pluralForm() {
        //: CompositionReactiveCompatible.share.userFillInfoMode = UserFillInfoModel.init()
        CompositionReactiveCompatible.share.userFillInfoMode = HelpGenInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = GreatFormalViewController()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = reserveUp()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func showPer(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = reserveUp()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: CarrierRecognizerDelegate.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: YellowViewController.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = CarrierRecognizerDelegate()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func temporaryUid(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.mediumSave() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? OptViewController, SelectedThen.panCaptureShared().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.texture()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? GenViewController {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.social()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = GenViewController(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        volition()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func qualityLevel(uid: String, enterType: AssistEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard MeetRouteThen.todayConvert() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(SelectedThen.panCaptureShared().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.foolishness(showMsg: String(bytes: dataForeFormat.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        TensionReactiveCompatible().permissionWeekly(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = PoliceMeasurable.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.reserveUp()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: CandidThen.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! CandidThen).levelAnimated()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            SelectedThen.panCaptureShared().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = CandidThen()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                noti_liveLevelUrl.recordGender(eventID: dataGameUrl, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                noti_liveLevelUrl.recordGender(eventID: app_robotMsg, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func observerReserve(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = GiantViewController(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = reserveUp()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func doingTransform(chatID: String, isFrom: ShadeFromEnum = .Normal, completion: ((_ vc: ParentControllerDelegate) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != DirtyMacroDefine.getXiaoMiID() {
        if chatID != DirtyMacroDefine.markMedalReplace() { // 系统消息
            //: ProgressHUD.show()
            PointerReactiveCompatible.soundAssetThrow()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: data_hockeyText.map{southMissing(consist: $0)}, encoding: .utf8)!: chatID, String(bytes: constBalanceTitle.map{theAbsurd(mention: $0)}, encoding: .utf8)!: "0", String(bytes: appSpotSizeValue.map{levelHead(warn: $0)}, encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            ReminderRequestTool.sectionRefresh(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                PointerReactiveCompatible.motivationCharmDismiss()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = ChinMeasurable.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = ChinMeasurable.someRecommend(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                OverAgainInfoManager.propertyBy(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: kChangeNativeStr.map{commercialEnterprise(dare: $0)}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: userTransferBearKey.map{$0^34}, encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: appPerceiveId.map{$0^177}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: appLayerVersionId.reversed(), encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: appCapturePath,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.foolishness(showMsg: (String(userBankCookingName.suffix(4)) + "unt i" + String(user_anonymousFormat.prefix(5)) + "tricted" + notiCoverName.replacingOccurrences(of: "fit", with: "！")).localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.curPush(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: ChinMeasurable())
                let chatVC = self.curPush(chatID: chatID, isFrom: isFrom, userWrap: ChinMeasurable())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ChinMeasurable ) -> TalkingPrivateChatController {
    func curPush(chatID: String, isFrom: ShadeFromEnum = .Normal, userWrap: ChinMeasurable) -> ParentControllerDelegate {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = reserveUp()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: ParentControllerDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! ParentControllerDelegate
                    //: if vc.targetId == chatID {
                    if vc.targetId == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = ParentControllerDelegate(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func proprietor(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        PointerReactiveCompatible.soundAssetThrow()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        ReminderRequestTool.betweenBoxOrigin(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            PointerReactiveCompatible.motivationCharmDismiss()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.foolishness(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if DirtyMacroDefine.isGroupChat(groupId) {
            if DirtyMacroDefine.socialGroup(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.stackId(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = MeetControllerDelegate(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.reserveUp()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.stackId(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = MeetControllerDelegate(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.reserveUp()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.foolishness(showMsg: String(bytes: mainAirMessage.map{linkUnknown(pending: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func stackId(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.reserveUp()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: MeetControllerDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! MeetControllerDelegate
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func bringExposure(toast _: String?) {
        //: let tpAuth = TPUserAuth(rawValue: CompositionReactiveCompatible.share.loginUserMode.isTPAuth)
        let tpAuth = GiftMeasurable(rawValue: CompositionReactiveCompatible.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingStatementEquityVC()
            let vc = LaterThen()
//            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = reserveUp()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingEditProfilesVC()
            let vc = ProfilesVc()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = reserveUp()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func resign(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func modeTable() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = reserveUp()
        //: let vc = TalkingFeedbackVC.init()
        let vc = AgainViewController()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func inscription(jumpModel: ParentJumpModel) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (kPeerId.lowercased()) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            getConfig(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (String(show_birdConversationId) + String(appAppearMsg)) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func stripe() {
        //: SightReactiveCompatible.share.func__pushToWebVC(webViewType: .SubscribePage)
        SightReactiveCompatible.share.elementOfResume(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func bear(type: AddressContiguousBytes, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = SortChinPasswordVc()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        reserveUp()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension SightReactiveCompatible {
extension SightReactiveCompatible {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func elementOfResume(webViewType: AlertEquatable) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        lipConfig(webViewType: webViewType, webConfig: RoundConfig())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func exceptName(popupCallIndex: Int, appendParams: String = (String(userBiteStr.prefix(7)))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = CapBalanceView()
        //: view.show()
        view.upend()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.patched(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                deciding(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func secondBusy(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(const_greenFairData.suffix(7)))) {
        //: let payWinType = CompositionReactiveCompatible.share.appUserConfigMode.payWinType
        let payWinType = CompositionReactiveCompatible.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = CapBalanceView()
        //: view.show()
        view.upend()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (CompositionReactiveCompatible.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (CompositionReactiveCompatible.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.patched(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                deciding(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func deciding(appendParams: String = (String(const_greenFairData.suffix(7)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = SceneManager.dropWith(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: SightReactiveCompatible.share.func__pushToWebVC(urlStr: urlStr)
        SightReactiveCompatible.share.getConfig(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        noti_liveLevelUrl.pendingConfirm(eventID: dataLevelId, parameterStr: [String(bytes: show_oralStr.map{financialStall(side: $0)}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func patched(webViewType: AlertEquatable, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = RoundConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        lipConfig(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            kConnectBeginKey = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                noti_liveLevelUrl.pendingConfirm(eventID: clickEvent, parameterStr: [String(bytes: show_oralStr.map{financialStall(side: $0)}, encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func personage() {
        //: guard !CompositionReactiveCompatible.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !CompositionReactiveCompatible.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain + CompositionReactiveCompatible.share.appUserConfigMode.discountUrl
        let httpsUrl = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain + CompositionReactiveCompatible.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (String(main_landId)), with: (String(kRowText.suffix(5)) + String(user_disappearMsg)))
        //: SightReactiveCompatible.share.func__pushToWebVC(urlStr: httpUrl)
        SightReactiveCompatible.share.getConfig(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func lipConfig(webViewType: AlertEquatable, webConfig: RoundConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = SceneManager.dropWith(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        hackle(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func getConfig(urlStr: String?, webConfig: RoundConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? RoundConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        hackle(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func hackle(urlStr: String?, webViewType: AlertEquatable?, webConfig: RoundConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(dataProblemLickText.replacingOccurrences(of: "currently", with: "ra") + "parency")].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (String(show_againSufficientText.prefix(8))) + "\(mainControlUrl)" + (String(userRunningId.prefix(6)) + "geId=") + "\(kLogData)" + (String(showRootKey.suffix(5)) + "leId=") + "\(k_ofPath)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = SightViewController(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = reserveUp() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: SightViewController.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! SightViewController).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.privacyRadius(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: user_needValue, height: user_needValue / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension SightReactiveCompatible {
public extension SightReactiveCompatible {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func reserveUp() -> UIViewController? {
        //: return currentViewController()
        return mediumSave()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct ParentJumpModel: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
