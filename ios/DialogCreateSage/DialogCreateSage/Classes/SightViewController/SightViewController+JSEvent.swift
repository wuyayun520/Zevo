
//: Declare String Begin

/*: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION" :*/
fileprivate let show_simultaneouslyMsg:[UInt8] = [0x47,0x55,0x52,0x46,0x59,0x55,0x47,0x4f,0x44,0x58,0x59,0x42,0x54,0x4f,0x42,0x55,0x56,0x42,0x55,0x43,0x58,0x53,0x5f,0x59,0x5e,0x4f,0x5e,0x5f,0x44,0x59,0x56,0x59,0x53,0x51,0x44,0x59,0x5f,0x5e]

/*: "goodsId" :*/
fileprivate let const_decreaseStr:String = "occuro"
fileprivate let data_veryPath:[Character] = ["o","d","s","I","d"]

/*: "source" :*/
fileprivate let dataEnoughFormat:[Character] = ["s","o","u","r","c","e"]

/*: "type" :*/
fileprivate let noti_amberMsg:String = "tofficialpe"

/*: "title" :*/
fileprivate let userCalculationId:String = "titlaccompany"

/*: "url" :*/
fileprivate let k_regularData:[Character] = ["u","r","l"]

/*: "money" :*/
fileprivate let k_thenUrl:String = "monya"

/*: "Other" :*/
fileprivate let showIncludeFormat:[Character] = ["O","t","h","e","r"]

/*: "scene" :*/
fileprivate let user_courseData:String = "scchildnchild"

/*: "show" :*/
fileprivate let appJustFormat:String = "seffw"

/*: "target" :*/
fileprivate let kBlockContent:String = "title"
fileprivate let app_successKey:String = "argmonth"

/*: "eventName" :*/
fileprivate let constMenuStockId:[Character] = ["e","v","e","n","t"]
fileprivate let mainErrStr:String = "opening worker curve press sightName"

/*: "jsonString" :*/
fileprivate let show_normalValue:String = "jquoten"
fileprivate let const_manualPath:String = "Stringhang native"

/*: "coin" :*/
fileprivate let constNumberTitle:[Character] = ["c","o","i","n"]

/*: "uid" :*/
fileprivate let const_complyRowFormat:String = "mixid"

/*: "closeWeb" :*/
fileprivate let show_errorValue:String = "closeWexit listen translation nothing"
fileprivate let show_scriptStr:String = "average"

/*: "未实现的js事件： :*/
fileprivate let notiRecordName:String = "未实mini"
fileprivate let const_equitySecureFormat:[Character] = ["："]

/*: "PurchaseClick" :*/
fileprivate let appSortStr:[Character] = ["P","u","r","c","h","a"]
fileprivate let constInsertData:String = "tot notice estate hockeyseClick"

/*: "Retry After or Go to " :*/
fileprivate let constChanceKey:String = "property displayRetry "
fileprivate let kNuclearAnotherData:String = "r Go to seat lack project gap"

/*: "Feedback" :*/
fileprivate let dataDayPath:String = "notice interest poFee"
fileprivate let noti_stackText:[Character] = ["d","b","a","c","k"]

/*: " to contact us" :*/
fileprivate let constDiscountId:[Character] = [" ","t","o"," ","c","o","n","t","a","c","t"," ","u","s"]

/*: "Apple" :*/
fileprivate let k_collectStr:[Character] = ["A","p","p","l","e"]

/*: " apple支付充值失败： :*/
fileprivate let data_containerId:[Character] = [""," ","a","p","p","l","e","\u{652f}","付","充","值","失","败","\u{ff1a}"]

/*: "payResultCallback();" :*/
fileprivate let user_laterData:[Character] = ["p","a","y","R","e","s","u","l","t","C","a","l","l","b","a","c","k","(",")"]
fileprivate let data_suspendId:[Character] = [";"]

/*: "USD" :*/
fileprivate let appEntityId:String = "workplaceSD"

/*: "amount" :*/
fileprivate let constWindowRaftUrl:[UInt8] = [0x83,0x8f,0x8d,0x97,0x8c,0x96]

private func fullMetalJacket(cooking num: UInt8) -> UInt8 {
    return num ^ 226
}

/*: "getSystemNotificationtStatus(true)" :*/
fileprivate let k_rangeKey:[UInt8] = [0xfe,0xfc,0xb,0xea,0x10,0xa,0xb,0xfc,0x4,0xe5,0x6,0xb,0x0,0xfd,0x0,0xfa,0xf8,0xb,0x0,0x6,0x5,0xb,0xea,0xb,0xf8,0xb,0xc,0xa,0xbf,0xb,0x9,0xc,0xfc,0xc0]

fileprivate func indexSolution(cur num: UInt8) -> UInt8 {
    let value = Int(num) + 105
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "getSystemNotificationtStatus(false)" :*/
fileprivate let dataCookieValue:[UInt8] = [0x82,0x80,0x91,0xb6,0x9c,0x96,0x91,0x80,0x88,0xab,0x8a,0x91,0x8c,0x83,0x8c,0x86,0x84,0x91,0x8c,0x8a,0x8b,0x91,0xb6,0x91,0x84,0x91,0x90,0x96,0xcd,0x83,0x84,0x89,0x96,0x80,0xcc]

/*: "mfBean" :*/
fileprivate let constBroadBrightValue:[UInt8] = [0xbe,0xb5,0x91,0xb6,0xb2,0xbd]

private func presentBear(note num: UInt8) -> UInt8 {
    return num ^ 211
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SightViewController+JSEvent.swift
//  AbroadTalking
//
//  Created by young on 2022/9/6.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

// 通知三方H5刷新金币
//: public let WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION = NSNotification.Name(rawValue: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION")
public let main_depthValue = NSNotification.Name(rawValue: String(bytes: show_simultaneouslyMsg.map{$0^16}, encoding: .utf8)!)

//: extension TalkingWebViewController {
extension SightViewController {
    /// 处理JS事件
    /// - Parameters:
    ///   - message: 消息
    //: func HandleScriptMessage(_ message: WKScriptMessage) {
    func midMessage(_ message: WKScriptMessage) {
        //: let type = WebMethodType(rawValue: message.name)
        let type = BarbBidirectionalCollection(rawValue: message.name)
        //: let json = JSON(message.body)
        let json = JSON(message.body)
        //: switch type {
        switch type {
        //: case .MethodType_Exit:
        case .MethodType_Exit:
            //: self.func__closeAction(sender: nil)
            self.simultaneouslyAnimal(sender: nil)

        //: case .MethodType_needLogin:
        case .MethodType_needLogin:
            //: needLogin()
            cryMake()

        //: case .MethodType_apPay:
        case .MethodType_apPay:
            //: applePay(productId: json["goodsId"].stringValue, source: json["source"].intValue, payType: .Pay)
            reconstruct(productId: json[(const_decreaseStr.replacingOccurrences(of: "occur", with: "g") + String(data_veryPath))].stringValue, source: json[(String(dataEnoughFormat))].intValue, payType: .Pay)

        //: case .MethodType_apPay_subscribe:
        case .MethodType_apPay_subscribe:
            //: if json["type"].stringValue == "6" {
            if json[(noti_amberMsg.replacingOccurrences(of: "official", with: "y"))].stringValue == "6" {
                //: isVideoCallRechargeHalfPage = true
                isVideoCallRechargeHalfPage = true
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                kConnectBeginKey = dataLevelId
                //: } else if json["type"].stringValue == "7" { // 点击订阅弹窗的订阅按钮
            } else if json[(noti_amberMsg.replacingOccurrences(of: "official", with: "y"))].stringValue == "7" { // 点击订阅弹窗的订阅按钮
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                kConnectBeginKey = dataLevelId
            }
            //: applePay_subscribe(type: json["type"].stringValue, productId: json["goodsId"].stringValue, payType: .Subscribe)
            rewardType(type: json[(noti_amberMsg.replacingOccurrences(of: "official", with: "y"))].stringValue, productId: json[(const_decreaseStr.replacingOccurrences(of: "occur", with: "g") + String(data_veryPath))].stringValue, payType: .Subscribe)

        //: case .MethodType_changeMenuButton:
        case .MethodType_changeMenuButton:
            //: changeRightMenuButton(title: json["title"].stringValue, url: json["url"].stringValue)
            downwardlyRemark(title: json[(userCalculationId.replacingOccurrences(of: "accompany", with: "e"))].stringValue, url: json[(String(k_regularData))].stringValue)

        //: case .MethodType_refreshCoin:
        case .MethodType_refreshCoin:
            //: refreshCoin(price: Double(json["money"].stringValue) ?? 0, payMode: "Other", scene: json["scene"].stringValue)
            circlet(price: Double(json[(k_thenUrl.replacingOccurrences(of: "ya", with: "ey"))].stringValue) ?? 0, payMode: (String(showIncludeFormat)), scene: json[(user_courseData.replacingOccurrences(of: "child", with: "e"))].stringValue)

        //: case .MethodType_appearsOnGiftBroadcast:
        case .MethodType_appearsOnGiftBroadcast:
            //: appearsOnGiftBroadcast(show: json["show"].boolValue)
            broadcastSelectedShow(show: json[(appJustFormat.replacingOccurrences(of: "eff", with: "ho"))].boolValue)

        //: case.MethodType_ToUrl:
        case .MethodType_ToUrl:
            //: openTourl(url: json.rawString() ?? "")
            wantUrl(url: json.rawString() ?? "")

        //: case .MethodType_customerService:
        case .MethodType_customerService:
            //: SightReactiveCompatible.share.func__pushToPriveteChatVC(chatID: DirtyMacroDefine.getCustomerServiceID())
            SightReactiveCompatible.share.doingTransform(chatID: DirtyMacroDefine.resGain())

        //: case.setRightMenuButton:
        case .setRightMenuButton:
            //: break
            break

        //: case .MethodType_taskCenter:
        case .MethodType_taskCenter:
            //: let target = json["target"].intValue
            let target = json[(kBlockContent.replacingOccurrences(of: "title", with: "t") + app_successKey.replacingOccurrences(of: "month", with: "et"))].intValue
            //: self.reloadInputViews()
            self.reloadInputViews()
            //: switch target {
            switch target {
            //: case 1:
            case 1: /// 签到领金币页
                //: SightReactiveCompatible.share.func__pushToSubscribePageWebVC()
                SightReactiveCompatible.share.stripe()

            //: case 2:
            case 2: /// 个人信息编辑页面
                //: let vc = TalkingEditProfilesVC.init()
                let vc = ProfilesVc()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)

            //: case 3:
            case 3: /// 真人认证页面
                //: verifyBtnClick()
                ransackedAppearance()
            //: case 4:
            case 4: /// 发布动态页面
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                marriageLicense(type: SuccessSignedSum.Moment.rawValue)
                /// 防止动态首页广播还没创建
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    //: NotificationCenter.default.post(name: FREEBTN_UPLOAD_POST_NOTIFICATION,
                    NotificationCenter.default.post(name: data_restoreMsg,
                                                    //: object: nil,
                                                    object: nil,
                                                    //: userInfo: nil)
                                                    userInfo: nil)
                }
            //: case 5:
            case 5: /// 我的页面
                //: tabberSelete(type: TabBarItemType.Account.rawValue)
                marriageLicense(type: SuccessSignedSum.Account.rawValue)
            //: case 6:
            case 6: /// 首页
                //: tabberSelete(type: TabBarItemType.Social.rawValue)
                marriageLicense(type: SuccessSignedSum.Social.rawValue)
            //: case 7:
            case 7: /// 消息列表页
                //: tabberSelete(type: TabBarItemType.Message.rawValue)
                marriageLicense(type: SuccessSignedSum.Message.rawValue)
            //: case 8:
            case 8: /// Greet setting页
                //: let vc = TalkingAutoGreetSettingVC()
                let vc = FormalDataSource()
                //: navigationController?.pushViewController(vc, animated: true)
                navigationController?.pushViewController(vc, animated: true)
            //: case 9:
            case 9: /// 动态列表页
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                marriageLicense(type: SuccessSignedSum.Moment.rawValue)
            //: default:
            default:
                //: break
                break
            }
        //: case .MethodType_systemNotification:
        case .MethodType_systemNotification:
            //: let type = json["type"].intValue
            let type = json[(noti_amberMsg.replacingOccurrences(of: "official", with: "y"))].intValue
            //: changeNotifaStatus(type: type)
            lightElement(type: type)

        //: case .MethodType_refreshPoint:
        case .MethodType_refreshPoint:
            //: refreshPoint()
            blindAttracter()

        //: case .MethodType_onEvent:
        case .MethodType_onEvent:
            //: reportEvent(eventName: json["eventName"].stringValue, jsonStr: json["jsonString"].stringValue)
            resistance(eventName: json[(String(constMenuStockId) + String(mainErrStr.suffix(4)))].stringValue, jsonStr: json[(show_normalValue.replacingOccurrences(of: "quote", with: "so") + String(const_manualPath.prefix(6)))].stringValue)

        //: case .MethodType_refreshCoinAll:
        case .MethodType_refreshCoinAll:
            //: req_refreshCoinAll(coin: json["coin"].stringValue)
            onStart(coin: json[(String(constNumberTitle))].stringValue)

        //: case .MethodType_refreshPackage:
        case .MethodType_refreshPackage:
            //: req_refreshPackage()
            feedbackFun()

        //: case .MethodType_vibrate:
        case .MethodType_vibrate:
            //: phoneVibrate()
            transformVibrate()

        //: case .MethodType_goFreeVideo:
        case .MethodType_goFreeVideo:
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
            PlightThen.end { _, _, _ in
                //: if CompositionReactiveCompatible.share.loginUserMode.jumpType == 1 {
                if CompositionReactiveCompatible.share.loginUserMode.jumpType == 1 {
                    //: self.func__closeAction(sender: nil)
                    self.simultaneouslyAnimal(sender: nil)
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                        //: SightReactiveCompatible.share.func__pushToRandomVideoVC(isBeginRand: false)
                        SightReactiveCompatible.share.rewardRemove(isBeginRand: false)
                    }
                }
            }

        //: case .MethodType_goMessageDetail:
        case .MethodType_goMessageDetail:
            //: SightReactiveCompatible.share.func__pushToPriveteChatVC(chatID: json["uid"].stringValue)
            SightReactiveCompatible.share.doingTransform(chatID: json[(const_complyRowFormat.replacingOccurrences(of: "mix", with: "u"))].stringValue)

        //: case .MethodType_goPersonInfo:
        case .MethodType_goPersonInfo:
            //: SightReactiveCompatible.share.func__pushToUserDetailVC(uid: json["uid"].stringValue)
            SightReactiveCompatible.share.temporaryUid(uid: json[(const_complyRowFormat.replacingOccurrences(of: "mix", with: "u"))].stringValue)

        //: case .MethodType_showRechargeDialog:
        case .MethodType_showRechargeDialog:
            //: SightReactiveCompatible.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            SightReactiveCompatible.share.patched(webViewType: .RechargeHalfPage)

        //: case .OpenRetainPopup:
        case .OpenRetainPopup:
            //: let view = TalkingRechargeDetentionView.init()
            let view = RelevantView()
            //: view.dtype = json["type"].intValue
            view.dtype = json[(noti_amberMsg.replacingOccurrences(of: "official", with: "y"))].intValue
            //: view.closeWeb = json["closeWeb"].boolValue
            view.closeWeb = json[(String(show_errorValue.prefix(6)) + show_scriptStr.replacingOccurrences(of: "average", with: "eb"))].boolValue
            //: view.show()
            view.focusShow()
            //: view.closWebClock = { [weak self] in
            view.closWebClock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: self.func__closeAction(sender: nil)
                self.simultaneouslyAnimal(sender: nil)
            }

        //: case .openBrowserWithUrl:
        case .openBrowserWithUrl:
            //: openToOutUrl(url: json.rawString() ?? "")
            randomUrl(url: json.rawString() ?? "")
        //: case .none:
        case .none:
            //: printLog(message: "未实现的js事件：\(message.name)")
            printLog(message: (notiRecordName.replacingOccurrences(of: "mini", with: "现的") + "js事件" + String(const_equitySecureFormat)) + "\(message.name)")
        }
    }

    // MARK: - Event

    /// 苹果订阅
    /// - Parameters:
    ///   - type: 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    ///   - productId: 商品Id
    ///   - payType: 支付类型
    //: private func applePay_subscribe(type: String, productId: String, payType: ApplePayType) {
    private func rewardType(type _: String, productId: String, payType: RecoverType) {
        //: applePay(productId: productId, payType: payType)
        reconstruct(productId: productId, payType: payType)
    }

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: private func applePay(productId: String, source: Int = -1, payType: ApplePayType) {
    private func reconstruct(productId: String, source: Int = -1, payType: RecoverType) {
        //: if CompositionReactiveCompatible.share.loginUid.isEmptyString {
        if CompositionReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: FirebaseAnalyticsManager.share.logEvent(name: "PurchaseClick")
        WarningThen.share.builder(name: (String(appSortStr) + String(constInsertData.suffix(7))))

        //: ProgressHUD.show()
        PointerReactiveCompatible.soundAssetThrow()
        //: var index = isVideoCallRechargeHalfPage ? 1: 0
        var index = isVideoCallRechargeHalfPage ? 1 : 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
        BlockTransactionObserver.shared.blackHandle(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
            //: ProgressHUD.dismiss()
            PointerReactiveCompatible.motivationCharmDismiss()
            //: DispatchQueue.main.async { [self] in
            DispatchQueue.main.async { [self] in
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us".localized)
                    PointerReactiveCompatible.stateToast((String(constChanceKey.suffix(6)) + "After o" + String(kNuclearAnotherData.prefix(8))) + "\"" + (String(dataDayPath.suffix(3)) + String(noti_stackText)) + "\"" + (String(constDiscountId)).localized)
                //: case .veritySucceed:
                case .veritySucceed:
                    //: appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.price(price: reportMoney, payType: payType)
                //: case .renewSucceed:
                case .renewSucceed:
                    //: let scene = (payType == .Subscribe) ? "1":"2"
                    let scene = (payType == .Subscribe) ? "1" : "2"
                    //: refreshCoin(price: reportMoney, payMode: "Apple", scene: scene)
                    circlet(price: reportMoney, payMode: (String(k_collectStr)), scene: scene)
                //: default:
                default:
                    //: printLog(message: " apple支付充值失败：\(status.rawValue)")
                    printLog(message: (String(data_containerId)) + "\(status.rawValue)")
                }
            }
        }
    }

    /// 设置导航右侧按钮
    /// - Parameters:
    ///   - title: 标题
    ///   - url: 地址
    //: private func changeRightMenuButton(title: String, url: String) {
    private func downwardlyRemark(title: String, url: String) {
        //: let rightBtn = TalkingButton()
        let rightBtn = AfterView()
        //: rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        //: rightBtn.setTitle(title, for: .normal)
        rightBtn.setTitle(title, for: .normal)
        //: rightBtn.setTitleColor(.RGBA(51, 51, 51, 1), for: .normal)
        rightBtn.setTitleColor(.pushDown(51, 51, 51, 1), for: .normal)
        //: rightBtn.addTarget(self, action: #selector(rightButtonClick), for: .touchUpInside)
        rightBtn.addTarget(self, action: #selector(promptButton), for: .touchUpInside)
        //: rightBtn.sizeToFit()
        rightBtn.sizeToFit()
        //: rightBtn.userInfo = ["url": url]
        rightBtn.userInfo = [(String(k_regularData)): url]
        //: self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
        self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
    }

    //: @objc func rightButtonClick(button: TalkingButton) {
    @objc func promptButton(button: AfterView) {
        //: guard let urlString = button.userInfo?["url"] as? String else {
        guard let urlString = button.userInfo?[(String(k_regularData))] as? String else {
            //: return
            return
        }

        //: self.urlStr = urlString
        self.urlStr = urlString
        //: func__startRequest()
        startRequest()
    }

    /// 退出登录
    //: private func needLogin() {
    private func cryMake() {
        //: guard Int(CompositionReactiveCompatible.share.loginUid) ?? 0 > 0 else {
        guard Int(CompositionReactiveCompatible.share.loginUid) ?? 0 > 0 else {
            //: self.webView.reload()
            self.webView.reload()
            //: return
            return
        }

        //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION,
        NotificationCenter.default.post(name: constMinNameTitle,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
    }

    /// 苹果 支付/订阅 成功验证通过
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payType: 支付类型
    //: func appleIAPVeritySucceed(price: Double, payType: ApplePayType) {
    func price(price: Double, payType: RecoverType) {
        //: self.webView.evaluateJavaScript("payResultCallback();") { data, error in
        self.webView.evaluateJavaScript((String(user_laterData) + String(data_suspendId))) { _, _ in
        }
        //: let scene = (payType == .Subscribe) ? "1":"2"
        let scene = (payType == .Subscribe) ? "1" : "2"
        //: refreshCoin(price: price, payMode: "Apple", scene: scene)
        circlet(price: price, payMode: (String(k_collectStr)), scene: scene)
    }

    /// 支付/订阅 成功刷新个人信息【Apple/Other】
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payMode: 类型
    ///   - scene: 1：订阅，2：内购
    //: private func refreshCoin(price: Double, payMode: String, scene: String) {
    private func circlet(price: Double, payMode: String, scene: String) {
        //: NotificationCenter.default.post(name: UPDATE_USER_GETMYINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: noti_logStr, object: nil)
        //: NotificationCenter.default.post(name: UPDATE_USER_GETINFOCOLUMN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: const_valueName, object: nil)
        //: NotificationCenter.default.post(name: REFRESH_INDEX_GETCONFIG_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: mainSceneFormat, object: nil)
        //: NotificationCenter.default.post(name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: appPriceMsg, object: nil)
        // 成功埋点
        //: if scene == "1" {
        if scene == "1" {
            //: TalkingAdjustManager.share.addSubscribeEvent(price: price, currency: "USD")
            AuthorReminderReactiveCompatible.share.perusing(price: price, currency: (appEntityId.replacingOccurrences(of: "workplace", with: "U")))
            //: } else {
        } else {
            //: TalkingAdjustManager.share.addPurchasedEvent(price: price, currency: "USD")
            AuthorReminderReactiveCompatible.share.nearVideo(price: price, currency: (appEntityId.replacingOccurrences(of: "workplace", with: "U")))
        }
        //: FirebaseAnalyticsManager.share.trackPurchase(payType: payMode, price: price, currency: "USD")
        WarningThen.share.trackGame(payType: payMode, price: price, currency: (appEntityId.replacingOccurrences(of: "workplace", with: "U")))

        // 半屏充值成功埋点
        //: if RECHARGE_CLICK_EVENT.count > 0 {
        if kConnectBeginKey.count > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: RECHARGE_CLICK_EVENT, parameterStr: ["amount": price])
            noti_liveLevelUrl.pendingConfirm(eventID: kConnectBeginKey, parameterStr: [String(bytes: constWindowRaftUrl.map{fullMetalJacket(cooking: $0)}, encoding: .utf8)!: price])
        }

        // 支付成功发送通知，调用三方js事件
        //: NotificationCenter.default.post(name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: main_depthValue, object: nil)
    }

    /// 广播设置
    /// - Parameter show: 是否上广播
    //: private func appearsOnGiftBroadcast(show: Bool) {
    private func broadcastSelectedShow(show _: Bool) {}

    //: private func verifyBtnClick() {
    private func ransackedAppearance() {
        //: SightReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
        SightReactiveCompatible.share.bringExposure(toast: nil)
    }

    /// 底部tabbar切换
    //: private func tabberSelete(type: Int) {
    private func marriageLicense(type: Int) {
        //: guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        //: let vc = self.tabBarController?.viewControllers?[type]
        let vc = self.tabBarController?.viewControllers?[type]
        //: self.tabBarController?.tabBar.isHidden = false
        self.tabBarController?.tabBar.isHidden = false
        //: self.tabBarController?.selectedIndex = type
        self.tabBarController?.selectedIndex = type
        //: self.tabBarController?.selectedViewController = vc
        self.tabBarController?.selectedViewController = vc
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 获取系统通知状态
    //: private func changeNotifaStatus(type: Int) {
    private func lightElement(type: Int) {
        //: if type == 0 {
        if type == 0 {
            //: if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
            if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
                //: if #available(iOS 10.0, *) {
                if #available(iOS 10.0, *) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    //: } else {
                } else {
                    //: if UIApplication.shared.canOpenURL(url) {
                    if UIApplication.shared.canOpenURL(url) {
                        //: UIApplication.shared.openURL(url)
                        UIApplication.shared.openURL(url)
                    }
                }
            }
            //: } else if type == 1 {
        } else if type == 1 {
            //: AppManagerRequest.func__requestUserConfig { succeed, result, errorModel in }
            PlightThen.dismissImage { _, _, _ in }
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in }
            PlightThen.end { _, _, _ in }
            //: TalkingPermissionTool.checkPushNotification { isShow in
            MeetRouteThen.showThat { isShow in
                //: DispatchQueue.main.sync {
                DispatchQueue.main.sync {
                    //: let str = isShow ? "getSystemNotificationtStatus(true)" : "getSystemNotificationtStatus(false)"
                    let str = isShow ? String(bytes: k_rangeKey.map{indexSolution(cur: $0)}, encoding: .utf8)! : String(bytes: dataCookieValue.map{$0^229}, encoding: .utf8)!
                    //: self.webView.evaluateJavaScript(str) { data, error in
                    self.webView.evaluateJavaScript(str) { _, _ in
                    }
                }
            }
        }
    }

    //: private func refreshPoint() {
    private func blindAttracter() {
        //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
        PlightThen.end { _, _, _ in
            //: if TalkingLiveManager.shared().isLive {
            if SelectedThen.panCaptureShared().isLive {
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": 0.0])
                NotificationCenter.default.post(name: mainMomentKey, object: nil, userInfo: [String(bytes: constBroadBrightValue.map{presentBear(note: $0)}, encoding: .utf8)!: 0.0])
            }
        }
    }

    /// 刷新金币
    /// - Parameter coin: 传' ', 刷新接口；传其他，进行相加（含负数））
    //: private func req_refreshCoinAll(coin: String) {
    private func onStart(coin: String) {
        //: let value: Double = Double(coin) ?? 0
        let value = Double(coin) ?? 0
        //: if value == 0 {
        if value == 0 { // 刷新接口
            //: AppManagerRequest.func__getInfoColumn { succeed, result, errorModel in
            PlightThen.bugOut { _, _, _ in
            }
            //: } else {
        } else { // 加减
            //: let newCoin = Double(CompositionReactiveCompatible.share.loginUserMode.mf_coin)! + value
            let newCoin = Double(CompositionReactiveCompatible.share.loginUserMode.mf_coin)! + value
            //: CompositionReactiveCompatible.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
            CompositionReactiveCompatible.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
        }
    }

    /// 刷新礼物背包
    //: private func req_refreshPackage() {
    private func feedbackFun() {
        //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: noti_buildId, object: nil)
    }

    /// 事件上报
    /// - Parameters:
    ///   - eventName: 事件名称
    ///   - jsonStr: 事件参数
    //: private func reportEvent(eventName: String, jsonStr: String) {
    private func resistance(eventName: String, jsonStr: String) {
        //: uploadRecord.uploadRecordEvent(eventID: eventName, toUid: "", jsonStr: jsonStr)
        noti_liveLevelUrl.recordGender(eventID: eventName, toUid: "", jsonStr: jsonStr)
    }

    /// 手机震动
    //: private func phoneVibrate() {
    private func transformVibrate() {
        //: AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
        AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
    }
}
