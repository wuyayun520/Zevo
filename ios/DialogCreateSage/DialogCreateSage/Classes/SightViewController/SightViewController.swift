
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_yourText:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Load..." :*/
fileprivate let main_dayMessage:String = "screen interactionLoad."
fileprivate let show_taskId:[Character] = [".","."]

/*: "Skip" :*/
fileprivate let dataTaMeetOrientationStr:String = "Skiptrack round appeal"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let constVarietyUrl:[UInt8] = [0x18,0x24,0x24,0x20,0x4,0x3f,0x3f,0x3c,0x7e,0x1e,0x31,0x24,0x39,0x26,0x35,0x4,0x3f,0x1a,0x23,0x78,0x77,0x22,0x35,0x33,0x38,0x31,0x22,0x37,0x35,0x77,0x79]

private func automaticallyEmber(never num: UInt8) -> UInt8 {
    return num ^ 80
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let show_interestedObserveReloadTitle:[UInt8] = [0x49,0x57,0x50,0x5a,0x51,0x49,0x10,0x51,0x50,0x6e,0x5f,0x59,0x5b,0x6d,0x56,0x51,0x49,0x18,0x18,0x51,0x50,0x6e,0x5f,0x59,0x5b,0x6d,0x56,0x51,0x49,0x16,0x17,0x5]

private func videoCos(gender num: UInt8) -> UInt8 {
    return num ^ 62
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let constBlankPath:String = "accuracy shrink spreadjsEvent"
fileprivate let notiMaterialPath:String = "(onPreward template factor during exist"
fileprivate let user_revenueData:String = "ageShi entity audience if"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let showModifyId:[UInt8] = [0x9d,0x83,0x84,0x8e,0x85,0x9d,0xc4,0x85,0x84,0xba,0x8b,0x8d,0x8f,0xa2,0x83,0x8e,0x8f,0xcc,0xcc,0x85,0x84,0xba,0x8b,0x8d,0x8f,0xa2,0x83,0x8e,0x8f,0xc2,0xc3,0xd1]

private func popularMark(asset num: UInt8) -> UInt8 {
    return num ^ 234
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let userWaveUrl:String = "jsEvent(oconsent decision expected something visitor"
fileprivate let kBagFoundationContent:String = "mp do remainingnPag"

/*: "title" :*/
fileprivate let mainProfileText:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let dataFragmentStr:String = "canGoBackreject m compute"

/*: "PHPSESSID= :*/
fileprivate let user_guideMsg:String = "PHPlaugh"

/*: ";uid= :*/
fileprivate let noti_includeId:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let user_priorityFormat:String = "remark"
fileprivate let showBigName:[Character] = ["o","o","k","i","e"]

/*: "nav_back_black_nor" :*/
fileprivate let k_goValue:String = "editor discoursenav_b"
fileprivate let data_withMsg:String = "lack_norbull one re spread mode"

/*: "ico_intimate_list_sm" :*/
fileprivate let data_docPath:[Character] = ["i","c","o","_","i","n","t","i","m","a","t","e","_"]
fileprivate let const_temporarySaleStandStr:[Character] = ["l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let mainUpperPath:[UInt8] = [0x4c,0x47,0x4b,0x5d,0x45,0x4d,0x46,0x5c,0x6,0x4f,0x4d,0x5c,0x6d,0x44,0x4d,0x45,0x4d,0x46,0x5c,0x5b,0x6a,0x51,0x7c,0x49,0x4f,0x66,0x49,0x45,0x4d,0x0,0xf,0x4a,0x47,0x4c,0x51,0xf,0x1,0x73,0x18,0x75,0x6,0x5b,0x5c,0x51,0x44,0x4d,0x6,0x4a,0x49,0x4b,0x43,0x4f,0x5a,0x47,0x5d,0x46,0x4c,0x15,0xf,0x5a,0x4f,0x4a,0x49,0x0,0x18,0x4,0x18,0x4,0x18,0x4,0x18,0x1,0xf]

/*: "Alert" :*/
fileprivate let k_seriesMsg:String = "formal device decrease soonAlert"

/*: "Cancle" :*/
fileprivate let main_fourValue:[Character] = ["C","a","n","c","l","e"]

/*: "Done" :*/
fileprivate let userCleanOrbText:String = "enhance branchDone"

/*: "js call method name =  :*/
fileprivate let user_individualValue:String = "game exit her unknown yourjs ca"
fileprivate let mainAngleFormat:String = "thvoiced"
fileprivate let constActorName:String = " "

/*: , message =  :*/
fileprivate let notiCropId:String = ",column"
fileprivate let constBeforeId:String = "mbroadcasts"

/*: "http://" :*/
fileprivate let data_pieceMsg:String = "http://refuse depict coordinator"

/*: "https://" :*/
fileprivate let mainServeFormat:String = "HTTP"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let user_skilledPath:[UInt8] = [0xb8,0xaf,0xbc,0xee,0xad,0xa1,0xa1,0xa5,0xa7,0xab,0x80,0xaf,0xa3,0xab,0xbd,0xee,0xf3,0xee,0xaa,0xa1,0xad,0xbb,0xa3,0xab,0xa0,0xba,0xe0,0xad,0xa1,0xa1,0xa5,0xa7,0xab,0xe0,0xbd,0xbe,0xa2,0xa7,0xba,0xe6,0xe9,0xf5,0xee,0xe9,0xe7,0xe0,0xa3,0xaf,0xbe,0xe6,0xa8,0xbb,0xa0,0xad,0xba,0xa7,0xa1,0xa0,0xe6,0xad,0xa1,0xa1,0xa5,0xa7,0xab,0xe7,0xee,0xb5,0xee,0xbc,0xab,0xba,0xbb,0xbc,0xa0,0xee,0xad,0xa1,0xa1,0xa5,0xa7,0xab,0xe0,0xbd,0xbe,0xa2,0xa7,0xba,0xe6,0xe9,0xf3,0xe9,0xe7,0x95,0xfe,0x93,0xee,0xb3,0xee,0xe7,0xf5,0xc4]

/*: "uid= :*/
fileprivate let kPileMsg:String = "uid=scan man"

/*: ;domain= :*/
fileprivate let constProgressData:String = "hair night over coat error;domain="

/*: ;path=/" :*/
fileprivate let data_immediateName:[Character] = [";","p","a","t","h"]
fileprivate let k_delayPath:[Character] = ["=","/"]

/*: "document.cookie=' :*/
fileprivate let show_successfullyName:String = "dopowumen"
fileprivate let kProfileKey:[Character] = ["t",".","c","o","o","k","i","e","=","\'"]

/*: ;domin= :*/
fileprivate let constSuccessfulTitle:String = ";domin=male multiple purchase of"

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let userPermissionFormat:[UInt8] = [0x45,0x4a,0xc,0x4,0x4f,0x43,0x43,0x47,0x45,0x49,0x62,0x4d,0x41,0x49,0x5f,0x2,0x45,0x42,0x48,0x49,0x54,0x63,0x4a,0x4,0xb,0x7c,0x64,0x7c,0x7f,0x69,0x7f,0x7f,0x65,0x68,0xb,0x5,0xc,0x11,0x11,0xc,0x1,0x1d,0x5,0xc,0x57,0xc,0x48,0x43,0x4f,0x59,0x41,0x49,0x42,0x58,0x2,0x4f,0x43,0x43,0x47,0x45,0x49,0x11,0xb]

private func colorRes(trade num: UInt8) -> UInt8 {
    return num ^ 44
}

/*: "h5Domain =  :*/
fileprivate let k_boneMessage:String = "h5Domasplit take if"
fileprivate let main_screenName:[Character] = ["i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let showCuteData:String = "getAlcease anima miss successful"
fileprivate let appMutualMessage:[Character] = ["e","s"," ","="," "]

/*: "PHPSESSID" :*/
fileprivate let kBoyName:String = "justPS"

/*: "uid" :*/
fileprivate let const_vehicleTitle:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let showRoundUrl:[Character] = ["a","p"]
fileprivate let mainAspectBrowId:[Character] = ["p","C","o","o","k","i","e","s"," ","="," "]

/*: "UID" :*/
fileprivate let appMinimizePath:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let notiFlexibleData:String = "nprofileig"
fileprivate let kLifeEmberStr:String = "userAglens want arc describe"
fileprivate let showStillData:String = "escant"

/*: "xunwu_iphone" :*/
fileprivate let mainMonthPickUrl:String = "stem depict addressxunwu_i"
fileprivate let const_termsName:[Character] = ["p","h","o","n","e"]

/*: " xunwu_device_id/ :*/
fileprivate let dataHomeUrl:String = "change extent can line xunwu"
fileprivate let showWindowPath:String = "_devrank read club"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let showNothingValue:String = "xdifferencewu"
fileprivate let user_allegedGivingMsg:String = "no lick selected identify financialunwu_ve"
fileprivate let dataGameName:String = "rsion/punish product"

/*:  xunwu_package/ :*/
fileprivate let app_editorProductSituationKey:[Character] = [" ","x","u","n","w"]
fileprivate let kMinId:String = "u_packnear smooth bit"

/*:  xunwu_bundleId/ :*/
fileprivate let app_plantTitle:[Character] = [" ","x","u","n","w","u","_","b","u"]
fileprivate let main_statementFormat:String = "related military want install wherendleId/"

/*:   webkit/1" :*/
fileprivate let appPlainPath:[Character] = [" "," ","w","e","b","k","i","t","/","1"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SightViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class SightViewController: SeeViewController {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: RoundConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: AlertEquatable?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: AlertEquatable?, webConfig: RoundConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_yourText.reversed(), encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        alongOn()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        signalThrough()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(main_dayMessage.suffix(5)) + String(show_taskId))
        //: addNotification()
        errPopular()
        //: func__checkConfig()
        funcConfig()
        //: func__setUpWebView()
        instantiate()
        //: func__registerObserver()
        listenerInErr()
        //: func__beginStartRequest()
        palliate()
        //: func_checkAppleIAP()
        occurIap()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: SightReactiveCompatible.share.func__pushToWebVC(webViewType: .DiscountPopup)
                SightReactiveCompatible.share.elementOfResume(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: show_voiceValue, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            anyEarlierFollowing(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.rawMonthCombine(), .font: UIFont.processAcross(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(dataTaMeetOrientationStr.prefix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension SightViewController {
    /// 添加通知
    //: private func addNotification() {
    private func errPopular() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(stripReason),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: main_depthValue,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(alongOn),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func stripReason() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: constVarietyUrl.map{automaticallyEmber(never: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func alongOn() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: show_interestedObserveReloadTitle.map{videoCos(gender: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(constBlankPath.suffix(7)) + String(notiMaterialPath.prefix(4)) + String(user_revenueData.prefix(4)) + "how): ") + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func signalThrough() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: showModifyId.map{popularMark(asset: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(userWaveUrl.prefix(9)) + String(kBagFoundationContent.suffix(4)) + "eHide): ") + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension SightViewController {
    //: func func__registerObserver() {
    func listenerInErr() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(mainProfileText)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(dataFragmentStr.prefix(9))))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(undercoverOperationView), name: kReasonDataName, object: nil)
    }

    //: func func__setUpWebView() {
    func instantiate() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        blockContent()
        //: func__addBridgeMethod()
        nearSystem()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.wateringPot()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            PointerReactiveCompatible.panoramicView(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(noti_guideRequestId)
            }
        }
    }

    //: func func__beginStartRequest() {
    func palliate() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.startRequest()
            }
            //: } else {
        } else {
            //: func__startRequest()
            startRequest()
        }
    }

    //: func func__startRequest() {
    func startRequest() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = CompositionReactiveCompatible.share.loginUid
        let uid = CompositionReactiveCompatible.share.loginUid
        //: let ssid = CompositionReactiveCompatible.share.loginSessionId
        let ssid = CompositionReactiveCompatible.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (user_guideMsg.replacingOccurrences(of: "laugh", with: "S") + "ESSID=") + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(noti_includeId)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (user_priorityFormat.replacingOccurrences(of: "remark", with: "C") + String(showBigName)))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        sinceOnceColor()
        //: func__initRightButton()
        bitRes()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func occurIap() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            BlockTransactionObserver.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.price(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            BlockTransactionObserver.shared.unfinished()
        }
    }

    //: func func__initLeftButton() {
    func stintForCoatButton() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.parentSet(name: (String(k_goValue.suffix(5)) + "ack_b" + String(data_withMsg.prefix(8)))), style: .plain, target: self, action: #selector(simultaneouslyAnimal(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func bitRes() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(AlertEquatable.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.parentSet(name: (String(data_docPath) + String(const_temporarySaleStandStr))), style: .plain, target: self, action: #selector(enableWeekAction))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func simultaneouslyAnimal(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        shakeOn()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.mediumSave() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: SightViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! SightViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! SightViewController).alongOn()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func funcConfig() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func chooseCollection(sucess: Bool) {
        //: ProgressHUD.dismiss()
        PointerReactiveCompatible.motivationCharmDismiss()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func sinceOnceColor() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: mainUpperPath.map{$0^40}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func wantUrl(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: SightReactiveCompatible.share.func__pushToWebVC(urlStr: url)
            SightReactiveCompatible.share.getConfig(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func randomUrl(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func undercoverOperationView() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: kReasonDataName, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func enableWeekAction() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = ArrowUpView(frame: CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle))
        //: view.show()
        view.descriptionTo()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension SightViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            PointerReactiveCompatible.panoramicView(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            chooseCollection(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        stintForCoatButton()
        //: func__clearJSBgColor()
        sinceOnceColor()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            chooseCollection(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            chooseCollection(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(k_seriesMsg.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(main_fourValue)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(userCleanOrbText.suffix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension SightViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(user_individualValue.suffix(5)) + "ll me" + mainAngleFormat.replacingOccurrences(of: "voice", with: "o") + " name =" + constActorName.capitalized) + "\(message.name)" + (notiCropId.replacingOccurrences(of: "column", with: " ") + constBeforeId.replacingOccurrences(of: "broadcast", with: "es") + "age = ") + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.midMessage(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func nearSystem() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in BarbBidirectionalCollection.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func shakeOn() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in BarbBidirectionalCollection.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension SightViewController {
    //: func func__addUserScript() {
    func oldBy() {
        //: var htmlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(data_pieceMsg.prefix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(data_pieceMsg.prefix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((mainServeFormat.lowercased() + "s://")) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (mainServeFormat.lowercased() + "s://"), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = CompositionReactiveCompatible.share.loginUserMode.userID
        let tempUid: String = CompositionReactiveCompatible.share.loginUserMode.userID
        //: let tempSessionID: String = CompositionReactiveCompatible.share.loginSessionId
        let tempSessionID: String = CompositionReactiveCompatible.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: user_skilledPath.map{$0^206}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(kPileMsg.prefix(4))) + "\(tempUid)" + (String(constProgressData.suffix(8))) + "\(htmlDomain)" + (String(data_immediateName) + String(k_delayPath))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((show_successfullyName.replacingOccurrences(of: "pow", with: "c") + String(kProfileKey)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (user_guideMsg.replacingOccurrences(of: "laugh", with: "S") + "ESSID=") + "\(tempSessionID)" + (String(constSuccessfulTitle.prefix(7))) + "\(htmlDomain)" + (String(data_immediateName) + String(k_delayPath))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((show_successfullyName.replacingOccurrences(of: "pow", with: "c") + String(kProfileKey)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: userPermissionFormat.map{colorRes(trade: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(kPileMsg.prefix(4))) + "\(tempUid)" + (String(constProgressData.suffix(8))) + "\(htmlDomain)" + (String(data_immediateName) + String(k_delayPath))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((show_successfullyName.replacingOccurrences(of: "pow", with: "c") + String(kProfileKey)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(kPileMsg.prefix(4))) + "\(tempSessionID)" + (String(constProgressData.suffix(8))) + "\(htmlDomain)" + (String(data_immediateName) + String(k_delayPath))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((show_successfullyName.replacingOccurrences(of: "pow", with: "c") + String(kProfileKey)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func anyEarlierFollowing(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain
            let htmlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(k_boneMessage.prefix(6)) + String(main_screenName)) + "\(htmlDomain)")
            //: let userID: String = CompositionReactiveCompatible.share.loginUserMode.userID
            let userID: String = CompositionReactiveCompatible.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(showCuteData.prefix(5)) + "lCooki" + String(appMutualMessage)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (kBoyName.replacingOccurrences(of: "just", with: "PH") + "ESSID") {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(const_vehicleTitle)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.palliate()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.lacuna(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func lacuna(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain
                let htmlDomain: String = CompositionReactiveCompatible.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(k_boneMessage.prefix(6)) + String(main_screenName)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: CosSweetReactiveCompatible.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(showRoundUrl) + String(mainAspectBrowId)) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(appMinimizePath)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(const_vehicleTitle))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (kBoyName.replacingOccurrences(of: "just", with: "PH") + "ESSID"), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (kBoyName.replacingOccurrences(of: "just", with: "PH") + "ESSID")
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.palliate()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func blockContent() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((notiFlexibleData.replacingOccurrences(of: "profile", with: "av") + "ator." + String(kLifeEmberStr.prefix(6)) + showStillData.replacingOccurrences(of: "scan", with: "n"))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (notiFlexibleData.replacingOccurrences(of: "profile", with: "av") + "ator." + String(kLifeEmberStr.prefix(6)) + showStillData.replacingOccurrences(of: "scan", with: "n")))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(mainMonthPickUrl.suffix(7)) + String(const_termsName))) {
                //: let str = getUserAgentString()
                let str = handleTitle()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.animaEditor { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(dataHomeUrl.suffix(6)) + String(showWindowPath.prefix(4)) + "ice_id/") + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func handleTitle() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (showNothingValue.replacingOccurrences(of: "difference", with: "un") + "_iphone x" + String(user_allegedGivingMsg.suffix(7)) + String(dataGameName.prefix(6))) + "\(mainControlUrl)" + (String(app_editorProductSituationKey) + String(kMinId.prefix(6)) + "age/") + "\(kLogData)" + (String(app_plantTitle) + String(main_statementFormat.suffix(7))) + "\(k_ofPath)" + (String(appPlainPath))
    }
}
