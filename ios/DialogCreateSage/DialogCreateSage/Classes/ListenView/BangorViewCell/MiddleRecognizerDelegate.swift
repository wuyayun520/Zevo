
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let app_dotRoundPath:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation." :*/
fileprivate let userOuterRootConversionData:[UInt8] = [0x3e,0x5f,0x54,0x5d,0xf,0x63,0x57,0x54,0x5d,0xf,0x14,0x2f,0xf,0x66,0x58,0x5b,0x5b,0xf,0x61,0x54,0x52,0x5e,0x5c,0x5c,0x54,0x5d,0x53,0xf,0x64,0x62,0x54,0x61,0x62,0xf,0x63,0x5e,0xf,0x68,0x5e,0x64,0x1d,0xf,0x30,0xf,0x5d,0x54,0x66,0xf,0x52,0x57,0x50,0x63,0xf,0x66,0x58,0x5b,0x5b,0xf,0x51,0x54,0xf,0x5c,0x50,0x53,0x54,0xf,0x50,0x55,0x63,0x54,0x61,0xf,0x50,0xf,0x62,0x64,0x52,0x52,0x54,0x62,0x62,0x55,0x64,0x5b,0xf,0x61,0x54,0x52,0x5e,0x5c,0x5c,0x54,0x5d,0x53,0x50,0x63,0x58,0x5e,0x5d,0x1d]

fileprivate func wayLose(estimated num: UInt8) -> UInt8 {
    let value = Int(num) + 17
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Your avatar and nickname will not be displayed in the ranking." :*/
fileprivate let user_languageTitle:[UInt8] = [0x15,0x23,0x39,0x3e,0x6c,0x2d,0x3a,0x2d,0x38,0x2d,0x3e,0x6c,0x2d,0x22,0x28,0x6c,0x22,0x25,0x2f,0x27,0x22,0x2d,0x21,0x29,0x6c,0x3b,0x25,0x20,0x20,0x6c,0x22,0x23,0x38,0x6c,0x2e,0x29,0x6c,0x28,0x25,0x3f,0x3c,0x20,0x2d,0x35,0x29,0x28,0x6c,0x25,0x22,0x6c,0x38,0x24,0x29,0x6c,0x3e,0x2d,0x22,0x27,0x25,0x22,0x2b,0x62]

private func literalTheory(life num: UInt8) -> UInt8 {
    return num ^ 76
}

/*: "Open then Applyname will recommend users to you. Use your identity to greet each other." :*/
fileprivate let noti_satisfyMethodStr:[UInt8] = [0xcd,0xee,0xe3,0xec,0x9e,0xf2,0xe6,0xe3,0xec,0x9e,0xbf,0xee,0xee,0xea,0xf7,0xec,0xdf,0xeb,0xe3,0x9e,0xf5,0xe7,0xea,0xea,0x9e,0xf0,0xe3,0xe1,0xed,0xeb,0xeb,0xe3,0xec,0xe2,0x9e,0xf3,0xf1,0xe3,0xf0,0xf1,0x9e,0xf2,0xed,0x9e,0xf7,0xed,0xf3,0xac,0x9e,0xd3,0xf1,0xe3,0x9e,0xf7,0xed,0xf3,0xf0,0x9e,0xe7,0xe2,0xe3,0xec,0xf2,0xe7,0xf2,0xf7,0x9e,0xf2,0xed,0x9e,0xe5,0xf0,0xe3,0xe3,0xf2,0x9e,0xe3,0xdf,0xe1,0xe6,0x9e,0xed,0xf2,0xe6,0xe3,0xf0,0xac]

fileprivate func dismissLand(city num: UInt8) -> UInt8 {
    let value = Int(num) + 130
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Open then %@ will recommend users to you. Use your identity to greet each other." :*/
fileprivate let const_bothMsg:[UInt8] = [0x5c,0x63,0x76,0x7d,0x33,0x67,0x7b,0x76,0x7d,0x33,0x36,0x53,0x33,0x64,0x7a,0x7f,0x7f,0x33,0x61,0x76,0x70,0x7c,0x7e,0x7e,0x76,0x7d,0x77,0x33,0x66,0x60,0x76,0x61,0x60,0x33,0x67,0x7c,0x33,0x6a,0x7c,0x66,0x3d,0x33,0x46,0x60,0x76,0x33,0x6a,0x7c,0x66,0x61,0x33,0x7a,0x77,0x76,0x7d,0x67,0x7a,0x67,0x6a,0x33,0x67,0x7c,0x33,0x74,0x61,0x76,0x76,0x67,0x33,0x76,0x72,0x70,0x7b,0x33,0x7c,0x67,0x7b,0x76,0x61,0x3d]

/*: "#FF2348" :*/
fileprivate let show_universalUrl:[Character] = ["#","F","F","2","3","4","8"]

/*: "To set up a password, you need to verify the account, choose your verification method" :*/
fileprivate let user_southName:[UInt8] = [0x64,0x6f,0x68,0x74,0x65,0x6d,0x20,0x6e,0x6f,0x69,0x74,0x61,0x63,0x69,0x66,0x69,0x72,0x65,0x76,0x20,0x72,0x75,0x6f,0x79,0x20,0x65,0x73,0x6f,0x6f,0x68,0x63,0x20,0x2c,0x74,0x6e,0x75,0x6f,0x63,0x63,0x61,0x20,0x65,0x68,0x74,0x20,0x79,0x66,0x69,0x72,0x65,0x76,0x20,0x6f,0x74,0x20,0x64,0x65,0x65,0x6e,0x20,0x75,0x6f,0x79,0x20,0x2c,0x64,0x72,0x6f,0x77,0x73,0x73,0x61,0x70,0x20,0x61,0x20,0x70,0x75,0x20,0x74,0x65,0x73,0x20,0x6f,0x54]

/*: "Verified by Phone" :*/
fileprivate let kFaultMessage:[Character] = ["V","e","r","i","f","i","e","d"," ","b","y"," ","P","h","o","n","e"]

/*: "Verified by Email" :*/
fileprivate let k_lastValue:String = "failure reader week bubbleVeri"
fileprivate let constRealmDragFormat:String = "beyond gold tool button by Email"

/*: "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? " :*/
fileprivate let user_goingPath:[UInt8] = []

fileprivate func lastComplaint(perceive num: UInt8) -> UInt8 {
    let value = Int(num) + 146
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "UNRECOVERABLE" :*/
fileprivate let app_withinStr:String = "UNremoteCOV"
fileprivate let constCurrentlyPath:String = "hour"
fileprivate let userPowConnectKey:String = "rable"

/*: "Delete Account" :*/
fileprivate let show_guideContent:String = "Deletfrank live"
fileprivate let userSystemSId:[Character] = ["o","u","n","t"]

/*: "Cancel" :*/
fileprivate let kStartStr:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let user_animaId:[Character] = ["D","e","l","e","t","e"]

/*: "Logout succeeded!" :*/
fileprivate let noti_equallyLittleFormat:String = "Logoutslow hello"
fileprivate let notiCustomName:[Character] = [" ","s","u","c","c","e","e","d","e","d","!"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MiddleRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/4/26.
//

//: import UIKit
import UIKit

//: enum SettingsUniversal: String {
enum UniversalSchemaConvertible: String {
    //: case Contact_information          = "Contact information"
    case Contact_information = "Contact information"
    //: case Notifications                = "Notifications"
    case Notifications
    //: case Social_Preferences           = "Social Preferences"
    case Social_Preferences = "Social Preferences"
}

//: enum SettingsUniversalType: String {
enum GetHashable: String {
    //: case Phone_Number          = "Phone Number"
    case Phone_Number = "Phone Number"
    //: case Email_Address         = "Email Address"
    case Email_Address = "Email Address"
    //: case Turnon_Notif          = "Turn on Notification"
    case Turnon_Notif = "Turn on Notification"
    //: case Same_gendered         = "Same-gendered messaging"
    case Same_gendered = "Same-gendered messaging"
    //: case Cupid                 = "Cupid messaging"
    case Cupid = "Cupid messaging"
    //: case Ranking               = "Ranking anonymous"
    case Ranking = "Ranking anonymous"
    //: case Automatic             = "Automatic message"
    case Automatic = "Automatic message"
    //: case SetPassword           = "Set New Password"
    case SetPassword = "Set New Password"
    //: case DeleteA               = "Delete Account"
    case DeleteA = "Delete Account"
}

//: class TalkingSettingsUniversalVC: TalkingBaseViewController {
class MiddleRecognizerDelegate: MergeContactViewController {
    //: fileprivate lazy var bag = DisposeBag()
    fileprivate lazy var bag = DisposeBag()
    //: var settingModel = TalkingSettingModel()
    var settingModel = BeneathTransformable()

    //: var  type = SettingsUniversal.Contact_information
    var type = UniversalSchemaConvertible.Contact_information

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: app_dotRoundPath.reversed(), encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.wateringPot()
        //: designView()
        whoView()
        //: getSettingData()
        destroy()
    }

    // MARK: - Lazy load

    //: private lazy var DataSource: [SettingsUniversalType] = {
    private lazy var DataSource: [GetHashable] = {
        //: var array = [SettingsUniversalType]()
        var array = [GetHashable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle - mainExplainFaceName), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: private lazy var DetailSource: [String] = {
    private lazy var DetailSource: [String] = {
        //: var array = ["", "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation.".localizedArguments(AppName), "Your avatar and nickname will not be displayed in the ranking.".localized, "Open then Applyname will recommend users to you. Use your identity to greet each other.".localized]
        var array = ["", String(bytes: userOuterRootConversionData.map{wayLose(estimated: $0)}, encoding: .utf8)!.temporaryWorker(k_warnUrl), String(bytes: user_languageTitle.map{literalTheory(life: $0)}, encoding: .utf8)!.localized, String(bytes: noti_satisfyMethodStr.map{dismissLand(city: $0)}, encoding: .utf8)!.localized]
        //: if CompositionReactiveCompatible.share.loginUserMode.sex == "2" {
        if CompositionReactiveCompatible.share.loginUserMode.sex == "2" {
            //: array[1] = "Open then %@ will recommend users to you. Use your identity to greet each other.".localizedArguments(AppName)
            array[1] = String(bytes: const_bothMsg.map{$0^19}, encoding: .utf8)!.temporaryWorker(k_warnUrl)
        }
        //: return array
        return array
        //: }()
    }()
}

// MARK: - Load data

//: extension TalkingSettingsUniversalVC {
extension MiddleRecognizerDelegate {
    //: func getSettingData() {
    func destroy() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetSetting(params: dict) { succeed, result, errorModel in
        GreatThen.exceptHalf(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<BeneathTransformable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func setUnicersalView(type: SettingsUniversal) {
    func roundForm(type: UniversalSchemaConvertible) {
        //: self.type = type
        self.type = type
        //: self.title = type.rawValue.localized
        self.title = type.rawValue.localized
        //: switch type {
        switch type {
        //: case .Contact_information:
        case .Contact_information:
            //: if CompositionReactiveCompatible.share.appConfigMode.enableSmsLogin {
            if CompositionReactiveCompatible.share.appConfigMode.enableSmsLogin {
                //: DataSource.append(.Phone_Number)
                DataSource.append(.Phone_Number)
            }
            //: DataSource.append(.Email_Address)
            DataSource.append(.Email_Address)
            //: if (CompositionReactiveCompatible.share.appConfigMode.enableSmsLogin &&
            if (CompositionReactiveCompatible.share.appConfigMode.enableSmsLogin &&
                //: CompositionReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0) ||
                CompositionReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0) ||
                //: CompositionReactiveCompatible.share.loginUserMode.email.count > 0 {
                CompositionReactiveCompatible.share.loginUserMode.email.count > 0
            {
                //: DataSource.append(.SetPassword)
                DataSource.append(.SetPassword)
            }
            //: DataSource.append(.DeleteA)
            DataSource.append(.DeleteA)
        //: break
        //: case .Notifications:
        case .Notifications:
            //: DataSource.append(.Turnon_Notif)
            DataSource.append(.Turnon_Notif)
        //: break
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: DataSource.append(.Same_gendered)
            DataSource.append(.Same_gendered)
            //: DataSource.append(.Cupid)
            DataSource.append(.Cupid)
            //: DataSource.append(.Ranking)
            DataSource.append(.Ranking)
            //: DataSource.append(.Automatic)
            DataSource.append(.Automatic)
            //: break
        }
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    /// 获取修改【手机号】密码验证码
    //: private func req_sendModifyPwdSms() {
    private func modifySms() {
        //: ProgressHUD.show()
        PointerReactiveCompatible.soundAssetThrow()
        //: TalkingLoginRequestTool.req_sendModifyPwdSms { succeed, result, errorModel in
        MidRequestTool.day { succeed, _, _ in
            //: ProgressHUD.dismiss()
            PointerReactiveCompatible.motivationCharmDismiss()
            //: guard succeed else { return }
            guard succeed else { return }
            //: SightReactiveCompatible.share.func__pushToSetNewPasswordVC(type: .PhoneLogin, content: CompositionReactiveCompatible.share.loginUserMode.mobile ?? "", isNewPwd: false)
            SightReactiveCompatible.share.bear(type: .PhoneLogin, content: CompositionReactiveCompatible.share.loginUserMode.mobile ?? "", isNewPwd: false)
        }
    }

    /// 获取修改【邮箱】密码验证码
    //: private func req_sendModifyPwdEmail() {
    private func quantityRelation() {
        //: ProgressHUD.show()
        PointerReactiveCompatible.soundAssetThrow()
        //: TalkingLoginRequestTool.req_sendModifyPwdEmail { succeed, result, errorModel in
        MidRequestTool.tagDown { succeed, _, _ in
            //: ProgressHUD.dismiss()
            PointerReactiveCompatible.motivationCharmDismiss()
            //: guard succeed else { return }
            guard succeed else { return }
            //: SightReactiveCompatible.share.func__pushToSetNewPasswordVC(type: .EmailLogin, content: CompositionReactiveCompatible.share.loginUserMode.email, isNewPwd: false)
            SightReactiveCompatible.share.bear(type: .EmailLogin, content: CompositionReactiveCompatible.share.loginUserMode.email, isNewPwd: false)
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingSettingsUniversalVC: UITableViewDelegate, UITableViewDataSource {
extension MiddleRecognizerDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return DataSource.count
        return DataSource.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: switch type {
        switch type {
        //: case .Contact_information, .Notifications:
        case .Contact_information, .Notifications:
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: if DataSource[indexPath.row] == .Same_gendered {
            if DataSource[indexPath.row] == .Same_gendered {
                //: return actualHeight(h: 50)
                return actualHeight(h: 50)
                //: }else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: if CompositionReactiveCompatible.share.loginUserMode.sex == "2" {
                if CompositionReactiveCompatible.share.loginUserMode.sex == "2" {
                    //: return actualHeight(h: 89)
                    return actualHeight(h: 89)
                    //: } else {
                } else {
                    //: return actualHeight(h: 109)
                    return actualHeight(h: 109)
                }
                //: }  else if (DataSource[indexPath.row] == .Ranking ||  DataSource[indexPath.row] == .Automatic) {
            } else if DataSource[indexPath.row] == .Ranking || DataSource[indexPath.row] == .Automatic {
                //: return actualHeight(h: 92)
                return actualHeight(h: 92)
            }
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        }
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: switch self.type {
        switch self.type {
        //: case .Contact_information:
        case .Contact_information:
            //: let cell: TalkingSettingCell = tableView.dequeueReusableCell(withIdentifier: TalkingSettingCell.className(), for: indexPath) as! TalkingSettingCell
            let cell: NameSettingCell = tableView.dequeueReusableCell(withIdentifier: NameSettingCell.className(), for: indexPath) as! NameSettingCell

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.memory(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: cell.titleBLB.textColor = UIColor.appTitleColor()
            cell.titleBLB.textColor = UIColor.antiquityColor()

            //: if DataSource[indexPath.row] == SettingsUniversalType.Phone_Number {
            if DataSource[indexPath.row] == GetHashable.Phone_Number {
                //: if CompositionReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0 {
                if CompositionReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0 {
                    //: cell.setCellDetails(detail: CompositionReactiveCompatible.share.loginUserMode.mobile ?? "")
                    cell.set(detail: CompositionReactiveCompatible.share.loginUserMode.mobile ?? "")
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: cell.titleBLB.textColor = .appTitleColor()
                cell.titleBLB.textColor = .antiquityColor()
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.Email_Address {
            } else if DataSource[indexPath.row] == GetHashable.Email_Address {
                //: if CompositionReactiveCompatible.share.loginUserMode.email.count > 0 {
                if CompositionReactiveCompatible.share.loginUserMode.email.count > 0 {
                    //: cell.setCellDetails(detail: CompositionReactiveCompatible.share.loginUserMode.email )
                    cell.set(detail: CompositionReactiveCompatible.share.loginUserMode.email)
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.SetPassword {
            } else if DataSource[indexPath.row] == GetHashable.SetPassword {
                //: cell.editImag.isHidden = false
                cell.editImag.isHidden = false
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.DeleteA {
            } else if DataSource[indexPath.row] == GetHashable.DeleteA {
                //: cell.editImag.isHidden = true
                cell.editImag.isHidden = true
                //: cell.titleBLB.textColor = UIColor.init(hex: "#FF2348")
                cell.titleBLB.textColor = UIColor(hex: (String(show_universalUrl)))
            }

            //: return cell
            return cell

        //: case .Notifications, .Social_Preferences:
        case .Notifications, .Social_Preferences:
            //: let cell: TalkingSettingNotifCell = tableView.dequeueReusableCell(withIdentifier: TalkingSettingNotifCell.className(), for: indexPath) as! TalkingSettingNotifCell
            let cell: DeviseNotifCell = tableView.dequeueReusableCell(withIdentifier: DeviseNotifCell.className(), for: indexPath) as! DeviseNotifCell

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.report(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: if self.type == .Social_Preferences {
            if self.type == .Social_Preferences {
                //: cell.setCellDetails(detail: DetailSource[indexPath.row])
                cell.cell(detail: DetailSource[indexPath.row])
            }
            //: if DataSource[indexPath.row] == .Turnon_Notif {
            if DataSource[indexPath.row] == .Turnon_Notif {
                //: UNUserNotificationCenter.current().getNotificationSettings { settings in
                UNUserNotificationCenter.current().getNotificationSettings { settings in
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: let result = settings.authorizationStatus == .authorized
                        let result = settings.authorizationStatus == .authorized
                        //: cell.setCellNotif(isShow: result ? 1 : -1)
                        cell.launchFamily(isShow: result ? 1 : -1)
                    }
                }
                //: } else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: cell.setCellNotif(isShow: self.settingModel.isCupid ?? 1)
                cell.launchFamily(isShow: self.settingModel.isCupid ?? 1)
                //: } else if DataSource[indexPath.row] == .Ranking {
            } else if DataSource[indexPath.row] == .Ranking {
                //: cell.setCellNotif(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                cell.launchFamily(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                //: } else if DataSource[indexPath.row] == .Same_gendered {
            } else if DataSource[indexPath.row] == .Same_gendered {
                //: cell.setCellNotif(isShow: self.settingModel.isSameGendered ?? -1)
                cell.launchFamily(isShow: self.settingModel.isSameGendered ?? -1)
                //: } else if DataSource[indexPath.row] == .Automatic {
            } else if DataSource[indexPath.row] == .Automatic {
                //: cell.setCellNotif(isShow: self.settingModel.autoGreetAuth ?? -1)
                cell.launchFamily(isShow: self.settingModel.autoGreetAuth ?? -1)
            }
            //: return cell
            return cell
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if self.type == .Contact_information {
        if self.type == .Contact_information {
            //: if DataSource[indexPath.row].rawValue == SettingsUniversalType.Phone_Number.rawValue {
            if DataSource[indexPath.row].rawValue == GetHashable.Phone_Number.rawValue {
                //: let mobile = CompositionReactiveCompatible.share.loginUserMode.mobile ?? ""
                let mobile = CompositionReactiveCompatible.share.loginUserMode.mobile ?? ""
                //: if mobile.isEmptyString {
                if mobile.isEmptyString {
                    //: let vc = TalkingBindPhoneVC()
                    let vc = ExecThen()
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.Email_Address.rawValue {
            } else if DataSource[indexPath.row].rawValue == GetHashable.Email_Address.rawValue {
                //: if CompositionReactiveCompatible.share.loginUserMode.email.count <= 0 {
                if CompositionReactiveCompatible.share.loginUserMode.email.count <= 0 {
                    //: let vc = TalkingLoginBindEmailVC()
                    let vc = CarrierRecognizerDelegate()
                    //: vc.isBack = true
                    vc.isBack = true
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.DeleteA.rawValue {
            } else if DataSource[indexPath.row].rawValue == GetHashable.DeleteA.rawValue {
                //: writeOff()
                poetize()
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.SetPassword.rawValue {
            } else if DataSource[indexPath.row].rawValue == GetHashable.SetPassword.rawValue {
                //: let haveMobile = CompositionReactiveCompatible.share.appConfigMode.enableSmsLogin && (CompositionReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0)
                let haveMobile = CompositionReactiveCompatible.share.appConfigMode.enableSmsLogin && (CompositionReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0)
                //: let haveEmail = CompositionReactiveCompatible.share.loginUserMode.email.count > 0
                let haveEmail = CompositionReactiveCompatible.share.loginUserMode.email.count > 0
                //: if haveMobile, haveEmail {
                if haveMobile, haveEmail { // 手机号和邮箱都存在
                    //: let vc = TalkingMunuPopView(frame: self.view.frame)
                    let vc = AddressPopView(frame: self.view.frame)
                    //: vc.title = "To set up a password, you need to verify the account, choose your verification method".localized
                    vc.title = String(bytes: user_southName.reversed(), encoding: .utf8)!.localized
                    //: vc.titleFont = UIFont.pingfangRugularFont(fontSize: 12)
                    vc.titleFont = UIFont.joinSize(fontSize: 12)
                    //: vc.titleColor = UIColor.appValueDetailColor()
                    vc.titleColor = UIColor.pointOfTotalersection()
                    //: vc.cellTitleFont = UIFont.pingfangMediumFont(fontSize: 16)
                    vc.cellTitleFont = UIFont.counteraction(fontSize: 16)
                    //: vc.hasCancel = false
                    vc.hasCancel = false
                    //: vc.initwithList(cellTitleList: ["Verified by Phone".localized, "Verified by Email".localized])
                    vc.mapFile(cellTitleList: [(String(kFaultMessage)).localized, (String(k_lastValue.suffix(4)) + "fied" + String(constRealmDragFormat.suffix(9))).localized])
                    //: vc.munuBlock = { [weak self] index, str in
                    vc.munuBlock = { [weak self] index, _ in
                        //: guard let self = self else { return }
                        guard let self = self else { return }
                        //: if index == 0 {
                        if index == 0 {
                            //: self.req_sendModifyPwdSms()
                            self.modifySms()
                            //: } else if index == 1 {
                        } else if index == 1 {
                            //: self.req_sendModifyPwdEmail()
                            self.quantityRelation()
                        }
                    }

                    //: } else if haveMobile {
                } else if haveMobile {
                    //: req_sendModifyPwdSms()
                    modifySms()

                    //: } else if haveEmail {
                } else if haveEmail {
                    //: req_sendModifyPwdEmail()
                    quantityRelation()
                }
            }
        }
    }
}

//: extension TalkingSettingsUniversalVC {
extension MiddleRecognizerDelegate {
    /// 注销
    //: private func writeOff() {
    private func poetize() {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard MeetRouteThen.todayConvert() == false else { return }
        //: guard TalkingSocketManager.shared.isStrategy == false else {
        guard FileSocketManager.shared.isStrategy == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.foolishness(showMsg: constEnterMessage)
            //: return
            return
        }

        //: var seconds = 15
        var seconds = 15
        //: let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        //: timer.schedule(deadline: .now(), repeating: 1.0)
        timer.schedule(deadline: .now(), repeating: 1.0)

        //: let str = "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? ".localized
        let str = String(bytes: user_goingPath.map{lastComplaint(perceive: $0)}, encoding: .utf8)!.localized

        //: let range =  str.exMatchStrRange("UNRECOVERABLE")
        let range = str.duplicateParty((app_withinStr.replacingOccurrences(of: "remote", with: "RE") + constCurrentlyPath.replacingOccurrences(of: "hour", with: "E") + userPowConnectKey.uppercased()))

        //: let string = NSMutableAttributedString.init(string: str )
        let string = NSMutableAttributedString(string: str)
        //: if range.count > 0 {
        if range.count > 0 {
            //: string.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: range.first!)
            string.addAttribute(.foregroundColor, value: UIColor.spendColor(), range: range.first!)
        }
        //: TalkingAlertShow.attribAlert(title: "Delete Account".localized, attributedMessage: string, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized) {
        YetAlertShow.memoryAbove(title: (String(show_guideContent.prefix(5)) + "e Acc" + String(userSystemSId)).localized, attributedMessage: string, leftBtnTitle: (String(kStartStr)).localized, rightBtnTitle: (String(user_animaId)).localized) {
            //: TalkingAlertShow.hideAlert()
            YetAlertShow.usance()
            //: timer.cancel()
            timer.cancel()
            //: } rightBlock: {
        } rightBlock: {
            //: TalkingAlertShow.hideAlert()
            YetAlertShow.usance()

            //: self.LockAccount()
            self.tarStack()
        }

        //: timer.setEventHandler {
        timer.setEventHandler {
            //: seconds -= 1
            seconds -= 1
            //: if seconds <= 0 {
            if seconds <= 0 {
                //: timer.cancel()
                timer.cancel()
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized, isTouch: true)
                YetAlertShow.rightForce(title: (String(user_animaId)).localized, isTouch: true)
                //: } else {
            } else {
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized + " (\(seconds)s)", isTouch: false)
                YetAlertShow.rightForce(title: (String(user_animaId)).localized + " (\(seconds)s)", isTouch: false)
            }
        }
        //: timer.resume()
        timer.resume()
    }

    //: private func LockAccount() {
    private func tarStack() {
        //: ProgressHUD.show()
        PointerReactiveCompatible.soundAssetThrow()
        //: TalkingMeRequestTool.req_LockAccount(params: [:]) { succeed, result, errorModel in
        GreatThen.someOperation(params: [:]) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            PointerReactiveCompatible.motivationCharmDismiss()
            //: if succeed {
            if succeed {
                //: self.logoutTool()
                self.aboveBegin()
                //: self.func__showStatusBarSuccessMsg(showMsg: "Logout succeeded!".localized)
                self.through(showMsg: (String(noti_equallyLittleFormat.prefix(6)) + String(notiCustomName)).localized)
            }
        }
    }

    //: private func logoutTool() {
    private func aboveBegin() {
        //: guard TalkingSocketManager.shared.isStrategy == false else {
        guard FileSocketManager.shared.isStrategy == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.foolishness(showMsg: constEnterMessage)
            //: return
            return
        }

        //: ProgressHUD.show()
        PointerReactiveCompatible.soundAssetThrow()
        //: TalkingLoginRequestTool.req_loginOut { t in
        MidRequestTool.disappear { t in
            //: ProgressHUD.dismiss()
            PointerReactiveCompatible.motivationCharmDismiss()
            //: if t {
            if t {
                //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: constMinNameTitle, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - UI

//: extension TalkingSettingsUniversalVC {
extension MiddleRecognizerDelegate {
    //: private func designView() {
    private func whoView() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingSettingCell.self, forCellReuseIdentifier: TalkingSettingCell.className())
        MainTable.register(NameSettingCell.self, forCellReuseIdentifier: NameSettingCell.className())
        //: MainTable.register(TalkingSettingNotifCell.self, forCellReuseIdentifier: TalkingSettingNotifCell.className())
        MainTable.register(DeviseNotifCell.self, forCellReuseIdentifier: DeviseNotifCell.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.top.equalTo(20)
            make.top.equalTo(20)
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}
