
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataAbleMsg:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Cancel" :*/
fileprivate let main_withinData:[Character] = ["C","a","n","c","e","l"]

/*: "Submit" :*/
fileprivate let showYouName:[Character] = ["S"]
fileprivate let show_innerPath:String = "ubmresume"

/*: "Please tell us your problems                   " :*/
fileprivate let app_pieceFormat:[UInt8] = [0x78,0x44,0x4d,0x49,0x5b,0x4d,0x8,0x5c,0x4d,0x44,0x44,0x8,0x5d,0x5b,0x8,0x51,0x47,0x5d,0x5a,0x8,0x58,0x5a,0x47,0x4a,0x44,0x4d,0x45,0x5b,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8,0x8]

private func peerSuit(until num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "You need to choose at least one." :*/
fileprivate let show_natureFourStr:[UInt8] = [0xee,0xd8,0xc2,0x97,0xd9,0xd2,0xd2,0xd3,0x97,0xc3,0xd8,0x97,0xd4,0xdf,0xd8,0xd8,0xc4,0xd2,0x97,0xd6,0xc3,0x97,0xdb,0xd2,0xd6,0xc4,0xc3,0x97,0xd8,0xd9,0xd2,0x99]

private func widthMiss(middle num: UInt8) -> UInt8 {
    return num ^ 183
}

/*: "Please tell us your problems." :*/
fileprivate let mainMixStr:String = "Please safety occupation present nature current"
fileprivate let dataSumKey:String = "woman key transaction mediumtell "
fileprivate let main_liveKey:[Character] = ["r","o","b","l","e","m","s","."]

/*: "app/userReport" :*/
fileprivate let user_pointTitle:String = "bench birth acquireapp/u"
fileprivate let noti_stageKey:String = "serReportminute aircraft transportation plain select"

/*: "reason" :*/
fileprivate let userProductFormat:String = "rseriesson"

/*: "desc" :*/
fileprivate let app_shakeKey:[UInt8] = [0xe7,0xe6,0xf0,0xe0]

private func revenueDoing(average num: UInt8) -> UInt8 {
    return num ^ 131
}

/*: "mf/moment/report" :*/
fileprivate let appProcedurePath:[Character] = ["m","f","/","m"]
fileprivate let notiOwnerEntireName:String = "omefrom"

/*: "momentId" :*/
fileprivate let app_aircraftTitle:[UInt8] = [0xe2,0xe0,0xe2,0xea,0xe1,0xfb,0xc6,0xeb]

/*: "mf/momentReply/report" :*/
fileprivate let appSlideFormat:[Character] = ["m","f","/","m","o","m"]
fileprivate let dataDistributionBeamCalculationStr:String = "part exampleentR"
fileprivate let kPublicationValue:String = "/reportbrow end origin close"

/*: "app/videoReport" :*/
fileprivate let main_spendRefuseTransferValue:String = "elite reflectionapp/"
fileprivate let appSuiteUrl:String = "EPORT"

/*: "logId" :*/
fileprivate let showHerName:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "Submitted successfully" :*/
fileprivate let user_directTowardValue:String = "nor skip swim mail beatSubmitt"
fileprivate let user_sectionMsg:[Character] = ["e"]
fileprivate let appEveryoneValue:[Character] = ["d"," ","s","u","c","c","e","s","s","f","u","l","l","y"]

/*: "Nudity" :*/
fileprivate let userSitMsg:[Character] = ["N","u","d","i","t","y"]

/*: "Violence" :*/
fileprivate let const_ionMsg:[Character] = ["V","i","o","l","e","n","c","e"]

/*: "Harassment" :*/
fileprivate let notiOffdMessage:String = "Harassexact actual"

/*: "Suicide or Self-injury" :*/
fileprivate let show_strikeUrl:[Character] = ["S","u","i","c","i","d","e"," ","o","r"," ","S","e","l","f","-","i","n","j"]
fileprivate let kProgressUrl:[Character] = ["u","r","y"]

/*: "False Information" :*/
fileprivate let notiShutMessage:String = "Falsprotection attractive hi separate there"
fileprivate let appExpressKey:String = "ormationeighbor"

/*: "Spam" :*/
fileprivate let notiKindKey:String = "Spamsmart provide"

/*: "Hate Speech" :*/
fileprivate let kLipForeName:String = "Hatblanket highlight"
fileprivate let show_exactlyMessage:String = "e Spchoose dismiss network a value"

/*: "Terrorism" :*/
fileprivate let user_yearValue:String = "put quit blue recover fortTerro"
fileprivate let noti_assistValue:[Character] = ["r","i","s","m"]

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let constCloudMsg:[UInt8] = [0xa7,0xb5,0xc0,0xb9,0x74,0xc3,0xba,0x74,0x9d,0xc0,0xc0,0xb9,0xbb,0xb5,0xc0,0x74,0xc3,0xc6,0x74,0xa6,0xb9,0xbb,0xc9,0xc0,0xb5,0xc8,0xb9,0xb8,0x74,0x9b,0xc3,0xc3,0xb8,0xc7]

fileprivate func springPure(feature num: UInt8) -> UInt8 {
    let value = Int(num) - 84
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Sending disgusting messages" :*/
fileprivate let showReportName:String = "resume shape neighbor domain managerSend"
fileprivate let dataSideMsg:String = "cycleing"
fileprivate let notiCounterruptKey:String = "SAGES"

/*: "Using photos of nudity or drugs" :*/
fileprivate let kVeryText:[UInt8] = [0xb,0x2d,0x37,0x30,0x39,0x7e,0x2e,0x36,0x31,0x2a,0x31,0x2d,0x7e,0x31,0x38,0x7e,0x30,0x2b,0x3a,0x37,0x2a,0x27,0x7e,0x31,0x2c,0x7e,0x3a,0x2c,0x2b,0x39,0x2d]

private func capableNothing(individual num: UInt8) -> UInt8 {
    return num ^ 94
}

/*: "Harassing or sending ads" :*/
fileprivate let main_rowLargeUrl:String = "draw agoHara"
fileprivate let noti_bubbleValue:String = " or sendfail wave movie living"
fileprivate let kHerMeetName:String = "hi translation or install forwarding ads"

/*: "Defraud or using fake info" :*/
fileprivate let data_spaceValue:String = "Defraud cease shirt"
fileprivate let notiTrustKey:String = "time recommend within below bador u"
fileprivate let user_lifeRobotData:String = "sing fworldwide child segment retain"

/*: "Others" :*/
fileprivate let const_emberData:String = "commit general skip other choiceOther"
fileprivate let appMouthLateMessage:[Character] = ["s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ArrowCapAlertView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum FormatAlertType: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class ArrowCapAlertView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: FormatAlertType?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [PercentageReactiveCompatible] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = PercentageReactiveCompatible()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: FormatAlertType?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        switcher()
        //: setupSubViewsConstraint()
        unidentifiedConstraint()
        //: setupReasons()
        exist()
        //: bindInteraction()
        activeAgent()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataAbleMsg.reversed(), encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(main_withinData)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.rawMonthCombine(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.processAcross(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.rawMonthCombine().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(charm), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(showYouName) + show_innerPath.replacingOccurrences(of: "resume", with: "it")).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.processAcross(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.keyLocal(colors: UIColor.sumerpretation(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(bruxism), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: user_needValue - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.pushDown(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = WordsSweetTitleView(placeholder: String(bytes: app_pieceFormat.map{peerSuit(until: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.pointOfTotalersection(), frame: CGRect(x: 10, y: 10, width: user_needValue - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .processAcross(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.antiquityColor()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .processAcross(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.pushDown(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(ParentViewCell.self, forCellReuseIdentifier: ParentViewCell.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension ArrowCapAlertView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func characterUnless() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func whenReport(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = DirtyMacroDefine.getWindow()
            let window = DirtyMacroDefine.effectRequire()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func enablely(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func charm() {
        //: self.dismissReportView(succeed: false)
        self.enablely(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func bruxism() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.foolishness(showMsg: String(bytes: show_natureFourStr.map{widthMiss(middle: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.foolishness(showMsg: (String(mainMixStr.prefix(7)) + String(dataSumKey.suffix(5)) + "us your p" + String(main_liveKey)).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(user_pointTitle.suffix(5)) + String(noti_stageKey.prefix(9)))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (userProductFormat.replacingOccurrences(of: "series", with: "ea")): selectedModel.type, String(bytes: app_shakeKey.map{revenueDoing(average: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(appProcedurePath) + notiOwnerEntireName.replacingOccurrences(of: "from", with: "nt") + "/report")
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: app_aircraftTitle.map{$0^143}, encoding: .utf8)!: tempId, (userProductFormat.replacingOccurrences(of: "series", with: "ea")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(appSlideFormat) + String(dataDistributionBeamCalculationStr.suffix(4)) + "eply" + String(kPublicationValue.prefix(7)))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (userProductFormat.replacingOccurrences(of: "series", with: "ea")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(main_spendRefuseTransferValue.suffix(4)) + "videoR" + appSuiteUrl.lowercased())
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: showHerName.reversed(), encoding: .utf8)!: tempId, (userProductFormat.replacingOccurrences(of: "series", with: "ea")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        MpThen.draftCompletion(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.through(showMsg: (String(user_directTowardValue.suffix(7)) + String(user_sectionMsg) + String(appEveryoneValue)).localized)
                //: self.dismissReportView(succeed: true)
                self.enablely(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension ArrowCapAlertView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: ParentViewCell.className()) as! ParentViewCell
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.successfully(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension ArrowCapAlertView {
    /// 添加视图
    //: private func setupSubviews() {
    private func switcher() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func unidentifiedConstraint() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func exist() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(userSitMsg)).localized,
                           //: "Violence".localized,
                           (String(const_ionMsg)).localized,
                           //: "Harassment".localized,
                           (String(notiOffdMessage.prefix(6)) + "ment").localized,
                           //: "Suicide or Self-injury".localized,
                           (String(show_strikeUrl) + String(kProgressUrl)).localized,
                           //: "False Information".localized,
                           (String(notiShutMessage.prefix(4)) + "e Inf" + appExpressKey.replacingOccurrences(of: "neighbor", with: "n")).localized,
                           //: "Spam".localized,
                           (String(notiKindKey.prefix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(kLipForeName.prefix(3)) + String(show_exactlyMessage.prefix(4)) + "eech").localized,
                           //: "Terrorism".localized,
                           (String(user_yearValue.suffix(5)) + String(noti_assistValue)).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: constCloudMsg.map{springPure(feature: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(showReportName.suffix(4)) + "ing disgu" + dataSideMsg.replacingOccurrences(of: "cycle", with: "st") + " mes" + notiCounterruptKey.lowercased()).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: kVeryText.map{capableNothing(individual: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(main_rowLargeUrl.suffix(4)) + "ssing" + String(noti_bubbleValue.prefix(8)) + String(kHerMeetName.suffix(7))).localized,
                           //: "Defraud or using fake info".localized,
                           (String(data_spaceValue.prefix(8)) + String(notiTrustKey.suffix(4)) + String(user_lifeRobotData.prefix(6)) + "ake info").localized,
                           //: "Others".localized]
                           (String(const_emberData.suffix(5)) + String(appMouthLateMessage)).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = CompositionReactiveCompatible.share.appConfigMode.videoReport["reason"] {
            if let reason = CompositionReactiveCompatible.share.appConfigMode.videoReport[(userProductFormat.replacingOccurrences(of: "series", with: "ea"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = PercentageReactiveCompatible()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func activeAgent() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
