
//: Declare String Begin

/*: "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$" :*/
fileprivate let user_donStr:[UInt8] = [0xdb,0xad,0xde,0xe4,0xa8,0xff,0xc4,0xa8,0xdf,0xb5,0xa8,0xbc,0xda,0xd9,0xab,0xa8,0xd8,0xae,0xac,0xc5,0xad,0xde,0xd9,0xe1,0xe4,0xa8,0xff,0xd9,0xab,0xa8,0xd8,0xae,0xac,0xd9,0xab,0xad,0xde,0xe4,0xa8,0xff,0xd9,0xab,0xd8,0xfe,0xb7,0xa9,0xb3,0xf8,0xac,0xa1]

private func rugbyBall(hypothesis num: UInt8) -> UInt8 {
    return num ^ 133
}

/*: "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$" :*/
fileprivate let notiStandardFormat:[UInt8] = [0xac,0xda,0xda,0xc3,0xa9,0xc1,0xc6,0xc7,0xc5,0xca,0xaf,0xa9,0xc2,0xdf,0xcb,0xaf,0x89,0xcb,0x8f,0xdb,0x8e,0xda,0xda,0xc2,0xae,0x96,0x89,0xc0,0x8f,0xdf,0xae,0x96,0x89,0xca,0x8f,0xdb,0x8e,0xda,0xc2,0xae,0x96,0x89,0xc1,0x8f,0xdf,0xae,0x96,0x89,0xc5,0xde,0xca,0x8f,0xdb,0x8e,0xda,0xc2,0xae,0x96,0x89,0xc3,0xc2,0xde,0xc3,0xc3,0x8f,0xdb,0xdb,0xd6]

/*: "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)" :*/
fileprivate let constAppropriateName:[UInt8] = [0x29,0x24,0x7d,0x30,0x31,0x2c,0x32,0x7b,0x5d,0x5f,0x39,0x2d,0x30,0x7a,0x2d,0x61,0x5a,0x2d,0x41,0x35,0x41,0x46,0x39,0x75,0x5c,0x2d,0x30,0x30,0x45,0x34,0x75,0x5c,0x5b,0x28,0x5e]

/*: "^[0-8]\\d{5}(?!\\d)$" :*/
fileprivate let noti_discourseContent:String = "^[0-"
fileprivate let main_normalMailServerMessage:String = "}(?!\\d)$"

/*: "^[a-zA-Z0-9]{6,20}+$" :*/
fileprivate let appThinMessage:String = "include carry ignore hockey^[a-zA"
fileprivate let dataNumbBounceBeautifulName:[Character] = ["-","Z","0","-","9","]","{"]
fileprivate let show_dialogWaitTitle:String = "6,20}+$"

/*: "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$" :*/
fileprivate let notiTapFormat:[UInt8] = [0x9e,0xe8,0xa8,0xb4,0xb4,0xb0,0xb3,0xff,0xfa,0x9c,0xef,0x9c,0xef,0xe9,0xff,0xe8,0x9b,0x9c,0xa4,0xa1,0xed,0xba,0x9c,0xee,0xed,0x9d,0xeb,0xe9,0x9c,0xee,0xe8,0x9b,0xa1,0xed,0xba,0x9c,0xee,0x9d,0xbb,0xf2,0xec,0xf6,0xbd,0xe9,0xe8,0x9b,0x9c,0xef,0x9c,0xb7,0xe0,0x9c,0xee,0xed,0x9d,0xea,0xe9,0xea,0x9c,0xef,0xff,0xe4]

private func bearChart(unit num: UInt8) -> UInt8 {
    return num ^ 192
}

/*: "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$" :*/
fileprivate let app_thumbName:[UInt8] = [0x9,0xd3,0xea,0xe5,0xd3,0xea,0xe5,0xdd,0xe0,0x6,0xdb,0xd8,0xe0,0x8,0x27,0xdd,0x6,0xdb,0xd8,0xdf,0x8,0x6,0xdb,0xd8,0xe4,0x8,0x27,0x6,0xdb,0xdc,0x8,0xea,0x6,0xdb,0xd8,0xe4,0x8,0x6,0xdb,0xd8,0xe4,0x8,0xea,0xd4,0x7,0xd9,0xd4,0xcb,0x26,0xde,0x28,0xd3,0xea,0xe5,0xdd,0xe0,0x6,0xdb,0xd8,0xe0,0x8,0x27,0xdd,0x6,0xdb,0xd8,0xdf,0x8,0x6,0xdb,0xd8,0xe4,0x8,0x27,0x6,0xdb,0xdc,0x8,0xea,0x6,0xdb,0xd8,0xe4,0x8,0x6,0xdb,0xd8,0xe4,0x8,0xea,0xd4,0xcf]

fileprivate func automaticRepresentation(raft num: UInt8) -> UInt8 {
    let value = Int(num) + 85
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)" :*/
fileprivate let data_rowStr:[UInt8] = [0x29,0x24,0x29,0x5d,0x58,0x78,0x5b,0x7c,0x5d,0x39,0x2d,0x30,0x5b,0x28,0x7d,0x37,0x31,0x7b,0x5d,0x39,0x2d,0x30,0x5b,0x28,0x7c,0x29,0x24,0x7d,0x35,0x31,0x7b,0x5d,0x39,0x2d,0x30,0x5b,0x5e,0x28]

/*: "SELF MATCHES %@" :*/
fileprivate let app_guidanceTitle:String = "SELF fair hang except cute"
fileprivate let const_arcWeMsg:String = "inmate listen birthday paperHES %@"

/*: "Feedback" :*/
fileprivate let show_albumData:[Character] = ["F","e","e","d","b","a","c","k"]

/*: "Enter your feedback…" :*/
fileprivate let noti_trainName:[Character] = ["E","n","t","e","r"," ","y","o","u"]
fileprivate let notiLaughNeedText:String = "r fenuclear wide equity comply"

/*: "#999999" :*/
fileprivate let user_attentionStr:String = "#damdam9"
fileprivate let appPictureStr:String = "9"

/*: "Your email (Required) " :*/
fileprivate let data_piId:String = "branch far bearYour "
fileprivate let data_tillImageUrl:String = "eslowil"
fileprivate let userFutureUrl:String = "red) city day shut"

/*: "icon_me_feelback_star" :*/
fileprivate let const_namePath:[Character] = ["i","c","o","n","_","m","e","_","f","e","e","l","b","a","c","k","_"]
fileprivate let noti_authorName:String = "sofficialar"

/*: "#CCCCCC" :*/
fileprivate let dataDetailedFormat:String = "#CCCCCCwhite closed"

/*: "Send" :*/
fileprivate let constCommercialFormat:String = "Sendhang lack workplace host"

/*: "#D0D0D0" :*/
fileprivate let appRepresentativeTitle:[Character] = ["#"]
fileprivate let constProposePath:[Character] = ["D","0","D","0","D","0"]

/*: "Please fill in the content" :*/
fileprivate let main_principalFormat:String = "precedent deliver bear headPleas"
fileprivate let appCleanUrl:String = "report exposure needl in t"
fileprivate let main_writeValue:String = "availablet"

/*: "Please enter the correct email address" :*/
fileprivate let constCookieLiveText:[UInt8] = [0x3e,0x2,0xb,0xf,0x1d,0xb,0x4e,0xb,0x0,0x1a,0xb,0x1c,0x4e,0x1a,0x6,0xb,0x4e,0xd,0x1,0x1c,0x1c,0xb,0xd,0x1a,0x4e,0xb,0x3,0xf,0x7,0x2,0x4e,0xf,0xa,0xa,0x1c,0xb,0x1d,0x1d]

private func compoundLens(sequence num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "content" :*/
fileprivate let constDisableKey:[Character] = ["c","o","n","t","e","n","t"]

/*: "contactWay" :*/
fileprivate let show_cognitiveStr:String = "secure"
fileprivate let mainHypothesisData:String = "sacrifice behaviorntactWay"

/*: "platform" :*/
fileprivate let show_mixValue:String = "pfactort"

/*: "iphone" :*/
fileprivate let k_roundUrl:String = "ipworldwidene"

/*: "version" :*/
fileprivate let showCustomerText:[Character] = ["v"]
fileprivate let mainErrorText:[Character] = ["e","r","s","i","o","n"]

/*: "type" :*/
fileprivate let show_exceptKey:String = "TYPE"

/*: "Operation succeeded" :*/
fileprivate let notiCompletePath:String = "save retOper"
fileprivate let user_recordData:String = "succworkplaceworkplaced"
fileprivate let show_mustMsg:String = "esmooth"

/*: "Problem statements" :*/
fileprivate let noti_equityPath:[Character] = ["P","r","o","b","l","e","m"," ","s","t","a","t","e","m","e","n","t","s"]

/*: "Feature advice" :*/
fileprivate let appConferencePath:String = "column processingFeatu"
fileprivate let dataSacrificeTitle:String = "vcomparece"

/*: "Operation questions" :*/
fileprivate let noti_followingCitySinkFormat:String = "Operakit customer my stage robot"
fileprivate let k_applicationContent:[Character] = ["e","s","t","i","o","n","s"]

/*: "Others" :*/
fileprivate let app_eliteData:[Character] = ["O","t","h","e","r","s"]

/*: "#EFEDFF" :*/
fileprivate let app_spendVidData:String = "purchase again crop memory#EFED"
fileprivate let constOfferPath:[Character] = ["F","F"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AgainViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/15.
//

//: import UIKit
import UIKit

//: enum Validate {
enum GreatValidate {
    //: case email(_: String)
    case email(_: String) //  邮箱
    //: case phoneNumber(_: String)
    case phoneNumber(_: String) //  手机号
    //: case userName(_: String)
    case userName(_: String) //  用户名
    //: case password(_: String)
    case password(_: String) //  密码
    //: case nickName(_: String)
    case nickName(_: String) //  昵称
    //: case postalCode(_: String)
    case postalCode(_: String) //  邮编
    //: case URL(_: String)
    case URL(_: String) //  URL
    //: case IP(_: String)
    case IP(_: String) //  IP
    //: case isChinese(_: String)
    case isChinese(_: String) //  是否全是中文字符
    //: case idNumber(_: String)
    case idNumber(_: String) //  身份证号

    //: var isRight: Bool {
    var isRight: Bool {
        //: var predicateStr: String!
        var predicateStr: String!
        //: var currentObject: String!
        var currentObject: String!
        //: switch self {
        switch self {
        //: case let .email(str):
        case let .email(str):
            //: predicateStr = "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$"
            predicateStr = String(bytes: user_donStr.map{rugbyBall(hypothesis: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .phoneNumber(str):
        case let .phoneNumber(str):
            //: predicateStr = "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$"
            predicateStr = String(bytes: notiStandardFormat.map{$0^242}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .userName(str):
        case let .userName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: constAppropriateName.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .postalCode(str):
        case let .postalCode(str):
            //: predicateStr = "^[0-8]\\d{5}(?!\\d)$"
            predicateStr = (noti_discourseContent.capitalized + "8]\\d{5" + main_normalMailServerMessage)
            //: currentObject = str
            currentObject = str
        //: case let .password(str):
        case let .password(str):
            //: predicateStr = "^[a-zA-Z0-9]{6,20}+$"
            predicateStr = (String(appThinMessage.suffix(6)) + String(dataNumbBounceBeautifulName) + show_dialogWaitTitle.capitalized)
            //: currentObject = str
            currentObject = str
        //: case let .nickName(str):
        case let .nickName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: constAppropriateName.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .URL(str):
        case let .URL(str):
            //: predicateStr = "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$"
            predicateStr = String(bytes: notiTapFormat.map{bearChart(unit: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .IP(str):
        case let .IP(str):
            //: predicateStr = "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
            predicateStr = String(bytes: app_thumbName.map{automaticRepresentation(raft: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let . isChinese(str):
        case let .isChinese(str):
            //: predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            //: currentObject = str
            currentObject = str
        //: case let . idNumber(str):
        case let .idNumber(str):
            //: predicateStr = "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)"
            predicateStr = String(bytes: data_rowStr.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        }
        //: let predicate =  NSPredicate(format: "SELF MATCHES %@", predicateStr)
        let predicate = NSPredicate(format: (String(app_guidanceTitle.prefix(5)) + "MATC" + String(const_arcWeMsg.suffix(6))), predicateStr)
        //: return predicate.evaluate(with: currentObject)
        return predicate.evaluate(with: currentObject)
    }
}

//: class TalkingFeedbackVC: TalkingBaseViewController {
class AgainViewController: MergeContactViewController {
    //: let limitCount = 300
    let limitCount = 300
    //: let EmialCount = 50
    let EmialCount = 50
    //: var seleteBtn: UIButton?
    var seleteBtn: UIButton?

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()

        //: self.title = "Feedback".localized
        self.title = (String(show_albumData)).localized
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: designView()
        rank()
        //: addTapGestureRecognizer()
        mixInThroughLimit()
        //: addKeyboardNotification()
        arouse()
    }

    // MARK: - Lazy load

    //: lazy var backView: UIScrollView = {
    lazy var backView: UIScrollView = {
        //: let view = UIScrollView.init()
        let view = UIScrollView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.showsHorizontalScrollIndicator = false
        view.showsHorizontalScrollIndicator = false
        //: view.showsVerticalScrollIndicator = false
        view.showsVerticalScrollIndicator = false
        //: return view
        return view
        //: }()
    }()

    //: lazy var contView: UIView = {
    lazy var contView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var inputTView: UIView = {
    lazy var inputTView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: view.backgroundColor = .appBgColor()
        view.backgroundColor = .wateringPot()
        //: return view
        return view
        //: }()
    }()

    //: lazy var textView: TalkingTextView = {
    lazy var textView: FormalHasDelegate = {
        //: let textView = TalkingTextView.init()
        let textView = FormalHasDelegate()
        //: textView.placeholder = "Enter your feedback…".localized
        textView.placeholder = (String(noti_trainName) + String(notiLaughNeedText.prefix(4)) + "edback…").localized
        //: textView.placeholderColor = UIColor.init(hex: "#999999")!
        textView.placeholderColor = UIColor(hex: (user_attentionStr.replacingOccurrences(of: "dam", with: "99") + appPictureStr.capitalized))!
        //: textView.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        textView.font = UIFont.processAcross(type: .Regular, fontSize: 15)
        //: textView.textColor = UIColor.appTitleColor()
        textView.textColor = UIColor.antiquityColor()
        //: textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)
        textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)

        //: textView.textContainer.maximumNumberOfLines = 0
        textView.textContainer.maximumNumberOfLines = 0
        //: textView.textContainer.lineBreakMode = .byTruncatingTail
        textView.textContainer.lineBreakMode = .byTruncatingTail
        //: textView.delegate = self
        textView.delegate = self
        //: textView.returnKeyType = UIReturnKeyType.done
        textView.returnKeyType = UIReturnKeyType.done
        //: textView.bounces = false
        textView.bounces = false
        //: textView.backgroundColor = .appBgColor()
        textView.backgroundColor = .wateringPot()
        //: numberLB.text =  "0/\(limitCount)"
        numberLB.text = "0/\(limitCount)"
        //: return textView
        return textView
        //: }()
    }()

    //: lazy var numberLB: UILabel = {
    lazy var numberLB: UILabel = {
        //: let LB = UILabel.init()
        let LB = UILabel()
        //: LB.textColor = UIColor.init(hex: "#999999")
        LB.textColor = UIColor(hex: (user_attentionStr.replacingOccurrences(of: "dam", with: "99") + appPictureStr.capitalized))
        //: LB.font = UIFont.pingfangFont(type: .Regular, fontSize: 14)
        LB.font = UIFont.processAcross(type: .Regular, fontSize: 14)
        //: LB.text = "0/\(limitCount)"
        LB.text = "0/\(limitCount)"
        //: LB.textAlignment = .right
        LB.textAlignment = .right
        //: return LB
        return LB
        //: }()
    }()

    //: lazy var emailTF: UITextField = {
    lazy var emailTF: UITextField = {
        //: let tf = UITextField.init()
        let tf = UITextField()
        //: tf.backgroundColor = .appBgColor()
        tf.backgroundColor = .wateringPot()
        //: tf.textColor = UIColor.appTitleColor()
        tf.textColor = UIColor.antiquityColor()
        //: tf.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
        tf.font = UIFont.processAcross(type: .Regular, fontSize: 16)
        //: tf.delegate = self
        tf.delegate = self
        //: tf.returnKeyType = UIReturnKeyType.done
        tf.returnKeyType = UIReturnKeyType.done
        //: tf.attributedPlaceholder = NSAttributedString.init(string: "Your email (Required) ".localized, attributes: [
        tf.attributedPlaceholder = NSAttributedString(string: (String(data_piId.suffix(5)) + data_tillImageUrl.replacingOccurrences(of: "slow", with: "ma") + " (Requi" + String(userFutureUrl.prefix(5))).localized, attributes: [
            //: NSAttributedString.Key.foregroundColor: UIColor.init(hex: "#999999")!])
            NSAttributedString.Key.foregroundColor: UIColor(hex: (user_attentionStr.replacingOccurrences(of: "dam", with: "99") + appPictureStr.capitalized))!])
        //: tf.clipsToBounds = true
        tf.clipsToBounds = true
        //: tf.layer.cornerRadius = 8
        tf.layer.cornerRadius = 8
        //: if LanguageManager.shared.direction == .rightToLeft {
        if ReadingLanguageManager.shared.direction == .rightToLeft {
            //: tf.textAlignment = .right
            tf.textAlignment = .right
        }
        //: let leftView = UIView()
        let leftView = UIView()
        //: leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))
        leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))

        //: let leftImgView = UIImageView.init()
        let leftImgView = UIImageView()
        //: leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        //: leftImgView.image = UIImage.BundleImageNamed(name: "icon_me_feelback_star")
        leftImgView.image = UIImage.parentSet(name: (String(const_namePath) + noti_authorName.replacingOccurrences(of: "official", with: "t")))
        //: leftView.addSubview(leftImgView)
        leftView.addSubview(leftImgView)
        //: tf.leftView = leftView
        tf.leftView = leftView
        //: tf.leftViewMode = .always
        tf.leftViewMode = .always
        //: return tf
        return tf
        //: }()
    }()

    //: lazy var doneBtn: UIButton = {
    lazy var doneBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#CCCCCC")!, forState: .disabled)
        btn.mottle(color: UIColor(hex: (String(dataDetailedFormat.prefix(7))))!, forState: .disabled)
        //: btn.setBackgroundColor(color: UIColor.appThemeColor(), forState: .normal)
        btn.mottle(color: UIColor.rawMonthCombine(), forState: .normal)
        //: btn.setTitle("Send".localized, for: .normal)
        btn.setTitle((String(constCommercialFormat.prefix(4))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.processAcross(type: .Medium, fontSize: 17)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fitFrontClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingFeedbackVC {
extension AgainViewController {
    //: @objc func TagBtnClick(sender: UIButton) {
    @objc func northExplain(sender: UIButton) {
        //: seleteBtn?.isSelected = false
        seleteBtn?.isSelected = false
        //: seleteBtn?.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
        seleteBtn?.layer.borderColor = UIColor(hex: (String(appRepresentativeTitle) + String(constProposePath)))?.cgColor
        //: sender.isSelected = !sender.isSelected
        sender.isSelected = !sender.isSelected
        //: seleteBtn = sender
        seleteBtn = sender
        //: seleteBtn!.layer.borderColor = UIColor.clear.cgColor
        seleteBtn!.layer.borderColor = UIColor.clear.cgColor
    }

    //: @objc func doneBtnClick() {
    @objc func fitFrontClick() {
        //: if seleteBtn == nil || textView.text.count<=0 || emailTF.text!.count<=0 {
        if seleteBtn == nil || textView.text.count <= 0 || emailTF.text!.count <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please fill in the content".localized)
            self.foolishness(showMsg: (String(main_principalFormat.suffix(5)) + "e fil" + String(appCleanUrl.suffix(6)) + "he cont" + main_writeValue.replacingOccurrences(of: "available", with: "en")).localized)
            //: return
            return
        }

        //: if LanguageManager.shared.direction == .leftToRight {
        if ReadingLanguageManager.shared.direction == .leftToRight {
            //: if !Validate.email(emailTF.text!).isRight {
            if !GreatValidate.email(emailTF.text!).isRight {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please enter the correct email address".localized)
                self.foolishness(showMsg: String(bytes: constCookieLiveText.map{compoundLens(sequence: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["content"] = textView.text
        dict[(String(constDisableKey))] = textView.text
        //: dict["contactWay"] = emailTF.text
        dict[(show_cognitiveStr.replacingOccurrences(of: "secure", with: "co") + String(mainHypothesisData.suffix(8)))] = emailTF.text
        //: dict["platform"] = "iphone"
        dict[(show_mixValue.replacingOccurrences(of: "factor", with: "la") + "form")] = (k_roundUrl.replacingOccurrences(of: "worldwide", with: "ho"))
        //: dict["version"] = AppVersion
        dict[(String(showCustomerText) + String(mainErrorText))] = show_jumpKey
        //: dict["type"] = String((seleteBtn?.tag ?? 100)-100)
        dict[(show_exceptKey.lowercased())] = String((seleteBtn?.tag ?? 100) - 100)

        //: ProgressHUD.show()
        PointerReactiveCompatible.soundAssetThrow()

        //: TalkingMeRequestTool.req_Feedback(params: dict) { succeed, result, errorModel in
        GreatThen.application(params: dict) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            PointerReactiveCompatible.motivationCharmDismiss()
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Operation succeeded".localized)
                self.through(showMsg: (String(notiCompletePath.suffix(4)) + "ation " + user_recordData.replacingOccurrences(of: "workplace", with: "e") + show_mustMsg.replacingOccurrences(of: "smooth", with: "d")).localized)
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)
            }
        }
    }

    //: func addKeyboardNotification() {
    func arouse() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillShow(notification:)),
                                               selector: #selector(male(notification:)),
                                               //: name: UIResponder.keyboardWillShowNotification,
                                               name: UIResponder.keyboardWillShowNotification,
                                               //: object: nil)
                                               object: nil)
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillHide(notification:)),
                                               selector: #selector(insert(notification:)),
                                               //: name: UIResponder.keyboardWillHideNotification,
                                               name: UIResponder.keyboardWillHideNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func keyboardWillShow(notification: Notification) {
    @objc func male(notification: Notification) {
        //: guard emailTF.isFirstResponder else { return }
        guard emailTF.isFirstResponder else { return }
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        //: let frame = value?.cgRectValue ?? .zero
        let frame = value?.cgRectValue ?? .zero
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        //: guard (emailTF.bottom + StatusBarNavigationBarHeight) > frame.minY else { return }
        guard (emailTF.bottom + userResolutionUrl) > frame.minY else { return }

        //: let y = (emailTF.bottom + StatusBarNavigationBarHeight) - frame.minY
        let y = (emailTF.bottom + userResolutionUrl) - frame.minY
        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
            self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
        }
    }

    //: @objc func keyboardWillHide(notification: Notification) {
    @objc func insert(notification: Notification) {
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25

        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = .identity
            self.backView.transform = .identity
        }
    }
}

// MARK: - UITextViewDelegate, UITextFieldDelegate

//: extension TalkingFeedbackVC: UITextViewDelegate, UITextFieldDelegate {
extension AgainViewController: UITextViewDelegate, UITextFieldDelegate {
    //: public func textViewDidChange(_ textView: UITextView) {
    public func textViewDidChange(_ textView: UITextView) {
        //: if textView.text.count > limitCount {
        if textView.text.count > limitCount {
            // 获得已输出字数与正输入字母数
            //: let selectRange = textView.markedTextRange
            let selectRange = textView.markedTextRange

            // 获取高亮部分 － 如果有联想词则解包成功
            //: if let selectRange = selectRange {
            if let selectRange = selectRange {
                //: let position =  textView.position(from: (selectRange.start), offset: 0)
                let position = textView.position(from: selectRange.start, offset: 0)
                //: if (position != nil) {
                if position != nil {
                    //: return
                    return
                }
            }

            //: let textContent = textView.text ?? ""
            let textContent = textView.text ?? ""
            //: let textNum = textContent.count
            let textNum = textContent.count

            // 截取
            //: if textNum > limitCount && limitCount > 0 {
            if textNum > limitCount && limitCount > 0 {
                //: textView.text = string_prefix(index: limitCount, text: textContent)
                textView.text = satisfy(index: limitCount, text: textContent)
            }
        }
        //: numberLB.text =  "\(textView.text.count)/\(limitCount)"
        numberLB.text = "\(textView.text.count)/\(limitCount)"
    }

    //: public func textViewShouldBeginEditing(_ textView: UITextView) -> Bool {
    public func textViewShouldBeginEditing(_: UITextView) -> Bool {
        //: numberLB.isHidden = false
        numberLB.isHidden = false
        //: return true
        return true
    }

    //: public func textViewDidEndEditing(_ textView: UITextView) {
    public func textViewDidEndEditing(_ textView: UITextView) {
        //: if textView.text.isEmpty {
        if textView.text.isEmpty {
            //: numberLB.isHidden = false
            numberLB.isHidden = false
            //: } else {
        } else {
            //: numberLB.isHidden = true
            numberLB.isHidden = true
        }
    }

    //: public func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
    public func textView(_ textView: UITextView, shouldChangeTextIn _: NSRange, replacementText text: String) -> Bool {
        //: if textView.returnKeyType == .done {
        if textView.returnKeyType == .done {
            //: if text == "\n" {
            if text == "\n" {
                //: textView.resignFirstResponder()
                textView.resignFirstResponder()
            }
            //: return true
            return true
        }
        //: return true
        return true
    }

    // 字符串的截取 从头截取到指定index
    //: private func string_prefix(index: Int,text:String) -> String {
    private func satisfy(index: Int, text: String) -> String {
        //: if text.count <= index {
        if text.count <= index {
            //: return text
            return text
            //: } else {
        } else {
            //: let index = text.index(text.startIndex, offsetBy: index)
            let index = text.index(text.startIndex, offsetBy: index)
            //: let str = text.prefix(upTo: index)
            let str = text.prefix(upTo: index)
            //: return String(str)
            return String(str)
        }
    }

    //: func textFieldShouldReturn(_ textField: UITextField) -> Bool {
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
        //: textField.resignFirstResponder()
        textField.resignFirstResponder()

        //: return true
        return true
    }

    //: func textField(_ textField: UITextField, shouldChangeCharactersIn range: NSRange, replacementString string: String) -> Bool {
    func textField(_ textField: UITextField, shouldChangeCharactersIn _: NSRange, replacementString string: String) -> Bool {
        //: if textField.text!.count+string.count>EmialCount || textField.text!.count+string.count <= 0 {
        if textField.text!.count + string.count > EmialCount || textField.text!.count + string.count <= 0 {
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - UI

//: extension TalkingFeedbackVC {
extension AgainViewController {
    //: func designView() {
    func rank() {
        //: self.view.addSubview(backView)
        self.view.addSubview(backView)
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: backView.addSubview(contView)
        backView.addSubview(contView)
        //: contView.snp.makeConstraints { make in
        contView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(backView)
            make.leading.trailing.top.bottom.equalTo(backView)
        }

        //: contView.addSubview(inputTView)
        contView.addSubview(inputTView)
        //: inputTView.snp.makeConstraints { make in
        inputTView.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(contView).offset(128)
            make.top.equalTo(contView).offset(128)
            //: make.height.equalTo(210)
            make.height.equalTo(210)
        }

        //: inputTView.addSubview(textView)
        inputTView.addSubview(textView)
        //: inputTView.addSubview(numberLB)
        inputTView.addSubview(numberLB)
        //: textView.snp.makeConstraints { make in
        textView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(inputTView)
            make.leading.trailing.top.equalTo(inputTView)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
        }
        //: numberLB.snp.makeConstraints { make in
        numberLB.snp.makeConstraints { make in
            //: make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: contView.addSubview(emailTF)
        contView.addSubview(emailTF)
        //: emailTF.snp.makeConstraints { make in
        emailTF.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(inputTView.snp.bottom).offset(16)
            make.top.equalTo(inputTView.snp.bottom).offset(16)
            //: make.height.equalTo(49)
            make.height.equalTo(49)
        }

        //: contView.addSubview(doneBtn)
        contView.addSubview(doneBtn)
        //: doneBtn.snp.makeConstraints { make in
        doneBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(54)
            make.leading.equalTo(contView).offset(54)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-54)
            make.trailing.equalTo(contView.snp.trailing).offset(-54)
            //: make.top.equalTo(emailTF.snp.bottom).offset(111)
            make.top.equalTo(emailTF.snp.bottom).offset(111)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }

        //: let arr = ["Problem statements".localized,
        let arr = [(String(noti_equityPath)).localized,
                   //: "Feature advice".localized,
                   (String(appConferencePath.suffix(5)) + "re ad" + dataSacrificeTitle.replacingOccurrences(of: "compare", with: "i")).localized,
                   //: "Operation questions".localized,
                   (String(noti_followingCitySinkFormat.prefix(5)) + "tion qu" + String(k_applicationContent)).localized,
                   //: "Others".localized]
                   (String(app_eliteData)).localized]

        //: for i in 0..<arr.count {
        for i in 0 ..< arr.count {
            //: let xz = 15
            let xz = 15
            //: let yz = 16
            let yz = 16
            //: let Width = (Int(ScreenWidth)-xz*3)/2
            let Width = (Int(user_needValue) - xz * 3) / 2
            //: let height = 38
            let height = 38
            //: let X = xz+(xz + Width)*(i%2)
            let X = xz + (xz + Width) * (i % 2)
            //: let Y = yz+(yz+height)*(i/2)
            let Y = yz + (yz + height) * (i / 2)

            //: let btn = UIButton.init()
            let btn = UIButton()
            //: btn.frame =  CGRect.init(x: X, y: Y , width: Width, height: height)
            btn.frame = CGRect(x: X, y: Y, width: Width, height: height)
            //: btn.setTitle(arr[i], for: .normal)
            btn.setTitle(arr[i], for: .normal)
            //: btn.setTitleColor(.black, for: .normal)
            btn.setTitleColor(.black, for: .normal)
            //: btn.setTitleColor(.appThemeColor(), for: .selected)
            btn.setTitleColor(.rawMonthCombine(), for: .selected)

            //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
            btn.titleLabel?.font = UIFont.processAcross(type: .Regular, fontSize: 15)
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            if CosSweetReactiveCompatible.share.interfaceLang == ShadesOfTransformable.es.rawValue || CosSweetReactiveCompatible.share.interfaceLang == ShadesOfTransformable.pt.rawValue {
                //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 13)
                btn.titleLabel?.font = UIFont.processAcross(type: .Regular, fontSize: 13)
            }
            //: btn.layer.cornerRadius = 19
            btn.layer.cornerRadius = 19
            //: btn.layer.borderWidth = 1
            btn.layer.borderWidth = 1
            //: btn.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
            btn.layer.borderColor = UIColor(hex: (String(appRepresentativeTitle) + String(constProposePath)))?.cgColor
            //: btn.setBackgroundColor(color: UIColor.init(hex: "#EFEDFF")!, forState: .selected)
            btn.mottle(color: UIColor(hex: (String(app_spendVidData.suffix(5)) + String(constOfferPath)))!, forState: .selected)
            //: btn.setBackgroundColor(color: .white, forState: .normal)
            btn.mottle(color: .white, forState: .normal)
            //: btn.tag = 100+i
            btn.tag = 100 + i
            //: btn.addTarget( self, action: #selector(TagBtnClick(sender:)), for: .touchUpInside)
            btn.addTarget(self, action: #selector(northExplain(sender:)), for: .touchUpInside)
            //: contView.addSubview(btn)
            contView.addSubview(btn)
        }

        //: contView.snp.remakeConstraints { make in
        contView.snp.remakeConstraints { make in
            //: make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            //: make.width.equalTo(backView)
            make.width.equalTo(backView)
        }
    }
}
