
//: Declare String Begin

/*: "user/updateInfo" :*/
fileprivate let userMixInmateMessage:[Character] = ["u","s","e","r","/","u","p"]
fileprivate let noti_takeData:[Character] = ["d","a","t","e","I","n","f","o"]

/*: "gallery/getGalleryByUid" :*/
fileprivate let user_pureFormat:[Character] = ["g","a","l"]
fileprivate let kAskPadId:[Character] = ["l","e","r","y","/","g","e","t","G","a","l","l","e","r","y","B","y","U","i","d"]

/*: "gallery/upload" :*/
fileprivate let const_withinStr:String = "ouralle"
fileprivate let noti_cramPath:String = "loaassist"

/*: "gallery/deletePic" :*/
fileprivate let dataPriorityId:[Character] = ["g","a","l","l","e","r","y","/","d","e","l","e","t","e"]
fileprivate let kAnimaSideMsg:String = "author table contain requirePic"

/*: "user/tagDelete" :*/
fileprivate let data_faultSnapColorPath:String = "user/curve across birth stream discover"
fileprivate let mainMyId:String = "eleranke"

/*: "mf/user/tagEdit" :*/
fileprivate let mainCompeteText:[Character] = ["m","f","/","u","s","e","r","/","t","a","g","E","d"]
fileprivate let dataFromMsg:String = "iitem"

/*: "app/feedback" :*/
fileprivate let kEstablishMsg:String = "app/fdirection promotion"
fileprivate let noti_administrativeData:String = "ACK"

/*: "IM/getMyBlackList" :*/
fileprivate let dataMouthMessage:[Character] = ["I","M","/","g","e","t","M","y","B","l","a","c","k","L"]
fileprivate let constIdentificationTitle:String = "IST"

/*: "mf/user/getSetting" :*/
fileprivate let user_viaFormat:String = "mf/uscommon anima carrier close player"
fileprivate let user_situationKey:String = "Settinaccess be round king act"
fileprivate let mainCookieFormat:String = "huge"

/*: "mf/user/setConfig" :*/
fileprivate let userListenerData:String = "mf/user/balance random numb circle product"
fileprivate let kExplainLookPath:[Character] = ["s","e","t","C","o","n","f","i","g"]

/*: "mf/user/settingStatus" :*/
fileprivate let k_edgeEmptyMsg:[Character] = ["m","f","/","u","s","e","r","/","s","e","t","t","i","n","g","S"]
fileprivate let mainProperlyValue:String = "TATUS"

/*: "type" :*/
fileprivate let mainProductionName:[UInt8] = [0xe6,0xeb,0xe2,0xf7]

/*: "user/lockAccount" :*/
fileprivate let app_hisId:String = "unable totaluser/"
fileprivate let main_unitValue:String = "COUNT"

/*: "mf/user/editInfo" :*/
fileprivate let k_howPath:[Character] = ["m","f","/","u","s","e","r","/","e"]
fileprivate let dataLaunchTitle:String = "nowi"
fileprivate let k_audienceTitle:[Character] = ["t","I","n","f","o"]

/*: "mf/userSign/list" :*/
fileprivate let appRootUrl:String = "gesture see accounting inner stagemf/u"
fileprivate let mainSaveStr:String = "maybe protection tool gloryign/"
fileprivate let user_commercialPath:String = "lguidancest"

/*: "mf/userSign/signIn" :*/
fileprivate let const_specialResolutionMessage:[Character] = ["m","f","/","u","s","e","r","S","i","g","n","/","s","i"]
fileprivate let notiPictureTowardName:String = "gnInapproval edge he"

/*: "mf/dating/saveVideo" :*/
fileprivate let appHelloIndexBurnData:String = "extension do catch identifymf/da"
fileprivate let notiSameText:String = "SAVE"

/*: "mf/dating/getConfig" :*/
fileprivate let show_freshData:[Character] = ["m","f"]
fileprivate let user_liftData:String = "merge hand feather before/dati"
fileprivate let main_stillMessage:String = "Configtap head secret third"

/*: "mf/dating/deleteVideo" :*/
fileprivate let k_operationMsg:String = "mf/dwide plant bag leading"
fileprivate let user_installationData:String = "stop shade countg/de"
fileprivate let kEnterpriseTitle:[Character] = ["o"]

/*: "mf/greet/updateAudioRemark" :*/
fileprivate let constMedalId:String = "mf/guser reply"
fileprivate let notiConvertBuildMessage:String = "/updateanimal solution go"
fileprivate let user_neighborArmWearMessage:String = "lip tag mentallyAudi"

/*: "mf/greet/list" :*/
fileprivate let k_ablePath:String = "mf/greetshared format lay signature"
fileprivate let dataListenRecognizeFormat:[Character] = ["/","l","i","s","t"]

/*: "mf/greet/add" :*/
fileprivate let k_fromStr:[Character] = ["m","f","/","g","r"]
fileprivate let appSitData:String = "tail upon accounting program serviceeet/add"

/*: "mf/greet/del" :*/
fileprivate let k_issueData:String = "mf/grtip loop auto administrative sink"
fileprivate let mainNativeData:[Character] = ["l"]

/*: "mf/greet/getSettings" :*/
fileprivate let appConversationFormat:[Character] = ["m"]
fileprivate let user_promptName:[Character] = ["f","/","g","r","e","e","t","/","g","e","t","S","e","t","t","i","n","g","s"]

/*: "mf/greet/extraSetting" :*/
fileprivate let appBearKey:String = "mf/gcircle dialog allow"
fileprivate let show_superiorPath:String = "extraSdimension uniform true commit"
fileprivate let notiMedicationValue:String = "recognize"

/*: "game/list" :*/
fileprivate let show_blankUrl:[Character] = ["g","a","m","e","/","l","i","s","t"]

/*: "category" :*/
fileprivate let appMemberBodyTitle:[UInt8] = [0xb2,0xb0,0xa5,0xb4,0xb6,0xbe,0xa3,0xa8]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GreatThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/9.
//

//: import UIKit
import UIKit

//: class TalkingMeRequestTool: NSObject {
class GreatThen: NSObject {
    /// 资料完善
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateUserInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func service(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "user/updateInfo"
        reqModel.requestPath = (String(userMixInmateMessage) + String(noti_takeData))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户相册
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GalleryByUid(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func toolPer(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "gallery/getGalleryByUid"
        reqModel.requestPath = (String(user_pureFormat) + String(kAskPadId))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UploadPic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func face(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "gallery/upload"
        reqModel.requestPath = (const_withinStr.replacingOccurrences(of: "our", with: "g") + "ry/up" + noti_cramPath.replacingOccurrences(of: "assist", with: "d"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeletePic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sceneApp(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "gallery/deletePic"
        reqModel.requestPath = (String(dataPriorityId) + String(kAnimaSideMsg.suffix(3)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除单个用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func show(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "user/tagDelete"
        reqModel.requestPath = (String(data_faultSnapColorPath.prefix(5)) + "tagD" + mainMyId.replacingOccurrences(of: "rank", with: "t"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 编辑用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_EditTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func completion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/tagEdit"
        reqModel.requestPath = (String(mainCompeteText) + dataFromMsg.replacingOccurrences(of: "item", with: "t"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 意见反馈
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_Feedback(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func application(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "app/feedback"
        reqModel.requestPath = (String(kEstablishMsg.prefix(5)) + "eedb" + noti_administrativeData.lowercased())
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取黑名单列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_BlackList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func gestureClick(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "IM/getMyBlackList"
        reqModel.requestPath = (String(dataMouthMessage) + constIdentificationTitle.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取设置信息接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func exceptHalf(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getSetting"
        reqModel.requestPath = (String(user_viaFormat.prefix(5)) + "er/get" + String(user_situationKey.prefix(6)) + mainCookieFormat.replacingOccurrences(of: "huge", with: "g"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SettingChange(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func toALesserExtent(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/setConfig"
        reqModel.requestPath = (String(userListenerData.prefix(8)) + String(kExplainLookPath))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 女性busy状态上报接口
    /// - Parameters:
    ///   - type: 0 :不活跃， 1:活跃
    ///   - completion: 回调
    //: class func req_settingStatus(type: Int, completion: @escaping FinishBlock) {
    class func clearMale(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/settingStatus"
        reqModel.requestPath = (String(k_edgeEmptyMsg) + mainProperlyValue.lowercased())
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: mainProductionName.map{$0^146}, encoding: .utf8)!: type]
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 注销账号
    /// - Parameters:
    /// - completion: 回调
    //: class func req_LockAccount(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func someOperation(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "user/lockAccount"
        reqModel.requestPath = (String(app_hisId.suffix(5)) + "lockAc" + main_unitValue.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心修改私信价格、开启语音授权
    /// - Parameters:
    /// - completion: 回调
    //: class func req_EditInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func resolutionSpringSort(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/user/editInfo"
        reqModel.requestPath = (String(k_howPath) + dataLaunchTitle.replacingOccurrences(of: "now", with: "d") + String(k_audienceTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取签到日历列表
    /// - Parameter completion: 回调
    //: class func req_getUserSignList(completion: @escaping FinishBlock) {
    class func present(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/userSign/list"
        reqModel.requestPath = (String(appRootUrl.suffix(4)) + "serS" + String(mainSaveStr.suffix(4)) + user_commercialPath.replacingOccurrences(of: "guidance", with: "i"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户签到
    /// - Parameter completion: 回调
    //: class func req_userSignIn(completion: @escaping FinishBlock) {
    class func careerDit(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/userSign/signIn"
        reqModel.requestPath = (String(const_specialResolutionMessage) + String(notiPictureTowardName.prefix(4)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户资料视频地址上传
    /// - Parameters:
    ///   - completion: 回调
    //: class func uploadVideoUserEdit(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func contactProduct(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/dating/saveVideo"
        reqModel.requestPath = (String(appHelloIndexBurnData.suffix(5)) + "ting/" + notiSameText.lowercased() + "Video")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func getUploadVideoInfo(completion: @escaping FinishBlock) {
    class func schedule(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/dating/getConfig"
        reqModel.requestPath = (String(show_freshData) + String(user_liftData.suffix(5)) + "ng/get" + String(main_stillMessage.prefix(6)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func deleteUserVideo(completion: @escaping FinishBlock) {
    class func comeIn(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/dating/deleteVideo"
        reqModel.requestPath = (String(k_operationMsg.prefix(4)) + "atin" + String(user_installationData.suffix(4)) + "leteVide" + String(kEnterpriseTitle))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新语音备注
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(completion: @escaping FinishBlock) {
    class func doList(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(constMedalId.prefix(4)) + "reet" + String(notiConvertBuildMessage.prefix(7)) + String(user_neighborArmWearMessage.suffix(4)) + "oRemark")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-查看设置列表
    /// - Parameters: type: 1=语音， 2=文本
    ///   - completion: 回调
    //: class func req_GetGreetList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func applicationChild(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/list"
        reqModel.requestPath = (String(k_ablePath.prefix(8)) + String(dataListenRecognizeFormat))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-添加自定义文案/语音/图片
    /// - Parameters: type: 1：语音；2：文案；3：图片; 4:私密照片
    ///   - completion: 回调
    //: class func req_UploadGreetAdd(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func menstrualPhase(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/add"
        reqModel.requestPath = (String(k_fromStr) + String(appSitData.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-设置音频备注
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func spark(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(constMedalId.prefix(4)) + "reet" + String(notiConvertBuildMessage.prefix(7)) + String(user_neighborArmWearMessage.suffix(4)) + "oRemark")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-删除文案/音频
    ///   - completion: 回调
    //: class func req_DeleteAudio(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func nearbyParams(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/del"
        reqModel.requestPath = (String(k_issueData.prefix(5)) + "eet/de" + String(mainNativeData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 招呼设置-查看设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetGreetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func putCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getSettings"
        reqModel.requestPath = (String(appConversationFormat) + String(user_promptName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 图片打招呼-额外配置礼物信息
    /// - Parameters: unlockGiftId:解锁礼物id, isBurn:设置的阅后即焚状态
    ///   - completion: 回调
    //: class func req_UploadGreetExtraSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func screenCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/extraSetting"
        reqModel.requestPath = (String(appBearKey.prefix(4)) + "reet/" + String(show_superiorPath.prefix(6)) + "ettin" + notiMedicationValue.replacingOccurrences(of: "recognize", with: "g"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取游戏列表
    /// - Parameters:
    ///   - category: 1=我的页面，2=私聊页面
    ///   - completion: 回调
    //: class func req_getGameList(category: Int, completion: @escaping FinishBlock) {
    class func method(category: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "game/list"
        reqModel.requestPath = (String(show_blankUrl))
        //: reqModel.params = ["category": category]
        reqModel.params = [String(bytes: appMemberBodyTitle.map{$0^209}, encoding: .utf8)!: category]
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
