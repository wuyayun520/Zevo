
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let noti_streetFaceMsg:[UInt8] = [0xba,0xbd,0xba,0xa7,0xfb,0xb0,0xbc,0xb7,0xb6,0xa1,0xe9,0xfa,0xf3,0xbb,0xb2,0xa0,0xf3,0xbd,0xbc,0xa7,0xf3,0xb1,0xb6,0xb6,0xbd,0xf3,0xba,0xbe,0xa3,0xbf,0xb6,0xbe,0xb6,0xbd,0xa7,0xb6,0xb7]

private func miniHang(provide num: UInt8) -> UInt8 {
    return num ^ 211
}

/*: "Text greeting" :*/
fileprivate let show_marriageText:String = "Text gfill dialog element aspect"
fileprivate let show_challengePackPileMsg:[Character] = ["n","g"]

/*: "Add" :*/
fileprivate let k_needControlUrl:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let noti_onceContent:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let show_haulValue:[Character] = ["a","d"]
fileprivate let const_itData:[Character] = ["d","S","t","a","t","u","s"]

/*: "list" :*/
fileprivate let k_universalMessage:String = "lisdetailed"

/*: "Are you sure to delete this message？" :*/
fileprivate let show_bounceMsg:[UInt8] = [0xf9,0xca,0xdd,0x98,0xc1,0xd7,0xcd,0x98,0xcb,0xcd,0xca,0xdd,0x98,0xcc,0xd7,0x98,0xdc,0xdd,0xd4,0xdd,0xcc,0xdd,0x98,0xcc,0xd0,0xd1,0xcb,0x98,0xd5,0xdd,0xcb,0xcb,0xd9,0xdf,0xdd,0x57,0x4,0x27]

private func positionForce(everyone num: UInt8) -> UInt8 {
    return num ^ 184
}

/*: "Cancel" :*/
fileprivate let main_personalityPath:String = "acquisition extend strategy broadcast giveCancel"

/*: "Delete" :*/
fileprivate let mainStatusAirName:[Character] = ["D","e","l","e","t","e"]

/*: "The upper limit has been reached" :*/
fileprivate let showHeartMessage:[UInt8] = [0xdc,0xe0,0xed,0xa8,0xfd,0xf8,0xf8,0xed,0xfa,0xa8,0xe4,0xe1,0xe5,0xe1,0xfc,0xa8,0xe0,0xe9,0xfb,0xa8,0xea,0xed,0xed,0xe6,0xa8,0xfa,0xed,0xe9,0xeb,0xe0,0xed,0xec]

private func makeSeparate(run num: UInt8) -> UInt8 {
    return num ^ 136
}

/*: "text" :*/
fileprivate let mainRenderPureUrl:String = "relationexrelation"

/*: "Successfully added, please wait for approval" :*/
fileprivate let data_ballName:[UInt8] = [0x46,0x68,0x56,0x56,0x58,0x66,0x66,0x59,0x68,0x5f,0x5f,0x6c,0x13,0x54,0x57,0x57,0x58,0x57,0x1f,0x13,0x63,0x5f,0x58,0x54,0x66,0x58,0x13,0x6a,0x54,0x5c,0x67,0x13,0x59,0x62,0x65,0x13,0x54,0x63,0x63,0x65,0x62,0x69,0x54,0x5f]

fileprivate func businessConcern(rating num: UInt8) -> UInt8 {
    let value = Int(num) - 243
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Any-Hex/Java" :*/
fileprivate let k_recoverTitle:String = "margin hodAny-Hex/"
fileprivate let constShirtUrl:[Character] = ["J","a","v","a"]

/*: "#FF506D" :*/
fileprivate let k_subName:String = "asset region with ta#FF506D"

/*: "btn_message_delete_nor" :*/
fileprivate let main_acquireRangePath:String = "btn_metopic now news range"
fileprivate let appAccelerateRecordMessage:String = "lackelet"

/*: "Click the button below to add\ncontent" :*/
fileprivate let data_permissionUrl:[UInt8] = [0xc8,0xe7,0xe2,0xe8,0xe0,0xab,0xff,0xe3,0xee,0xab,0xe9,0xfe,0xff,0xff,0xe4,0xe5,0xab,0xe9,0xee,0xe7,0xe4,0xfc,0xab,0xff,0xe4,0xab,0xea,0xef,0xef,0x81,0xe8,0xe4,0xe5,0xff,0xee,0xe5,0xff]

private func beanPlant(canvas num: UInt8) -> UInt8 {
    return num ^ 139
}

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let dataAccessSiblingStr:[UInt8] = [0x37,0x8,0x5,0xe,0x40,0x1,0x4,0x4,0x9,0xe,0x7,0x40,0xd,0x15,0xc,0x14,0x9,0x10,0xc,0x5,0x40,0x14,0x5,0x18,0x14,0x40,0xd,0x5,0x13,0x13,0x1,0x7,0x5,0x4c,0x40,0x14,0x8,0x5,0x40,0x13,0x19,0x13,0x14,0x40,0x5,0xd,0x40,0x17,0x9,0xc,0xc,0x40,0x13,0x5,0xe,0x4,0x40,0x14,0x8,0x5,0xd,0x40,0x2,0x19,0x40,0x14,0x15,0x12,0xe,0x13,0x5b,0x13,0xc,0x9,0x4,0x5,0x40,0x14,0xf,0x40,0x14,0x8,0x5,0x40,0xd,0x5,0x13,0x13,0x1,0x7,0x5,0x40,0x14,0xf,0x40,0x4,0x5,0xc,0x5,0x14,0x5]

private func individualEssay(standard num: UInt8) -> UInt8 {
    return num ^ 96
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AppearanceReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class AppearanceReactiveCompatible: MergeContactViewController {
    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: MilitaryTextView?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: noti_streetFaceMsg.map{miniHang(provide: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(show_marriageText.prefix(6)) + "reeti" + String(show_challengePackPileMsg)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.wateringPot()
        //: designView()
        edit()
        //: getAudioListData()
        menuData()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [OuterTransformable] = {
        //: var array = [TalkingGreetModel]()
        var array = [OuterTransformable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: user_needValue, height: kStreamTitle - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(k_needControlUrl)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.processAcross(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.keyLocal(colors: UIColor.sumerpretation(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(skip), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension AppearanceReactiveCompatible {
    //: func getAudioListData() {
    func menuData() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(String(noti_onceContent))] = 2
        //: ProgressHUD.show()
        PointerReactiveCompatible.soundAssetThrow()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        GreatThen.applicationChild(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            PointerReactiveCompatible.motivationCharmDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.can()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(show_haulValue) + String(const_itData))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(k_universalMessage.replacingOccurrences(of: "detailed", with: "t"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [OuterTransformable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<OuterTransformable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func button(index: Int) {
        //: let config = ShowAlertConfig()
        let config = ReminderAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.processAcross(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        YetAlertShow.humanRightModel(message: String(bytes: show_bounceMsg.map{positionForce(everyone: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(main_personalityPath.suffix(6))).localized, rightBtnTitle: (String(mainStatusAirName)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            YetAlertShow.usance()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            YetAlertShow.usance()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: OuterTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            PointerReactiveCompatible.soundAssetThrow()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            GreatThen.nearbyParams(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                PointerReactiveCompatible.motivationCharmDismiss()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.menuData()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension AppearanceReactiveCompatible {
    //: @objc func addButtonClick() {
    @objc func skip() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.foolishness(showMsg: String(bytes: showHeartMessage.map{makeSeparate(run: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = MilitaryTextView(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.run()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.adminKeep(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(String(noti_onceContent))] = 2
            //: dict["text"] = tempStr
            dict[(mainRenderPureUrl.replacingOccurrences(of: "relation", with: "t"))] = tempStr
            //: ProgressHUD.show()
            PointerReactiveCompatible.soundAssetThrow()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            GreatThen.menstrualPhase(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                PointerReactiveCompatible.motivationCharmDismiss()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.through(showMsg: String(bytes: data_ballName.map{businessConcern(rating: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.menuData()
                //: self.recordView?.dismiss()
                self.recordView?.rating()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func adminKeep(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(k_recoverTitle.suffix(8)) + String(constShirtUrl)) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension AppearanceReactiveCompatible: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: user_needValue - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.processAcross(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetTextCell.className()
        let identifier = MeetNameCell.className()
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: MeetNameCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! MeetNameCell

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.textualMatter(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(mainStatusAirName)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.button(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(k_subName.suffix(7))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.button(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.parentSet(name: (String(main_acquireRangePath.prefix(6)) + "ssage_" + appAccelerateRecordMessage.replacingOccurrences(of: "lack", with: "d") + "e_nor")).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = AllOverWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension AppearanceReactiveCompatible {
    //: private func setupEmptyDataView() {
    private func can() {
        //: var style = EmptyStyle()
        var style = SceneEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: data_permissionUrl.map{beanPlant(canvas: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .wateringPot()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func edit() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: TalkingGreetTextCell.className())
        MainTable.register(MeetNameCell.self, forCellReuseIdentifier: MeetNameCell.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: user_needValue, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.pointOfTotalersection()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.joinSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: dataAccessSiblingStr.map{individualEssay(standard: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
