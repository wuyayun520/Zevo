
//: Declare String Begin

/*: "cmd" :*/
fileprivate let notiListStr:[UInt8] = [0x5,0xf,0x6]

fileprivate func averageOutside(currency num: UInt8) -> UInt8 {
    let value = Int(num) + 94
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let mainSoonValue:String = "willncel"
fileprivate let const_runPath:String = "s galleryCall"

/*: "data" :*/
fileprivate let userErrorStr:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "logId" :*/
fileprivate let kTransportationData:[Character] = ["l","o","g","I","d"]

/*: "#322870" :*/
fileprivate let user_shallKey:[Character] = ["#","3","2","2","8"]
fileprivate let data_underId:[Character] = ["7","0"]

/*: "Tap to start video chatting" :*/
fileprivate let const_immediateMsg:[Character] = ["T","a","p"," ","t","o"," ","s","t","a","r","t"," ","v","i","d","e","o"," ","c","h"]
fileprivate let const_dueValue:[Character] = ["a"]
fileprivate let app_stackAssertId:String = "ttprogramng"

/*: "icon_close_nor" :*/
fileprivate let constFloorKey:String = "icon_event baseball compete unable coordinator"
fileprivate let mainReaderFormat:String = "remaining eastern mystery type holde_nor"

/*: "icon_views_number" :*/
fileprivate let appBridgeTitle:[Character] = ["i","c","o","n","_","v","i","e","w","s","_","n","u","m","b"]
fileprivate let user_cookieSeparatePath:String = "common"

/*: "%@ chatting" :*/
fileprivate let show_suitTitle:String = "%@ cok license m quantity"
fileprivate let kBookFormat:[Character] = ["n","g"]

/*: "icon_lounge" :*/
fileprivate let const_phaseId:String = "inmate environment dialog accounticon_l"
fileprivate let constFeatureFormat:String = "laboratorynge"

/*: "icon_rank_coin" :*/
fileprivate let dataApartmentName:[Character] = ["i","c","o","n","_"]
fileprivate let showProduceHeavyPath:[Character] = ["r","a","n"]
fileprivate let userPriorityMsg:[Character] = ["k","_","c","o","i","n"]

/*: "vip_btn_top" :*/
fileprivate let kFoodMsg:String = "vip_world secure"

/*: "btn_nearby_settings_nor" :*/
fileprivate let appCreationMsg:[Character] = ["b","t","n","_","n","e","a","r","b","y","_"]
fileprivate let mainCrySaleEnhanceTitle:String = "warningttin"
fileprivate let kFundDisableDatePath:[Character] = ["g","s","_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let showRestrictionMsg:String = "birth agree trail financial%@ Coin"
fileprivate let const_enableFormat:[Character] = ["s","/","m","i","n"]

/*: "get json error" :*/
fileprivate let dataAddressMessage:String = "left consumer database nativeget js"
fileprivate let appIssueStartId:[Character] = ["o","n"," ","e","r","r","o","r"]

/*: "matchId" :*/
fileprivate let const_screenText:[Character] = ["m","a","t","c","h","I","d"]

/*: "errno" :*/
fileprivate let const_neverName:[UInt8] = [0x37,0x20,0x20,0x3c,0x3d]

private func zoneMeAdColorful(oral num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "VIPFee" :*/
fileprivate let show_strategicValue:[Character] = ["V","I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let data_addressPath:[Character] = ["M","a","t","c","h","i","n","g"," ","n","o","w"]

/*: "userTouch" :*/
fileprivate let noti_regainMessage:[Character] = ["u","s","e","r","T","o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let k_ehKey:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V","i","d","e","o","I","n","f"]
fileprivate let showPhenomenonName:[Character] = ["o"]

/*: "fromType" :*/
fileprivate let const_coordinateValue:[UInt8] = [0x65,0x70,0x79,0x54,0x6d,0x6f,0x72,0x66]

/*: "You've been barred from receiving calls" :*/
fileprivate let kLiftMessage:[UInt8] = [0xcf,0xf9,0xe3,0xb1,0xe0,0xf3,0xb6,0xf4,0xf3,0xf3,0xf8,0xb6,0xf4,0xf7,0xe4,0xe4,0xf3,0xf2,0xb6,0xf0,0xe4,0xf9,0xfb,0xb6,0xe4,0xf3,0xf5,0xf3,0xff,0xe0,0xff,0xf8,0xf1,0xb6,0xf5,0xf7,0xfa,0xfa,0xe5]

private func aboutPi(communicate num: UInt8) -> UInt8 {
    return num ^ 150
}

/*: "source" :*/
fileprivate let data_animalId:[UInt8] = [0x65,0x63,0x72,0x75,0x6f,0x73]

/*: "type" :*/
fileprivate let appRestaurantId:[UInt8] = [0x48,0x45,0x4c,0x59]

private func problemFactor(act num: UInt8) -> UInt8 {
    return num ^ 60
}

/*: "uid" :*/
fileprivate let const_bridgeStr:[UInt8] = [0x39,0x25,0x28]

private func connectTotalo(pop num: UInt8) -> UInt8 {
    return num ^ 76
}

/*: "fromFreeCall" :*/
fileprivate let show_toleranceName:String = "stick"
fileprivate let const_anyoneMessage:[Character] = ["r","o","m","F","r","e","e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let notiEffKey:[Character] = ["r","e","q","u","e","s","t","C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let dataMiddleStr:String = "turkey utilize camera builderonVi"
fileprivate let user_groundContent:String = "atprofessional"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let show_cancelId:[UInt8] = [0x57,0x4d,0x79,0x78,0x63,0x61,0x6d,0x78,0x65,0x6f,0x2c,0x7a,0x65,0x68,0x69,0x63,0x51,0x2c,0x5e,0x69,0x6f,0x69,0x65,0x7a,0x69,0x2c,0x7a,0x65,0x68,0x69,0x63,0x2c,0x6f,0x6d,0x60,0x60,0x2c,0x65,0x62,0x6f,0x63,0x61,0x65,0x62,0x6b]

private func firstEvent(center num: UInt8) -> UInt8 {
    return num ^ 12
}

/*: "refuseCall" :*/
fileprivate let kSpanValue:[Character] = ["r","e","f","u","s","e","C"]
fileprivate let k_viewCurveKey:String = "ALL"

/*: "onCancelCall" :*/
fileprivate let data_itemOriginMsg:String = "onCantime exception debate"
fileprivate let dataMutualForceText:[Character] = ["a","l","l"]

/*: "retryAfter" :*/
fileprivate let app_platReportValue:[Character] = ["r","e","t","r","y","A","f","t"]
fileprivate let k_controlMessage:[Character] = ["e","r"]

/*: "onRequestCall" :*/
fileprivate let main_trailTingMsg:[Character] = ["o","n","R","e","q"]
fileprivate let appWaveMessage:String = "uelatert"

/*: "failType" :*/
fileprivate let main_disableName:[Character] = ["f","a","i"]
fileprivate let appWorkStr:String = "lTypedimension multiple"

/*: "callWaitingTime" :*/
fileprivate let noti_clingCloudCornerKey:String = "carequestrequest"
fileprivate let showSumStr:String = "ngTimemessage only if template"

/*: "toUid" :*/
fileprivate let notiRefuseStr:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let notiKingName:[Character] = ["F","r","e","e"," ","1"," ","m","i","n"]

/*: "%@/min" :*/
fileprivate let const_helpSatisfyStr:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  HaphazardMatchDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class HaphazardMatchDelegate: MergeContactViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.denominate()
            }
        }
        //: self.resetCamera()
        self.link()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        openSet()
        //: setupSubViewsConstraint()
        extraGapConstraint()
        //: bindInteraction()
        clingBring()
        //: videoManager.startPreview(isMatch: true)
        videoManager.parallel(isMatch: true)
        //: setPlayer(isRandon: false)
        seat(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            embarkWithinTelevisionSwitcher()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            FileSocketManager.shared.bagOriginal(info: [String(bytes: notiListStr.map{averageOutside(currency: $0)}, encoding: .utf8)!: (mainSoonValue.replacingOccurrences(of: "will", with: "ca") + String(const_runPath.suffix(4))), String(bytes: userErrorStr.reversed(), encoding: .utf8)!: [(String(kTransportationData)): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        undoItem()
        //: if TalkingSocketManager.shared.isStrategy == false {
        if FileSocketManager.shared.isStrategy == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            FileSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            FileSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            FileSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            FileSocketManager.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: RaceThen = {
        //: let m = TalkingVideoChatManager()
        let m = RaceThen()
        //: let info = TalkingVideoChatModel()
        let info = CompositionRoundChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .antiquityColor()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(embarkWithinTelevisionSwitcher), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: user_needValue + 1, height: kStreamTitle + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(user_shallKey) + String(data_underId)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(const_immediateMsg) + String(const_dueValue) + app_stackAssertId.replacingOccurrences(of: "program", with: "i")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.parentSet(name: (String(constFloorKey.prefix(5)) + "clos" + String(mainReaderFormat.suffix(5)))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fasteningCap), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: AfterView = {
        //: let btn = TalkingButton()
        let btn = AfterView()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.parentSet(name: (String(appBridgeTitle) + user_cookieSeparatePath.replacingOccurrences(of: "common", with: "er"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.parentSet(name: (String(appBridgeTitle) + user_cookieSeparatePath.replacingOccurrences(of: "common", with: "er"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.processAcross(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(show_suitTitle.prefix(4)) + "hatti" + String(kBookFormat)).temporaryWorker(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.parentSet(name: (String(const_phaseId.suffix(6)) + constFeatureFormat.replacingOccurrences(of: "laboratory", with: "ou")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.parentSet(name: (String(dataApartmentName) + String(showProduceHeavyPath) + String(userPriorityMsg)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.processAcross(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.parentSet(name: (String(kFoodMsg.prefix(4)) + "btn_top"))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: AfterView = {
        //: let btn = TalkingButton()
        let btn = AfterView()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(velvetTitleured), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.parentSet(name: (String(appCreationMsg) + mainCrySaleEnhanceTitle.replacingOccurrences(of: "warning", with: "se") + String(kFundDisableDatePath))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.parentSet(name: (String(dataApartmentName) + String(showProduceHeavyPath) + String(userPriorityMsg)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .colorful()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(showRestrictionMsg.suffix(7)) + String(const_enableFormat)).temporaryWorker(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.processAcross(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: ChinPromiseViewReactiveCompatible = //: return ChinPromiseViewReactiveCompatible(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension HaphazardMatchDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func seat(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = AuthorReactiveCompatible.default.typeTo(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(dataAddressMessage.suffix(6)) + String(appIssueStartId)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func visualAspect(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(const_screenText)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: notiListStr.map{averageOutside(currency: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: const_neverName.map{zoneMeAdColorful(oral: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        PreviouslyVideoManager.designCompletion(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.haulUp()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(const_screenText))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func beforeWhen() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        PreviouslyVideoManager.punish { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(show_strategicValue))].intValue
            //: self.refreshVideoCallPrice()
            self.denominate()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension HaphazardMatchDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func velvetTitleured() {
        //: SightReactiveCompatible.share.func__pushToSubscribeAlert()
        SightReactiveCompatible.share.deciding()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func fasteningCap() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        proximateDemonstrate()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        noti_liveLevelUrl.recordGender(eventID: kPathMessage)

        //: if CompositionReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, CompositionReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
        if CompositionReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, CompositionReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.announcementOf()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        mediumSave()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func embarkWithinTelevisionSwitcher() {
        //: guard TalkingSocketManager.shared.isStrategy == false else {
        guard FileSocketManager.shared.isStrategy == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.foolishness(showMsg: constEnterMessage)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        pictureOn()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(data_addressPath)).localized
        //: setPlayer(isRandon: true)
        seat(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if CompositionReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
        if CompositionReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            noti_liveLevelUrl.recordGender(eventID: userAppAcceptUrl)
            //: req_getVideoInfo()
            selectionOfInfo()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            noti_liveLevelUrl.recordGender(eventID: notiMaleMessage)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            FileSocketManager.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            FileSocketManager.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            FileSocketManager.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            FileSocketManager.shared.errorDelegate = self
            //: restart_randomVideo()
            record()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func record(cmd: String = (String(noti_regainMessage)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        undoItem()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        visualAspect(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func haulUp() {
        // 停止定时器
        //: destroyWaitingTimer()
        undoItem()
        //: showCloseBtn()
        proximateDemonstrate()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(const_immediateMsg) + String(const_dueValue) + app_stackAssertId.replacingOccurrences(of: "program", with: "i")).localized
        //: setPlayer(isRandon: false)
        seat(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isStrategy == false {
        if FileSocketManager.shared.isStrategy == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            FileSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            FileSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            FileSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            FileSocketManager.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        denominate()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func undoItem() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func selectionOfInfo() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = RoundReactiveCompatible()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(k_ehKey) + String(showPhenomenonName))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: const_coordinateValue.reversed(), encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        constNextKey.swaddlingClothes(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.haulUp()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: CompositionReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    CompositionReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.embarkWithinTelevisionSwitcher()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.foolishness(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = PoliceVisualCommunicationHandyJSON.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = CapReactiveCompatible()
                //: player.setMute(bEnable: true)
                player.doinglyTurn(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.glitter(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                YellowManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                MeetRouteThen.hairTip { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.finished()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func finished() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        noti_liveLevelUrl.recordGender(eventID: noti_saveTitle)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard YellowManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.foolishness(showMsg: String(bytes: kLiftMessage.map{aboutPi(communicate: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            YellowManager.shared.promptCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.haulUp()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = SiblingPlayerDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.mediumSave()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.haulUp()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func pictureOn() {
        //: if CompositionReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if CompositionReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(CompositionReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(CompositionReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func proximateDemonstrate() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func link() {
        //: guard TalkingSocketManager.shared.isStrategy == false else {
        guard FileSocketManager.shared.isStrategy == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.parallel()
        }
    }
}

// MARK: - BenchAddressObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension HaphazardMatchDelegate: BenchAddressObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func delayGenderTrack(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = RoundingModelType.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(const_screenText)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: data_animalId.reversed(), encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: appRestaurantId.map{problemFactor(act: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: const_bridgeStr.map{connectTotalo(pop: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = CompositionArrowPriorityTarget.randow
        //: if CompositionReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && CompositionReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if CompositionReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && CompositionReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (show_toleranceName.replacingOccurrences(of: "stick", with: "f") + String(const_anyoneMessage)))
            //: TalkingSocketManager.shared.isFreeCall = true
            FileSocketManager.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: notiListStr.map{averageOutside(currency: $0)}, encoding: .utf8)!: (String(notiEffKey)), String(bytes: userErrorStr.reversed(), encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        FileSocketManager.shared.bagOriginal(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            FileSocketManager.shared.bagOriginal(info: [String(bytes: notiListStr.map{averageOutside(currency: $0)}, encoding: .utf8)!: (mainSoonValue.replacingOccurrences(of: "will", with: "ca") + String(const_runPath.suffix(4))), String(bytes: userErrorStr.reversed(), encoding: .utf8)!: [(String(kTransportationData)): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.record(cmd: (String(dataMiddleStr.suffix(4)) + "deoM" + user_groundContent.replacingOccurrences(of: "professional", with: "ch")))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func uniformListen(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(kTransportationData))].intValue
    }
}

// MARK: - FormatSceneObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension HaphazardMatchDelegate: FormatSceneObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func zygotene(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func submitPerform(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func rewardOverlook(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func down(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            AppBaseDelegate.smallFeature(body: String(bytes: show_cancelId.map{firstEvent(center: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = RoundingModelType.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            strategicWarning(toUid: String(uid))
        }
        //: stop_randomVideo()
        haulUp()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.reduceDark()
        //: CompositionReactiveCompatible.share.start1v1TalkCall(info: data, situation: .Calling)
        CompositionReactiveCompatible.share.startOutSkin(info: data, situation: .Calling)
    }
}

// MARK: - FactorObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension HaphazardMatchDelegate: FactorObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func entire(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        record(cmd: (String(kSpanValue) + k_viewCurveKey.lowercased()))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func acceptShouldTag(data _: [String: Any]) {}
}

// MARK: - ToolMeetErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension HaphazardMatchDelegate: ToolMeetErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func lock(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(data_itemOriginMsg.prefix(5)) + "celC" + String(dataMutualForceText)) { // 取消通话异常
            //: stop_randomVideo()
            haulUp()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(dataMiddleStr.suffix(4)) + "deoM" + user_groundContent.replacingOccurrences(of: "professional", with: "ch")), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            haulUp()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(const_screenText))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.foolishness(showMsg: msg)
            //: uploadFailTime(type: 2)
            body(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(dataMiddleStr.suffix(4)) + "deoM" + user_groundContent.replacingOccurrences(of: "professional", with: "ch")), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(app_platReportValue) + String(k_controlMessage))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(const_screenText))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.record(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(main_trailTingMsg) + appWaveMessage.replacingOccurrences(of: "later", with: "s") + "Call"), errorNo == AdminGiantSumLiteral.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            haulUp()
            //: guard CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue else { return }
            //: SightReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            SightReactiveCompatible.share.secondBusy(clickEvent: main_currencyId, sufficient: false)
            //: uploadFailTime(type: 1)
            body(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        record(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension HaphazardMatchDelegate {
    //: func uploadFailTime(type: Int) {
    func body(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(main_disableName) + String(appWorkStr.prefix(5)))] = type
        //: parm["callWaitingTime"] = time
        parm[(noti_clingCloudCornerKey.replacingOccurrences(of: "request", with: "l") + "Waiti" + String(showSumStr.prefix(6)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        noti_liveLevelUrl.pendingConfirm(eventID: main_underValue, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func strategicWarning(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(noti_clingCloudCornerKey.replacingOccurrences(of: "request", with: "l") + "Waiti" + String(showSumStr.prefix(6)))] = time
        //: parm["toUid"] = toUid
        parm[(String(notiRefuseStr))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        noti_liveLevelUrl.pendingConfirm(eventID: appReloadTimeUrl, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension HaphazardMatchDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func denominate() {
        //: guard CompositionReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard CompositionReactiveCompatible.share.appStatus == ColumnExInsetTarget.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !CompositionReactiveCompatible.share.loginUserMode.loungePlus
        vipIcon.isHidden = !CompositionReactiveCompatible.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = CompositionArrowPriorityTarget.randow
        //: if CompositionReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && CompositionReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if CompositionReactiveCompatible.share.loginUserMode.freeCallTimes > 0, CompositionReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if CompositionReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
            if CompositionReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(notiKingName)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = CompositionReactiveCompatible.share.loginUserMode.loungePlus ? vipFee:fee
            let price = CompositionReactiveCompatible.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(const_helpSatisfyStr)).temporaryWorker(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(const_helpSatisfyStr)).temporaryWorker(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.counteraction(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = CompositionReactiveCompatible.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = CompositionReactiveCompatible.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = CompositionReactiveCompatible.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = CompositionReactiveCompatible.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !CompositionReactiveCompatible.share.loginUserMode.loungePlus
            priceStackView.isHidden = !CompositionReactiveCompatible.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func openSet() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func extraGapConstraint() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(noti_guideRequestId + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func clingBring() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(denominate),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: constAddKey,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(beforeWhen),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: userRobotUrl,
                                               //: object: nil)
                                               object: nil)
    }
}
